# 语巢 · 更新与决策记录

> **用途**：记录所有已敲定的更新 / 调整，以及「进行中、待办、待确认」的事项。
> **约定**：每完成或敲定一处改动，就在这里追加 / 更新一条；开工前先读这里找回上下文，确保中断的任务能接上。

## 关键偏好（长期有效）
- 干净清爽、简单为主；进阶功能做成可选入口，不塞进主流程。
- 商定方案做好记录、防止丢失；改动前先说方案。
- `.env.local` 里的 key 值（ANTHROPIC / OPENAI / DEEPSEEK）绝不打印。

---

## 进行中 / 暂停
- 2026-08-28 **云盘备份 → Google Drive（已取消/暂缓：无正式域名）**：用户决定先不做云盘备份（还没正式域名）。方案已敲定备用——OAuth `drive.file` 最小权限 → 授权回调 + refresh token 存 `user_settings` → 调 Google Drive 上传（复用现有「导出全部数据 JSON」）。**届时前置**：去 Google Cloud Console 建 OAuth 客户端（Web，回调 `https://<域名>/api/drive/callback`），把 `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` 填进 `.env.local` 后再实现授权 + 上传 + 设置页「备份到 Google Drive」按钮。
- 2026-08-23 **Vercel 生产部署已完成（build 绿、`readyState: READY`），正式域名 `https://nestlingo-psi.vercel.app`**。CLI 已写入 10 个 production 环境变量（含 `NEXT_PUBLIC_DISABLE_YTDLP=1`，禁用 yt-dlp 三项）。**仍待人工做一次**：① Supabase 后台 Authentication → URL Configuration 加 Site URL=`https://nestlingo-psi.vercel.app`、Redirect URLs=`https://nestlingo-psi.vercel.app/auth/callback`、Allowed Origins=`https://nestlingo-psi.vercel.app`（否则登录回调报 redirect 不合法）；② 若手机打开显示 401/Vercel 登录页，说明 Deployment Protection 开着，去 Vercel 项目 Settings → Deployment Protection 关掉 Vercel Authentication；③ 手机浏览器任意网络打开该域名实测登录 + 各项功能（yt-dlp 三项按钮已灰化）。
- 2026-08-22 **⚠️ 自检 DEbug 的 schema 迁移待落地**：跑一次 schema（`supabase db push` 或重跑 `schema.sql`）应用 ① `materials.collection_id` 由 cascade 改为 set null（幂等 alter）② 新增素材存储桶两条 policy（`materials_bucket_update` / `materials_bucket_delete`）。见「已完成」顶部最新一条的 ⚠️ 需人工落地。
- 2026-08-22 **闪卡 `lang` 语言字段主链路已落地**（新增 + 显示/筛选 + 主题一致性 + 返回原处 + 当前合集记进度，见「已完成」顶部最新一条，tsc + build 通过）。**仍需人工做一次**：① 跑一次 schema（本地 `supabase db push` 或重跑 `schema.sql`）给 `cards` 补 `lang` / `reading` 两列（`alter add column if not exists` 幂等，存量卡 `lang` 为 null → `cardLang` 自动回退、`reading` 为 null → 正面纯文本，均不影响使用）；② 端到端点一遍（转卡预览语言下拉、编辑改语言、主题选语言进详情/背测、闪卡列表「开始背诵」返回原处、背几张后回主页看「正在背的合集」被记住；转成日语闪卡后正面汉字上方标假名、背诵面/卡片页/侧栏都能看到）。
- 2026-08-22 **完形填空复习题型已做完整并打磨到可交付**（`mode=cloze`，见「已完成」顶部）：① 没语境句的生词由 `/api/ai/cloze-examples` 现造例句并带小「AI」标识（**又调：进页不自动调 AI，改点「用 AI 造例句」按钮才生成，省 token**；生词背面「释义 例：例句」已拆分，出题只用例句、词义只取释义，不再混排）；② 揭示后去掉记忆曲线打分、改成直接「下一题」（纯练习不计 SM-2）；③「显示答案」= 开关（只填空、再点隐藏、显示态输入核对即进整句）；④ 整句展示例句高亮 + 紧跟其下的整句中文译 + 词义柔和框 + 语法提示；⑤ 译文/词义排版统一。**发音**：Edge TTS / Voicebox 那条**先搁置**——edge-tts@1.0.1 实测合成 403（微软加 `Sec-MS-GEC` 鉴权、老包没实现；voices 列表可用但合成被拒），免费与靠谱两条路都不成立，用户决定暂时不动发音，后期再说。**接下来待定**：见下方待办/待确认选一条推进。（学伴「原文段分段发音」已单独延后，见待办 0 ⑤。转录仅剩「无字幕视频听声转录」半条未做，见待办 1。Apple Podcast 单集已做。）
- 2026-08-22 **素材库 Round 4 已落地**（见「已完成」顶部最新一条，tsc + build 通过）。**仍需人工做一次**：① 跑一次 schema（本地 `supabase db push` 或重跑 `schema.sql`）给 `material_collections` 补 `lang` / `type` 两列（`alter add column if not exists` 幂等，存量合集 `lang`/`type` 为 null → 筛选按里面素材兜底，不影响使用）；② 端到端点一遍（收藏单集播客链只出那一集、整订阅链出最近 30 集且最新在前、文章无正文不再黑屏而是「提取正文/打开原文」、合集行有「合集」标识与语言/类型徽章、顶栏按钮点开表单紧贴按钮、新建/编辑合集可定义类型与语言）。
- 2026-08-26 **笔记编辑页「标题和编辑栏之间大空档」待浏览器实查**：网页端笔记内页标题与富文本编辑栏之间空隙偏大，静态看两者毗邻无 margin（标题 `px-4 pt-2`、RichTextEditor 根 `flex flex-1 flex-col`、工具栏是第一个子元素），疑为编辑器内容区 `flex-1` 撑高 / `.tiptap min-height:240px` 把留白摊在正文下方所致（空档方向存疑）。等网页端点开看截图定位后再动；**工具栏左对齐正文（px-4 md:px-8）本轮已修**，观感若是「正文下留白」可考虑只对短内容收窄编辑容器高度。

## 已完成（时间倒序）
- 2026-09-09 **Vercel 生产部署成功（`nestlingo-euaetp4wb-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「桌面端笔记内页两侧绿色背景外露修复」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-09-09 **桌面端笔记内页两侧绿色背景外露修复（内容列铺白，tsc + build 绿，已部署）**。用户反馈「网页端笔记内页两边露出最底层绿色背景，显得不干净」。根因：`note-editor.tsx` 编辑内容容器用 `justify-center` + 内层 `max-w-3xl/4xl/5xl` 居中收缩，外层容器（编辑区）无背景 → 宽屏时白色内容列比可用区窄，两侧露出 body 的页面渐变绿底；且该页加载态本来就是 `bg-white`（`loading: () => <div className="flex-1 bg-white">`），加载完反而变透明、不一致。修复：给内容外层 `div`（`showCards` 两分支）加 `bg-white`，让内容列居中在白色上、绿底不再从两侧露出（与 Notion「白色阅读面 + 居中内容列」一致）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-09 **Vercel 生产部署成功（`nestlingo-ch0r630e2-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「笔记页文字质感向 Notion 靠拢」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-09 **笔记页文字质感向 Notion 靠拢（抗锯齿平滑 + 大标题负字距，tsc + build 绿，已部署）**。用户反馈「笔记页不如 Notion 清爽、文字有点安卓味」，问是否字体原因。诊断：字体栈本就正确（iOS 用 SF Pro、安卓用自托管 Inter、中文/韩文回退系统，与 Notion「不加载网页字体、用系统原生」同一策略），**不是字体家族问题**；「安卓味」来自渲染细节——① 未开文字抗锯齿平滑（苹果上默认渲染偏粗糊，最不像原生）；② 大标题无负字距（苹果大字号带 -0.02em 轻微 tracking 才显得锐利干净）。落地 `globals.css`：body 加 `-webkit-font-smoothing: antialiased` + `-moz-osx-font-smoothing: grayscale` + `text-rendering: optimizeLegibility`；`.tiptap h1/h2` 加 `letter-spacing: -0.02em`、`h3` 加 `-0.01em`，新增 `.note-title-input { letter-spacing: -0.02em }`。`NODE_USE_ENV_PROXY=1 npm run build` 绿。
- 2026-09-09 **Vercel 生产部署成功（`nestlingo-gw1ohk1tq-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「服务端 Supabase 读缓存加速」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-09 **服务端 Supabase 读缓存（移动端 / 导航加载提速，tsc + build 绿，已部署）**。用户反馈「手机移动端加载太慢」，排查根因：每次页面导航都打多趟 Supabase 网络往返（`(app)/layout.tsx` 每页 `getUser()` 网络校验 + notes 列表等读查询），自用且加载慢主要来自网络往返而非网速（Supabase 区域已在新加坡，非瓶颈）。落地：① 新增 `src/lib/supabase/query-cache.ts`——进程内短 TTL（15s）缓存 `cached(key, fn)` / `clearQueryCache()`（Vercel serverless 实例内窗口期命中，能消掉绝大多数往返）。② 新增 `src/app/api/cache/purge/route.ts`（POST，清空读缓存）。③ `src/lib/supabase/client.ts` 的 `createClient()` 用 Proxy 拦截 `.from(...)` 的 insert/update/delete/upsert——一发起写就 fire-and-forget 调 `/api/cache/purge`，保证写后 `router.refresh()` 重新渲染的服务端查询读到新数据、不命中旧缓存。④ `src/lib/supabase/queries.ts` 加 `qcached(name, args, fn)`（按「用户 + 查询名 + 参数」分 key），把热路径读函数包上缓存：`listFolders/listNotes/getNote/getFolder/listCards/listCardGroups/getReviewOverview/listDueFiles/listReviewCards/listWeakCards/listOrphanCards/listAllCards/listMaterials/listMaterialCollections/listFavorites/listWordCards`；`getUserSettings/getUserAiProviders` 有意不缓存（AI 路由 / 设置需新鲜）。⑤ `(app)/layout.tsx` 的 `getUser()` 改用 `cached` 按「用户 + token 过期时间」分 key 短缓存——省掉每页一次网络往返，token 过期/会话刷新时 key 变会重新校验、不会误放陈旧用户。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-08 **Vercel 生产部署成功（`nestlingo-kejkmf1tr-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「备注纯无标题 callout + 原文改文章」改动。`vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-08 **新增「备注」纯无标题 callout + 「原文」改「文章」（tsc + build 绿，未部署）**。用户定案：① 原文（article）标签改名「文章」；② 新增一个不带任何标题的纯 callout，内容不转成闪卡，用来放课堂笔记等不需要做卡的知识点。落地：① `callout-extension.ts` `KIND_META.article.label`「原文→文章」，新增 `note: { label: "", color: "#9ca3af" }`；`renderHTML` 对 `note` 特判——不输出 `.callout-label` 行，只输出正文框（div[data-kind=note] + callout-body）。② `doc-to-text.ts` callout 序列化 label：「原文→文章」、新增 `note→"备注"`（内部标记，供 parse-sections 跳过）。③ `parse-sections.ts` `ARTICLE_TITLES→SKIP_TITLES`、`isArticleTitle→isSkipTitle`，集合加「备注」（文章/备注区域转卡整段跳过，含表格）。④ `rich-text-editor.tsx`：`calloutsInline` 加 note 按钮（StickyNote 图标、label「备注」、置末尾）+ 原文 label 改「文章」+ `insertKindCallout` 类型加 `note`。⑤ `globals.css` 新增 `.callout[data-kind="note"]`（编辑器浅灰底+虚线边，无标签）与 `.share-content .callout[data-kind="note"]`（分享图同款）；⑥ `share-modal.tsx` 按条切页对空 label 的 note callout 跳过标签 part。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-07 **Vercel 生产部署成功（`nestlingo-4nqu61qrl-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「移动端表格所有列均分铺满 + 文件夹/笔记卡片文字大一号」本轮全部改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes` 一次通过。
- 2026-09-07 **移动端表格改为「所有列均分铺满一屏」+ 文件夹/笔记卡片文字大一号、文件夹排版更宽松（tsc 绿，未部署）**。用户两点：① 移动端文件夹太挤、高度窄、局促；文件夹文字与笔记文件卡片预览文字整体大一个字号，文件夹排版再宽松；② 表格能否直接自适应把移动端屏幕所有列都展示（而不是横向滚动）。落地：① `globals.css` 加 `@media (max-width:767px){ .tiptap table col { width: auto !important } }`——忽略 resizable 拖拽/粘贴带来的固定列宽（colwidth），`table-layout:fixed;width:100%` 下所有列均分铺满一屏、全部展示（超长内容靠 td 的 `overflow-wrap:anywhere`/`word-break:break-word` 换行，不溢出），替代上一轮的横向滚动方案（`overflow-x:auto` 保留作兜底）。② `notes-browser.tsx`：文件夹树（仅移动端 `md:hidden`）——`FolderTreeNode` 行 `py-2.5→py-3.5`、图标 `h-4→h-5`、文件夹名 `text-sm→text-base`、计数 `text-xs→text-sm`、行间距 `space-y-1.5→space-y-2.5`、子层 `mt-1.5→mt-2`；「全部笔记」入口同样 `py-3→py-3.5` + 图标/字号放大。笔记卡片 `NoteItem`（桌面侧栏与移动端共用，用响应式 `md:` 前缀只放大移动端、桌面侧栏保持原字）：列表标题 `text-sm→text-base md:text-sm`、摘要 `text-sm→text-base md:text-sm`、文件夹标签/时间 `text-xs→text-sm md:text-xs`；网格（仅移动端）标题 `text-base→text-lg`、摘要 `text-sm→text-base`、时间/标签 `text-xs→text-sm`。`npx tsc --noEmit` 通过。
- 2026-09-07 **移动端笔记表格自适应 + 表格操作栏不再被挤成竖排（tsc 绿，未部署）**。用户三点：① 移动端表格只能看到两列；② 表格功能栏一直开着；③ 功能栏在移动端被挤压、每个功能文字竖排。根因/落地：① 表格被 resizable 存了固定列宽（`colwidth`），列宽之和超过窄屏宽度时 `table-layout:fixed` 的表按列宽溢出、`.tableWrapper` 无横向滚动 → 右侧列被裁、只见两列。`globals.css` 给 `.tiptap .tableWrapper` / `.tiptap .table-scroll` 加 `overflow-x: auto` + `-webkit-overflow-scrolling: touch`，超宽表格横向滚动看全列（桌面表 `width:100%` 不溢出、无滚动条，不受影响）。②③ 表格操作栏（光标在表格内才显示，桌面吸顶栏下、移动吸底栏上）里 `ToolButton` 无 `shrink-0`/`whitespace-nowrap`，窄屏时按钮被压缩、文字「＋上/标题行」折成竖排而非横向滚动。给 `ToolButton` 基类加 `shrink-0 whitespace-nowrap`（工具栏按钮本就不该收缩，横向滚动靠容器 `overflow-x-auto`），表格操作栏「行/列」label 与分隔线 span 也补 `shrink-0`。`npx tsc --noEmit` 通过。
- 2026-09-07 **Vercel 生产部署成功（`nestlingo-btwpj56nr-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖本轮全部未部署改动——文件夹上移/下移、笔记列表时间沉底+⋯菜单右上角、文件夹栏收窄、顶层表格分享切页修复、分享图每页自适应高度、PWA manifest 链接修复。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-07 **修复安卓 PWA 无法安装（manifest 未链接，tsc 绿，已部署）**。用户反馈安卓 Chrome「安装应用 / 添加到主屏幕」没有反应、看不到入口，想直接做 APK 安装。根因：`manifest.ts` 只生成了 `/manifest.webmanifest` 文件，但 HTML 里没加 `<link rel="manifest">`，Chrome 检测不到 manifest → 判定不了「可安装 PWA」→ 安装入口不出现（新版 Chrome 对非可安装站点已移除「添加到主屏幕」，只剩对合格 PWA 的「安装应用」）。修复：根 `layout.tsx` 的 `metadata` 加 `manifest: "/manifest.webmanifest"`（Next 据此自动输出 `<link rel="manifest">`）。`npx tsc --noEmit` 通过；需重新部署后安卓 Chrome 菜单里才会出现「安装应用」。`sw.js` 已有 fetch handler、`manifest.ts` 已有 name/display:standalone/start_url/icons(192/512/maskable)，可安装条件本就满足，缺的只是 link。
- 2026-09-07 **分享成图：表格切页尽量铺满 + 每页自适应高度（tsc + build 绿，未部署）**。用户反馈「表格一页只有 9 行、画幅还剩很多空间，能不能每张图自适应铺满、留够边距就行」。根因两点：① `.share-content table { margin: 0.5em auto }` 的上下 margin，在按行切页时每个单行小表格各算一份 margin，把测量行高撑大，导致提前断页（每页放不满就断）；② 预览每页固定 `PAGE_HEIGHT = ratio.h`，内容不满一页时下方大片空白。落地：① globals.css 把表格 margin 改为 `margin: 0`（flex gap 已隔开块间距；测量与预览都无 margin，行高一致、不再被撑大）；② `share-modal.tsx` 改「自然高度」——切页算法 `closePage` 时记录每页内容高度 `pageHeights`（各块高度之和去掉最后一个 gap），预览每页渲染高度 = `pageHeights[pi] + PAGE_PADDING*2`（不再固定 `PAGE_HEIGHT`），内容区去掉 `flex-1`、footer 去掉 `marginTop:auto`（紧跟内容不沉底）。比例选择器语义变为「最长单页上限」：断页阈值仍用 `CONTENT_MAX = ratio.h - 64`，但每页实际高度贴合内容、无空白。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-07 **修复顶层表格分享成图仍只显示一页（tsc + build 绿，未部署）**。用户反馈「分享成图这里表格还是只展示一页，其他都没了」。根因：`share-modal.tsx` 的 `flatten` 表格拆行逻辑只在「按条切页视图 + callout 内嵌表格」时被调用（`viewMode==="flat" && block.type==="callout"` 才对 children 跑 `flatten`），**顶层表格**在两种视图下都走 `addNode(block)` 整块保留，超页被 `overflow:hidden` 裁掉、只剩第一页。修复：`blocks.forEach` 加 `else if (block.type === "table") flatten(block)`，让顶层表格也按行拆（表头 + 逐行，超页时跨页重复表头、每页 `buildPageUnits` 合并回一张连续表格），笔记样式/按条切页两种视图都生效；无表头表格不重复表头、含 rowspan 整块保留的兜底不变。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-07 **笔记列表「时间」沉底（标题→摘要→时间，tsc 绿，未部署）**。用户对卡片排版优化只勾了「时间沉底」：原来顺序「标题→时间→摘要」，时间夹在标题与摘要之间、层次略乱。落地 `notes-browser.tsx` `NoteItem` 列表分支：时间 `<p>` 从标题下移到摘要（及文件夹标签）之后、`</Link>` 前，最终顺序「标题→文件夹标签→摘要→时间」，时间作最次要信息沉底。`npx tsc --noEmit` 通过。
- 2026-09-07 **笔记列表 ⋯ 菜单移到右上角 + 桌面文件夹栏收窄（tsc 绿，未部署）**。用户两点：① 笔记文件隐藏菜单（⋯）放右上角；② 网页端左侧导航栏偏宽。落地：① `notes-browser.tsx` `NoteItem` 列表分支右侧菜单容器 `<div className="relative pr-1">` → `<div className="relative self-start pr-1 pt-2.5">`（`self-start` 覆盖外层 `items-center`、`pt-2.5` 对齐标题行，使 ⋯ 落在右上角，不再垂直居中）；② `notes-workspace.tsx` `folderResize` 默认宽 `initial: 208 → 176`（`usePanelResize` 仅在 localStorage 无存储值时用 initial，若之前手动拖过宽度则浏览器里仍记旧值、需手动再拖一次或清 `nestlingo:folders-width`）。`npx tsc --noEmit` 通过。
- 2026-09-07 **笔记列表「时间」移到标题下方独立一行，条目更透气（tsc 绿，未部署）**。用户对齐小米笔记：笔记文件预览里时间单独放左侧下面，让每条高度高一点、不显挤；我们原来时间挤在右侧与 ⋯ 菜单竖排。落地 `notes-browser.tsx` 的 `NoteItem` 列表分支：时间由右侧 `<div className="flex flex-col items-end gap-1 pr-1">` 挪进左侧 `<Link>` 内标题下、独立 `<p className="mt-0.5 text-xs text-zinc-400">{time}</p>`，右侧只剩 ⋯ 菜单（`relative pr-1`、外层 `items-center` 使其垂直居中）；同时 `NoteList` 列表 `space-y-1.5 → space-y-2` 增大条目间距。桌面侧栏与移动端都走 `NoteList` 一处生效；网格视图时间本就在底部 footer 左侧，不改。`npx tsc --noEmit` 通过。
- 2026-09-07 **文件夹支持上移/下移排序（tsc 绿，未部署）**。用户问「文件夹要不要设排序功能，能否把一些子文件夹放在前面/后面」。定案：在文件夹 ⋯ 菜单加「上移 / 下移」两项，与相邻兄弟交换位置。落地：`folders` 表早已有 `position int not null default 0`，`listFolders` 早已按 `position, created_at` 排序，缺的是改 position 的 UI——`notes-browser.tsx`（移动端）与 `notes-workspace.tsx`（桌面侧栏）各加 `moveFolder(folder, dir)`：取该文件夹同级兄弟（按 `parent_id` 过滤、已是显示顺序）找下标、与相邻兄弟交换后把整层兄弟 `position` 重写成 0..n-1 并逐条 `.update({ position })`（解决存量全 0 时靠 created_at 排序、单纯 swap 无效的问题）；`FolderTreeNode` / `renderFolder` 各加 `siblings` prop 算 `canMoveUp/canMoveDown` 驱动按钮禁用态（首项不可上移、末项不可下移）。`npx tsc --noEmit` 通过。
- 2026-09-07 **笔记编辑栏加「左对齐 / 居中 / 右对齐」（tsc + build 绿，未部署）**。用户反馈：表格单元格里粘贴进来的内容有时是居中的，想改成左对齐却没有入口。落地：新增依赖 `@tiptap/extension-text-align@3.30.1`（精确版本，避免 `^` 漂移到 3.31.x 与 core@3.30.1 的精确 peer 冲突）；`editor-extensions.ts` 注册 `TextAlign.configure({ types: ["heading", "paragraph"] })`（表格单元格内也是段落，故能对齐单元格内容；defaultAlignment 不设=不强制内联 text-align）；`rich-text-editor.tsx` 工具栏 `formatButtons` 末尾加三个按钮（AlignLeft/AlignCenter/AlignRight，`setTextAlign`），`toolbarActive` 加 `align` 字段（`isActive({textAlign})`）驱动激活态，桌面顶栏与移动端「格式」面板共用 `formatButtons` 自动都生效。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-07 **① 笔记生词表格「读音」列转卡放背面；② 表格分享成图按行切分 + 每页重复表头（tsc + build 绿，未部署）**。① 用户反馈表格转卡的读音与 AI 解释不一致——`parse-cards.ts` 的 `parseTable` 原把读音列（hint）拼到正面「词 (读音)」，AI 解释的读音在背面「读音：xxx」。定案放背面：`if (hint && backIdx !== -1) back = back ? \`读音：${hint}\n${back}\` : \`读音：${hint}\``，正面只留词；`card-back.tsx` 的 `KNOWN_LABELS`/`parseBack` 已识别「读音：」冒号标签，无需改。② 用户反馈表格「生成分享图片」时整块超一页被裁（块不可再分）。定案按行切+每页重复表头：`share-modal.tsx` 在 `flatten` 把顶层 `table` 拆成「表头 part（可选）+ 逐行 part」，每个 part 是一个单行小 `<table>`（用于测高）；切页算法 `headerByGroup` 预取表头高度、表体行断页且上一页已有同 `groupId` 时新页先重复表头；渲染用 `buildPageUnits` 把同页同 `groupId` 的表头+若干行合并回一个 `<table>`（保持连续无间隙）。新增 `splitTable`/`hasRowspan`/`makeTable`/`renderTableSlice`/`buildPageUnits` 纯函数；空表/含 rowspan 整块保留不切（兜底）；callout 内嵌表格仅 flat 视图切、note 视图不切。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-06 **Vercel 生产部署成功（`nestlingo-63gih7vmn-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「设置加默认随机顺序背诵」改动，schema 迁移已人工落地后部署。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-09-06 **设置加「默认随机顺序背诵」开关（tsc + build 绿）**。用户此前问：Anki 里词是自动随机背、能不能在设置里加牌组/卡片功能或简单点，我建议轻量方案「设置里加默认随机顺序背诵开关（默认关），打开后每次进背诵自动洗牌」，用户答「可以加这个小设置」。落地：① `UserSettings` 加 `review_shuffle: boolean`，`getUserSettings` select/返回默认 `false`；② `settings-form` 学习组加「默认随机顺序背诵」开关（复用提醒开关样式），`save()` upsert `review_shuffle`；③ `review-session` 加 `shuffleDefault` prop——挂载即用 Fisher-Yates 洗牌初始队列、`shuffled` 初始为 `shuffleDefault`，`orderedRef` 仍存原始顺序快照，「随机」按钮可临时切回顺序；④ `review/page.tsx` 三处（`scope=weak` / `note` / `theme`）读 `getUserSettings().review_shuffle` 并传给 `ReviewSession`（`note`/`theme` 分支把 `getUserSettings` 一次读回 daily_goal+review_shuffle，避免重复请求）。**⚠️ 需人工做一次 schema 迁移**：`supabase/schema.sql` 已加幂等 `alter table public.user_settings add column if not exists review_shuffle boolean not null default false;`（跑 `supabase db push` 或重跑 schema 落库，否则设置保存会因列不存在报错）。`npx tsc --noEmit` 通过。
- 2026-09-02 **Vercel 生产部署成功（`nestlingo-kjh2079x2-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「CSV 导出去表头」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `fetch failed`、重试即过）。
- 2026-09-02 **闪卡 CSV 导出去掉表头行（tsc + build 绿）**。用户反馈：把导出的 CSV 导入 Anki 时，第一行「正面,背面」表头也被当成一张卡。根因：Anki 导入时若没勾「第一行是字段名」（或字段名与笔记类型不匹配），会把表头当数据。定案：`exportCardsCsv` 不再输出表头，只输出数据行（`正面,背面` 每行一张），导入 Anki 第一行即数据、不会多卡、也无需额外设置。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-02 **Vercel 生产部署成功（`nestlingo-mudxxyknf-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「CSV 导出只留正面/背面」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-09-02 **闪卡 CSV 导出只留「正面 / 背面」两列，去掉「类别 / 标签」（tsc + build 绿）**。用户反馈：导出一个闪卡合集，CSV 里多出了「类别」和「标签」两列，一般只要正面和背面就行。落地 `export-data.ts` 的 `exportCardsCsv`：`header` 由 `["正面","背面","类别","标签"]` 改为 `["正面","背面"]`，rows 只取 `front` / `back`（不再引用 `kind` / `tags`）。四处调用（`data-backup` / `export-cards-button` / `cards-view` / `group-browser`）都走这个函数，自动生效。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-02 **Vercel 生产部署成功（`nestlingo-q878oi0mm-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「背诵随机顺序」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-02 **背诵加「随机顺序」开关（tsc + build 绿）**。用户要：背诵时可以选随机顺序（不按 position 原始顺序）。落地 `review-session`：顶部进度行「词库」旁加「随机」toggle（Shuffle 图标，开启时 teal 高亮），默认关闭=按合集原顺序。开启 → Fisher-Yates 洗牌当前待学队列；关闭 → 用挂载时的 `orderedRef` 原始顺序快照把「还没背的卡」重排回原顺序（已背的卡不在 queue 里、不受影响）。「忘记→队尾」逻辑不变。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-02 **Vercel 生产部署成功（`nestlingo-lwkebx5jr-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「回退阅读模式 mark 快捷键」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-09-02 **回退「阅读模式 mark 快捷键」改动（tsc + build 绿）**。用户反馈：阅读模式下用高亮快捷键会触发其他快捷键操作（如 ⌘⇧H 在系统层面被截获/冲突），体验不对，决定恢复。落地：删掉 `editorProps.handleDOMEvents.keydown` + `toggleMark` 函数 + `EditorView` 类型 import，阅读态恢复纯查义（点词弹卡、无快捷键干预）。「读音/搭配缺换行」修复保留不动。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-09-02 **Vercel 生产部署成功（`nestlingo-1aixjsjt2-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「修复读音/搭配缺换行 + 阅读模式 mark 快捷键」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `Not authorized`、重试即过）。
- 2026-09-02 **① 修复「解释→导入笔记」读音与搭配之间缺分隔、转卡后无换行；② 阅读模式下支持 mark 快捷键（高亮/加粗/斜体/下划线/删除线/代码）（tsc + build 绿）**。① 用户反馈：泰语词 AI 解释「导入到笔记」后，读音和后面搭配之间没空格，转成闪卡背面不换行。根因：`importToNote` 拼 `paraNode` 时 `rest = [b, e].filter(Boolean).join("")`——`b`（释义+读音，`\n` 已折双空格）与 `e`（搭配+例句）用**空串** `join("")` 直接粘连，导致「`读音：xxx`」「`搭配：xxx`」之间无分隔符，转卡时 `parseCards` 按双空格拆、拆不出换行。修复：`join("")` → `join("  ")`（与 `itemLine` 第 67 行 `line = line + "  " + e` 双空格拼接对齐），转卡即拆回换行。② 用户希望阅读模式下仍能用快捷键处理文字（如高亮）。根因：阅读态 `editor.setEditable(false)`，prosemirror-view 的 `dispatchEvent` 对 keydown 有 `(view.editable || !(event.type in editHandlers))` 门禁，keydown 属 editHandlers，故 Tiptap 默认 keymap（走 `someProp("handleKeyDown")`）整体失效。落地：`editorProps` 加 `handleDOMEvents.keydown`（`runCustomHandler` 在 editable 门禁**之前**执行，非 editable 也触发）——仅阅读态拦截 mark 快捷键（⌘/Ctrl + B/I/U/E、⇧S、⇧H），`event.preventDefault()` 后调新增的 `toggleMark(view, markName)` 直接基于 `view` dispatch（`state.doc.rangeHasMark` 决定 add/remove，空选区跳过），绕过门禁。已核对 prosemirror-view 源码：非 editable 下 `hasFocusAndSelection`/`hasSelection` 只要选区在 `view.dom` 内仍同步 selection（`view.editable || view.dom.contains(...)`），故鼠标拖选后 `state.selection` 正确、toggle 生效；`globals.css` 的 `user-select:none` 只在 `.callout-label`，正文可选。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-3szvg4vhe-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「右上角『完成』→『保存』、删菜单重复保存项」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **笔记右上角按钮「完成」→「保存」，并删掉 ⋯ 菜单里重复的「保存」项（tsc + build 绿）**。用户问：有了阅读/编辑模式按钮后，右上角「完成」还要吗、还是写「保存」。定案：**保留按钮、文案改「保存」**——该页是自动保存（内容停顿 ~1s 落库），右上角是唯一显眼的「立即保存/确保已存」入口，不能删；但叫「完成」语义不清（"完成"什么？又易与阅读/编辑切换混淆），改「保存」更直白，且与菜单原「保存」一致。onClick 仍走 `finishEdit`（清挂起的自动保存 timer + 立即 `save()`，避免保存后 ~1s 又重复写库）。同时删掉 ⋯ 菜单里的「保存」项（右上角已有，去冗余），菜单剩「查看闪卡 / 删除」。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-ksmpdpksl-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「笔记编辑/阅读模式」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **笔记「编辑 / 阅读模式」落地（tsc + build 绿）**。用户反馈：移动端点词弹卡虽用 touchstart preventDefault 挡住软键盘，但会阻断滚动、滚动误触明显；提议做编辑/阅读模式（内容格式样式不变）。定案：**默认编辑**、**阅读态纯查义**。落地：① `note-editor` 头部加 BookOpen/Pencil 切换按钮 + `readOnly` state，传给 `RichTextEditor`；② `rich-text-editor` 加 `readOnly?` prop，`useEffect` 里 `editor?.setEditable(!readOnly)`（放 `useEditor` 之后避免 TDZ），`readOnlyRef` 供 `handleClick` 读最新值；③ 点词弹卡只在阅读态触发——`handleClick` 开头 `if (!readOnlyRef.current) return false`（编辑态正常编辑、不弹卡）；④ 删掉上一轮 `handleDOMEvents.touchstart` preventDefault hack（阅读态 contenteditable=false 本身不落光标 / 不弹键盘，滚动正常）；⑤ 两套工具栏（桌面 `md:block` / 移动 `md:hidden`）都包 `{!readOnly && ...}` 隐藏。已核对 prosemirror-view：`handleClick` 经 pointer up 分发、无 `editable` 门禁，非 editable 也触发。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-nzo8aym9e-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「移动端点词弹卡阻止软键盘」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（网络偶发 `fetch failed`、重试即过）。
- 2026-08-30 **移动端点下划线词弹卡不再落光标 / 弹软键盘（tsc + build 绿）**。用户反馈：手机点词弹卡时，click 之前 touch 已让光标落到词上并弹软键盘，体验差。根因：`handleClick` 是 click 事件，发生在 touch 序列之后，此时 iOS/Android 已 focus + 弹键盘。落地 `rich-text-editor` 的 `editorProps.handleDOMEvents.touchstart`：仅触屏设备（`COARSE_POINTER`）命中已收录词时，**显式 `event.preventDefault()`**（ProseMirror 的 `handleDOMEvents` 返回 true 不会自动阻止默认行为，已核对 `prosemirror-view` 源码 `runCustomHandler`）——阻止默认 focus + 合成 click，直接 `setCardDetail` 弹卡；命中判断与 `handleClick` 同一套（`posAtCoords` + `frontAtPos` + `collectedCardsRef`）。桌面端鼠标无 touch、不触发这条，仍走 `handleClick`；点普通文字不命中、不 preventDefault、正常编辑。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-imwo89m85-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「normalizeFront 抽到 lib」重构。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **`normalizeFront` 去重归一化抽到 `@/lib/normalize-front.ts` 统一（tsc + build 绿）**。之前三处重复定义（`rich-text-editor` / `ai-assistant-panel` / `convert-to-cards`，逻辑完全一致：去括号读音 + 压空白 + trim），改去重规则要改三处、易漏。落地：新增 `src/lib/normalize-front.ts` 导出 `normalizeFront`（带 `?? ""` 空值兜底），三组件删本地定义、改 import。② 读音格式：手动粘贴的括号读音保持「所见即所得」不动（用户定），AI 链路已统一读音放背面 `读音：`，无需再改。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-qmfbhph8j-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「标识语义拆分定案 + cookies 修复」本轮改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`（首次 `fetch failed`、重试即过）。
- 2026-08-30 **标识语义拆分定案（tsc + build 绿）**：用户澄清「导入笔记加粗」与「进闪卡下划线」是两个不同逻辑，最终定案：**① 导入笔记 → 正面生词加粗**——`importToNote` 拼词条行时把正面 `front` 用 bold mark 写进 JSON（持久、只影响本次导入的行、手写词条不受影响；`docToText` 忽略 mark，转卡/搜索不受影响），撤掉上一轮会话级 `explained` 装饰全套（`explainedTextsRef`/`explainedWordDecorations`/`explainedWordKey`/插件/`.explained-word` CSS）；**② 进闪卡 → 下划线，只标在原文（article）callout 内**——`wordDecorations` 与 `frontAtPos` 加 `inArticleCallout(doc,pos)` 判据（向上找 `callout` 祖先 `kind==="article"`），生词区/正文不标，场景=阅读文章时点词查义；点下划线词弹该词闪卡（`handleClick`，用户定「保持点词弹卡」不改为双击），移动端 tap 同样触发、弹层窄屏已适配。**③ 修复 `cookies was called outside a request scope`**：根因是客户端组件里误调服务端 `listCards`（内部 `cookies()`），改成浏览器客户端 `createClient()` 直接查 `cards` 表并删掉 `listCards` import。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-f9piuhf8i-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「读音格式统一 + 标识持久化锚定闪卡 + 点下划线词看闪卡 + 移除 explained 加粗」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **笔记内页「读音/标识/点开看闪卡」统一（tsc + build 绿）**。用户三点反馈一次落地：① **读音格式统一**——`【读音】xxx` 方括号全改成冒号标签 `读音：xxx`，与「搭配：」「例句：」同款（三处拼装：`rich-text-editor` 的 `collect()` / `importToNote()`、`ai-note.ts` 的 `pointsToNoteContent()`）；② **标识统一为「进闪卡」并支持点开看闪卡**——移除上轮的「已解释加粗」`explained-word` 全套（`explainedTextsRef` / `explainedWordDecorations` / 插件 / `globals.css` 规则 / `importToNote` 里的 `explainedTextsRef.current.add(front)`），标识只锚定「已进闪卡」这一唯一事实源，数据源由会话级 ref 改**持久化**：新增 `collectedCardsRef`（`normalizeFront(front)→卡片`）+ `collectedFrontsRef`（原始 front 集合），挂载 `useEffect` 从 `listCards(noteId)` 拉全量填充并 `dispatch` 刷新装饰；「收录」`collect()` 与「记录」`recordInto()` 两个入库入口成功后都同步更新映射 + dispatch，使两处都能让原文现下划线、刷新重开也仍在；③ **点下划线词弹该词闪卡**——`editorProps` 加 `handleClick`（非拖选、`posAtCoords` 定位、`frontAtPos` 反查命中的 front、`collectedCardsRef` 取卡、`setCardDetail`），新增 `cardDetail` state + 轻量弹层（复用 `CardFront`/`CardBack`，点遮罩或 ✕ 关闭），不碰 `explain`/`recordMode` 的 selectionUpdate 清理逻辑。④ **「记录」不加导入笔记按钮**（用户决定不加）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-ovc4wyufw-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「记录背面输入框改多行 textarea」改动。`vercel deploy --prod --yes`。
- 2026-08-30 **笔记「记录到闪卡」背面输入框由单行 input 改多行 textarea，支持换行写多行释义/搭配/例句（tsc 绿）**。用户要背面能换行（写搭配/例句等标签时背诵页能按标签展示）。落地 `rich-text-editor` 记录弹窗：背面 `<input>` → `<textarea rows={3} resize-none>`，placeholder 改 `释义\n搭配：…\n例句：…`，label 提示「可换行写多行释义/搭配」；换行原样存进 `cards.back`（`recordBack.trim() || null`），`card-back` 按换行识别标签、照常展示。正面仍单行 input 不变。`npx tsc --noEmit` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-d0uqlrceb-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「转卡解析按首次出现的分隔符切正反面」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **转卡解析改「按行内首次出现的分隔符切正反面」，修复「词  释义  搭配：xxx」把「搭配」吸进正面（tsc 绿 + 运行时验证）**。用户反馈：记录 `딸꾹  嗝儿  搭配：딸꾹질하다 打嗝…` 期望正面=딸꾹、背面=嗝儿+搭配标签，实际却切成正面=`딸꾹  嗝儿  搭配`、反面=搭配内容。根因：`parse-cards` 的 `splitLine` 固定优先级「冒号 > 破折号 > 双空格」，非贪婪冒号正则 `^(.+?)\s*[:：]\s*(.+)$` 会跨过前面的双空格、把背面「搭配：」的冒号误当切分点。定案：改「谁先出现谁就是主分隔符」——四个内建分隔符（冒号/破折号带空格/破折号无空格/双空格）各自 match 后按 `m[1].length`（front 原始长度，即分隔符位置）取最小者切。落地 `splitLine` 用 `seps[]` 收集候选 + `reduce` 取 `at` 最小。运行时验证 9 例全对：`딸꾹  嗝儿  搭配：…`→正面 `딸꾹`、`词：一个  很长的  释义`→正面 `词`（冒号先）、`look after：照顾`→正面 `look after`、`well-being  幸福`→连字符词不拆。`npx tsc --noEmit` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-6c1p9rp3z-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「已解释标记加粗 600→700」改动。`vercel deploy --prod --yes`。
- 2026-08-30 **「已解释」标记加粗由半粗 600 改全粗 700（tsc 绿）**。用户要更明显的加粗。落地 `globals.css` 的 `.explained-word` `font-weight:600→700`。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-jh1i35hs5-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「已解释标记改加粗 + 只标词条行行首生词」改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **「已解释」标记改加粗 + 只标词条行行首生词，不再全文匹配（tsc 绿）**。用户澄清：只想把**行首的生词**区分一下，不是后面释义/例句里再出现的同词都加样式；且不要下划线、问加粗/高亮/列表哪个好。定案**加粗**——高亮会跟手动 mark 高亮混淆（之前下划线方案正是为避开 mark）、列表是改结构且用户记笔记不用列表；加粗最干净、半粗 600 减重排。落地：`explained` 装饰不再复用 `wordDecorations`（全文匹配），新增 `explainedWordDecorations`——遍历到「文本块的首个文本节点」（`$p.parent.isTextblock && $p.parentOffset===0`），判该块开头是否精确命中某个 front 且词后是「空 / 全半角冒号 / 破折号 / 双空格」（与 `resolveWordBlock` 判据一致，避免误标句首词），命中则只给行首那段 front 加 `explained-word`；`globals.css` 的 `.explained-word` 由点状下划线改为 `font-weight:600`。`collected`（收录到闪卡）仍走 `wordDecorations` 全文匹配原文选中词、不变。`npx tsc --noEmit` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`nestlingo-2p04rz6vv-muriel-s-project.vercel.app`，READY，Production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖「解释→导入笔记过的词加区分样式 + 解释覆盖判据加回词后内容」两处改动。`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-30 **「解释→导入笔记」过的词加区分样式（点状下划线），与「已收录到闪卡」的实线下划线区分（tsc 绿）**。用户想一眼看出哪些词已经点过「解释」覆盖过、哪些没解释过。落地 `rich-text-editor`：复用已有「已收录」装饰（`collectedTextsRef` + `CollectedWords` 插件 + `collected-word` 实线淡下划线）那套机制，新增平行的 `explainedTextsRef`（存 front 原形）+ `explainedWordKey` 插件，`importToNote()` 一开始就 `explainedTextsRef.current.add(front)` 标记本会话导入过的词；把原 `collectedWordDecorations` 泛化成 `wordDecorations(doc, words, className)`（两个装饰共用，去重复循环），扩展改名为 `WordDecorations`、`addProseMirrorPlugins` 里挂两个 Plugin（collected / explained）。`globals.css` 新增 `.tiptap .explained-word`：`underline dotted` + 72% 品牌色，与 `collected-word` 的 42% 实线区分（都不改笔记 JSON，纯 Decoration）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **解释覆盖加回「词后内容」判据：避免句首词被误覆盖（tsc 绿）**。用户追问：句子的第一个词也满足「词在行首」，点解释会覆盖整句、怎么避免。定案：覆盖除「词在行首」外还要看词后——词后是「空 / 释义分隔符（`：:—–-`）/ 双空格」才算词条行并覆盖；词后是「单空格 / 直接跟字」（句首词：英语 `Contact is`、韩语 `연락은`、中文 `联系很`）则不覆盖、走「追加到生词区」。落地 `resolveWordBlock` 判据加回 `after` 检查（`after.trim()==="" || /^[：:—–\-]/ || /^\s{2,}/`），保留上一条的 textblock 定位 + article callout 排除。`npx tsc --noEmit` 全绿。
- 2026-08-30 **解释「导入到笔记」覆盖规则简化：词在行首即覆盖，去掉词后内容判断（tsc 绿）**。用户反馈：词后面什么都没有（裸词）、只想让 AI 直接解释，担心又新建 callout；并提议把规则再简化。定案：① 选中词在**行首**（词前无字）→ 覆盖这一行，与位置无关（普通正文 / 生词 callout / 标题下），**不再判断词后内容**（「词」「词：释义」「词 释义」「词  释义」都覆盖，单/双空格不再区分）；② 选中词**夹在句中**（句子里选词）→ 不覆盖，走「追加到生词区」（有生词 callout 进 callout、有「生词」标题进标题、都没有新建 callout）；③ **原文（article）callout 内**选中词 → 不覆盖，同样走「追加到生词区」（把词从原文抽出来，不破坏原文）。落地 `rich-text-editor` 的 `resolveWordBlock`：向上定位文本块（`isTextblock`，paragraph/heading）→ 祖先命中 article callout 则 `return null` → 词前有字则 `return null` → 否则返回该块 range 覆盖。此条取代上一条「恢复双空格」的窄判据。`npx tsc --noEmit` 全绿。
- 2026-08-30 **解释「导入到笔记」恢复「词 + 双空格 + 释义」词条行覆盖（tsc 绿）**。用户反馈：写生词时词后跟两个空格再写释义（如「연락  联系」），选中词点解释→导入，结果又新建 callout、没覆盖原词行。根因：上一条「覆盖判定简化」把 `resolveWordBlock` 的双空格判据（`\s{2,}`）当作误判风险去掉了，正好漏掉「词后双空格接释义」这种词条写法。落地：判据恢复 `|| /^\s{2,}/.test(after)`——选中词在行首、且词后为空 / 跟释义分隔符（`：:—–-`）/ 跟双空格分隔的释义，都视为词条行并覆盖这一行（与位置无关）；「句子里选词」（词前有字）仍不覆盖、照旧新建 callout。权衡：恢复双空格会让「词 + 双空格 + 句子」这种罕见写法也被当作词条覆盖，但用户实际记笔记用的是「词 + 双空格 + 释义」，故保留。`npx tsc --noEmit` 全绿。
- 2026-08-30 **Vercel 生产部署成功（`dpl_cGBePAQ34JjU3FjSXMpFMUZawNf3`，READY，production，主域名 `nestlingo-psi.vercel.app` 已 alias）**：覆盖本轮「解释链路覆盖判定简化 + 句子里选词恢复新建 callout」等改动。`NODE_USE_ENV_PROXY=1 npm run build` 本地门禁绿；首次 `vercel deploy --prod --yes` 偶发 `Not authorized`、重试一次即过。
- 2026-08-30 **解释链路覆盖判定简化：覆盖条件统一为「词条行」（与位置无关），消除两类误覆盖风险（tsc 绿）**。部署前审查发现两个风险：① 原 `resolveWordBlock` 的「word callout 内 / 「生词」标题下」位置分支只要在生词区就覆盖整行、不检查是否词条行，若用户在生词区写了句子、选中句子里词会整行被替换成「词：释义」；② 词条行判据里的 `\s{2,}` 空格分隔会把「词（双空格）句子」误判成词条（韩语/英语等空格分词语言里句中词后可能有两个空格）。落地：`resolveWordBlock` 简化为单一判据——选中词在行首、且词后为空或跟明确释义分隔符（`：:—–-`）才算词条行并覆盖这一行，与位置无关（callout 内/标题下/裸段落同一规则）；去掉空格判据。`npx tsc --noEmit` 全绿。
- 2026-08-30 **解释链路恢复「句子里选词 → 新建生词 callout」，最终定案（tsc 绿）**。用户澄清场景：写了一句话/长句子，选中句子里一个词点解释→导入，这种情况应该新建生词 callout（把词从句子抽成独立生词条目），问是否规则里该保留。核查：上一轮为「统一」把解释链路的「都没有 → 文末新建 callout」误删成「文末普通段落」，正好把「句子里选词」这个合理场景也删了。恢复 `rich-text-editor` 追加分支 3 为「新建生词 callout」（`insertContent({type:"callout", attrs:{kind:"word"}, content:[paraNode]})`）。**最终定案（解释链路）**：① 选中词所在行是词条（word callout 内 / 「生词」标题下 / 裸段落词在行首）→ 覆盖这一行；② 否则（句子里选词）→ 追加到生词区：有生词 callout 进 callout 末尾、有「生词」标题插标题区、都没有则新建生词 callout。语伴链路仍按此前定案（无选区：词面去重覆盖 + callout→标题→文末裸段落，都不建 callout）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **「解释」「语伴」加入笔记放置规则统一：解释链路去掉「文末新建 callout」、改文末普通段落（tsc 绿）**。用户指出：解释链路整理出来的「都没有 → 文末新建生词 callout」不对——没有标题、没有 callout 时应该直接覆盖选中词这一行；且「解释」和「语伴」两条加入笔记规则要统一。落地 `rich-text-editor`：① `resolveWordBlock` 词条行判据放宽——选中词在行首、且词后为空 / 跟释义分隔符（`：:—–-`）/ 双空格，都算词条行并覆盖这一行；② 追加分支 3「文末新建生词 callout」改为「文末追加普通段落」（`insertContent(paraNode)`，不包 callout），与语伴 `addToNote` 分支 c「裸段落落文末」对齐；③ 顶部注释同步。两条链路统一后放置规则：覆盖优先（解释按选区、语伴按归一化词面去重）→ 有 callout 进 callout → 有标题进标题区 → 都没有则文末追加普通段落（**都不新建 callout**）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **「解释」气泡：无 callout/无标题时也覆盖选中词所在行（tsc 绿）**。用户反馈：写了一个词（去掉了「生词」标题、就是普通段落里单独一行），选中点解释→导入，结果在文末新建了 callout，而不是覆盖这一行。根因：`importToNote` 的 `resolveWordBlock()` 只覆盖「word callout 内」和「生词标题下」两种情况，其余一律走「追加 / 新建 callout」。落地：`resolveWordBlock` 新增第 3 分支——既不在 callout、也不在标题下时，判这一行本身是不是「生词词条」：选中词在行首（`before.trim()===""`）且词后为空或跟释义分隔符（`/^[：:—–\-]/`），就返回该段落 range 覆盖，否则仍走追加。于是「词独占一行」「词：释义里选中词」都覆盖，「长句子里选中词」不覆盖（避免破坏原文）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **AI 语伴「加入笔记」生词读音统一放背面（tsc 绿）**。用户问：为什么语伴加入笔记的词，读音又出现在词后 `（读音）` 里、之前不是改过吗？答：上一条「读音统一放背面」只改了「解释」气泡的 `importToNote()` 与「收录到闪卡」`collect()`，**漏了 AI 语伴这条链路**——它走的是 `ai-note.ts` 的 `pointsToNoteContent()`，词分支仍 `front = 原词 + （读音）`（把读音拼进正面）。落地：`pointsToNoteContent` 生词分支正面只留 `clean(w.front)`，背面改为 `[释义(多义项折顿号), 【读音】reading].filter(Boolean).join("\n")`——`itemLine` 把 `\n` 折双空格、转卡时 `parseCards` 拆回换行，与「解释」按钮的背面 `[词性] 释义\n【读音】` 同款格式；`companion-chat` / `assistant.ts` 两个调用方（默认 `list:true`）随之自动生效。`npx tsc --noEmit` 全绿。
- 2026-08-30 **AI 语伴「加入笔记」改回「解释」按钮同款放置规则：callout → 标题 → 文末段落（tsc 绿）**。用户明确要的不是上一版的「重建整篇 callout」，而是与「解释」气泡「导入到笔记」一致的放置规则：有对应 kind 的 callout 就加进 callout 里，没有 callout 但有「生词/例句/语法」标题就加在标题下，两者都没有就直接以普通段落追加到文末（**不新建 callout**）。同时保留「按归一化正面去重覆盖」和「用普通段落不用列表」。落地：**① `parse-sections`** 导出 `EXAMPLE_TITLES`/`GRAMMAR_TITLES`（`WORD_TITLES` 已有）；**② `ai-note`** `pointsToNoteContent` 加 `opts?: { list?: boolean }`（默认 `true`，`companion-chat`/`assistant.ts` 既有调用不变），把 `item()` 的行拼装提成 `itemLine()`、新增 `paraItem()`，`list:false` 时 word/example/grammar 分支直接产段落而非 `bulletList`；**③ `ai-assistant-panel`** 重写 `addToNote()`——按 `["word","example","grammar"]` 逐个 kind 独立放置，只动目标 callout（`content[idx].content = inner`，保留 attrs）/ 目标标题区（`splice` 插到下一个 heading/callout 前）/ 文末（`push(...inner)` 裸段落），原文仍以 callout 追加末尾，不再整篇重建。`npx tsc --noEmit` 全绿。
- 2026-08-30 **修「加入笔记」把 callout 之外的正文吸进生词 callout（tsc 绿）**。用户反馈：语伴给的词点「加入笔记」后，笔记里不在生词 callout 里的内容也被搬进了生词 callout。根因：`addToNote`（及 `notePointsOf`）用 `parseNote(docToText(整篇))` 抽现有卡片——`parseNote` 是纯文本按行切分，一个 callout 之后、直到下一个标题/分割线之间的正文段落会被误归进上一个 callout 区域，于是「生词 callout 之后的普通段落」被当成生词卡、重建时写回生词 callout。落地：新增 `existingCardsOf(editor)` 走节点结构——只遍历顶层 `callout` 节点（kind ∈ 生词/例句/语法），对每个 callout 单独 `docToText({content: node.content})` 序列化其**子内容**（不含标签行）再 `parseCards(..., {bareToCard:true, scope:"callout"})`，callout 之外的正文段落根本不参与；`addToNote` 与 `notePointsOf` 都改用它，删掉对 `parseNote` 的依赖（改 import `parseCards`）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **AI 语伴「加入笔记」智能合并，删掉「覆盖原始笔记」按钮（tsc 绿）**。用户反馈：给语伴发一个笔记里没有的新词，返回仍带「覆盖原始笔记」按钮、点了还提示覆盖，但笔记里根本没这个词、无从覆盖。定案**合并进「加入笔记」**——一个按钮「有则覆盖、无则追加」。落地 `ai-assistant-panel`：① 新增本地 `normalizeFront`（去括号读音+压空白，同转卡去重逻辑）；② 重写 `addToNote()`——解析当前笔记卡片，对每个勾选点按「kind + 归一化正面」匹配：命中的覆盖原行（富化版替换薄的），未命中的追加；原文（article）不覆盖、照旧追加到末尾；匹配由旧 `replaceOriginal` 的**精确 front** 改为归一化（修复「연락（열락）」「연락」因读音后缀不匹配导致覆盖失效的隐患）；反馈文案按「覆盖 X 项 / 新增 Y 项」区分；③ 删掉 `replaceOriginal()` 与「覆盖原始笔记」按钮（其「补全富化」场景现由「加入笔记」同一入口完成）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **生词/例句/语法 callout 配色改浅米低饱和耐看（tsc 绿）**。用户反馈生词 callout 要换浅浅米色系、例句与语法也挑更浅更耐看的背景。定稿配色：**生词 = 米色**（背景 `#f6f0e4` / 边框 `#e6d9c0` / 标签 `#8a6b3a`）、**例句 = 鼠尾草绿**（背景 `#eef4eb` / 边框 `#d9e4d1` / 标签 `#4a7a5d`）、**语法 = 薰衣草紫**（背景 `#edeaf6` / 边框 `#dcd7ec` / 标签 `#6a5fc0`），原文 callout 灰不变。落地四处：`globals.css` 的 `.tiptap .callout` 与 `.share-content .callout` 两个规则块同步换新背景/边框/标签色；`callout-extension.ts` 的 `KIND_META` 标签色由 `#9a7328`/`#2b795c`/`#5a4ac0` 改为 `#8a6b3a`/`#4a7a5d`/`#6a5fc0`（编辑器/分享图标签用）；`rich-text-editor` 工具栏 `calloutsInline` 图标色同步三处。`npx tsc --noEmit` 全绿。
- 2026-08-30 **解释「导入到笔记」：选中的词若在生词区域内就覆盖那一行，否则照旧追加（tsc 绿）**。用户提：在「生词」标题下重解释一个已有生词，导入时应直接覆盖原那一行、别再多插一行。落地 `rich-text-editor`：`explain` 状态加 `from`/`to`（选区位置）；`importToNote()` 新增 `resolveWordBlock()`——用 `editor.state.doc.resolve(explain.from)` 定位选中词所在段落块，判两点：① 向上找祖先，命中 `callout` 且 `attrs.kind==="word"` 即在生词 callout 内；② 否则取顶层节点 `$p.index(0)` 往前找最近的 heading/callout，命中 `WORD_TITLES`（生词表/生词/词汇…）即在「生词」标题下。命中则 `deleteRange({from,to})+insertContentAt(from, paraNode)` 整体替换那一行（段落），否则走原有三处追加逻辑不变。`npx tsc --noEmit` 全绿。
- 2026-08-30 **转成闪卡每卡加「AI 解释」+ 补全去空行 + 解释落笔记改段落 + 韩语音变读音（tsc 绿）**。用户四点：① 「转成闪卡」只有批量「AI 补全」、想要每张卡也有单卡「AI 解释」（同详情页）；② 补全后背面标签之间空一行没必要；③ 笔记内「解释→导入到笔记」落成无序列表、用户习惯不用列表；④ 韩语读音想要实际发音（音变），发音与拼写相同就不写。落地：**① 单卡 AI 解释**——`convert-to-cards` 的 `CardDraft` 加 `onExplain`/`explaining`/`explainError` 三 prop，`textarea` 下方加紫「AI 解释」按钮（`Sparkles` 图标，文案 loading 变「AI 解释中…」，`disabled={explaining || !front.trim()}`）；父组件新增 `explainingKey`（`"si:ci"`，一次一张）+ `explainErrors`（按 `si:ci` 存红字）+ `explainCard(si,ci)`（调 `/api/ai/card-explain`、成功 `updateCard(si,ci,"back",explanation)`）；批量 `fillAll` 不动。**② 去空行**——`card-explain` 路由把 `const explanation = text.trim()` 改为 `text.split(/\n+/).map(trim).filter(Boolean).join("\n")`，折叠掉模型自发吐的 `\n\n`，四个消费端同时生效。**③ 落笔记改段落**——`rich-text-editor` 的 `importToNote()` 不再用 `item()`（`item()` 仍被 ai-note 多处用，保留），改内联拼 `line`（`\n`→双空格）并构造 `{ type:"paragraph", content:[{type:"text",text:line}] }`；三处插入改段落：callout 内 `content.push(paraNode)`、标题后插到 word 区域末尾（扫到下一 heading/callout 前 `splice`）、文末 callout `content:[paraNode]`；删掉文件里已不用的 `item` import。`docToText` 对段落与列表输出纯文本一致，转成闪卡识别不受影响。**④ 韩语音变读音**——`card-explain` word 卡读音要求与 `explain` 路由 `phonetic` 字段，韩语都由「罗马转写/罗马字」改成「给韩文实际发音——仅当有音变、实际发音与拼写不同时（如 연락→열락、국물→궁물）才写，相同时留空」；泰语罗马转写、英语 IPA、日语中文不补均不动。`npx tsc --noEmit` 全绿。
- 2026-08-30 **修「解释」已导入状态残留 + 读音统一放背面（tsc 绿）**。用户报两点：① 笔记里解释词 A 并「导入到笔记」后，换词 B 再点解释，气泡按钮仍显示「已导入 ✓」（`importedToNote`/`importingToNote` 没随选区变化重置，`collected` 有重置、它俩漏了）；② 读音位置不一致——「收录到闪卡」「AI 补全」都把读音放背面（`【读音】`/`读音：` 行），唯独「导入到笔记」把读音拼进正面 `（读音）`。定案**统一放背面**。落地 `rich-text-editor`：**① 状态残留**——`editor.on("selectionUpdate")` 的 `update` 里三处 `setCollected(false)`（`from===to` 分支 / 超长分支 / 正常分支）旁边各补 `setImportedToNote(false)` + `setImportingToNote(false)`，换词/取消选区即回「导入到笔记」未导入态；**② 读音统一**——`importToNote()` 删掉 `const phonetic = …（读音）` 与 `front = baseForm（读音）`，正面只留 `result.baseForm`，背面改为 `[词性] 释义\n【读音】phonetic` 数组 `.filter(Boolean).join("\n")`（`item()` 把 `\n` 折成双空格、转卡时再由 `parseCards` 拆回换行，与 `collect()` 的 `【读音】` 行格式一致）。`npx tsc --noEmit` 全绿。
- 2026-08-30 **完形填空改回「用 AI 造例句」按钮 + 识别规则优化（只挖目标词本身）+ 去掉「今日目标」提示（tsc 绿）**。用户澄清上一版「完形填空完全交给 AI 自动出题」理解偏了：没有例句标签的才手动 AI 造例句、可不作为题目，改回原来的「用 AI 造例句」按钮。落地：**① 撤销 AI 自动出题**——`cloze.ts` 恢复 `buildClozeItems`（先用现成语境句规则出题 + 无例句进 `missing`），`cloze-session` 恢复 `missingWords` + 「用 AI 造例句」按钮（点才调 AI、省 token）；`backMeaning` 选择题修复保留。**② 识别规则优化（针对自建笔记）**——正面先 `clozeWord` 去掉「(读音)」括号再分类/匹配/挖空：`วิเคราะห์(wí-krór)`、`แผนแพร่ (pôjie prâe)` 这类带读音词不再被误判成句子、也不把读音挖进空里；生词判定 `isWordCard`（kind=word，或未分类且去掉括号后仍单 token）、句子池 `isSentenceCard`（kind=example，或未分类且去掉括号后仍多词）；**去掉括号后仍是多词的短语/搭配 → 进 missing 交给 AI 只挖核心词**（不把整个搭配挖空）。**③** `cloze-examples` 路由保留 prompt 增强（只把最核心那一个单词用 ⟦⟧ 包起来）+ 接受 `meaning`。**④ 去掉完形填空头部「今日目标 N 道（共 M 道，其余下次）」**（用户：测试题不需要 20 题目标提示；截断逻辑 `slice(0, dailyGoal)` 保留，仅去展示文案）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **选择题选项只显示释义 + 完形填空交给 AI 只挖目标词（tsc 绿）**。用户反馈三点：① 选择题选项里混进了例句/拓展；② 完形填空把整个搭配词组挖空了、只想挖要背的那个词；③ 手写数据里句子/词组难区分，完形填空让 AI 完成。落地：**① 选择题**——`card-back` 的 `backMeaning` 原来 `return text || back.trim()`，当背面只有例句/拓展、抽不出任何释义时会回退成整段背面（把例句/拓展当答案混进选项）；改为「有标签但无释义 → 返回空串」，该卡不再参与出题（`buildQuestions` 的 `.filter(Boolean)` 自动排除），无标签的纯自由文本仍整段当释义。**②③ 完形填空 AI 化**——`cloze.ts` 删掉规则化句子池（`isSentenceLike`/`matchIndex`/`pickSentence`/`splitWordBack`/`buildClozeItems`），改 `collectClozeWords` 只收集生词卡（`{cardId, word, meaning, lang}`，`clozeWord` 先去掉「(读音)」括号、`wordMeaning` 只取释义）；`cloze-session` 进入即自动调 `/api/ai/cloze-examples`（loading「正在用 AI 出题…」+ 失败重试），去掉「用 AI 造例句」手动按钮与 missing 流程；`cloze-examples` 路由增强——prompt 加「只把最核心、最该背的那一个单词用 ⟦⟧ 包起来，短语/搭配只包其中一个词、依据词义判断，不要包整个短语」，并接受 `meaning` 传入模型帮助判断核心词。`npx tsc --noEmit` 全绿。
- 2026-08-29 **再部署 Vercel 生产成功（`dpl_BjQMP5ZTEChHgkzU49tLGqXeLCii`，READY，production）**：覆盖「原文 callout 翻译补上标题 + 列表翻译」两处改动，`NODE_USE_ENV_PROXY=1 npm run build` 绿后 `vercel deploy --prod --yes`。
- 2026-08-29 **「原文」callout 翻译补上小标题：标题（heading）块也会被翻成中文并显示在标题下方（tsc 绿）**。用户反馈：笔记内页「原文」callout 粘贴的文章，别的段落都翻了、独独简短标题没翻。根因：`translateCallout`（`src/lib/translate-apply`）逐块重建时**只把 `paragraph` 当「可翻译块」**，其它任何块（标题/列表/引用/图片）一律 `kind:"keep"` 原样保留、根本不发翻译请求；而文章标题从网页/富文本粘贴进来常被存成 `heading` 节点，于是标题被跳过。与长度无关——`/api/ai/translate` 只过滤 `p.length > 0`，无最短长度限制，所以「简短标题没翻」其实是「标题是 heading、不是 paragraph」的表征。落地：`translateCallout` 的判断由 `child.type.name !== "paragraph"` 改为新增 `isTranslatableBlock`（`paragraph` + `heading` 都可翻）；重建函数 `toTidyTextPara` 改为 `toTidyTextBlock`——保留原块类型与 attrs（段落照旧、标题保留 level），内容统一替换为整理后的纯文本；译文仍作为带 `translation` mark 的独立 `paragraph` 插回原块**正下方**，标题下即浮现一行中文。**追加：列表也要翻**（用户确认）——`bulletList` / `orderedList` 整条重建，翻译逐个落在**每个列表项内部**（首段清成原文纯文本 → 旧译文丢弃 → 紧跟译文段；嵌套列表等子块原样保留），已译项重翻时只取非译文段落文本、不混进旧译。引用/图片/表格等仍维持不翻、原样保留。`npx tsc --noEmit` 全绿。
- 2026-08-29 **Vercel 生产部署成功（`dpl_DKDa4gshJ4jkx5BdGRVczz2BEtvc`，status Ready，主域名 `nestlingo-psi.vercel.app` 已挂该次部署）**。部署前核验：① 生产库关键列已全部存在——用 anon key 探测 `cards.lang`/`cards.reading`/`notes.cards_title`/`material_collections.lang`/`materials.collection_id` 均 `PRESENT`（新代码依赖的列无缺口，之前「需人工跑 schema」项解除）；② `NODE_USE_ENV_PROXY=1 npm run build` 本地门禁绿；③ `vercel deploy --prod --yes` 成功（首次 `--prod` 无 `--yes` 被服务端拒 `Not authorized`，补 `--yes` 即过，账号 murielxyz 对 `muriel-s-project/nestlingo` 有权限）。✓ 已部署。
- 2026-08-29 **背诵页头行删掉「今日目标 N 张（共 M 张，其余下次）」：只留「已学 X · 待学 Y」+ 词库（tsc 绿）**。用户反馈背诵页头部 已学/待学/今日目标/词库 挤在一行太满，问「今日目标」有没有必要留。定案：删掉。`review-session` 头部右侧只保留「词库」按钮（「今日目标」那行本就只在「合集卡数 > 每日目标」时出现，是被每日目标截断的提示；截断逻辑 `sessionItems = items.slice(0, dailyGoal)` 保留，仅去掉展示文案）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **转卡读音规则改为「所见即所得」：不再自动抽取/搬移读音，正反面按输入原样保留（tsc 绿）**。承接用户对读音写法的疑问——旧规则会在转卡时自动抽走正面括号/括号读音、并固定塞到背面「【读音】」第二行（还因 `card-back` 的释义居中被放到释义下方），多种格式下结果不稳、且违背「`วิเคราะห์(wí-krór)` 就应正面原样」的直觉。定稿**所见即所得**：正反面完全按输入保留，只在明确分隔符（双空格 / — / ： / Tab）处切正反面，读音留在写的那一侧（跟在词后→正面，跟在释义后→背面），所有语言同一套规则、无意外。落地：① `ParsedCard` 删 `hint` 字段；② 删 `extractParenHint`/`extractReadingByScript` 及 `applyReading` 的【】搬移；③ `makeCard` 改为原样返回；④ `parseTable` 的「读音」列拼回正面成「词 (读音)」；⑤ 结尾 map 只做背面 `normalizeBack`。运行时验证：`แผนแพร่ (pôjie prâe)  发表`→正面`แผนแพร่ (pôjie prâe)`/背面`发表`；`วิเคราะห์(wí-krór)  分析`→正面`วิเคราะห์(wí-krór)`/背面`分析`；`ปรับปรุง  调整，提升[bprâp bprung]`→背面`调整，提升[bprâp bprung]`；`คุณภาพ  质量【kun-ná-pâap】`→背面`质量【kun-ná-pâap】`。与 AI 生成的 `cards.reading` 字段互不影响（那是另一条链路）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **闪卡侧栏编辑表单 AI 按钮样式 + 文案统一（参照闪卡详情页 `card-detail` 编辑表单同款，tsc 绿）**。用户反馈笔记内闪卡栏编辑表单的「AI 补全」原做成朴素文字链接、易误触；照详情页样式改成 `inline-flex items-center gap-1.5 rounded-lg border border-violet-200 px-3 py-2 text-xs font-medium text-violet-600 hover:bg-violet-50 disabled:opacity-60`，带 `Sparkles` 图标；随后用户要求去掉 ✨ 前缀、且文案与详情页统一，按钮文字改「AI 解释」/「AI 解释中…」（aria-label「AI 解释背面」），相关注释与错误文案「解释失败」同步。`npx tsc --noEmit` 全绿。
- 2026-08-29 **「记录」去重命中不自动收起改灰提示/按钮「已存在 ✓」+ 收录去重统一走 normalizeFront + 闪卡侧栏每张卡编辑加 AI 补全（tsc 全绿）**。承接上一条「记录成功自动收起」，用户又四点：**① 已在闪卡里去重命中不要自动消失，用户手动 ✕ 关**——`rich-text-editor` 的 `recordInto` 去重分支已 `return` 不调 `closeRecordSoon()`；**② 该分支按钮文案改「已存在 ✓」、错误色改淡灰**——表单底部改 `{recordDup ? <p className="text-xs text-zinc-400">这个词已经在闪卡里了。</p> : recordError ? <p ...text-red-600> : null}`，按钮 `recording ? "存入中…" : recorded ? (recordDup ? "已存在 ✓" : "已存入 ✓") : "存入闪卡"`；选项框「记录」onClick 加 `setRecordDup(false)`（再开表单时清掉去重态）。**③ 统一去重走 normalizeFront**——`collect()`「收录到闪卡」原来用 `eq("front", result.baseForm)` 精确比对（收录带括号读音的变体可能漏判/误判），改为同 `recordInto`：一次 `select("front")` 拉全合集、`normalizeFront`（去括号读音+压空白）Set 比对，同词绝不重复。**④ 闪卡侧栏每张卡编辑加 AI 补全**——`card-sidebar` 编辑表单底部新增「✨ AI 补全」（`Sparkles` 图标）：调 `/api/ai/card-explain`（`{front, back, kind}`）用正面+已有背面生成更完整解释、覆盖到背面输入框（可再改后保存）；新增 `aiBusy/aiError` 状态（补全中禁用、失败红字），`startEdit` 时清 `aiError`；与「转成闪卡」的 `fillAll` 同款接口、不再另开 AI。`npx tsc --noEmit` 全绿。
- 2026-08-29 **精读「记录」成功后自动收起弹窗 + 去掉取消按钮 + 转成闪卡也通知侧栏刷新 + 收录到闪卡排到合集末尾（tsc 全绿）**。用户反馈：记录到闪卡成功后还得手动点「取消」才关掉弹窗，希望成功即自动消失、并把取消按钮去掉。落地 `rich-text-editor`：① 新增 `recordCloseTimerRef` + `closeRecordSoon()`——`recordInto` 成功后（含「已在闪卡里」去重命中）`setRecorded(true)` 后再延迟 0.5s `setRecordMode(false)` 自动收起，让「已存入 ✓」闪一下再消失；② 表单底部去掉「取消」按钮，只留「存入闪卡」主按钮（右上角 ✕ 仍可手动关）；③ 卸载时清掉该定时器。另补一致性：`convert-to-cards.save()` 入库成功后同样 `dispatchEvent("ln-cards-changed")`，让开着闪卡侧栏时「转成闪卡」入库也能即时刷新；`collect()`「收录到闪卡」原来写死 `position:0`（收录的词插到**最前**），与「记录」的 `max+1`（排到末尾）不一致，现改为同样取 `max+1`，两种入口加卡都落在合集尾部。`npx tsc --noEmit` 全绿。
- 2026-08-29 **收口：拖拽方向修正 + 记录背面落库 + 侧栏即时刷新 + 撤销笔记区收起 + 核实合集改名链路（tsc 全绿）**。用户实测上一版侧栏/笔记区布局后报告五点：① 网页端拖拽调宽度方向反了；② 笔记区收窄箭头也反了，干脆把「收起」撤掉、只留拖拽；③ 「记录」背面的含义录入后右边闪卡没及时更新，且那张卡没有背面的文字；④ 手机端的记录/解释选项别跟原生选中菜单打架；⑤ 闪卡合集改名仍在同步原笔记标题。逐条落地：**① 拖拽方向**——`use-panel-resize` 规则统一为「把手永远跟手（拖到哪边界就到哪）」：右侧闪卡栏（`card-sidebar`，把手在左缘）`flip` 改 `false`（往右拖 → 边界往右 → 变窄）；左侧两栏（`notes-workspace` 文件夹/笔记列表，把手在右缘）`flip` 改 `true`（往右拖 → 变宽）；hook 顶注与 dir 行注释同步更正（原注释与代码方向相反，正是反的根因）。**② 撤销笔记区收起**——`notes-workspace` 删掉 `listCollapsed` 状态、中间列的条件渲染与窄「»」展开条、以及两栏收起状态的 localStorage 持久化与 `toggle*Collapsed`；文件夹栏与笔记列表栏恢复「仅 ≡ 收起/展开文件夹（进笔记自动收起）」的原行为，仅保留栏宽拖拽（两个 `usePanelResize` 照旧）。**③ 记录背面没存进去 + 侧栏不刷新**——`rich-text-editor` 的 `recordInto()` 插入 `cards` 时 `back` 被误写为 `back: null`（背面含义丢了），改回 `back`；成功插入后 `window.dispatchEvent(new CustomEvent("ln-cards-changed"))`，`card-sidebar` 监听该事件立即 `load()` 重拉（不再需要关了再开）；`collect()`「收录到闪卡」成功处也补发同事件。**④ 手机端选项框**——确认 `COARSE_POINTER`（粗指针）分支已把「记录/解释/复制」气泡放选区**下方**（原生菜单在选区上方），两者不叠，无需再改。**⑤ 合集改名**——核查 `cards-view`/`note-cards-tabs` 的 `commitRename` 均按 `source_type==='cards'` 分写 `title`/`cards_title`；两个纯卡文件入口（`add-cards-button`/`companion-chat`）建的空笔记才标 `source_type='cards'`，带正文的笔记恒为 null → 走 `cards_title`、不碰 `title`。用 anon key `select("cards_title")` 实测线上库**该列已存在**，改名链路正确；若仍见同步，多为改的是纯闪卡文件（其标题即合集名、无独立原笔记）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **侧栏布局优化：闪卡侧栏可搜索/末尾追加/拖调宽 + 笔记区两栏可拖宽/收起 + 修「记录」弹窗输入框点不进去（tsc 全绿）**。用户精读对照原文与闪卡列表，提三点：① 闪卡侧栏想按顺序往下记，添加放列表末尾、加搜索；② 右侧闪卡栏固定 380px，桌面想自由拖宽/收窄；③ 笔记区左侧文件夹栏 + 笔记列表栏想可拖调宽、可收起。另报告「记录」弹窗正/背面输入框点不进去。落地：**新增 `src/lib/use-panel-resize.ts`**——可复用拖拽调宽 hook（`nestlingo:` 前缀 key + 挂载后 useEffect 读回填 hydration 安全 + clamp min/max + `flip` 区分左右栏拖拽方向 + 拖拽期间 body `user-select:none`，仓库原无任何面板拖宽实现）。**① 闪卡侧栏 `card-sidebar`**——删除顶部「＋ 添加闪卡 / 转成闪卡」对（转成仍由笔记页 ⋯ 菜单提供），`onConvert` prop 一并去除；「＋ 添加闪卡」移到**列表末尾**（追加式），点击展开原有 粘贴→识别→整批入库 面板；头部加放大镜图标 toggle 出一行搜索框（复用 note-cards 搜索样式，按 `front` + `(back ?? '')` 过滤，与 kind tab 同时生效）；`aside` 由 `md:w-[380px]` 改为 `style={{ width }}` + `relative` + 左侧贴壁拖拽把手（`flip:true`，默认380 最小260 最大600，key `nestlingo:cards-sidebar-width`）。**② 笔记区 `notes-workspace`**——文件夹栏（`nestlingo:folders-width` 默认208 最小160 最大320）、笔记列表栏（`nestlingo:notes-list-width` 默认288 最小224 最大420）各改 `style={{ width }}` + 右侧拖拽把手（`flip:false`）；两栏均可一键收起且持久化（`nestlingo:folders-collapsed` / `nestlingo:notes-list-collapsed`），文件夹栏仍保留进笔记自动收起，笔记列表栏收起后被一个窄的纵向「»」展开条代替。**③ 修「记录」输入框 bug**——`rich-text-editor` 记录弹窗根 div 原来 `onMouseDown={(e)=>e.preventDefault()}`（本是为气泡按钮保住选区），这会挡住 input 聚焦、导致正/背面打不了字；已从记录弹窗容器移除该 handler（选项框按钮仍保留、不影响 text 选区），输入框恢复正常。`npx tsc --noEmit` 全绿。
- 2026-08-29 **精读「记录」快速收录：选中即弹选项框（记录/解释/复制），记录直接存进闪卡合集（tsc 全绿）**。用户反馈精读遇到生词要反复上拉回原文、复制再下拉到生词区粘贴，上下拉太麻烦，且不想太依赖解释气泡。方向敲定为像手机备忘录那样：**选中后浮出选项框**（记录/解释/复制），点「记录」弹出轻量小表单直接存卡。落地 `rich-text-editor`：① 原单个「✨ 解释」胶囊改成三项选项框——**记录**（进小表单）/ **解释**（跑 AI，走原「导入到笔记 / 收录到闪卡」逻辑不改）/ **复制**（`navigator.clipboard.writeText`，按钮标签短暂变「已复制 ✓」）；② 新增 `recordMode` 等状态，点「记录」预填正面为选中文本（可改，因为有些不是原形）、背面留空可填；③ 小表单含类别切换（生词/例句/语法）+ 正面 `recordFront`（可编辑）+ 背面 `recordBack`（可选，留空则只存正面）+ 存入闪卡/取消；④ 新增 `recordInto()`——按 `note_id` 归到本笔记合集，**去重**（复用转卡逻辑 `normalizeFront`：正面去掉括号读音/空白后比对，已有该词就不再插、提示「已经在闪卡里了」），`position` 取本笔记最大+1 排到合集末尾；`kind=记录类别`、`lang=detectCardLang({front})`；⑤ 选区变化（或消失）时自动关闭记录态/复制态，不打断阅读。生词区仍由用户手动做（本次「记录」不写入笔记正文，直接进闪卡合集），转卡去重逻辑已有、无需另改。`npx tsc --noEmit` 全绿。
- 2026-08-29 **闪卡合集改名不再同步原始笔记标题（tsc 全绿）**。用户反馈在闪卡页/合集卡改合集名会把原笔记标题也改了。根因：数据模型里「闪卡合集」就是一篇笔记——卡片按 `note_id` 挂载，合集标题即 `notes.title` 这一个字段（没有独立 collections 表），改名合集 `notes.update({ title })` 改的就是笔记标题本身。落地：给 `notes` 加可空 `cards_title` 列（幂等 `alter ... add column if not exists`，schema.sql）；所有合集标题展示位统一取 `cards_title ?? title`；改名合集按 `source_type` 分流——纯卡片文件（`source_type='cards'`，无独立笔记，且不出现于笔记列表）仍写 `title`，普通带卡笔记写 `cards_title`（不碰笔记标题）。改动八处：① schema.sql 加列；② `types.ts` `Note` 加 `cards_title`；③ `queries.ts` 新增导出 `noteDisplayTitle(note)=cards_title ?? title`，`listCardGroups` + `getReviewOverview` 的 `.select` 加 `cards_title`、标题改为 `cards_title ?? title`；④ `review/page.tsx` 6 处 `getNote(...)?title` 改用 `noteDisplayTitle`（「独立闪卡」等回退保留）；⑤ `notes/[id]/cards/page.tsx` 传 `noteDisplayTitle(note)`；⑥ `cards-view.tsx` `commitRename` 按 source_type 写 `title`/`cards_title`；⑦ `note-cards-tabs.tsx` `commitRename` 同样分流；⑧ `notes-browser.tsx` 笔记改名保持写 `title`（与合集名天然解耦）。⚠️ 需人工跑一次 schema（`supabase db push` 或重跑 schema.sql）给 notes 补 `cards_title` 列。`npx tsc --noEmit` 全绿。
- 2026-08-29 **精读「解释气泡」统一生词卡样式 + 导入单行修复 + 解释结果缓存 + 读音落背面标签（tsc 全绿）**。用户实测上一版解释气泡的「导入到笔记」会把搭配/例句的换行带进列表项，转成闪卡被拆成**多张卡**；搭配里不同词组还用 `+` 隔开；「释义」标签转卡后像消失（实为被提到顶部居中、无 chip）。评审又加三点诉求：统一各 AI 生词卡样式、解释结果缓存、导入追加到生词列表末尾。本轮按用户截图定稿的排版落地：**背面 = `[词性] 释义`（居中）＋ 分隔线 ＋ 读音/搭配/例句 chip（靠左，读音不居中、保留可编辑）**。改动七处：① **`explain` 路由**——`collocations` 提示词去 `+`、强制每个搭配独占一行「原文 中文释义」（严禁 `+`/`/`/顿号挤一行），从源头消除 `+`；② **`parse-cards` `applyReading`**——`【读音】` 由前置改为**插到首行（释义）之后**，避免裸释义掉进详情不再居中（`card-back` 的 `splitMeaning` 只把第一个带标签块前的裸行当居中含义）；③ **`ai-note`**——导出 `item()`（语伴「加入笔记」拼单行列表项核心）；④ **`parse-sections`**——导出 `WORD_TITLES`（含生词表/生词/词汇表/词汇/单词表/vocabulary/vocab）；⑤ **新增 `explain-cache.ts`**——解释结果 localStorage 缓存，key `ln_explain_cache`，TTL 7 天、最多 200 条、超出淘汰最旧，读写 try/catch；⑥ **`rich-text-editor`**——`runExplain()` 发请求前先命中缓存（同词直接 `setResult` 秒出、成功后回写）；气泡「搭配」显示把 `+`/`／` 折成换行（每个搭配各占一行）；`collect()` 去掉旧的「词性 · /读音/」meta 行，改拼 `[词性] 释义` ＋ `【读音】` ＋ `搭配：` ＋ `例句：`（读音写进背面标签、**不**塞 `reading` 字段）；`importToNote()` 改用 `item()` 复用，`back` 含 `[词性]` 前缀、`extra` 折成单行（转卡即一张），标题判定改用 `WORD_TITLES`，且命中「生词」标题后**向后扫描**（跳过空段落）追加到第一个 `bulletList` 末尾、扫到下一个 heading/callout 仍无列表才在标题后新建；⑦ **`card-explain` 路由**——生词 prompt 改为释义置首行裸行（词性 `[名词]` 并进、不加「释义：」标签），读音/搭配/例句用冒号标签，与规范一致。语伴生成闪卡（`companion-chat.generateCards`）读音仍在 `cards.reading` 灰字，作为用户接受的一处例外、未动。`npx tsc --noEmit` 全绿。
- 2026-08-29 **精读「解释」气泡统一：加「导入到笔记」智能合并 + 「词组/常用搭配」合并为「搭配」+ 泰语读音带声调 + 标签顺序统一 搭配在前例句在后（tsc 全绿）**。用户反馈解释气泡只能「收录到闪卡」、还没转成闪卡的词没地方去，且解释结果的格式与闪卡 AI 补全模板不一致（泰语读音没声调、`词组` 标签没被背面识别、AI 语伴的「拓展」里混进常用搭配）。落地六处统一：① **`explain` 路由**——`phonetic` 要求泰语给罗马音并带声调符号（如 ก๋วยเตี๋ยว → gŭuay-dtĭieow），`collocations` 语义改「常用搭配/词组，合并为『搭配』，1~3 个、每行『原文 + 中文』」；② **`card-back`**——`KNOWN_LABELS` 加「词组」，`LABEL_ALIAS` 归一化（词组→搭配、常用搭配→搭配），背面任何来源的搭配都统一显示成「搭配」chip；③ **`card-explain` 路由**——生词标签改为 读音/释义/搭配/例句，搭配「可缺省、合并写一起、标签用搭配」，拓展只在该词有需特别解释的用法/语气/文化背景时才写；④ **`assistant` 路由**——word 的 `note` 语义改「搭配：常用搭配、词组合并写一起」，规则明确「常用搭配/词组统一写进 note（搭配），只有需特别解释的用法说明才写 note」；⑤ **`ai-note` + `companion-chat`**——生词背面/笔记行标签顺序统一为「释义 → 搭配 → 例句」（搭配在例句前、例句在后），`拓展：` 改 `搭配：`；⑥ **`rich-text-editor`**——`collect()` 的 `【词组】` 改 `【搭配】`，解释气泡新增「导入到笔记」按钮（`importToNote()`）：拼成「词（读音）：[词性] 释义  搭配…  例句…」列表项后智能合并——优先夹进第一个 `kind=word` 的 callout 末尾、没有 callout 就找含「生词」的标题插到其下方列表末尾、两者都没有就在文末新建生词 callout（同一篇笔记多次导入的词合进同一处）；两个按钮都加 `importingToNote/importedToNote` 即时反馈（导入到笔记在上、收录到闪卡在下）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **闪卡识别规则：保存按钮回底部 + 自定义规则改手风琴展开/收起（tsc 全绿）**。承接上一条，用户觉得保存按钮放顶部还是想放回底部，且自定义规则多了会把列表拉很长。落地 `recognition-rules-form`：① 保存按钮移回**表单底部**（`btn-brand w-full` 全宽主按钮，下方小字「改完点下方保存；留空则用内置默认规则」），顶部不再放；② 自定义规则列表改**手风琴**——每条默认收起只显示「▾ + 规则名称（未命名规则）+ 🗑 删除」，点标题行展开显示 名称输入 + 模式/类型/正则/作用范围 表单，`expandedRule` 状态一次只展开一条；`addRule` 新增后自动展开新规则方便直接填，`removeRule` 删除后收起避免索引错位。`npx tsc --noEmit` 全绿。
- 2026-08-29 **闪卡识别规则「保存」按钮上移到折叠面板顶部 + 改全宽主按钮（tsc 全绿）**。用户反馈「新增规则没有保存的地方」——原保存按钮在折叠面板内长表单最底部（全局选项下方）、且是小按钮不醒目，点「新增规则」后滚不到就以为没有保存。落地：`recognition-rules-form` 把保存按钮从表单底部上移到**表单最顶部**（折叠面板展开第一眼可见），样式由小 teal 按钮改为 `btn-brand w-full` 全宽主按钮（与 `settings-form`/`ai-model-form` 一致），按钮下加小字「改完点上方保存；留空则用内置默认规则」；`error` 提示仍留表单底部。`npx tsc --noEmit` 全绿。
- 2026-08-29 **笔记编辑器补键盘快捷键 ⌘K/Ctrl+K 链接（tsc 全绿）**。用户想给笔记内页加 Notion 式快捷键，且要跨端（macOS + Windows/Linux）。核查发现大多数标准快捷键**已由 Tiptap StarterKit + 各扩展默认启用、无需改代码**：加粗 ⌘B/Ctrl+B、斜体 ⌘I/Ctrl+I、下划线 ⌘U/Ctrl+U、删除线 ⌘⇧S/Ctrl+Shift+S、行内代码 ⌘E/Ctrl+E、高亮 ⌘⇧H/Ctrl+Shift+H（`@tiptap/extension-highlight` 自带 `Mod-Shift-h`）、无序列表 ⌘⇧8/Ctrl+Shift+8、有序列表 ⌘⇧9/Ctrl+Shift+9、引用 ⌘⇧B/Ctrl+Shift+B、标题 1~3 ⌘⌥1~3/Ctrl+Alt+1~3。唯一缺的是**链接**（`extension-link` 无 `addKeyboardShortcuts`）。落地：`rich-text-editor` 的 `editorProps.handleKeyDown`（Backspace 分支前）加 `if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "k") { toggleLink(); return true; }`——`metaKey` 覆盖 macOS ⌘、`ctrlKey` 覆盖 Windows/Linux Ctrl，复用现有 `toggleLink()`（选中文字有链接→取消；无链接→`window.prompt` 输地址→`setLink`）。2/3 列布局、块拖拽移动按用户决定**后置不做**。`npx tsc --noEmit` 全绿。
- 2026-08-29 **AI 例句括号强制写中文翻译 + 主题色移动端重开偶发回绿修复（tsc 全绿）**。两点：**① 语伴/补全例句括号里不能再抄原句**——用户反馈语伴生成的泰语生词例句是「泰语句子（泰语句子）」，括号里应写中文翻译却把原句抄了一遍。根因：`assistant` 与 `card-explain` 的 prompt 对「例句（附中文翻译）」写法太含糊，模型偶发把原句重复进括号。落地：两处路由的例句字段/规则统一改成「格式为『例句（简体中文翻译）』，括号里必须是这句的中文翻译、严禁重复原句或留空」——`assistant` SYSTEM_PROMPT 的 word.extra 字段描述、规则行、补全上下文三处；`card-explain` 生词与语法的例句要求两处。**② 主题色持久化兜底**——用户反馈改主题色后移动端关闭重开偶发回到默认绿、但设置页仍显示已选的蓝。根因：主题色只在 `layout` 首屏内联脚本设一次 `data-theme`，偶发没生效时（app 回绿），设置页 `ThemePicker` 的 mount effect 只 `setCurrent(readTheme())` 读状态、不重新应用到 DOM，于是「显示选了蓝但界面是绿」。落地：新增 `theme-sync` 全局客户端组件（挂 `<html data-theme>` 每次进页面读 localStorage 重应用，只读不写）；`layout` body 里加 `<ThemeSync />`；`ThemePicker` 的 mount effect 由只 setCurrent 改为 `setCurrent(k)` + `applyTheme(k)`（设置页打开也兜底重应用）；`STORAGE_KEY` 导出复用。`npx tsc --noEmit` 全绿。
- 2026-08-29 **练习输入框占位提示精简（tsc 全绿）**。用户反馈：看释义写词/看词写释义等填空输入的提示词太长，输入框里只显示一半、看着奇怪。落地：`mixed-session` 填空/反向题型 placeholder 由「写出这个词，然后点「显示答案」（自测，不计分）」/「输入你的理解，然后点「显示答案」（填空自测，不计分）」精简为「写下这个词」/「输入释义」；`cloze-session` 完形填空 placeholder 由「照着填好的空打一遍，再点「核对」」/「输入缺失的词（不记得就点「显示答案」）」精简为「照着空再打一遍」/「输入缺失的词」。`npx tsc --noEmit` 全绿。
- 2026-08-29 **完形填空：整组生词都没例句时，保留「用 AI 造例句」入口（tsc 全绿）**。用户担心合集里生词都没有例句时完形填空没法练、想保留一个点按钮生成例句的 AI 功能。排查发现该功能其实已在 `cloze-session` 里（`missingWords` + `generateMissing` 调 `/api/ai/cloze-examples`），但有 bug：当整组生词都没例句时，`buildClozeItems` 返回空队列，组件命中 `if (!current)` 直接显示「本轮完成」，那个「用 AI 造例句」按钮根本渲染不出来。落地：把「缺例句提示 + 用 AI 造例句按钮 + AI 报错」抽成 `missingPanel` JSX；`if (!current)` 分支里先判断 `missingWords.length > 0`——有就显示 `missingPanel` + 「这些生词还没有例句，点上方按钮生成后再练」卡片（带返回），生成成功后 `setQueue` 追加、正常进题；`missingWords` 空才走原「本轮完成」。主渲染里的内联 banner/aiError 改成复用 `missingPanel`（消除重复）。`npx tsc --noEmit` 全绿。
- 2026-08-29 **混合练习「看含义写词」题干只显示释义、不再带例句/拓展（tsc 全绿）**。用户反馈：反向题型（看释义写词）的题干显示整个背面（含 例句/拓展），会泄露答案（词可能在例句里看到），应与选择题/填空题一致只显示释义。根因：`mixed-session` 渲染题干时反向题用 `q.card.back`（整段背面）。落地：改为 `backMeaning(q.card.back ?? "")`（复用已有 `card-back.backMeaning`，只抽释义/译文/翻译行），反向题题干不再出现例句/拓展。`npx tsc --noEmit` 全绿。
- 2026-08-29 **闪卡详情页单一类型不显示分类 + 复习主页点「背」即时切换「当前在背」（tsc 全绿）**。两点：**① 详情页分类标签只在有其他类型时显示**——`note-cards-tabs` 原来 `hasKinds = presentKinds.length > 0`（只要有一类就显示「全部/生词」），改成 `presentKinds.some(k => k.key !== "word")`：整组合集只有「生词」一种类型时（或全无分类）不显示「全部/生词」筛选，出现「例句/语法」等其他类型才显示分类标签。**② 复习主页「闪卡合集」点「背」即时切当前合集**——原来「背」是 `<Link href={collectionHref(c)}>` 只跳页、不改「当前在背」（继续背大卡 + 「正在背」徽章要等真正背一张才由 review-session 落 localStorage）。新增 `startCollection(c)`：先 `setCurrent(c)` 即时切换当前合集，再写 `ln_active_collection`（key 用 `noteId??"orphans"`，与 cards-view/review-session 同格式），最后 `router.push(collectionHref(c))` 进背诵页；「背」按钮由 Link 改为 button 调它。`npx tsc --noEmit` 全绿。
- 2026-08-29 **完形填空只取例句、不再用常用搭配当题目（tsc 全绿）**。用户反馈：完形填空的题目句子混进了「常用搭配」（生词背面结构是 `释义  例句：…  拓展：…`，`拓展` 存的是常用搭配）。根因：`cloze.ts` 的 `splitWordBack` 用 `/例(?:句)?\s*[：:]/` 一刀切，`example` 取到「例句：」之后**全部内容**（连带后面的「拓展：搭配」）；且 `buildClozeItems` 里「没有拆出例句」时把**整个背面**当非外语候选句入池，搭配里的目标词被 `matchIndex` 命中 → 挖空句就成了「搭配」。落地两处：① `splitWordBack` 重写——例句只取到下一个标签行/换行为止（新增 `EXAMPLE_TERMINATORS` 标签集：拓展/搭配/相关/用法/注意/说明/接续/读音/释义/翻译/解析等，单行旧双空格格式再按 `\s{2,}标签：` 截断），不再把「拓展/搭配」混进例句；② `buildClozeItems` 删掉 `else pool.push({text:back, isForeign:false})` 回退分支——背面没有例句（纯释义 / 只有搭配）不再把整个背当候选句，这类生词交给 `missing` 让 AI 现造例句。`npx tsc --noEmit` 全绿。
- 2026-08-29 **三种练习题型三处修：选择题选项只取释义、完形填空喇叭上移、混合练习填空答案换行（tsc 全绿，已部署 `dpl_GTfJ2uvge4cG7zXRdTMghmtiufDT`）**。用户反馈三点：**① 选择题选项混进例句泄露答案**——`test-session`/`mixed-session` 的 `buildQuestions`/`buildMixedQuestions` 选项池原来用整个 `back`（含 例句/拓展/用法 等非释义行），现改用新增的 `card-back.backMeaning(back)`：复用 `parseBack`+`splitMeaning` 只抽「含义」行（释义/意思/含义/解释/词义/译文/翻译），无结构化标签则回退整段原文，选项与答案都改用它，避免在例句里看到答案。**② 完形填空喇叭压住句子**——`cloze-session` 喇叭原来 `absolute right-0 top-0` 叠在挖空句文字上，现移到标签行右侧（`mb-3 flex items-center justify-between`，标签左、喇叭右），句子容器去掉 `relative`/absolute，不再与句子冲撞。**③ 混合练习填空答案与下一题同排挤压**——`mixed-session` 答后块由 `flex items-center justify-between`（答案文字与「下一题」按钮同行）改为 `space-y-3` 竖排：答案 `<p>` 独占一行（`whitespace-pre-wrap` 可换行）、「下一题」按钮全宽另起一行，不再挤压。
- 2026-08-29 **笔记内页 AI 语伴「加入笔记」按钮加即时反馈（tsc 全绿，已部署 `dpl_9Lw2rde4apALJPtvR8JnHyYPrsWx`）**。用户反馈：语伴生成短文后点「加入笔记」，按钮本身没变化、只在底部冒一行提示，容易误以为没导进去。根因：`addToNote` 同步 `insertContent` 后只 `setDone`（底部提示文字），按钮样式不变。落地：`ai-assistant-panel` 加 `addedToNote` 状态 + `addedTimerRef`，点「加入笔记」时按钮即时变绿（实心 `bg-teal-600 text-white`）显示 `Check` 图标 +「已加入」，1.6s 后自动恢复原样（`FilePlus2` +「加入笔记」），同时保留底部 `done` 提示；`Check` 加入 lucide 导入。
- 2026-08-29 **全站审查 12 项高危问题全修（tsc + build 全绿，已部署 `dpl_3uduuviyqEJL7vD5xD9eMfJhaK8m`）**。四路并行 code review 后逐条落地：**① AI 路由输入上限**——`cloze-examples`（MAX_ITEMS 40 / MAX_WORD_LEN 100 / lang 白名单）、`cluster-cards`（MAX_CARDS 500 + truncated 标记）、`card-explain`（MAX_LEN 5000 + kind 白名单）、`story`（MAX_CARDS 40 / MAX_LEN 200）。**② SSRF 守卫**——`server-fetch` 补 CGNAT(100.64/10)+198.18/15、IPv6 去括号块 `::`/`::1`/`0:0:0:0:0:0:0:1`/ULA(`fc`/`fd`)/link-local(`fe8`-`feb`)/IPv4-mapped(`::ffff:`)，重定向手动跟随（≤5 跳）逐跳重验 + 丢 authorization/body。**③ provider 错误体脱敏**——`ai-client`（anthropicChat/openaiVision/anthropicVision 只 log `errText.slice(0,1000)` 并 throw 无 raw 的 `（status）请稍后重试`）、`transcribe` 同款。**④ yt-dlp**——`transcribe` 音频下载加 `--max-filesize 25m`。**⑤ parse-cards CSV 表格检测**——从「整块每行列数严格一致」改为「连续含逗号行成块，块首行能被 classifyHeader 识别成表头才当表格解析」，列数不齐用空串兜底。**⑥ srs.ts**——rating=1 忘记改次日（`intervalDays=1` 不再当天到期死循环）、rating=2 困难基数 ≤1 时按 reps 递增不再原地踏步。**⑦ materials-view 批选勾选冒泡**——两处批量勾选按钮加 `e.stopPropagation()`。**⑧ companion-favorites 导入弹窗 + cloze-session 返回**——导入弹窗补 ✕ 关闭按钮、遮罩 `onClick` 关闭、Esc 关闭（`useEffect` keydown）；`cloze-session` 无生词空态补「返回」、完成页补「再来一次」（`restart()` 重置全部会话态）+「返回」，新增 `backHref` prop（`review/page` 传 `/cards?view=theme` 或 `/review`）。**⑨ note-cards-tabs 搜索关闭清 query**——点标题栏搜索图标「由开到关」时 `setSearchQuery("")`。**⑩ layout.tsx 补服务端校验**——`getSession()` 之外补 `getUser()` 网络校验，token 过期/被吊销也 redirect `/login`。**⑪ queries get* 吞错改 maybeSingle**——`getNote`/`getFolder`/`getCard`/`getMaterial`/`getMaterialCollection` 从 `.single()`+`if(error)return null` 改为 `.maybeSingle()`+`if(error)throw`（真 DB/网络错误不再被吞成 404，只「无行」返回 null），`getCard` 内层 notes 查询同改。**⑫ profile-form refreshSession**——`updateUser` 后补 `auth.refreshSession()`（否则 access token 里 user_metadata 是旧值，服务端 getSession 读不到新昵称/头像）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **账户头像 + 昵称：emoji 头像（预设 emoji + 背景色）+ 昵称编辑，存 Supabase auth `user_metadata`（tsc + build 全绿）**。设置页账户分组原来只有 email + 首字母头像，现在重做成 iOS「账户」卡片：新增 `profile-form` 客户端组件——头像行显示 emoji（`avatar_emoji`）坐在柔和背景色（`avatar_color`）圆底上、无 emoji 回退 email 首字母；昵称显示 `name`（无则 email 前缀）；右侧铅笔进编辑态（昵称输入 + 14 个预设 emoji 网格 + 5 个柔和背景色：鼠尾草绿/雾霾蓝/藕粉/香芋紫/燕麦咖，固定 hex 不跟主题 hue 联动 + 保存/取消）。数据存 auth `user_metadata`（`updateUser({ data: { name, avatar_emoji, avatar_color } })`，跨设备同步、无需改表）。`LogoutButton` 从 `settings-form` 移入 `profile-form`；`settings/page` 账户分组改 `<ProfileForm email={userEmail} initial={…user_metadata} />`。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **设置页重排成 iOS 手机 app 样式 + 主题色选择器（莫兰迪五套）+ 桌面 ⋯ 移到标题栏 + 修 [] 读音转卡空括号残留（tsc + build 全绿）**。四点一起落地：**① 设置页重排**——新增 `settings-row`（`SettingsGroup` 小标题+白卡分组 / `SettingsRow` 左标签右控件），`settings/page` 删掉 `Section` + `card-soft` 老排版，改成竖排分组列表：账户（头像+邮箱+退出登录）/ 学习（`SettingsForm`）/ 外观（`ThemePicker`）/ 闪卡识别规则（`details` 折叠，summary 改成白卡行高）/ AI 模型（`AiModelForm`）/ 数据（`DataBackup`）；`settings-form`、`ai-model-form`、`data-backup` 全部重写成 `SettingsRow` 行式（每日背诵目标数字框 / 提醒 iOS 开关+时间 / 三个任务下拉 / 两个导出整行按钮），保存按钮统一 `btn-brand w-full`。**② 主题色选择器**——用户澄清「不是换颜色，是设置里加主题色选择」：`globals.css` 把 `--brand` 主色系全部改成 `oklch(… var(--brand-hue))`（teal scale / `btn-brand` 渐变 / tiptap 链接与选中格），`:root` 设 `--brand-hue:165`，新增 `:root[data-theme="sage|blue|rose|lavender|oat"]` 五套色相覆盖（鼠尾草绿 165 / 雾霾蓝 235 / 藕粉 15 / 香芋紫 300 / 燕麦咖 60）；`theme-picker` 客户端组件（五个色块按钮 + localStorage `ln_theme_color` 持久化）；`layout` `<head>` 内联脚本首屏前设 `data-theme` 避免闪回。**③ 桌面 ⋯ 到标题栏**——`note-cards-tabs` 统一手机 ⋯ 菜单到标题栏常显（去 `md:hidden`），桌面搜索行删掉 ⋯ 菜单只留「背诵+添加」，「添加/背诵」两项 `md:hidden` 只手机显示。**④ 空括号 bug**——`parse-cards` 的 `extractParenHint` 正则扩展支持 `[latin]`（原来只认圆括号/全角），`extractReadingByScript` 兜底 `.replace(/[[\]【】()（）]\s*[[\]【】()（）]/g,"")` 去掉抽走读音后残留的空括号对（`释义[kam sap]` → `【kam sap】\n释义`，不再多出一行空 `[]`）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **合集卡片张数/待学措辞与复习主页统一：改「共 N 张 · 待学 X」（tsc + build 全绿）**。用户希望跟闪卡页/复习主页「闪卡合集」列表一致（那里是 `共 N 张 · 待学 X`）。`cards-view` 的 `CollectionCard` 第二行左侧文案从「待学 5 · 10 张」改为「共 {count} 张 · 待学 {due}」（有待学时「· 待学 X」teal 强调，无待学只显示「共 N 张」灰），与 `review-home.tsx` 合集列表 `共 {total} 张 · 待学 {due}` 同款。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **合集卡片补「张数」+ 语言徽章与 ⋯ 同排对齐（tsc + build 全绿）**。用户反馈精简后想加回张数提示，且语言徽章和右上角 ⋯ 不对齐。重排 `cards-view` 的 `CollectionCard`：① 第二行左侧从只「待学 N」改成「待学 N · N 张」（有待学时「待学 N」teal + 「· N 张」灰；无待学只显示「N 张」）；② 标题行把 ⋯ 从 `absolute right-2 top-2` 浮层改回**同一 flex 行内**（图标+标题+语言徽章包进 Link，⋯ RowMenu 作为 Link 外的兄弟元素紧跟其后），语言徽章与 ⋯ 自然垂直居中对齐、不再错位；③ 因 ⋯ 已内联，去掉 return 里的 absolute 浮层容器。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡合集卡片精简为两行极简：去掉来源/张数/footer，待学数并进第二行、⋯ 挪回右上角浮层（tsc + build 全绿）**。用户觉得卡片字太多（「🔔 待学 N 张」「✓ 暂无待学」看着多字），要整体精简美观、不一定要保留所有元素。重排 `cards-view` 的 `CollectionCard`：① 卡片收成两行——标题行（图标+标题+语言徽章，`pr-8` 给右上角 ⋯ 让位）+ 第二行（左「待学 N」teal 小字、右「背」按钮）；② 删掉底部 footer（来源文件夹名/笔记添加/外部添加 · N 张 整块）和 `border-t`，来源/张数管理信息不再占卡片；③ 「背」按钮有待学=实心 `bg-teal-600`、无待学=描边弱化（暗示不急着背）；④ ⋯ 菜单（重命名/选择语言/导出/删除）从 footer 挪到右上角 `absolute right-2 top-2` 浮层（RowMenu 仍是 portal+fixed，逃出 overflow-hidden）；⑤ 待学只留「待学 N」三字，删 Bell/Check 图标与「张」「暂无待学」文字；⑥ 清理不再用的 `Bell`/`Folder` lucide import（`Inbox` 独立闪卡块仍用、`BookMarked` 标题图标仍用）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **合集卡片进度条改「待学 N 张」：去掉「已学 X/N」覆盖率进度条，避免「背完收工」错觉（tsc + build 全绿）**。用户担心「已学 X/N」进度条给人「已学完、不用再背」的错觉（SRS 里背过的卡会到期，不存在「背完」）。改为待学导向——待学 = 没背过的 + 背过但已到期的（复用复习主页 due 口径）。落地：`types` 的 `CardNoteGroup.reviewed` → `due`；`queries.listCardGroups` 第 1.5 步 `review_state` 改选 `card_id, due_at`，算每笔记待学数（`!reviewedIds.has(cid) || due_at <= now`）；`cards-view` 的 `Collection.reviewed` → `due`，进度行改为——`due>0` 显示 teal「Bell 待学 N 张」、`due=0` 显示淡灰「Check 暂无待学」，删掉进度条图形。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **详情页背诵降档回退：桌面改回上一版（背诵描边/添加实心）、手机背诵收回 ⋯ 菜单（tsc + build 全绿）**。用户反馈闪卡详情页「背诵」太显眼、且澄清只说手机端（桌面上一版很满意）。回退 `note-cards-tabs`：① 桌面搜索行「背诵」「添加」样式对调回来——背诵描边 `border-zinc-200`、添加实心 `bg-teal-600`（恢复上一版）；② 手机端删掉全宽 `btn-brand`「背诵」主按钮，把「背诵」加回手机 ⋯ 菜单（查看原始笔记/添加/背诵/选择/重命名/删除合集）。合集卡片「背」实心青色 + button-in-Link 修复保持不动。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **合集卡片「背」改实心青色 + 修 button-in-Link 隐藏 bug + 详情页背诵主操作/选择收 ⋯（tsc + build 全绿）**。用户对两处布局拿不准，一起定方案后敲定：① 合集卡片「背」按钮做成实心青色（同复习主页合集列表「背」）；② 详情页「背诵=主操作露出来、选择=偶发批量操作收 ⋯」。落地：**① 合集卡**——`cards-view` 的 `CollectionCard` 的「背」样式改 `rounded-lg bg-teal-600 px-3 py-1.5 text-sm font-semibold text-white`（去 `Play` 图标、只留「背」字，与 `review-home.tsx:340` 合集列表「背」一致）；同时修隐藏 bug——「背」`<button>` 原被包在 `<Link>` 里，点它会同时触发「跳背诵页」+「跳卡片列表」两个导航，现把标题行、进度区各自做成独立 `<Link>`，「背」做成 `<Link>` 之外的兄弟 `<button>`（`Play` 导入移除）。**② 详情页**——`note-cards-tabs` 桌面搜索行「背诵」与「添加」样式对调（背诵实心 `bg-teal-600` 主操作、添加描边次级）；手机端在类型标签后、卡片列表上加一个 `md:hidden` 全宽 `btn-brand`「背诵」主按钮（`RefreshCw` 图标），并从手机 ⋯ 菜单删掉「背诵」项（避免重复），⋯ 只剩 查看原始笔记/添加/选择/重命名/删除合集。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡合集卡片布局再调：⋯ 菜单 + 来源移到底部 footer（左来源右⋯），进度 + 背诵上移、背诵改「背」字（tsc + build 全绿）**。用户新想法：把 ⋯ 隐藏菜单跟来源一起放卡片底部（左边来源、右边 ⋯），进度和背诵按钮上移，背诵按钮改成跟之前合集卡一样简洁的「背」字。**落地**：`cards-view` 的 `CollectionCard` 重排——① 标题行 = 图标 + 标题 + 语言徽章（右侧；⋯ 移走后不再打架，去掉 `pr-8`）；② 进度行上移到标题下方 = 进度条/「未学习」+ 已学 X/N + 「背」主入口（`bg-teal-50` teal 胶囊、`Play` 图标 + 「背」）；③ 底部 `border-t` footer = 左侧来源（文件夹名/笔记添加/外部添加 + · N 张，仍是可点进卡片列表的 `Link`）+ 右侧 `RowMenu`（重命名/选择语言/导出/删除，`mt-auto` 贴底、各卡片 footer 对齐）。「选择语言」激活时 footer 下方照旧换成语言下拉。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡详情页（笔记内页）桌面端按钮精简：标题行去重命名、操作集中搜索行 + 批量横条「换语言」→「语言」（tsc + build 全绿）**。用户反馈电脑网页端闪卡详情页按钮乱——标题行有「重命名」铅笔、下面搜索行又有 添加/选择/背诵 三个按钮，两层都堆操作。**重排**：`note-cards-tabs` ① 桌面端标题行彻底干净（返回 + 标题，去掉单独「重命名」铅笔）；② 搜索行桌面端只留「背诵 + 添加」两个主按钮（删掉「选择」），右侧加一个 ⋯ 菜单（收 查看原始笔记 / 选择 / 重命名 / 删除合集）；③ 移动端标题行 ⋯ 菜单保持全操作（查看原始笔记 / 添加 / 背诵 / 选择 / 重命名 / 删除合集），新增「重命名」进菜单（原铅笔也去掉，两端统一）。`note-cards` 批量选择横条里的「换语言」按钮文案改「语言」（语言 chips 面板引导「换成」不变）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡合集卡片布局再调整：语言徽章挪到元信息行尾、背诵固定 footer 右下（tsc + build 全绿）**。上一版语言徽章放标题行右侧、与浮动在右上角的 ⋯ 菜单挤在一起显得乱，背诵按钮在「有进度 / 无进度」两种状态下位置还不一致。**重排**：`cards-view` 的 `CollectionCard` 改成清晰三层——① 标题行只留「图标 + 标题」（`pr-8` 给 ⋯ 让位，语言徽章移走）；② 元信息行「来源 · N 张」在左、语言徽章 `ml-auto` 放行尾（作为彩色小标识，行内元素少不再挤）；③ 底部加 `border-t` 分隔的统一 footer——有进度显示「进度条 + 已学 X/N」、无进度显示「未学习」（都占 `flex-1` 左对齐），右侧固定「背诵」按钮（不再 pl-11 缩进、两种状态同一位置）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **UI 批次六点收口：删除合集 / 合集卡语言标识换位 / 语伴「自由」→「自动识别」+ 全站删中文 / 删「读音放哪」规则 / 笔记闪卡小标识 / 错题集三按钮手机防挤压（tsc + build 全绿）**。六点逐条落地：**① 闪卡详情页 ⋯ 菜单加「删除合集」**——`note-cards-tabs` 的隐藏菜单加红字「删除合集」项（`deleteCollection`：卡片文件先 `detachMaterialsFromNote` 再删笔记；普通笔记只删其下全部卡片、正文保留，`window.confirm` 后 `router.push("/cards")` + `refresh`）。**② 合集卡语言标识换位**——`cards-view` 的 `CollectionCard` 语言 chip 从来源行 `ml-auto` 位移到标题行右侧（`text-[11px]` 小徽章），来源行只留「来源 · N 张」；「已学 X/N」从进度条下方移到进度行内（进度条与「背诵」按钮之间）。**③ 语伴语言「自由」→「自动识别」+ 全站删中文**——`companion-chat` 的 `LANGS` 首项 label 改「自动识别」；`lang-detect` 的 `LANG_ORDER` 删 `chinese`（所有语言下拉/chips/筛选共用该常量，自动全局生效；`detectCardLang`/`LANG_LABEL`/`LANG_COLOR` 仍保留中文兜底，纯汉字卡仍能显示徽章、只是不再作为可选/可筛语言）。**④ 删「读音放哪」规则**——用户确认该规则无用且 buggy，删除：`recognition-rules-form` 移除 `Values.reading`/`join`/`save` 及「读音放哪」UI 块；`types` 删 `RecognitionRules.reading`；`recognition-presets` 删 `EMPTY_RULES.reading`；`parse-cards` 的 `applyReading` 简化为固定放背面（`【读音】` 首行）。**⑤ 笔记加闪卡小标识**——`Note` 加 `cardCount?`；`queries.listNotes` 新增 `withCardCounts()`（一次 `.in("note_id")` 查 `cards.note_id` 计数，查不到按 0、不打断列表）；`notes-browser` 的 `NoteItem` 在列表视图标题行、网格视图底部行各加一个 teal 小徽章（`Layers` 图标 + 数量，`shrink-0` 不挤压标题）。**⑥ 错题集三按钮手机端重排**——`test-errors` 的 测试练习/开始背/清空 由单行 `ml-auto flex gap-2` 改为移动端竖排（`开始背` 全宽主按钮在上、测试练习+清空 各 `flex-1` 半宽在下），桌面 `sm:` 恢复单行原序。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **读音范围收窄：日语不填（正面词已带假名）、中文不填（无需音标），泰语/韩语/英语保留且不必每个词都给（tsc + build 全绿，已部署）**。承接上一条读音功能，用户反馈三点：日语词正面本身就带假名、不用再单独做假名读音；中文不需要拼音音标；泰语/韩语/英语暂时保留，但不用每个词都要读音。**落地**：① `assistant` 路由规则改「泰语=罗马音带声调、韩语=罗马转写、英语=音标；日语不填、中文不填；不必每个词都填，没有可靠读音就留空」，JSON 示例的 reading 描述同步改为「泰语罗马音带声调/韩语罗马转写/英语音标；日语、中文不填」；② `card-explain` 生词「读音」行同步（泰语/韩语罗马转写、英语音标；日语/中文不补）。`npx tsc` 隐于 build、`NODE_USE_ENV_PROXY=1 npm run build` 全绿；`vercel deploy --prod` 已部署。
- 2026-08-28 **语伴生成词汇闪卡带读音：泰语罗马音带声调、日语假名、韩语罗马转写、中文拼音、英语音标（tsc + build 全绿）**。用户反馈：语伴生成的词汇闪卡没读音，希望泰语给罗马音+声调符号（如 ก๋วยเตี๋ยว → gŭuay-dtĭieow）、日语给假名等。**落地**：① `AssistantPoint` 加 `reading?: string`（生词=读音，其余类型忽略）；② `assistant` 路由 SYSTEM_PROMPT 的 word 示例加 `"reading"` 字段 + 规则「生词必须给 reading，按目标语言给：泰语=罗马音带声调、日语=假名、韩语=罗马转写、中文=拼音带声调、英语=音标，只有生词才填、没有可靠读音才留空」，`parseReply` 解析 `reading`；③ 直接生成闪卡（`companion-chat generateCards`）把 `p.reading` 存进 `cards.reading`；④ `card-front` 的 `CardFront` 扩展——reading 是 furigana JSON 仍走汉字上方假名（ruby），**纯字符串读音（泰语罗马音/假名/拼音/音标）在词下方加一行小号灰字**（正面、背诵翻面都带，正面原词不变、不影响搜索/转卡）；⑤ 「加入笔记」路径 `pointsToNoteContent` 把读音以「（读音）」挂在词后——笔记里看得见，「转成闪卡」时 `parseCards` 会抽成【读音】存进背面；⑥ `points-preview` 语伴知识点预览在词后也带「（读音）」，收藏前就能看到读音。`npx tsc --noEmit` 全绿。
- 2026-08-28 **语伴生词释义多义项不再换行：AI 提示词要求同一行用顿号 + 入库兜底折换行（tsc + build 全绿）**。用户反馈：语伴生成的泰语词汇闪卡，释义区多义项被换行显示（如「好吃的」背面中间孤零零一行「美味的」），显得奇怪，希望多个含义用顿号隔开同一行。根因：AI 在 word 的 back（释义）字段里用换行分隔多个义项，generateCards 直接 `p.back` 原样入库（换行原样保留），「加入笔记」路径则经 `item()` 把换行折双空格、转卡时又被 wrapBackSpaces 拆回换行——都是 AI 输出换行所致，非识别规则主动拆（splitBySemicolon 默认关）。**落地**：① `assistant` 路由 SYSTEM_PROMPT 的 word back 描述 + 规则补「多个义项用顿号「、」隔开写在同一行，严禁换行分隔」；② `card-explain` word 释义行改「用顿号「、」隔开同一行（不要换行或分号）」；③ 入库兜底——`companion-chat generateCards` 生词释义 `p.back.replace(/\s*\n+\s*/g, "、")`、`ai-note pointsToNoteContent` word 分支 `clean(w.back).replace(/\s*\n+\s*/g, "、")`，即使 AI 仍输出换行也会折成顿号。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **语伴「收藏」按钮改为可取消收藏（toggle），修「取消收藏后对话仍显示已收藏、点不了」的卡死（tsc + build 全绿）**。根因：对话页收藏状态 `favorited` 只存内存 + localStorage，收藏按钮 `disabled={m.favorited || ...}`，一旦收藏过就永久点不了——即使在收藏夹页删了记录，对话页仍显示「已收藏」且无法重新收藏。**落地**：`companion-chat` 的 `collect` 改为 toggle——`msg.favorited` 时调 `deleteRecord(recordId)`（记录已不存在也不报错）并本地复位 `favorited=false/recordId=undefined`，否则走原收藏流程；按钮 `disabled` 只留 `collecting.has(i)`（去掉 `favorited`），已收藏图标改实心 Heart（`fill-current`）并加 `title="点击取消收藏"`，hover 加 `bg-teal-100`；`Check` 图标不再用、从 import 移除；`deleteRecord` 加入 import。「导入笔记」「生成闪卡」两个按钮本就不依赖 `favorited`、随时可点，无此问题。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **语伴知识点直接展示（对话气泡 + 收藏夹都可看到词汇内容，收藏前不再「只有笼统回复看不到词」）（tsc + build 全绿）**。用户反馈：让语伴生成「食物主题泰语闪卡合集」，回复里没有具体词汇内容，要点「导入笔记/生成闪卡」后才看得到，收藏没意义。根因：语伴返回的 `points`（结构化知识点 word/example/grammar/article）前端只存不用、从不渲染，消息气泡只显示 `reply` 文字。**落地**：新增 `src/components/points-preview.tsx` 的 `PointsPreview`（按 生词/例句/语法/原文 分组逐条渲染 front：back + 例句/拓展/接续/例 副行，标签 teal 小字）；`companion-chat` 消息气泡在 `reply` 下方、按钮栏上方插入 `<PointsPreview points={m.points} />`；`companion-favorites` 文字收藏展开后「AI 回答」框下方加同款 `<PointsPreview points={r.points} />`（浅边框块）。收藏前后都能直接看到具体词汇内容。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡合集「背诵」入口改到进度行右侧（▶ 图标右上角浮层太突兀，移走）（tsc + build 全绿）**。用户反馈右上角 ▶ 图标浮层太突兀，改排版：`cards-view` 的 `CollectionCard` 右上角浮层只留 ⋯ `RowMenu`（重命名/选择语言/导出/删除），标题行 `pr-24`→`pr-8`；「背诵」入口挪到进度行右侧——有进度时进度条 `flex-1` 右侧放 teal `▶ 背诵` 文字胶囊（`bg-teal-50`），未学习时 `justify-between` 左侧「未学习」右侧同款「背诵」按钮；新增 `goReview()`（`startReview()` 写 `ln_active_collection` + 跳 `/review?note=…`）复用两个按钮。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡合集「背」按钮移出 ⋯ 菜单改右上角图标 + 语伴 example extra 标签「用法」→「拓展」+ 原文 callout 翻译按段一一对应并顺带翻标题（tsc + build 全绿）**。三点：**① 背按钮回卡片**——用户反馈「背」收进 ⋯ 菜单后显示很奇怪，改回卡片上展示；`cards-view` 的 `CollectionCard` 右上角浮层改成「teal `Play` 图标按钮（`aria-label`/`title`=背诵，点击写 `ln_active_collection` 并跳 `/review?note=…`）+ 原 ⋯ `RowMenu`（只剩 重命名/选择语言/导出/删除）」；标题行 `pr-8`→`pr-24` 给双按钮让位，`Play` 加入 lucide 导入。**② example 标签**——`companion-chat generateCards` 的 `extraLabel = p.kind === "example" ? "用法" : "例句"` 改为 `"拓展"`（例句 kind 的 extra 是语法/用法说明，归「拓展」而非「用法」）。**③ 翻译逐段对齐 + 翻标题**——`/api/ai/translate` 输入从单 `{ text }` 改为 `{ paragraphs: string[] }`，逐段独立翻译（并发 3、`maxTokens 2000`），返回 `{ translations: string[] }` 与输入一一对应（长文不再因单次整文翻译而错位丢段）；`translate-apply` 改发 `{ paragraphs: origTexts }`、读 `data.translations` 数组按序插回；标题翻译——`callout-actions runCalloutAction` 加 `opts.onTranslated` 回调（翻译成功后再触发），`rich-text-editor` 加 `onTranslateTitle` prop（ref 存最新回调避免每次重渲染重挂点击处理器）并把它接到翻译动作；`note-editor` 新增 `translateTitle()`（标题单段数组调 translate 接口，译文与原文不同才替换、随后 `scheduleAutoSave`）并传 `onTranslateTitle`。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-28 **闪卡背面排版重做（背诵正面+含义居中/其余标签靠左）+ 语伴生词精简为 释义/例句/拓展 + 「背」按钮收进 ⋯ 菜单 + 相关词→相关（tsc + build 全绿）**。四点：**① 背面排版重做**——`card-back` 的 `CardBack` 签名改 `{ back, front?, reading?, center? }`：新增 `MEANING_LABELS`（释义/意思/含义/解释/词义/译文/翻译）与 `splitMeaning()`（把块切成「含义」与「其他标签」两块——命中 `MEANING_LABELS` 的标签进含义、无标签且出现在首个带标签块之前也进含义〈语伴的无标签释义首行〉、其余进其他标签）；`center`（背诵）模式 = 顶部**居中**显示「正面词（复用 `CardFront` 带假名）+ 含义」，下方 `border-t` 分隔后**靠左**显示 读音/例句/拓展 等带标签 chip；非 center（详情/翻面/错题）仍左轴 chip 模板、不居中不重复正面；`review-session` 主翻面传 `front`/`reading`。**② 相关词→相关**——`LABEL_ALIAS` 归一化（任何来源「相关词」chip 都显示成「相关」）；`analyze` 提示词「相关词」→「相关」。**③ 语伴生词结构精简**——`ai-note` 的 `AssistantPoint` 加 `note?`（拓展）；`assistant` word 拆 `back`=释义（可含 `[词性]`）/ `extra`=例句（只放完整自然句子、严禁用法说明/搭配/相关）/ `note`=拓展（搭配/相关/其他合并、没有就空）；`companion-chat generateCards` 生词背面 = `释义 + 例句： + 拓展：`（example 的 extra 归「用法」、grammar 归「例句」）；`pointsToNoteContent` 生词同样拼「例句/拓展」双标签；`card-explain` 生词 prompt 改为 读音/释义/例句/拓展（删「搭配」「相关」两行）；`analyze` word extra 补「只写真正有用的、没有就留空、不要写用法说明」。**④ 背按钮入菜单**——`cards-view` 合集卡删掉底部独立「背」`<Link>`，收进 ⋯ `RowMenu` 第一项（点击写 `ln_active_collection` 并跳 `/review?note=…`），`Play` 导入一并移除。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-27 **错题集「测试练习」入口 + 背诵背面仅无标签才居中 + 合集列表限数/查看更多/背切换 + 混合练习反向「看含义写词」+ 语伴西语例句提示词修正（tsc + build 全绿，已部署 `dpl_FqPgaksZ8BAbdaAZ38aXB5zEy5FG`）**。七点：**① 错题集测试练习**——`test-errors` 加「测试练习」按钮（与「开始背」并列）；`test-session` 加 `onCorrect?` 回调，答对即把该卡从 `test_errors` 删掉（测对通过不再显示在错题集），返回错题列表时 `router.refresh()`。**② 背诵背面居中细化**——`card-back` 的 `CardBack` 加 `center?` prop：只有 `center=true`（背诵）且背面**无任何结构化标签**（自由释义）才居中；带标签（模板）的背面一律靠左 chip 排版（删掉上一轮的 `MEANING_LABELS` 居中分支）；`review-session` 主翻面传 `center`，`card-tile`/`card-detail` 不传（**详情页翻面不居中**）。**③ AI 补全背面标签收口**——`card-explain` 生词 prompt：释义先给词性并用 `[]` 标出（如 `[名词]`）、「相关词」→「相关」、只补重要项不凑满；`card-back` 的 `KNOWN_LABELS` 补 `相关`（旧 `相关词` 仍识别）。**④ 闪卡合集列表限数 + 查看更多 + 背切换**——`cards-view` 合集默认只展示前 6 个、多出「查看更多合集（N）」；合集卡底部加「背」按钮：写入 `ln_active_collection` 并跳 `/review?note=…`（点背即切换「正在背的合集」）。**⑤ 背诵完成卡**——`review-session` 完成页喇叭烟花图标 `flex justify-center` 居中；新增「测试练习」按钮，对刚背过的卡直接做选择题测试（复用 `TestSession`）。**⑥ 混合练习反向题型**——`mixed-session` 加 `reverse`（看含义写词，答案=正面词），与填空一样不计分（`checkFill` 只 `setAnswered`），提示语区分、反向不播发音。**⑦ 语伴西语例句提示词**——`assistant` 的 `word.extra` 与规则明确「例句必须是完整、自然的原语言句子（附中文翻译），不要写『用于…』用法说明」，修西语日常用语「例句里是解释」的问题。
- 2026-08-27 **错题集菜单改「移出」+ 撤销闪卡背面模板、背面含义居中（tsc + build 全绿，已部署）**。三点：**① 错题集隐藏菜单「移出错题集」→「移出」**——`test-errors` 的 `CardTile deleteLabel` 改短。**② 混合练习「看词写含义」填空题已不判分（确认无需再改）**——上一轮已落地：`checkFill` 只 `setAnswered`、不 `record`、不计分，点「显示答案」后显示「答案：…」+ 下一题；本轮用户再提「看词写意思 / 看意思写词都不要判错」，核实现仅此一种填空方向、无反向「看意思写词」题型，故无需再动。**③ 撤销闪卡背面「模板」功能、保留「含义居中」**——用户看后觉得现状奇怪，撤销上一轮的模板库：删 `src/lib/card-back-template.ts`、`src/components/card-back-template-form.tsx`，移除 `settings/page.tsx` 的「闪卡背面模板」Section 与 `card-back.tsx` 的 `blocks` 分支及 hook；改 `card-back.tsx` 让**「含义」居中展示**——新增 `MEANING_LABELS`（释义/意思/含义/解释/词义），含义行居中（标签在上、内容在下），整张无结构化行的纯释义卡也居中，其余标签行（读音/词性/搭配/例句）照旧左轴 chip 排版。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；`vercel deploy --prod` 已部署（`dpl_2Gt6crnzMhmADpr5gUx87zQpWfcm`）。
- 2026-08-27 **闪卡背面「美化模板」落地（已撤销，见上一条）：预设「默认 / 分块」两种样式可筛选（tsc + build 全绿，已部署）**。用户拍板「至少两种模板可筛选、默认保持现状、无标签手动卡一整块浅色圆角块」。**落地方案**：背面板 = 只读渲染层，与存储的 `back` 纯文本解耦——不改库、不改编辑逻辑，只在 `card-back.tsx` 这一唯一背面渲染器内按模板切排版（它已被 card-tile / card-detail / review-session 共 4 处复用，改一处即全站生效）。**改动**：① 新增 `src/lib/card-back-template.ts`——`CardBackTemplate = "default" | "blocks"` + `CARD_BACK_TEMPLATES`（默认（现状）/ 分块 两条文案）+ `useCardBackTemplate()` hook（照 `notes-view.ts` 模式，key `nestlingo:card-back-template`、挂载后 `useEffect` 读、SSR 守卫、读写 try/catch、设备级 localStorage）。② `card-back.tsx` 的 `CardBack` 读 `template`，加 `blocks` 分支：无任何标签 → **一整块 `rounded-xl bg-zinc-50 p-3` 浅色圆角块**；有标签行 → 每行一块浅色圆角块（**标签标题在上** `text-[11px] text-teal-600` + **内容在下** `text-sm text-zinc-800`），复用既有 `KNOWN_LABELS`/`parseBack`；`default` 分支 = 现状 chip 左轴排版原样保留。③ 新增 `src/components/card-back-template-form.tsx`（client，radio 卡片点选即时写 localStorage、无保存按钮）。④ `settings/page.tsx` 加 `Section title="闪卡背面模板"` 渲染该表单。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；`vercel deploy --prod` 已部署（`dpl_2gNyn4aF2cwsCP6wTtfcBgsWSiR2`）。
- 2026-08-27 **闪卡搜索上移 / 错题集翻面卡 / 混合填空不判分 / 语伴卡片留空位 / 收藏对话展开只显回答（tsc + build 全绿，已部署）**。用户 6 条反馈逐条落地（第 6 条「卡片背面模板」走 plan 模式待确认，见「待确认」新条目）：**① 闪卡搜索框紧贴标题栏搜索按钮**——`cards-view` 搜索行从语言筛选下方挪到筛选**上方**（`mb-4`）；`note-cards` 加受控 `query`/`onQueryChange` + `hideSearch`，`note-cards-tabs` 把搜索框「提到」`</header>` 后、类型标签筛选前（并随它携带桌面 选择/添加/背诵 按钮），搜索框不再离顶部搜索按钮太远。**② 错题集闪卡翻面 + 编辑菜单**——`test-errors` 弃用「详情」链接，改渲染 `CardTile`（`grid-cols-1 sm:grid-cols-2`）正反面翻页；`card-tile` 加 `deleteLabel`（默认「删除」）+ `onDelete`（默认删整卡），错题集传 `deleteLabel="移出错题集"` + `onDelete=removeFromErrors`（只删 `test_errors` 行、不删卡片）。**③ 混合练习「看单词写含义」填空不判分**——`mixed-session` 的 `checkFill` 只 `setAnswered(true)`（不再 `record`/落错题集），删 `giveUp` 与 `normalizeAnswer`；填空 UI 合成单个「显示答案」按钮（placeholder「填空自测，不计分」），反馈块填空显示 `答案：{q.answer}`（灰色、无对错色）；`done` 成绩只按 `choice` 计分（`scored>0` 才算百分比，纯填空提示「本次均为填空题（自测，不计分）」，两种并存时附「N 道填空题不计分」）。**④ 语伴标题栏切分线与会话框留空位**——`companion-chat` 会话卡加 `mt-3`，切分线下不再与框顶线重合。**⑤ 收藏夹对话收藏**——`companion-favorites` 的 `textCard` 折叠态完整展示我的问题（`whitespace-pre-wrap` 不截断、删回复预览），展开后只保留「AI 回答」框（删「我的问题」框，避免重复）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；`vercel deploy --prod` 已部署（`dpl_Fq1Kuc2t8FZVacdmq3CcGs3dE5Lb`）。
- 2026-08-27 **语伴/收藏/闪卡/笔记/编辑器一批收口 + 全站语言筛选补西班牙语（tsc + build 全绿，已部署）**。用户 12 条反馈逐条落地：**① AI 语伴标题栏切分线**——`companion-chat` 头部加 `border-b border-black/[0.06]`（桌面 `md:border-b-0`），与其他一级页 `.page-header` 切分线一致。**② 收藏夹图标去计数 + 页面加统计**——`companion-chat` 头部「收藏夹」按钮去掉 `favoritedCount` 数字；`companion-favorites` 标题下新增一行小统计「共收藏 N 条 · 文字 M · 素材 K」，提醒收藏总量。**③ AI 对话收藏默认收起**——`companion-favorites` 的 `collapsed` 初始态填入**全部** `m:{id}` 与 `t:{id}`（文字 + 素材都默认折叠）。**④ 「生成闪卡合集」→「生成闪卡」**——`companion-chat` 按钮文案改「生成闪卡」（生成中仍「生成中…」）。**⑤ 生成闪卡/导入笔记用 AI 主题标题**——新增 `/api/ai/title`（auth 门控，`aiChat` 取 ≤12 字中文主题标题）；`companion-chat` 加 `titleFor()` 辅助，`importMsg`/`generateCards` 都先调它取主题标题（失败回退首行/固定文案），不再用用户原始提问当标题。**⑥ 收藏对话展开避免重复**——`companion-favorites` 的 `textCard` 重写：折叠态标题用 `r.prompt || 已收藏的对话` + 仅收起始显示回复预览，展开后分「我的问题」框（`bg-zinc-50`）+「AI 回答」框（`bg-teal-50/60`），消除展开后 AI 回答首行与预览重复。**⑦ 闪卡页重命名标题 + 批量改语言**——`note-cards-tabs` 加内联重命名（Pencil → input + Check/X，更新 `notes.title`）；`note-cards` 选择浮条加「换语言」按钮 + `langOpen` 语言 chips 弹层（`LANG_ORDER.map`，点即批量 `update cards.lang`，不影响现有全选/删除布局）。**⑧ 全站语言筛选补西班牙语**——`lang-detect.ts` 的 `Lang` 联合、`LANG_ORDER`、`LANG_LABEL`（西班牙语）、`LANG_COLOR`（rose）、`speechLang`（es-ES）加 `spanish`；`detectLang` 在英文判定前加 `ñ/Ñ/¿/¡` 命中→spanish（拉丁共享字母无法自动识别，仅靠西语特有字符兜底）；所有语言下拉/chips 因共用 `LANG_ORDER` 自动带出。**⑨ 闪卡合集页语言分类区域上移**——`cards-view` 语言筛选块从搜索行下方挪到**搜索行上方**（`mb-5`→`mb-4`），与合集详情页「生词/例句/语法」筛选区位置统一。**⑩ 笔记标题回车回正文**——`note-editor` 标题 textarea `onKeyDown` 捕获 Enter（非 Shift）`preventDefault` + `setFocusBodySignal`；`rich-text-editor` 加 `focusBodySignal` prop + effect `editor.chain().focus("start")`，标题回车不再转空行、焦点回正文框。**⑪ 编辑栏手机端防误触**——`rich-text-editor` 移除 `ToolButton`/`calloutsInline`/`aiButton` 的 `onTouchStart`/`onTouchEnd`（preventDefault 会吞原生 click、迫使 onTouchEnd 手动触发导致滑动松手误触），只留 `onMouseDown preventDefault` + 原生 `click`（浏览器原生滚动安全），配合既有 `onClickCapture` 滚动守卫双保险。**⑫ 商讨：闪卡背面美化模板（未实现，待确认）**——见「待确认」新条目。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；`vercel deploy --prod` 已部署；dev 需硬刷新。
- 2026-08-27 **桌面端闪卡侧栏「添加闪卡」改粘贴转卡 + 全站「复习→学习/闪卡」概念改名 + 手机端两处键盘/滚动修复 + 故事模式/收藏夹文案收口（tsc 全绿）**。用户新一批反馈逐条落地：**① 桌面端「添加闪卡」粘贴转卡**——`card-sidebar`（电脑端点笔记内「闪卡」按钮右侧弹出的闪卡列表）原来「＋ 添加闪卡」是单张 front/back 手动表单，现改成**粘贴内容 → `parseCards` 识别 → 可编辑草稿（正/背面、kind 徽标、删 ✕、＋加一行、重新粘贴）→ 整批入库**（同 `AddNoteSheet` 模式）；「＋ 添加闪卡」按钮点击时用 `dominantLangOf(cards)` 算合集主导语言作默认 `addLang`；空态文案同步改成「粘贴内容识别」。**② 全站概念改名（复习→学习/闪卡）**——一级导航「复习」→「**闪卡**」（`app-shell` NAV_ITEMS）；「**待加强**」→「**待复习**」（review-home 统计卡/按钮、review/page `?scope=weak` 标题与空态）；「**待复习 N 张 / 到期待复习**」→「**待学 N 张 / 到期待学**」（review-home 合集计数、review-session/cloze-session 进度行）；「复习」动作概念统一改「学习/学」——今日已复习→**今日已学**、共复习 X 张/道→**共学 X 张/道**、已复习→**已学**、本轮复习完成→**本轮学习完成**、这个合集/主题都复习完了→**都学完了**、复习提醒→**学习提醒**、该复习今天的单词→**该学今天的单词**、回复习主页→**回闪卡主页**、复习进度导出→**学习进度**、每日复习提醒→**每日学习提醒**；复习主页 `PageHeader` 标题「复习」→「**闪卡**」。内部注释/变量名不动，仅改用户可见文案。**③ 手机端笔记页「新建文件夹」取消后标题栏上移**——`bottom-sheet` 打开时锁背景滚动（body `position:fixed` + 记 `window.scrollY`）、关闭（含点遮罩/✕ 取消）恢复滚动位置，避免 iOS 键盘顶起标题栏后残留上移。**④ 手机端语伴页滚动/键盘**——`companion-chat` 用 `window.visualViewport` 实时跟踪可见高度（键盘弹出即收缩），会话页精确铺满可视区，消除「多余滑动空间」与「键盘把标题挤上去」。**⑤ 故事模式**「重新随机」→「**随机**」（story-mode）。**⑥ 收藏夹收藏素材默认收起**——`companion-favorites` 的 `collapsed` 初始态填入全部媒体 key（`m:{id}`），媒体卡片默认折叠、手动点标题展开；文字收藏仍默认展开。`npx tsc --noEmit` 全绿；dev 需硬刷新。
- 2026-08-26 **复习页合集标题去「· 生词」后缀 + 转成闪卡支持「收录进已有合集」+ 语伴「翻译」气泡去绿色突出（tsc + build 全绿）**。用户三点：① 复习页「闪卡合集」区把「【日语精读标题 · 生词】」单列，但整篇背诵本来就生词/例句/语法一起背，不该按 kind 拆分标题，直接显示笔记标题；闪卡合集页（`/cards`）本就正确，不动。② 笔记转成闪卡若内容少、单开一个合集可惜，想在转卡时**按语言并进已有的闪卡合集**（语言 + 生词/例句/语法都合并）；笔记内页「查看闪卡」后的列表「添加闪卡」已是粘贴转卡形式（`AddNoteSheet`，此前已做）。③ 语伴「翻译」快捷气泡别加绿色突出、跟其它模式气泡同款。**落地**：**① 复习标题**——`queries.ts` 的 `getReviewOverview` 合集分组从「按 `(note_id, kind)` + `KIND_SUFFIX` 加 `· 生词`」改为**按 `note_id` 一组**（生词/例句/语法都算同一篇笔记，`kind` 置 null、`title` 直接用笔记名、`key` 不再带 `::kind`），并删掉不再用的 `KIND_SUFFIX`；`review-home.tsx` 的 `mergeByNote` 本来就按 note 聚合、`findStoredActiveCollection` 用 `split("::")[0]` 兼容旧 key，无需改。测试/故事/完形仍走各自的 `kind` 内部过滤（故事挑 word、完形用例句），不受影响。**② 转卡并入合集**——`convert-to-cards` 新增「**收录到**」下拉（默认「本笔记（这篇）」）：打开时用浏览器 `createClient`（queries.ts 是 server-only，不能 import 进客户端组件）查 `cards(note_id, front, back, lang)` 按 note_id 聚合、`cardLang` 算主导语言、排除当前笔记，列「标题（N 张 · 语言）」；`existingFronts` 去重改按 `targetNoteId ?? noteId` 拉取，选中别的合集时 `save()` 的 `note_id` 用目标合集 id、去重也切到那个合集（跨合集自动跳过重复正面）。**③ 翻译气泡**——`ai-assistant-panel.tsx` 的「翻译」气泡 `border-teal-200 text-teal-600` 改为与 `MODE_BUBBLES` 同款 `border-zinc-200 text-zinc-400 hover:border-teal-200 hover:bg-teal-50 hover:text-teal-600`。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **合集卡片「⋯」菜单不再单独占一栏：挪到卡片右上角浮层 + 张数并入来源行（tsc + build 全绿）**。用户反馈单独一栏只放一个 ⋯ 菜单浪费空间，要求重排。**落地**：`cards-view.tsx` 的 `CollectionCard` 卡片容器加 `relative`；「⋯」`RowMenu` 从底部一整条 `border-t` 操作栏挪到**右上角 `absolute right-2 top-2` 浮层**（仍在 `Link` 外，点卡片进合集、点 ⋯ 不导航；复用 `row-menu` 上下自动翻转）；原头部右侧「N 张」teal chip 移除，改为**并入来源行**（`来源名 · N 张`，来源 span 加 `min-w-0` 防长文件夹名不截断），标题行加 `pr-8` 给右上角 ⋯ 让位；「选择语言」激活时的底部语言下拉 + 保存/取消**仅在该临时态出现**（正常态无底部栏）；重命名态隐藏 ⋯。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **闪卡合集卡片布局重构（语言 chip + 学习进度 + 导出并入 ⋯ 菜单）+ 闪卡编辑态操作右移（tsc + build 全绿）**。用户要求：① 合集卡片「导出」频率低、与 ⋯ 菜单分开显得散，合并成单个隐藏菜单（重命名/选择语言/导出/删除）；② 卡片显示语言 + 学习进度（能看出哪些背过）；③ 来源保留；④ 隐藏菜单下面有空位往下、贴屏底自动向上翻；⑤ 闪卡编辑态的操作按钮挪右边（编辑按钮在右、操作放右更顺手），AI解释去多余 emoji、放语言选择器左边同排。**落地**：**① 数据带「已学数」**——`types.ts` 的 `CardNoteGroup` 加 `reviewed`；`queries.ts` 的 `listCardGroups` 第 1 步 select 加 `id` 并记 `idsByNote`，新增一步 `.from("review_state").select("card_id").in(allIds)` 建 `reviewedIds`，返回时给每笔记算「有复习记录」的卡数（口径同复习页 `reviewed`）。**② 合集卡片**——`cards-view.tsx` 的 `Collection` 加 `reviewed`；卡片来源行右侧加语言 chip（复用 `LANG_COLOR/LANG_LABEL`）；新增进度区：`reviewed>0` 显示细进度条 + `已学 X/N`，`reviewed===0` 显示「未学习」；底部把「左 ⋯ 菜单 + 右导出按钮」合并成**单个右对齐 `RowMenu`**（复用 `row-menu.tsx`，它已做 portal + 实测尺寸的上下自动翻转），删除 `CollectionExportButton`、导出逻辑并进 `doExport`；`menuOpen` 状态及 `Download` 导入一并清理（`MoreHorizontal/Pencil/Trash2` 仍被 `OrphanMenu` 用）。**③ 闪卡编辑态**——`card-tile.tsx` 编辑态把「语言 label+select 行 / 全宽 AI 按钮 / 保存取消行」合成**一条右对齐操作行** `AI解释 · 语言▾ · 取消 · 保存`；AI解释去 emoji 保留 `Sparkles` 图标（`AI 解释`/`AI 解释中…`）、按钮与语言 select 高度对齐（`py-1.5`）；语言 select 去可见 label 加 `title/aria-label`；保存置最右（`font-medium` 主操作）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **笔记内页两处收口：移动端标题字号回正（不再被 16px 强制压小）+ 首行空段 Backspace 删除回焦标题（仿 Notion，tsc + build 全绿）**。用户反馈：① 手机端笔记标题比正文「副标题」还小；② 正文首行空段（下面跟着 callout）光标点过去按 Backspace 删不掉。**落地**：① **标题字号**——根因是 `globals.css` 的 `@media(max-width:767px){ input,textarea{ font-size:16px !important } }` 用 `!important` 把标题 textarea 的 `text-2xl` 压成 16px（比正文 h3「副标题」≈19.55px、h2≈22.95px、h1≈27.2px 都小）。修复：标题 textarea 类名 `text-2xl md:text-3xl`→`text-3xl` 并加 `note-title-input` 类；`globals.css` 在同媒体查询内新增 `.note-title-input{ font-size:1.875rem !important }`（特异性 0-1-0 覆盖 `input,textarea` 的 0-0-1，`1.875rem` 与 text-3xl 同值），移动端标题回到 ≈30px、桌面 30px 不变。② **首行空段删除回焦标题**——`rich-text-editor` 组件加 `onFocusTitle?` prop、`useEditor` 的 `editorProps` 新增 `handleKeyDown`：Backspace 时若选区为纯光标、文档首块是空段落且光标在其开头（`selection.from<=1`），`state.tr.delete(0, first.nodeSize)` 删掉该空段并 `onFocusTitle()`；`note-editor` 传 `onFocusTitle`（聚焦标题 textarea 并把光标置标题末尾 `setSelectionRange(len,len)`）。仿 Notion「删首行空行 → 光标回标题」。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **收口：素材观看页「打开音频/打开原始链接」回正 + 笔记编辑栏多处修复（表格栏位置/图片按钮/OCR/删本地音频/防误触）+ 撤销笔记内页「打开原始链接」（tsc + build 全绿）**。用户上一批反馈逐条落地：① **素材观看页链接回正**——上一轮把「打开原始链接」塞进内容框顶部右上角（音频处多出一块白色留白）是错的；现改回**放在观看区上方、右对齐（`mb-1 flex justify-end`，不放内容框内）**，文本 `youtube→「打开原始链接」、podcast/audio→「打开音频」`，音频素材也恢复「打开音频」入口（不再靠播放器内置，内置「原始链接」已删）。② **撤销笔记内页「打开原始链接」**——`media-embed-nodeview` 动作栏里新增的链接删掉、`ExternalLink` 导入一并移除（用户不想要）。③ **笔记内页音频封面**——`insertMedia` 粘贴媒体链接时把 `parsed.thumbnail` 一并写入 mediaEmbed 的 `cover` 属性（YouTube/音频有缩略图就显示封面）。④ **表格操作栏移到编辑栏下方**——`rich-text-editor` 的 `tableBar`（增删行列/删表）原在桌面/移动两套工具栏**上方**，现移到各自主工具栏**下方**（仍在 sticky 容器内、吸顶跟随）。⑤ **图片按钮无反应**——根因是三个隐藏 `<input type="file">` 被之前重构删掉，`fileInputRef/ocrFileRef` 悬空、`click()` 落空；现补回图片 + OCR 两个隐藏 input（`accept="image/*"`）。⑥ **媒体弹窗删「上传本地音频」**——移除该按钮及 `pickAudioTranscribe`/`audioFileRef`/`audioUploading`/`audioError`/`transcriptCallout`/`Upload` 导入（OCR「上传图片识图」保留，靠补回的 `ocrFileRef` input 生效）。⑦ **手机端工具栏防误触**——两条横向滑动区加 `onScroll` 记时间戳 + `onClickCapture`：滚动刚结束（<200ms）内吞掉这次 click（`preventDefault+stopPropagation`），手指滑动停在某按钮上不再误触发，布局不变。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **播客/音频播放器去「原始链接」+ 笔记内页音频加「打开原始链接」+ 网页端内容宽统一放大到 max-w-3xl（tsc + build 全绿）**。用户三点：① 播客播放器里已经「右下角有打开音频」，播放器内部那个「原始链接」冗余，去掉；② 笔记内页编辑栏导入的音频，在播放器区域外（贴近播放器）加一个「打开原始链接」，参考原文 callout 里「精读笔记 / 来源」的样式；③ 网页端各页内容宽是否再放大些、会不会让设置页怪。**落地**：① **`podcast-audio-player` 去掉内置「原始链接」**——删除 `openHref` prop 与顶部右上角「原始链接」按钮（`ExternalLink` 导入一并移除），播放器只剩封面/标题/进度/控制，「打开原始链接」交由调用方放播放器外。② **`material-watch` 同步**——两处 `PodcastAudioPlayer`（音频 embed + 文件音频）删 `openHref={material.url}`；并把素材观看页顶部右上角「原始链接」条件从 `youtube|podcast` 扩展到 **`youtube|podcast|audio`**（音频素材也在播放器外保留打开原文入口，不再靠播放器内置）。③ **笔记内页音频加「打开原始链接」**——`media-embed-nodeview` 动作栏最左新增 `<a href={attrs.original} target="_blank">`「打开原始链接」链接，样式照 callout「来源/精读笔记」按钮（`inline-flex items-center gap-0.5 rounded px-1.5 py-0.5 text-[11px] font-medium text-zinc-400 hover:bg-zinc-100 hover:text-teal-600` + `ExternalLink h-3 w-3`），`attrs.original` 存在且非 youtube/spotify（即音频）时显示；`PodcastAudioPlayer` 调用点删 `openHref`、`ExternalLink` 加入导入。④ **网页端内容宽放大**——`app-shell` 外层容器 `max-w-2xl`→**`max-w-3xl`(672→768px)**（手机不受影响，全 <768px；设置页单列表单在 768px 仍不显拉长、观感正常）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **全站内容区统一宽度 max-w-2xl（除笔记）+ 闪卡搜索框满宽 + 语伴标题栏提出卡片（tsc + build 全绿）**。用户反馈网页端各页内容宽窄不一（设置页 672px vs 闪卡合集页 1024px），闪卡合集页搜索框太靠左、右侧大空，语伴标题栏被会话卡约束。方案：**所有非笔记页统一内容宽 `max-w-2xl`(672px)，与设置页一致**。落地：① **`app-shell` 外层容器 `max-w-5xl`→`max-w-2xl`**（一处改、全局生效，闪卡/素材/词群/单卡/素材观看/收藏夹/复习/设置等全部统一到 672px；笔记区走 full-bleed 分支不受影响、布局不动）。② **闪卡合集页搜索框满宽**——`cards-view` 搜索框去掉 `sm:max-w-sm`、改 `flex-1` 撑满，与下方合集网格同宽，消除右侧大空；「+ 添加」按钮仍在搜索行右侧。③ **网格列数适配窄宽**——`cards-view` 合集网格 `lg:grid-cols-3`→`sm:grid-cols-2`（672px 下两列 ~325px）；`materials-view` 素材网格 `lg:grid-cols-4`→`sm:grid-cols-3`（去掉 4 列，避免 672px 挤压）。④ **语伴标题栏提出会话卡**——`companion-chat` 原来整页是一张含标题的圆角卡（标题被 `max-w-2xl` 卡约束）；重构为「外层 `max-w-2xl` 容器 + 顶部独立标题栏（`AI 语伴` 标题左 + 清空会话/收藏夹右，同其他一级页 `text-2xl`）+ 下方会话卡片（`flex-1 min-h-0` 占满剩余高度、内部自滚动）」，网页端与手机端同样结构，标题栏不再受会话卡限制。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **闪卡合集页/闪卡详情页搜索行统一、消除手机详情页多余搜索框（tsc + build 全绿）**。用户反馈网页端闪卡合集页与闪卡详情页布局乱、按原意搜索框应与功能按钮同排露出；手机端闪卡详情页凭空多出一个搜索框。修复：① **闪卡详情页去重**——`note-cards-tabs` 原来自己渲染一个搜索框（`searchQ`）+ `NoteCards` 又渲染一个（"搜索这个合集里的词 / 释义…"），两框并存是「多一个」的根因。现改成 **`NoteCards` 独占搜索行**：`note-cards-tabs` 删掉自带搜索框 / `searchQ`，只保留「类型标签（全部/生词/例句/语法）」过滤，文字搜索交给 `NoteCards`；`NoteCards` 加 `searchOpen` prop 承接显隐（移动端点标题栏放大镜展开、桌面 `md:block` 常驻），并加 `ref` 在「由关到开」时聚焦一次。② **搜索行右侧功能按钮**——`note-cards-tabs` 把「添加 / 选择 / 背诵」从 ⋯ 菜单抽到 `NoteCards` 搜索行右侧（`actions` prop，桌面 `md:flex` 显示：添加=填色主按钮、选择/背诵=描边按钮）；⋯ 菜单整体 `md:hidden`（仅手机保留，其中仍含查看原始笔记/添加/选择/背诵）。③ **闪卡合集页同款**——`cards-view` 顶部「添加」图标改为 `md:hidden`（仅手机标题栏），搜索框与「添加」按钮合成**同一行**（搜索框 `flex-1 sm:max-w-sm` + 右侧桌面「+ 添加」主按钮 `hidden md:inline-flex`），与闪卡详情页搜索行样式统一。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **素材观看页参照收藏夹重排 + 收藏夹混合排序/观看按钮/已导入标识/卡片样式统一 + 桌面搜索框回正 + 语伴媒体链接→带标题逐字稿（tsc + build 全绿）**。用户新一批反馈，逐条落地：**①素材观看页**——返回按钮文字「素材库」→「**收藏夹**」（`BackButton fallback=/companion/favorites`）；头部去掉「**待处理**」状态徽章（已导入的才显示「已导入」）；视频「打开原文」→「**原始链接**」；播客原始链接位置修正、与视频页统一——从页上方挪进**内容框顶部右上角**（`youtube|podcast` 都在播放器上、右上角）；`PodcastAudioPlayer` 手机端自适应（封面 `h-24 sm:h-32`、控制行居中、按键 `h-8 sm:h-9`、播放键 `h-12 sm:h-14`、`px-4 py-6 sm:px-8 sm:py-8`）。**②收藏夹**——文字 + 媒体**统一按时间倒序混合排**（`sortedItems`：records 用 `created_at`、materials 用 `created_at??updated_at`，不再分两块先后）；筛选面板标题「语言」→「**筛选**」（因其中还有类型）；媒体卡加**「观看」按钮**（`Play` 图标，放「导入笔记」**前**、跳 `/materials/{id}`）；媒体卡标题/缩略图**不再整卡可点**（点卡片只展开/收起，避免手机误触进观看页）；素材**已导入笔记**则「导入笔记」按钮→「**已导入**」（禁点、绿）且卡标题下附「已导入「笔记名」」；**统一会话收藏与素材收藏卡片样式**——两者底部都改为同一 `border-t` chip 条（语言用 `LANG_COLOR` 淡色、类型中性灰），位置颜色一致。**③桌面回正（搜索框 / 语伴卡片）**——`cards-view`、`note-cards-tabs` 的搜索框改回**桌面常驻**、手机才用标题栏搜索图标（图标 `md:hidden`，搜索框 `md:block` 常显）；语伴会话卡片保留但**居中 + `md:max-w-2xl`**（与设置/复习页统一，卡片 `md:mx-auto`）；复习主页内容包 `mx-auto max-w-2xl`。**④一级页标题**——核对复习/语伴/设置/闪卡/素材均 `text-2xl`、与设置页「设置」位置字号一致（笔记页未动）。**⑤语伴改「语伴」+ 去链接抓稿 + 媒体链接→带标题逐字稿**——笔记内页 AI 按钮「**AI 学伴**」→「**AI 语伴**」（`rich-text-editor` 按钮 title/aria + `ai-assistant-panel` 面板标题与注释）；`ai-assistant-panel` **删掉「链接→文字稿填输入框」**（`Link2` 按钮/`linkOpen`/`fetchLink`/`parseMediaUrl`/`YTDLP_DISABLED` 一并移除）；`companion-chat` 改**语伴功能**：直接发媒体链接（YouTube/音频直链）→ 先 `/api/materials/meta`（oEmbed）取**真实标题**、再 `/api/ai/transcribe` 生成**带标题逐字稿**，作为一条可「**导入笔记**」的回复（导入成 notes「原文」段 `appendNodesToNote`；逐字稿消息自动隐去「生成闪卡合集」按钮、对话框文案改「收录这份逐字稿」）。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **收藏页按反馈收口：标题去计数 / 筛选类型动态 / 导入图标换 / 导入媒体走 meta 可改字段 / 媒体点击预览 / 移语伴链接抓稿按钮（tsc + build 全绿）**。用户新一批收藏/语伴反馈逐条落地：① **收藏页标题**——「收藏 2/3」计数从标题行拿掉，改成纯「**收藏夹**」（数词统计不再进标题）。② **筛选类型动态**——`companion-favorites` 去掉硬编码 `TYPE_CHIPS`，改成 `presentTypes`（只列「数据里实际出现过」的类型：文字 / 视频 / 音频 / 播客 / 链接 / 媒体 / AI，没出现的不预设，参照素材库 `presentTypes` 同款），筛选面板类型区补「全部」按钮、为空时提示「暂无内容」，语言区维持 `presentLangs` 同逻辑。③ **导入按钮图标**——右上角「导入媒体」按钮 `Upload`（云上传）改成 **`Plus`**（往收夹导入 / 添加），语义更贴。④ **导入媒体重新识别 + 可改**——收藏页导入从「内联 URL 框 + `parseMediaUrl` 硬塞 `collectMediaRecord`（标题=「YouTube 视频」占位、无来源无语言）」改成**复用素材库 `AddMaterialPanel`**（`collections={[]} hideCollection` 存成单条）：走 `/api/materials/meta` 自动识别**标题 / 来源频道 / 类型 / 语言**，`oEmbed` 取真实标题（不再「视频标题没有」），语言由 `detectLang(title)` 智能判断、错了可手动改，缩略图一并抓回；`AddMaterialPanel` 加 `hideCollection` prop（收藏夹场景隐藏「归入合集」选择，直接存单条）。⑤ **媒体点击预览**——收藏夹里媒体卡标题/缩略图变成**可点链接**跳素材观看页 `/materials/{id}`（有播放器内嵌 YouTube / Spotify / 音频、可导入笔记），点击不再只当作展开箭头；展开/收起仍由右侧箭头控制。⑥ **语伴移链接抓稿按钮**——`companion-chat` 删掉底部「粘贴链接转文字稿」`Link2` 按钮及其输入面板 / `fetchLink`（用户反馈：贴 YouTube 链接直接生成文字稿进文本框很怪），媒体导入统一走收藏夹入口：收藏夹识别成的媒体文件可先导入笔记、再做文字稿等精读。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **语伴与收藏大改版：搜索框进标题栏 / 闪卡详情添加改粘贴识别 / 语伴会话卡片化+直接生成闪卡合集 / 收藏夹手风琴+搜索+语言类型筛选+导入媒体（tsc + build 全绿）**。用户新一批多需求，逐条落地：**① 搜索框→标题栏搜索图标（页面更干净）**——`cards-view` 标题栏右侧放**「搜索 + 添加」两个图标**（`h-9 w-9`），点搜索图标才展开搜索框（原常驻搜索行删除，`query` 状态与 `q` 过滤逻辑保留，补清空 `✕`）；`note-cards-tabs` 详情页标题栏改为**「搜索图标 + ⋯ 菜单」**，搜索图标展开文本框按正反面过滤卡片，⋯ 菜单去掉 `md:hidden` 改成全尺寸通用（添加 / 选择 / 背诵 / 查看原始笔记 都收进来），桌面端移除重复的 选择/添加/背诵 三按钮。**② 闪卡详情页添加改粘贴识别**——`AddNoteSheet` 从「单张正面/背面表单」改成**粘贴识别式**（同 `AddCardsModal` 模式）：粘贴词表 / Excel 表格 / 词—释义 → 「识别成卡片」复用 `parseCards` 出可编辑列表（正/背面、删除、加一行）→ 「确认添加（N张）」整批 `insert`；默认语言取合集主导语言；语法卡正面保持原文、不塞中文解释。**③ 语伴会话卡片化（非全屏）**——`companion-chat` 从全屏铺满改成**一组圆角会话卡片**：网页居中（`md:max-w-3xl`）、四周留白、带边框阴影更立体；手机端不铺满、四周留缝；头部改卡片内吸顶（不再全屏渐变头），会话区高度按 `env(safe-area-inset-top/bottom)` 与底部导航精确计算不溢出。**④ 语伴直接生成闪卡合集**——每条 AI 回复动作区加**「生成闪卡合集」**按钮，把 `points` 直接建成一个独立闪卡合集（`notes source_type:'cards'` + 卡片 `insert`，`detectCardLang` 定语言），免去「先导入笔记再转卡」；语法卡把 `conjugations` 接续规则与 `extra` 例句接到背面。**⑤ 语法点泰语在前**——`/api/ai/assistant` `SYSTEM_PROMPT` 的 grammar 点 `front` 从「原语言 + 简短中文名」改成**必须写目标语言典型例子**（如泰语礼貌词直接写 ครับ / ค่ะ），并加规则「语法点 front 用目标语言写、中文说明放 back、例句放 extra」，保证生成的卡片正面带得到泰语、不纯是中文语法解释。**⑥ 收藏夹手风琴 + 搜索 + 筛选 + 导入图标**——`companion-favorites` 文字/媒体卡片**默认折叠**（点头部展开回复，收起时只留一行预览，收藏多不再翻半天）；标题栏右侧改 **搜索 / 筛选 / 导入 三个图标**：搜索按 提问/回复/素材标题 过滤，筛选按**智能识别语言 + 类型**（文字 / 视频 / 音频 / 链接 / 媒体）过滤，导入图标走素材库同款**智能识别**（`parseMediaUrl` 判定视频/音频 → `collectMediaRecord` 入库，其余提示不识别）。原装饰性爱心图标移除。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。
- 2026-08-26 **四点收口：闪卡页桌面搜索框长度 / 桌面文件夹可折叠树 / 一级页标题位置统一 / 接入 Inter 字体（tsc + build 全绿）**。用户在手机上反映后逐项处理：① **闪卡合集页搜索框**——桌面端从 `sm:max-w-md`(448px) 收紧到 `sm:max-w-sm`(384px)，并去掉 `sm:justify-between`（搜索框与「添加闪卡」不再左右拉到两端留大片空档，改为同行靠左、正常 gap）。② **网页端文件夹可展开/收起**——`notes-workspace` 桌面侧栏从「平铺缩进树（全部永远展开）」改成**可折叠树**：用 `folderTree(folders)` 的 roots/children，递归 `renderFolder`；有子夹的顶层箭头 `ChevronRight` 点一下展开/收起（`openFolders: Set<id>` 记忆状态），无子夹同位留 `w-6` 占位对齐；点名字仍是筛选（父夹含其全部子夹下笔记），悬停 ⋯ 菜单不变；**初始全部展开**（保持原有「层层全可见」观感，想收哪层点箭头）。③ **移动端一级页标题位置统一**——各一级页标题本就都是 `text-2xl`(24px) 同字号，唯一偏差是笔记页移动端头 `pb-2`(8px) 比其它页 `.page-header`/语伴头的 `pb-3`(12px) 少 4px，导致标题相对底线下移；把 `notes-browser` 头部 `pb-2`→`pb-3`，六处一级页（笔记/复习/语伴/设置/闪卡/素材）标题字号与垂直位逐一对齐。④ **字体更像 iOS**——自托管 **Inter**（最接近 SF Pro 的可选字体，`next/font/google` 构建期下载并本地托管，浏览器不再请求 Google），作为**非苹果设备兜底**：真 iPhone/Mac 仍走 `-apple-system` 原生 SF Pro，安卓/网页端才落到 Inter；`--font-sans` 与 `body` 字体栈把 `var(--font-inter)` 放到 `-apple-system`/SF Pro 之后、`BlinkMacSystemFont` 之前。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。
- 2026-08-26 **修复「笔记内页打不开」根因 + 编辑栏 AI 学伴按钮被裁 + 手机端解释气泡与原生选中菜单重叠 + 移动端整体放大一档（tsc + build 全绿）**。本轮先复现再修：**① 笔记内页打不开（阻塞）**——本地用临时 probe 路由 + mock NoteEditor + Playwright 在手机视口复现到两条报错：`Duplicate extension names found: ['link']`（Tiptap v3 的 `StarterKit` 内置 link 扩展与自定 `LinkAuto` 重名）和 `Cannot read properties of null (reading 'can')`（`Editor.can()` 读 `this.commandManager.can()`，而 `commandManager` 已被 `destroy()` 置 null——编辑器处于「已销毁但实例仍 truthy」状态，旧 `e ? e.can()` 守卫拦不住）。**修复**：`editor-extensions.ts` 把 `StarterKit` 改 `StarterKit.configure({ link: false })`（关掉内置 link、保留 `openOnClick:false` 的 LinkAuto）；`rich-text-editor.tsx` 的 `toolbarActive` selector 加 `e.isDestroyed` 早退（返回全 false 默认值，等新建编辑器就绪）。两处修完 probe 零报错、正文正常渲染。**② 编辑栏 AI 学伴按钮看不到**——手机视口实测 AI 按钮右缘 412px > 390px，被裁出屏：`flex flex-1` 内层因 flex `min-width:auto` 不肯收缩，把工具栏撑到 428px。**修复**：note-editor 内容区容器加 `min-w-0`、RichTextEditor 根加 `min-w-0`，AI 按钮回到 339–373 完全可见。**③ 手机选字解释气泡与原生选中菜单重叠**——「✨ 解释」气泡本来放在选区上方（`top: explain.top - 8; translateY(-100%)`），恰与 iOS/Android 原生「拷贝/翻译」菜单同位置叠住。**修复**：粗指针（`pointer: coarse`，手机/平板）改放选区下方（`top: explain.bottom + 8`），原生菜单在上、气泡在下不再叠；鼠标仍放光标上方。实测粗指针下气泡 top 218 > 选区底 210、完全在视口内。**④ 移动端整体放大一档**——用户问「是否苹果规范、能否再大气些」：根字号移动端 `html{font-size:17px}`（接近 iOS 舒适阅读字号），所有 rem 尺寸成比例放大、布局关系不变；桌面 16px 不动。实测 17px 下无横向溢出、AI 按钮仍可见、正文正常。**清理**：删临时 probe 路由与脚本、撤销 proxy 放行、`rm -rf .next` 后 `npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿。本条**取代**「已完成」顶部先前「笔记打不开未能本地复现」的存疑条目（现已定位并修复）。
- 2026-08-26 **接上一轮「全站尺寸回正」的复查收口：选中浮条 / 语伴提示词与语言下拉 / 全站占位提示统一放大 + 笔记文件夹行与网格卡放大（tsc + build 全绿，已部署 Vercel）**。用户手机实测再指出：卡片选中后浮出的「全选 / 已选 N / 取消」横条太小；语伴页文本框提示词太小；语伴语言下拉（「自由」）文字太小；若小则回到之前的大尺寸并把其他页面相关文字统一；另笔记页文件夹行太窄、列表/网格两视图尺寸要调、网格卡能不能做大。**落地**：① **选中浮条**（note-cards）：容器 `rounded-full px-3 py-1.5`→`px-4 py-2.5`，左「全选/取消全选」、中「已选 N」、右「删除所选/取消」由 `text-xs`→**text-sm**（原本轮稍前刚被收小，现回大）。② **语伴页**：文本框提示词 `placeholder:text-xs`→`placeholder:text-sm`；语言 `<select>`（目标语言/自由）`px-1.5 py-0.5 text-xs text-zinc-500`→`px-2.5 py-1.5 text-sm text-zinc-600`。③ **全站占位提示统一**：跨 24 个文件把 `placeholder:text-xs` 全部收敛成 `placeholder:text-sm`（占位 = 正文 14px，不再比输入内容小一号），覆盖 login / notes-workspace / rich-text-editor / review-session / collect-words / recognition-rules-form / companion-favorites / folder-picker / import-material / collection-detail / cloze-session / ai-assistant / card-tile / card-sidebar / cards-view / group-browser / material-watch / materials-view / mixed-session / note-cards / notes-browser / companion-chat / theme-detail / add-material-panel 等。④ **语言 `<select>` 统一**：companion 下拉（上述）、review-session `px-2 py-1 text-sm`→`px-2.5 py-1.5`、rich-text-editor 工具栏块型下拉 `px-1.5 py-0.5`→`px-2 py-1`（文本本已 text-sm 不动）；其余（AddNoteSheet / 编辑合集 / 识别规则 / 块样式）本已 text-sm。⑤ **笔记文件夹行太窄**（notes-browser）：文件夹行与「全部笔记」行 `px-3 py-2.5`→**`px-4 py-3`**（更高更松）。⑥ **笔记网格卡做大**（notes-browser NoteItem 网格视图）：卡内衬 `px-3 py-2`→`px-3.5 py-2.5`、标题 `text-sm`→**text-base**、摘要 `text-xs`→`text-sm`、时间 `text-[11px]`→`text-xs`（两栏 `grid-cols-2` 不变，卡内容显著变大不再局促；列表视图同款内衬一并放大）。⑦ card-tile「点选这张/点击翻面」提示 `text-xs`→`text-sm`。审计：全站 `<select>` 仅 companion 一处 text-xs，已回 text-sm；`placeholder:text-xs` 剩 **0**。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；随后 `vercel deploy --prod --yes --scope muriel-s-project` 成功（首次「fetch failed」为瞬时、重试即过，deployment `nestlingo-1iyaxtv0k-muriel-s-project.vercel.app`，readyState Ready / target production / 生产别名 `nestlingo-psi.vercel.app` 已指向该部署）。dev 需硬刷新。
- 2026-08-26 **接上一轮「全站尺寸回正」逐点收口：语言筛选器/搜索框/新建添加提示/标题栏顶部留白/编辑栏单行横滑，全部放大到舒适尺寸并部署 Vercel（tsc + build 绿灯）**。用户在手机实测后列点：语言筛选器文字、新建添加的提示文字、搜索框大小、所有按钮图标、语伴页搜索框图标都要改；标题栏尤其按钮太靠手机顶部；笔记编辑栏在手机端若放不下一行，除三个固定按钮外其余应横滑自适应而非换第二行。**落地**：① **语言/筛选拆块**（text-xs→text-sm + py-1.5/py-1→py-2）——review/page、cards-view、test-errors、group-browser、materials-view、review-session、collect-words、filter-sheet。② **搜索框**：notes-browser、notes-workspace 搜索容器 px-2.5 py-1.5→px-3 py-2；collect-words 搜索输入 py-2→py-2.5。③ **新建/添加表单**：「标题/正面/背面/文件夹名/示例词」等输入 py-2→py-2.5、占位提示 `placeholder:text-xs`→`placeholder:text-sm`（note-cards-tabs AddNoteSheet、add-cards-button、share-modal、convert-to-cards、collect-words）。④ **语伴页**：头部「清空会话」「收藏夹」按钮 h-7→h-9、Heart 图标 h-3.5→h-4（底部工具 TOOL_BTN 本已 h-9 w-9）。⑤ **标题栏顶部留白**：`.page-header` + notes-browser/companion-chat/note-editor/mobile-folder-view 头部顶部 padding `env+0.75rem`→`env+1rem`（标题栏不再顶到状态栏）。⑥ **编辑栏单行横滑**：桌面端工具栏从 `flex flex-wrap`（会换行）改为 **单行横滑 + 固定撤销/重做/AI**（`no-scrollbar flex min-w-0 flex-1 overflow-x-auto` 滑动区 + `shrink-0` 固定组），与移动端同一套，任何宽度都不换第二行；移动端工具栏行 `py-1.5`→`py-2`、"Aa 格式"按钮 `px-2 py-1`→`px-2.5 py-1.5`。note-editor 头部因 pt 增高，rich-text-editor 两条吸顶工具栏 `top` 从 `env+3.75rem` 同步改 `env+4rem`。审计确认再无小尺寸图标按钮（h-7 仅在播客播放器/素材缩略图等非核心处）。`npx tsc --noEmit` + `npm run build` 全绿；`vercel deploy --prod` 成功（deployment `dpl_6AKK8UHsfNZp7FP9bdeAwDbCQWxg`，`nestlingo-psi.vercel.app` 生产别名已指向该部署，readyState READY / target production）。dev 需硬刷新。
- 2026-08-26 **全站尺寸回正到苹果 iOS 舒适规范：一级页标题 24px + 控件全面放大 + 笔记内页自适应（tsc + build 绿灯）**。用户在手机实测后反馈「一级页标题太小、之前改小处局促、笔记内页不铺满屏显示不全」，拍板：一级页大标题恢复 **text-2xl(24px)**，其余按 iOS ≈44pt 舒适可点目标统一放大。**标题层级**：① 一级页（笔记/复习/语伴/设置，另 /cards、/materials）h1 `text-xl`/`text-base`→**text-2xl**；② 次级页（闪卡详情/文件夹/主题/卡详情/合集/测试/收藏/复习全部子页）`text-base`→**text-lg**。**控件放大**：③ 新增 `.icon-btn` 工具类（`inline-flex h-9 w-9` 36px + 图标 h-5）统一一批手写 `h-7`/`p-1.5` 图标按钮（返回/新建/视图/⋯/头动作等）；④ `.btn-brand` `py-2`→`py-2.5`，填色/描边主按钮统一 py-2/2.5→40px；⑤ 搜索框输入 `py-2`→`py-2.5`(42px)、表单文本框 **py-2→py-2.5**、行内重命名 `py-1`→`py-1.5`、紧凑 chip/py-1 标签筛选项→py-1.5；⑥ `.page-header` 及各处手写头上/下 padding `0.5rem`→**0.75rem**（更松弛不局促），原生 `<select>` 一律不动。**笔记内页自适应（关键，note-editor）**：标题从 `rows=1 overflow-hidden` 纯 JS 增高改为 textarea 加 `min-h-[1.5em]`+`max-h-[40vh]`、`overflow-hidden`→`overflow-y-auto`（超高可滚、不再裁标题后半截），自动增高 effect 补 `window resize` 监听 + `document.fonts.ready`（中/泰/韩异体字或字体未定稿不量矮被剪）；页面根 `min-h-screen`→**`min-h-[100dvh]`** 与 AppShell 对齐，底部内容不再被顶出屏；头部 pt/pb 提高 + 控件 h-9 后背高 = `env+3.75rem`，`rich-text-editor` 两条吸顶工具栏 top 从 `env+2.75rem`**同步改 `env+3.75rem`**（避免与头部重叠错位）。**执行**：分三组并行（notes/文件夹组、review/卡片/词群组、素材/语伴/表单组），titles/sizes/paddings 只改尺寸不动逻辑/文案/颜色/断点。`npx tsc --noEmit` + `NODE_USE_ENV_PROXY=1 npm run build` 全绿；dev 需硬刷新。本条覆盖此前「各页吸顶头统一收薄到 28px（h-7）/标题压小」那一阵的过度局促，与本条之前的「纠正新建笔记/清理 ⋯」条目互补。
- 2026-08-26 **纠正：文件夹「新建笔记」改成与笔记首页一致（首页回原样，不再让首页跟着文件夹走）+ 网页端闪卡详情页标题栏 ⋯ 菜单清理（tsc + build 绿灯）**。上一条把**首页**新建笔记改成 ghost 去迁就文件夹，用户纠正方向反了：**文件夹里的新建笔记应跟笔记首页的一致**，不是反过来。① **首页回原样**——`notes-browser` 头部 `<NewNoteButton />`（icon 描边版，`border-zinc-200 text-zinc-600` icon h-4），与旁边的「新建文件夹」按钮同款，首页两个按钮不再样式打架。② **文件夹页改成 same**——`mobile-folder-view` 头部 `<NewNoteButton folderId variant="ghost">` → `<NewNoteButton>`（icon 描边版，与首页一致）；旁边`ViewToggleButton` className 同步改描边版（`border-zinc-200 text-zinc-600 hover:bg-zinc-50`），跟随新建笔记不跳色。③ **网页端闪卡详情页 ⋯ 菜单清理**——`note-cards-tabs` 标题栏右侧 ⋯ 菜单整体 `md:hidden`（桌面/网页隐藏，因 选择/添加/背诵 已在搜索行右侧，菜单重复）；移动端不动（添加/选择/背诵 仍收在 ⋯ 里）。菜单项里原有的 `md:hidden`（添加/选择/背诵）已随整层隐藏而冗余、无害。tsc + build 绿灯；dev 需硬刷新。本条**取代**上一条里「笔记首页新建/视图按钮样式」与「闪卡详情 ⋯ 菜单在桌面保留」的部分。
- 2026-08-26 **闪卡详情页网页端搜索旁加 选择/添加/背诵 + 移动端「开始背诵」缩短成「背诵」+ 笔记首页新建/视图按钮样式统一 + Vercel 生产部署（tsc + build 绿灯，已部署）**。用户反馈：网页端闪卡详情页（`/notes/{id}/cards`）可不同于移动端——**在搜索框同一行右侧加 选择 / 添加 / 背诵 三个按钮**（仿 `/cards` 闪卡页搜索行模式，`md:flex`）；移动端 ⋯ 菜单里的「开始背诵」缩短成「背诵」。另把笔记首页标题栏的**新建笔记**按钮样式/大小统一成与文件夹里笔记列表页一致（`<NewNoteButton variant="ghost" />`，原来桌面 icon 变体偏小），并让旁边**视图切换按钮**的颜色/样式跟随协调（同 `h-7 w-7 rounded-lg text-teal-600 hover:bg-teal-50`）。**改动**：① `note-cards-tabs`：桌面 `actions` prop 传 选择/添加/背诵 三按钮（选择=切换选取态、添加=弹 AddNoteSheet、背诵=`/review?note=`，脱胎于 `cards-view` 模式）；菜单「开始背诵」→「背诵」，且 添加/选择/背诵 三项加 `md:hidden`（桌面不重复）。② `note-cards`：加 `actions?: React.ReactNode` prop，搜索行改为 `flex items-center gap-2`，右侧桌面仅容器 `hidden shrink-0 md:flex`。③ `notes-browser`（笔记首页）：新建笔记换 `<NewNoteButton variant="ghost" />` 并置最右、新建文件夹置左。④ `mobile-folder-view`（文件夹列表页）：`ViewToggleButton` 加 ghost 同款 className（`h-7 w-7 rounded-lg text-teal-600 hover:bg-teal-50`）。⑤ 部署：`vercel deploy --prod --yes --scope muriel-s-project` 成功，deployment `dpl_FxD5fBxaAj4NR8bwta1AC1Uv6drY`，readyState READY、target production，别名指向 `https://nestlingo-psi.vercel.app`（正式域名）。tsc + build 绿灯；dev 需硬刷新。本条**取代**上一条的「笔记首页新建/视图按钮样式」部分（本轮统一收口）。
- 2026-08-26 **笔记视图（列表/网格）跨页面记住 + 语伴标题区补分隔线 + 笔记页新建按钮顺序（tsc + build 绿灯）**。用户三连：**① 笔记视图勿每次重进重置**——新增 `src/lib/notes-view.ts` 的 `useNotesView()` 钩子（`nestlingo:notes-view` key，挂载后读一次 localStorage、切换即写回；用 useEffect 读而非初始化同步读，避免 SSR 水合不一致）；接入 `mobile-folder-view`（文件夹/全部笔记页头部切换，受控）与 `NoteList`（非受控内状态），各页共用同一 key 故视图彼此打通。**② 语伴页标题区缺分隔线**——`companion-chat` 头部 `<header>` 的 `border-zinc-100`（太淡、在绿渐变上几乎不可见）改 `.page-header` 同款 `border-black/[0.06]`，吸顶不遮对话。**③ 笔记页标题按钮顺序**——`notes-browser` 头部从「新建笔记＋新建文件夹」对调为「新建文件夹左、新建笔记最右」（主要动作靠右，同其它页面）。tsc + build 绿灯；dev 需硬刷新。**注**：本轮「笔记打不开」未能本地复现（打开笔记需登录会话，RLS 拦截匿名读取；用 anon key 查库返回空、无法取到真实笔记内容），已核查笔记列表→文件夹→详情→编辑器→callout 插件的整条渲染链均无崩溃点，疑似前端运行时问题，等用户提供具体症状/控制台报错再定位。
- 2026-08-26 **合集详情页选择条再缩小 + 右侧「取消/删除所选」动态切换 + 添加表单带语言选择（默认合集语言）+ 闪卡页添加弹窗改「添加闪卡合集」（tsc + build 绿灯）**。承接上一条，用户逐点打磨：**① 选择浮条再小一点**——从 `max-w-md rounded-2xl px-4 py-2 text-sm` 收成 `max-w-sm rounded-full px-3 py-1.5 text-xs`，更贴底部、不占屏。**② 退出选择别再拉菜单**——浮条右侧按钮改成无选中时显示「取消」（点击直接 `onSetSelecting(false)` 退出选择模式），点全选后（有选中）才显示「删除所选」；即「进了选择能一键取消，不用再拉开 ⋯ 菜单」。浮条仍 `sticky bottom-4`（不压底导）、左全选 / 中已选 N / 右(取消|删除所选)。**③ 添加表单加语言筛选器**——`AddNoteSheet` 加 `defaultLang` prop；`collectionLang(cards)` 按卡片语言多数决算出合集主导语言作默认值；表单新增语言 `<select>`（`LANG_LABEL`），`insert` 用所选 `lang` 代替原来的 `detectCardLang` 自动识别。**④ 闪卡页添加弹窗标题**——`add-cards-button` 的 `AddCardsModal` `<h2>` 从「添加闪卡」改为「添加闪卡合集」，描述同步「存成一个闪卡合集」。tsc + build 绿灯；dev 需硬刷新。本条**取代**上一条里「AddNoteSheet 用 detectCardLang」部分（改由表单语言选择器接管）。
- 2026-08-26 **/cards 回到「标题栏小添加」+ 合集详情页标题进标题栏（与返回同行）+ 添加/背诵/选择收进头部 ⋯ 菜单（tsc + build 绿灯）**。承接上一条，用户逐点纠正：**① /cards 去 ⋯ 菜单**：不要批量选择 / 导出全部，改成**标题栏右侧一个小「添加」按钮**（`md:hidden` 手机端）；**桌面端**「添加闪卡」按钮放**搜索框同一行右侧**（`md:inline-flex`）。语言行继续保持单独一行自由换行。**② 合集详情页标题与返回同一行**：把标题从页内/下一行挪进**标题栏**（返回 ← + 标题同一行，同其它次级页）。**③ 添加 / 开始背诵 / 选择 收进标题栏右侧 ⋯ 菜单**（移除列表里的工具栏）；「查看原始笔记」也收进该菜单（非纯闪卡文件才显示）。**④ 「选择」→ 底部浮出小条**：NoteCards 内 `sticky bottom-4` 浮条，**左全选 / 右删除所选**（+已选数），批量删只作用于「当前可见 ∧ 选中」的卡。**⑤ 「添加」→ 弹出表单**：`AddNoteSheet` 底部弹出（移动端上下排列：正面/背面文本框竖排），桌面居中弹窗；`insert` 带 `detectCardLang`。**⑥ 故事模式选词文案简短**：把「勾选要编进故事的词（已选 N 个）」两个长句压成「选词 + 已选 N 胶囊」，标题行不再挤变形。**⑦ 删除 NoteCards 原工具列**（添加/开始背诵/批量开关），改由头部 ⋯ 菜单驱动；NoteCards 变纯「搜索 + 网格 + 可选选择性浮条」。tsc + build 绿灯；dev 需硬刷新。**本条**取代上一条（/cards ⋯ 菜单、批量、导出全部均已撤，合集详情页标题/工具列改动被本条的头部 ⋯ 菜单方案覆盖）。
- 2026-08-26 **/cards 头部 ⋯ 隐藏菜单 + 合集卡「选择语言」+ 合集详情页标题/工具列换位 + 语言下拉选项字号统一（tsc + build 绿灯）**。承接同日上一条，用户对上一版 /cards 布局仍不满意（添加闪卡位置、语言行久了会挤右侧批量按钮、合集页开始背诵/添加位置、标题不在标题栏）。**① /cards 头部改 ⋯ 隐藏菜单**：左上返回，「标题 闪卡」右 + 右上 ⋯ → 添加闪卡 / 选择合集（进入批量）/ 导出全部；「添加闪卡」不再作为裸露按钮摆搜索行/页头。**② 语言 chips 独立一行自由换行**：批量操作从语言行里撤出（语言再多种类也不挤），批量「全选/已选 N/删除所选(红)/完成」栏出现在列表上方。**③ 导出全部**：`exportAll` 直接拉 `cards` 全表做成 CSV（不受当前筛选影响）。**④ 合集卡 ⋯ 菜单加「选择语言」**：除重命名/删除外，新增内联语言下拉（保存/取消），一键把该笔记全部卡片 `lang` 改成所选语言，兜底自动识别失败、防止合集语言识别错。**⑤ 合集详情页标题进标题栏**：/notes/{id}/cards 标题行 = 返回 + 查看原始笔记 + 合集标题，去掉页内 `<h1>`（kind 标签页仍在）。**⑥ 合集详情页工具列换位**：批量操作（小文字）放左，「＋添加 / 开始背诵」（同组统一样式）放右。**⑦ 语言下拉选项字号统一**：把移动端 16px 防放大规则里的 `select` 剔除（原生下拉不触发聚焦缩放），新增 `select option{font-size:.875rem;line-height:normal}` 全局收敛所有下拉选项到 14px，同名下拉不再忽大忽小。tsc + build 绿灯；dev 需硬刷新。

- 2026-08-26 **复习页三版：背卡页按钮语义/词库弹窗高度/完形排布/添加闪卡位置与语言兜底/合集闪卡页批量化（tsc + build 绿灯）**。承接「复习页二版」，用户逐点微调。**① 复习主页按钮改名**：主卡「复习」改为「待加强」（`?scope=weak`，语义 = 上次评分≤2 或 反复忘且间隔<7 天，与非到期复习区分）；「闪卡合集」里若某合集正被背，「背」按钮灰化为不可点「正在背」（整行仍可点进看内容）。**② 词库弹窗固定高度**：背诵页抽出的「词库」弹窗外层改成 `h-[min(80vh,30rem)]`，待复习/已复习 两个 tab 切换不再因内容多少改变高度；底部只留 `查看全部 → 闪卡页`。**③ 故事模式**：默认随机选 8 个；新增「重新随机」按钮（一键换一批，数量也是随机 1~最大 8）；选词列表 `max-h-[45vh]` + `overscroll-contain` 消除无意义页面滑动。**④ 完形填空排布**：输入框 + 「核对」（去掉图标）一行、「显示/隐藏答案」下一行占满宽。**⑤ 测试入口 + 混合练习**：`测试练习 →` 进入 `TestPick` 选择题（选择题/完形填空/混合练习）；新增 `MixedSession`（`mode=mixed`，选择题与填空题随机交替、落错题集）。**⑥ /cards 添加闪卡归位**：从 `PageHeader` 头部动作区挪回 `CardsView` 的 actions（搜索行右，桌面同行/手机堆叠），空态右上单独渲染——不再挤在导航标题里显得突兀。**⑦ 添加闪卡弹窗加语言选择器**：默认「自动识别」，可批量强制一种语言兜底（防止特殊词/人名自动识别失败），`save()` 按 `forceLang` 覆盖每张卡。**⑧ 合集闪卡页（`/notes/{id}/cards`）重排**：`开始背诵` 从头部挪到列表搜索行右侧（头部只剩返回 + 查看原始笔记，更清爽）；`NoteCards` 工具栏改「批量操作」模式（右侧开关，展开后 全选/已选 N/删除所选(红)/完成 批量栏，参考卡片页合集列表），「＋ 添加」保留左侧（单张手动加卡仍需）。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **复习页二版：按「简单为主 + 能藏菜单」重塑练习排版与闪卡页归属（tsc + build 绿灯）**。基于上一版「复习+闪卡融合」的落地，用户逐点调整。**① 一级导航删闪卡**：`app-shell` NAV_ITEMS 去掉 `/cards`（笔记/复习/语伴/设置），闪卡页改为复习页「查看更多」进入的子页。**② 闪卡页 /cards 重排**：`PageHeader` 加 `backHref="/review"`（返回复习），「＋ 添加闪卡」从 `CardsView` 的 actions 挪到头部右侧动作区（与笔记页「新建」放头部一致，空态不再重复渲染）；`CardsView` 不再传 actions。**③ 继续背大卡改两行**：`继续背 | 复习` 一行、`故事模式 | 测试练习` 一行（grid-cols-2），去掉原「测试▾」下拉。**④ 测试练习 → 测试选择页**：新增 `TestPick`（`mode=test-pick`）——进一个页面挑出题方式：选择题（看词选释义）/ 完形填空（语境填词）/ 混合练习（两类随机交替），各自是该合集的会话。**⑤ 新增 `MixedSession`（`mode=mixed`）**：基于 `Card[]` 随机把每题出成「选择题」或「填空题」（输入答案），看答案记错题并落错题集，结束出成绩+错题回顾+再来一次。**⑥ 闪卡合集列表重排**（参考闪卡页合集卡片布局）：每行 = 图标（独立闪卡 `Layers`/笔记合集 `BookMarked`）+ 标题 + `共 N 张 · 待复习 M` + 「正在背」徽标 + 「背」按钮；**点整行 → 进该合集的闪卡页**（`/notes/{id}/cards`，独立闪卡回 `/cards`）查看内容，**点「背」→ 直接背诵**（背诵落 `ln_active_collection`，返回后该合集成为「正在背」）。**⑦ 切换合集弹窗去背/测**：纯点选切换（保留「正在背」徽标），不再塞背/测按钮。**⑧ 错题集入口文字改「查看更多 →」**，与闪卡合集板块统一。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **复习 + 闪卡融合成一页：复习主页为主、闪卡合集作板块（tsc + build 绿灯）**。用户要求「复习和闪卡融合成一个页面」，以复习页为主、闪卡合集是其中一个板块，按之前敲定的方案落地。**一句话模型**：一个合集 = 一篇笔记（`source_type:"cards"` 的笔记/独立闪卡即自由添加的合集），背诵按整篇笔记混合所有 kind；`笔记内闪卡列表`仍保留 生词/例句/语法 分类查看（kind 细分只在那看，背诵不细分）。**复习主页 `review-home.tsx` 重写**：① 顶部 4 统计卡（今日已复习/到期待复习/已掌握/待加强）保留作参考；② 主要大卡 **继续背**——当前合集标题 + `共 N 张 · 待复习 N 张` + 露卡面（下一张待复习卡正面）+ 练习方式按钮组 `继续背`/`测试▾`（标准/完形填空 dropdown）/`故事模式`/`复习`（原「复习待加强」改名为「复习」，`?scope=weak`）；③ **闪卡合集板块**：标题行右侧 `查看更多 →`（进 /cards 闪卡页，可看各合集 + 添加闪卡），下面最近在背/刚转成/新建的轻列表（点即切换当前合集）；④ **错题集**板块：预览前两条 + `错题集 →`（`?scope=errors`）；⑤ **切换合集**弹窗（点某合集即切换当前，也可直接背/测）。**合并逻辑 `mergeByNote`**：把 `noteId::kind` 级合集聚成 note 级（三种 kind 融合成一篇、背诵不细分），未归档卡归「独立闪卡」。**砍掉**：按主题 AI 整理板块（theme 视图）、全局「导出全部」按钮（每合集自带导出）。**背诵页 `review-session.tsx`**：底部常驻待复习/已复习列表块删掉，改**隐藏菜单「词库」**（进度行右侧小按钮）→ 底部弹出弹窗（待复习/已复习 两个 tab + 可编辑/删除的卡列表 + 底部 `查看全部 → 闪卡页` 链接），背诵界面保持专注不被打扰。**闪卡页 `cards/page.tsx`**：去掉按来源/按主题切换与 ExportCardsButton、只留 `添加闪卡`（每合集导出/管理仍由 `cards-view` 各卡自带）。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **修桌面端标题下空白：编辑栏改单行不换行（tsc + build 绿灯）**。用户反馈桌面端「标题和编辑栏中间又空了很大一片」——根因是上一条桌面端全暴露工具栏用 `flex flex-wrap`，在 `max-w-3xl/4xl` 容器里按钮太多会**换行占出额外高度**，标题下露出空白（与早前「消除换行占高与标题下空白」同因）。**改法**：桌面端内层行从 `flex flex-wrap` 改成 `flex items-center` + 内部 `no-scrollbar flex min-w-0 flex-1 overflow-x-auto`（**永不换行**，宽屏一行铺满全露出、窄了可横滑无滚动条），块样式 `<select>` 加 `shrink-0`；右侧 `shrink-0` 固定 `撤销/重做/AI`。桌面端因此既不换行占高、又保证全露出（未收进任何面板），callout 仍只显图标。移动端不变。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **编辑栏桌面端回到「全露出」、移动端保持顶部紧凑一条（tsc + build 绿灯）**。用户纠正：桌面端不该跟着变紧凑——**桌面端回到原全露出状态**（块样式 `select` + 字母格式 + callout 图标 + 插入三键 + 撤销/重做/AI 全露出、`flex-wrap` 换行、无面板），**移动端保持当前的顶部紧凑一条**（左边横滑区 + 右侧固定 撤销/重做/AI + Aa 格式面板）。**改法**：把上一条「顶部统一紧凑一条」拆成两条工具栏——桌面 `hidden md:block`（`applyBlockType/formatButtons/calloutsInline/insertButtons/aiButton/tableBar`，callout 同只显图标）；移动 `md:hidden`（原紧凑一条原样保留，滑动区 `px-2`）。两者都是 `sticky top-[calc(env(safe-area-inset-top)+2.75rem)]`，按断点择一显示；移动端尺寸自适应靠 `flex-1 min-w-0 overflow-x-auto` 滑动区 + 右侧 `shrink-0` 固定三键。tsc + build 绿灯；dev 需硬刷新。本条**取代**上一条「顶部统一紧凑一条」对桌面端的做法（移动端不变）。
- 2026-08-25 **编辑栏定稿「顶部紧凑一条」：滑动区 + 固定 撤销/重做/AI + 格式面板，callout 纯图标（tsc + build 绿灯）**。用户采纳 Notion 式底栏形式，但**底栏和「转成闪卡」悬浮按钮重合**，故把这一行**移回顶部**、改用**顶部紧凑一条**统一桌/移动端。拍板：**撤销/重做/AI 固定右侧**（同组、显眼、不随横滑），其余图标左侧可横滑；**callout 只显示图标**（桌面同）；**删掉「列表缩进」图标**（用户不需要，顺带解决超链接按钮被它占位的观感）；**超链接按钮功能本身正常**（选中文字加链接 / 已链接则去链接，`toggleLink` 未动）。**改法（`rich-text-editor.tsx` 四段）**：①**两套工具栏合成一条**——删桌面 `hidden md:block` 全露出长条 + 移动 `md:hidden` 底栏，改**单条 `sticky top-[calc(env(safe-area-inset-top)+2.75rem)]` 顶部吸顶栏**（吸在笔记页头部正下方，desktop env=0 处正是头部高 44px）：左侧 `no-scrollbar flex-1 overflow-x-auto min-w-0` 滑动区（`[Aa 格式][callout 图标][表格][图片][媒体]`）+ 竖分隔线 + 右侧 `shrink-0` 固定区（`撤销/重做/AI`），横滑只滚图标不带动页面、固定三键始终可见。②**「Aa 格式」面板改向下弹**（`absolute top-full` 吸在工具栏下）——块样式行（正文/标题/小标题/副标题/等宽）+ `grid-cols-5` 字母格式格网（加粗/斜体/下划线/删除线/高亮/列表/引用/链接）；桌面也用这块面板（原桌面端内联 `<select>` 块样式与内联字母格式按钮一并移除）。③**callout 只显图标**：`calloutsInline` 去掉 `<span>{label}</span>` 文字，只留彩色图标（`title={label}` 悬停提示）。④**删「列表缩进」**：移除 `formatButtons` 里的 `sinkListItem("listItem")` 按钮与 `IndentIncrease` 图标导入。**解决「格式面板被解释气泡遮挡」**：双向互斥——打开格式面板时清掉选中词「✨ 解释」气泡（`setExplain/result/explainError/collected` 复位）；选中词弹解释气泡时关掉格式面板（`selectionUpdate` 里 `setOpenPanel(null)`）。`openPanel` 类型保留 `"format"|"insert"|null`（插入面板已无入口，留空态）。媒体弹窗继续居中弹层；`tableBar` 渲染在工具栏下方。tsc + build 绿灯；dev 需硬刷新（Cmd+Shift+R）。本条**取代**上一条「桌面顶栏全露出 + 移动端吸键盘底栏」，最终编辑栏为**顶部统一紧凑一条**。
- 2026-08-25 **编辑栏改「桌面顶栏全露出 + 移动端吸键盘底栏（Notion 式）」（tsc + build 绿灯）**。用户彻底放弃上一版顶栏「格式/插入 双触发 + 面板」——选中词出现解释气泡后，点格式下拉会被遮挡、气泡不消失，据此弃用顶部面板形式，改学 Notion：**移动端底部编辑栏吸在键盘上，网页端保持最初全露出**。用户拍板：**撤销/重做/AI 固定不动**（显眼、不随横滑走），**其余图标可横滑**；「格式」收进面板，「插入」只有三个按钮（表格/图片/媒体）不值得做独立面板、直接放滑动区。**改法（`rich-text-editor.tsx` 工具栏整段重写，抽复用片段 `calloutsInline/formatButtons/blockStyleButtons/insertButtons/aiButton/tableBar`）**：①**桌面端（md+）** `hidden md:block` 顶部 `sticky top-12` 吸顶栏，**全部露出**、`flex-wrap` 换行铺开：`块样式 select + 字母格式（加粗/斜体/下划线/删除线/高亮/列表/引用/链接/缩进）+ callout 彩色图标（原文/生词/例句/语法）+ 表格/图片/媒体 + 撤销/重做/AI`，不再折叠成面板。②**移动端（<md）** `md:hidden` **`fixed inset-x-0 bottom-0 pb-[env(safe-area-inset-bottom)]` 吸键盘底部栏**：一行三区——固定区（`shrink-0`）`撤销/重做` → 滑动区（`no-scrollbar flex-1 overflow-x-auto min-w-0`）`Aa 格式 + callout + 表格/图片/媒体`，可左右横滑无滚动条 → 固定区 `AI`。③**「Aa 格式」向上弹面板**（`absolute bottom-full` 吸在工具栏上方，`openPanel==="format"`）：顶部块样式行（正文/标题/小标题/副标题/等宽）+ `grid-cols-5` 字母格式格网；backdrop/再点收起。**取消「插入」面板**（`openPanel` 只剩 `"format"|null`）；移除 `Plus` 图标（不再有「＋插入」触发按钮）；删掉 `ChevronDown` 在格式按钮上的多余逻辑保留。④**媒体弹窗改居中弹层**（原 `absolute right-0 top-full` 吸顶下拉，桌/移动各一套会打架）→ `fixed inset-0 m-auto w-[360px] z-40` + `bg-black/30` 遮罩，桌/移动通用。⑤**表格工具栏 `tableBar`**（原来嵌在吸顶 sticky 内）抽为变量，分别渲染在**桌面吸顶栏下方**与**移动底栏上方**（`border-t bg-teal-50/70`）。⑥**EditorContent 加 `pb-24 md:pb-0`**——移动端末行不被固定底栏遮住。`toolbarActive.strike/canUndo/canRedo/table` 沿用。tsc + build 绿灯；dev 需硬刷新（Cmd+Shift+R）。本条**取代**上一条「格式 / 插入 双触发」顶部面板做法，与上上条「Aa 格网」一同被取代。
- 2026-08-25 **编辑栏改「格式 / 插入 双触发 + callout 彩色图标 + 固定三键，均分铺满」（tsc + build 绿灯，已被更上一条取代）**。用户纠正：要的不是一个「Aa 格式」，而是**两个触发按钮**（格式、插入）+ callout + 固定三键的排法；且 callout 要用回正文里带**颜色的图标**；整行要**均分铺满**（不偏左不偏右）。**改法（`rich-text-editor` 工具栏再重写）**：①**常驻一行**（`flex flex-wrap items-center justify-between`）——**均分铺满**，任意移动端一屏满、窄屏换行后每行也均分、不残留歪斜空档；顺序 = `[Aa 格式][＋ 插入]` + callout + `[撤销][重做][AI]`；②**callout 彩色图标照正文配色**：原文(ScrollText/#a1a1aa)、生词(Sprout/#9a7328)、例句(MessageSquare/#2b795c)、语法(Puzzle/#5a4ac0)，图标+名称同色，`insertKindCallout`；③**两块面板**——「格式」= 块样式行（正文/标题/小标题/副标题/等宽）+ `grid-cols-5` 字母格式格网（加粗/斜体/下划线/删除线/高亮/列表/引用/链接/列表缩进）；「插入」= `grid-cols-3`（表格/图片/媒体）；**只开一个**（`openPanel: "format"|"insert"|null`，点一个自动顶掉另一个），backdrop/再点收起；④状态 `openMenu→openPanel`；补 `Plus/Sprout/MessageSquare/Puzzle/ScrollText/LucideIcon` 图标、`toolbarActive.strike`；**插入类与 callout 从字母格式面板里拆出来**、删「Aa 一个面板塞全部」的做法。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **编辑栏改「Aa 格式格网 + callout 常驻」（tsc + build 绿灯）**。用户觉得上一版「常用内联 + ⋯ 菜单」把太多按钮藏进 ⋯ 不好找，尤其 callout 应放外面；并给苹果备忘录「格式按钮弹格网」参考。**钉死三点**：callout 按钮露在外、原文放生词前、删「分列」。**改法（`rich-text-editor` 工具栏重写）**：①**常驻一行**（窄屏 flex-wrap 换行、不省宽无空档）= `[Aa 格式] | [原文][生词][例句][语法] | [撤销][重做][AI]`，callout 直接露外、原文最先；②**「Aa 格式」格网面板**（点开在工具栏下方、`formatOpen` 状态、backdrop 点击/再点收起）= 顶部块样式行（正文/标题/小标题/副标题/等宽）+ `grid-cols-6` 两行格网（加粗/斜体/下划线/删除线/高亮/列表/引用/链接/列表缩进/表格/图片/媒体）——**面板定宽、格网多列排，物理上不可能横向溢出**，根治上一版 select + 内联换行溢出；③**删分列**（`insertSplitColumns` 不再入口）、删旧 ⋯ 菜单与 `ToolbarMenu`/`MenuAction` 组件、`openMenu` 状态改 `formatOpen`、移除 `Ellipsis/Columns2/Minus/Sprout/MessageSquare/Puzzle/ScrollText` 图标、新增 `Strikethrough/IndentIncrease/Type`；`toolbarActive` 补 `strike`。**缩进**：用 `sinkListItem("listItem")`（列表内增加一级）；**解锁**：旧版 select+内联横向行因原生 `<select>` 固有宽度不收缩 + sticky 外层无 `overflow-x-clip` 而顶出屏，本版格网定宽根治。tsc + build 绿灯；dev 需硬刷新。
- 2026-08-25 **编辑栏改「常用内联 + ⋯ 菜单」全屏通用（tsc + build 绿灯）**。上一轮把桌面也统一成「三个折叠菜单 + 固定撤销/重做/AI」后，用户觉得光秃秃三个菜单 + 三个固定按钮太怪：要**自适应各端、窄屏无空档、撤销重做AI不固定、给方案**。用户拍板**「常用内联 + ⋯ 菜单」**。**改法（`rich-text-editor` 工具栏重写）**：①**高频直接可见**——块样式 `<select>`（正文/标题/小标题/副标题/等宽）+ 加粗/斜体/下划线/高亮/列表/引用/超链接 7 颗 `ToolButton` 内联；②**低频收进单个「⋯」`ToolbarMenu`**（icon 仅 Ellipsis、`label` 改为可选、只显图标不带短标签/箭头）——表格/分列/分割线/图片/媒体 + hairline + 生词/例句/语法/原文；③**撤销/重做/AI 学伴不再固定最右**，改成普通 `ToolButton` 排在同一行末尾（去掉原 `shrink-0` 固定组与竖分隔线）；④容器（`no-scrollbar flex min-w-0 flex-1 flex-wrap items-center gap-0.5`）用 **`flex-wrap`**——窄屏自然换行、绝不省宽留空档，桌面/移动一张图。**清理**：删掉原三个折叠菜单（文字/区块/附件）与全部 `md:` 桌面分支；移除 `Type/Layers/Paperclip` 图标（改纯内联后不需要），新增 `Ellipsis`；`openMenu` 状态从 `"text"|"block"|"attach"|null` 收成 `"more"|null`；`ToolbarMenu` 的 `label` 改可选。三个隐藏 `<input>`（图片/音频转录/OCR）保留；首条注释同步。tsc + build 绿灯；dev 需硬刷新（Cmd+Shift+R）。
- 2026-08-25 **桌面端笔记编辑栏也统一成「一行折叠菜单」——消除换行占高与标题下空白（tsc + build 绿灯）**。上一轮把移动端菜单做好、桌面端仍保留原内联全按钮行（`md:flex-wrap` 会换行占好几行高），用户反馈网页端编辑栏变得奇怪：标题与工具栏之间空很大、撤销/重做/AI 固定但其它按钮换行。**改法**：工具栏不再区分移动/桌面——**删掉桌面端内联长条（含其内联 blockType select 与硬编码按钮），菜单行去掉 `md:hidden` 全屏通用**：一行「文字（正文/标题/等宽 + 加粗/斜体/下划线/高亮/列表/引用/超链接）/ 区块（生词/例句/语法/原文）/ 附件（表格/分列/分割线/图片/媒体）」+ 撤销/重做/AI 固定最右，桌面与移动端观感一致、都不换行、下方不再露大片空白。补回原先藏在长条里的三个隐藏 `<input>`（图片 `fileInputRef` / 音频转录 `audioFileRef` / OCR `ocrFileRef`，否则「图片」「媒体」菜单点了没反应）。tsc + build 绿灯。
- 2026-08-25 **移动端编辑器工具栏改「一行折叠菜单」+ 全站输入框提示词统一缩号（tsc + build 绿灯）**。用户两联、已重启 dev（新 pid 15907, 页面需硬刷新）。①**工具栏左滑在设备模拟里仍不顺**——上一轮去掉 `touch-pan-y` 后横滑方向能动了，但手机模拟里「只滑得动一点点、撤销/重做/AI 看得到却总滑不到、停在表格那」；用户拍板「做不到想要的手感就合并成菜单」。**改法（参考备忘录）**：移动端(`md:hidden`)工具栏弃用左滑，改成**紧凑一行三个折叠菜单**——「文字」（正文/标题/小标题/副标题/等宽等段落样式 + 加粗/斜体/下划线/高亮/列表/引用/超链接）、「区块」（生词/例句/语法/原文）、「附件」（表格/分列/分割线/图片/媒体），点开下拉动作、操作多一层但不占宽；**撤销/重做/AI 学伴固定最右（`shrink-0`）两种模式始终可见**。桌面端(`md:`)保留原全部内联按钮一行（空间够则换行铺开）。新增两小组件 `ToolbarMenu`（图标+短标签+箭头下拉，`max-h-[70vh] overflow-y-auto`）与 `MenuAction`（图标+文字一整行动作行）；`openMenu` 状态同刻只开一个菜单。②**提示词统一缩号**——新建文件夹/子文件夹、新建合集、新建分类、添加闪卡弹窗、词群搜索框等所有 `text-sm` 但未单独设 placeholder 字号的地方，统一加 `placeholder:text-xs`（12px、比正文小一号），消除「文件框提示词偏大、与搜索框不一致」的观感（7 处：notes-browser 文件夹输入、notes-workspace 两框、collect-words 两处、recognition-rules、theme-detail、cloze-session）。tsc + build 绿灯。
- 2026-08-25 **移动端编辑栏工具栏「横滑失效」修复 + 新建流程统改底部弹出 + 笔记标题长自动换行 + AI 语伴标题吸顶（tsc + build 绿灯）**。用户四联反馈。①**工具栏设备模拟横滑不动**——上一轮为解决「横滑带动页面」加在滚动条上的 `touch-pan-y`（`touch-action: pan-y`）反而让浏览器只处理纵向手感、横向手势失效（设备模拟下只能滑一点点、`overflow-x-auto` 卷不出去、卡在表格按钮、撤销/重做/AI 三颗看不到）。**反转**：滚动条去掉 `touch-pan-y`（恢复默认 `overflow-x-auto` 自行处理横滑），`note-editor` 页面根 `flex min-h-screen flex-col` 加 `overflow-x-clip`（`clip` 不产生滚动容器、不破坏 `sticky` 吸顶，只把横向溢出夹在页内）——横滑只滚按钮条、不带动页面；撤销/重做/AI 固定组仍在滚动区右侧、随 `shrink-0` 始终可见。此条**取代**上一条「加 touch-pan-y」方案。②**新建流程统改底部弹出**——新增共享 `src/components/bottom-sheet.tsx`（全屏遮罩 + 底部圆角面板 `max-h-[80%]`，`{open,onClose,title,children}`）；`notes-browser` 新建文件夹/子文件夹、`materials-view` 新合集、`group-browser` 新建分类，从「页面里随便一处内联出现／居中弹窗」统一改为底部弹出（内容紧贴面板、不再跑到列表里、离触发按钮远）；新建笔记仍直接跳笔记内页（`new-note-button` 不改）。③**笔记标题长换行**——`note-editor` 标题 `<input>` 改自动增高 `<textarea>`（`rows=1` + `onInput` 复位后再取 `scrollHeight`），长标题不再被单行 `input` 裁掉后半截、能完整换行展示。④**AI 语伴标题吸顶**——`companion-chat` 头部改 `sticky top-0 z-20` + 内联 `background: var(--page-bg); backgroundAttachment: fixed`（同其它级页 `.page-header` 融合背景），对话变长时标题区吸顶不滚走。tsc + build 绿灯。
- 2026-08-25 **修理编辑器工具栏「横滑带动页面」+ 全站提示文字缩小一号（tsc + build 绿灯）**。上轮单行横滑改完后，用户反馈「编辑栏跳出页面，横滑会带动页面」——横滑按钮时整页跟着动。**根因**：工具栏滚动条只靠 `overflow-x-auto` 兜底，未约束触摸方向，手指左滑会滚到页面（整页横向 pan）；且页面根元素没防横向溢出。**改法**：① `rich-text-editor` 工具栏滚动条加 `touch-pan-y`（`touch-action: pan-y`）——横向手势只滚按钮条、纵向手势照常滚页面，**横滑不再带动页面**；② `note-editor` 页面根 `flex min-h-screen flex-col` 加 `overflow-x-clip`（`clip` 不产生滚动容器、不破坏 `sticky` 吸顶，只把横向溢出夹在页面内——防止任何子元素撑宽页面）；撤销/重做/AI 三颗保持原 `shrink-0` 固定组在右侧、不随横滑走。**提示文字缩小**：语伴输入框 placeholder 与文件夹弹层「新文件夹名」改 `placeholder:text-xs`（原继承 `text-sm`，比 14px 小一号），并按「所有搜索框/文本框提示词都改小」的诉求，用脚本给全站 `input/textarea` 里凡是 `text-sm` 且未单独设 placeholder 字号的地方统一加 `placeholder:text-xs`（23 个文件；排除 `text-2xl` 大标题占位符、`className` 走变量的表单标题，避免大标题 placeholder 被压小）。tsc + build 绿灯。
- 2026-08-25 **移动端编辑器工具栏改单行横滑 + 语伴页换绿色背景（tsc + build 绿灯）**。用户指出移动端编辑栏窄屏换行成三行太占空间：要**一行展示放得下的按钮，放不下的手指左滑拉出来（无滚动条），只有 撤销/重做/AI 学伴 这三颗固定不随横滑走**。**改法**：`rich-text-editor` 顶部工具栏外层改 `flex` 行 + 内部两区——①格式按钮区（块样式下拉/加粗/斜体/高亮/列表/引用/链接/表格/分列/分割线/图片/媒体/生词/例句/语法/原文）做成 `min-w-0 flex-1 overflow-x-auto no-scrollbar [&>*]:shrink-0 md:flex-wrap md:overflow-visible`：手机端一横排、放不下的左滑、无滚动条、图标不压缩；电脑端空间够则整块换行铺开（回归原 wrap 行为）。②撤销/重做/AI 包一个 `shrink-0` 固定组放在滚动区右侧、始终可见不随滑动。首条注释同步更新。**语伴页背景**：根容器去掉 `bg-white`（透出页面 `--page-bg` 淡绿渐变，同其它一级页），输入框改 `bg-white` 保证可读，消息气泡/空状态落在绿底上。tsc + build 绿灯。
- 2026-08-25 **置顶兼容未迁移库 + 笔记内页吸顶头收薄 + 语伴页头部改一级标题样式（tsc + build 绿灯）**。用户当轮报「笔记页读取失败：column notes.pinned does not exist」——schema 加了 `pinned` 列但**线上库还没跑迁移**，选中该列的查询直接 42703。**修法（先解阻塞、再靠迁移启用）**：① `listNotes` 先按 `pinned desc, updated_at desc` 置顶排序，若 PostgREST 报 42703 / does not exist，**退回落版**（不带 pinned、纯按更新时间倒序）——库没加列时页面照常读，加了列后自动启用置顶；`Note.pinned` 改**可选**（`pinned?`）。**待人工跑迁移**：在 Supabase SQL Editor 执行下面一句（schema.sql 已含，`supabase db push` 或重跑 schema.sql 亦可）：`alter table public.notes add column if not exists pinned boolean not null default false;`。② **笔记内页吸顶头收薄**：`note-editor` 顶部头里返回/分享/⋯/完成按钮统一 `h-7`(28px)、padding 改 `pt-[calc(env+0.5rem)] pb-2`，与其它页面同厚度。③ **语伴页**——头部改**一级页标题样式**（`text-xl` 标题左 + 动作右，吸顶），**去掉 logo 徽标 + 「全能语言助手」副标题**，只留「标题 + 清空会话 + 收藏夹」，**收藏夹挪到最右、清空放它左**；容器高度 `5rem→3.5rem` 消移动端底部露出的背景；输入框 placeholder 缩短为「输入词、句子或提问…」、`text-sm`，发送按钮与输入框同高 40px；底部工具行图标按钮加 `shrink-0`、内边距 `p-1.5→p-1`（图标不再被压窄），语言选择 `shrink-0` 收窄。tsc + build 绿灯。
- 2026-08-25 **文件置顶 + 「收录到文件夹/移动」改底部弹层 + 文件夹详情页头收窄（tsc + build 绿灯）**。用户五点：① 文件夹详情页的「N 篇笔记」要**居中**、标题**去图标省空间**；② 每个文件的 ⋯ 菜单加**置顶**；③ 笔记内页「收录到文件夹」菜单文件夹多时会拉得很长，参考苹果备忘录改成**点开从底部弹出文件夹单**，且把它放在「转成闪卡」上面；④ 问右上「完成」按钮是否保留。**改法**：① **文件夹详情页头**重排成 `[返回‹][标题(无图标,text-base,flex-1,pr-12)][居中"N 篇笔记"(absolute 水平居中)][视图切换][+新建]`——`MobileFolderView` 删掉 `icon` prop、`page.tsx` 不再传 (`Inbox`/`Folder` 图标移除)；①⑤ **置顶打通**：notes 表加 `pinned boolean not null default false`(schema.sql 幂等 alter)，`Note` 类型加 `pinned`，`listNotes` select 加 `pinned` 并按 `pinned desc, updated_at desc` 排序——置顶的笔记浮到文件夹/全部笔记顶部；文件 ⋯ 菜单加「置顶/取消置顶」，置顶的列表行/卡片标题前加 `Pin` 小图标。③② **底部弹层**：新增共享 `src/components/folder-picker-sheet.tsx`（全屏遮罩 + 底部上滑圆角面板 `max-h-[72%] overflow-y-auto`，层级缩进 + 「无文件夹」 + 内联新建文件夹，`title/currentFolderId/onPick/onCreateFolder/showUnfiled`）；**note-editor** 的 ⋯ 菜单里内联的 folder list + 新建文件夹删掉，换成单条「收录到文件夹（当前文件夹名）+ →」即打开弹层，置于「转成闪卡」上方；**notes-browser 的文件 ⋯ 菜单**「移动」也改用同一个弹层（替换原内联 folder list，`moveNote` 回调）。tsc + build 绿灯。
- 2026-08-25 **所有吸顶标题头统一收薄到同一厚度 + 一级/二级标题对齐（tsc + build 绿灯）**。用户在预览后比对：① 笔记页与复习页标题文字位置似有细微差别；② 点「继续背诵」进的闪卡背诵页（考试/错题/完形/待加强等会话子页）吸顶区比一级页还大，问能否收窄到与一级页一样薄，其它带标题页同理。**根因**：吸顶头行高由「行内最高元素」决定——一级页 (`PageHeader`, 无返回键) 最高是 `text-xl` 标题(28px)；会话页 `BackButton`(text-lg + py-1 ≈ 36px) 反而高于其 `text-base` 标题；笔记首页按钮(`px-2.5 py-2`+icon ≈ 32px)又高于复习页 title。故各头高/标题垂直位不齐。**改法（统一头行内容高 28px = `h-7`）**：① `.page-header` 上下 padding `0.75rem→0.5rem`（所有用它的一级/次级吸顶头整体变薄）；② `BackButton` 默认与 `PageHeader`/文件夹详情覆盖类改成 `inline-flex h-7 px-1.5`(28px，arrow 居中)，会话/主题/卡详情等用默认的次页头随之降到 28px；③ `NewNoteButton` icon 变体 `px-2.5 py-2→h-7 px-1.5`、ghost 变体 `p-1.5→h-7 w-7`（28px）；④ 笔记首页文件夹按钮同改 `h-7`、头部 padding `pt-[calc(env+0.75)]pb-3→pt-[calc(env+0.5)]pb-2`，标题行=28px 与复习页对齐；文件夹详情头 `pb-3→pb-2`。效果：一级页（笔记/复习/闪卡/设置）标题 text-xl 独占 28px 行，二级页（会话/文件夹/主题/卡详情…）标题更小（text-base）且 BackButton 28px 不撑高——各吸顶头部等高、标题垂直位一致，整体更薄。tsc + build 绿灯。
- 2026-08-25 **下级标题回「标题左 + 按钮同行」窄吸顶；弃大标题下滑收窄（tsc + build 绿灯）**。用户在预览后纠正：大标题下滑收窄不妥——**页面的标题应该跟新建按钮同一行才合理**（笔记跟新建按钮不在一行很怪）；搜索框改薄一点；其它一级页标题也照旧吸顶左部、只是窄一点；文件夹详情页布局可以，把「N 篇笔记」文字放到中间、跟标题和按钮同一行，标题行就能再收窄。**改法**：① **废弃 `CollapsibleHeader`（已删文件、无引用）**——不做下滑收窄；② **复习/闪卡/设置** 改回共享 `PageHeader`（本就「返回 + 标题左 + 动作右」单行窄吸顶头，`size="xl"`→一级页 text-xl），标题恢复吸顶左部；③ **笔记首页** 改用内联单行窄头（`标题"笔记" 左 + 新建文件夹/新建笔记 右` 同一行），搜索框移到标题下方、**更薄**（`px-2.5 py-1.5 rounded-lg`，原 `px-3 py-2 rounded-xl`）；④ **文件夹详情页** 把「N 篇笔记」从标题下一行并进标题行——`[返回‹][图标+文件夹名(左,truncate)] // [N 篇笔记(居中段靠右)] [视图切换][+新建]`，`justify-between` 标题行收窄成一行；⑤ **主次区分**——一级页标题 text-xl、二级页（文件夹详情 text-base）更小，所有顶部吸顶区都收紧成窄行。页面结构（「全部笔记」默认入口/可折叠文件夹树/搜索结果）与首屏只滚列表不变。上一「大标题下滑收窄 CollapsibleHeader」条目由此**取代**（其「全部笔记入口/自包含 NotesBrowser/all scope」等其余改动仍有效）。tsc + build 绿灯。
- 2026-08-25 **手机端笔记首页重排（借苹果备忘录）+ 大标题下滑收窄共享 CollapsibleHeader（tsc + build 绿灯）**。用户指出：笔记/文件夹多了以后一屏装不下，下滑会带着搜索框、新建按钮、`文件夹/全部笔记` tab 一起滑走。改法：**`NotesBrowser` 改成自包含 flex-col + 固定高**（`calc(100dvh-5rem-safe)`）+ 头部 `shrink-0` + 内容 `min-h-0 flex-1 overflow-y-auto`，只有列表滚、chrome 全部吸顶不滚走；`notes/page.tsx` 手机分支简化为直接渲染 `<NotesBrowser queryError/>`（页面查询错误也收纳进组件内容区）。**去 tab / 去未分类**：① `文件夹/全部笔记` segmented tab 删除（不再是 chrome）；② 首页拆成「纯文件夹列表」——顶部固定「**全部笔记**」默认入口（收纳整个库含未归档的，不可删/改名，点进 `/notes/folder/all`）；③ 「未分类」伪行删除（全部笔记已覆盖）；④ 顶栏 `新建文件夹 + 新建笔记` 并排、搜索框常驻。**共享 `CollapsibleHeader`（大标题下滑收窄）**：新增 `src/components/collapsible-header.tsx`，`{title, actions?, search?, scrollRef?, className?}`——顶部薄导航行（左返回/中部小标题/右动作）始终显示 + 大标题 `text-xl` 随滚动 `scrollTop>24` 收窄进薄行（`max-h-0 opacity-0`）；默认监听 `window` 滚动（复习/闪卡/设置），页面有内部滚动容器则传 `scrollRef`（笔记首页）。**应用到四个一级页**：复习主页、闪卡页、设置页手写 `<header class="page-header"><h1>` → `<CollapsibleHeader title class="page-header mb-*">`；笔记首页用 CollapsibleHeader。`PageHeader` 仍供次级页用。**「全部笔记」页**：`mobile-folder-view` 加 `id==="all"` scope（`folderNotes=notes` 不过滤）；`folder/[folderId]/page.tsx` 特判 `all` 构造伪文件夹「全部笔记」+ 传 `Folder` 图标，头部固定（返回+名+篇数+视图切换+新建）。**文件夹图标品牌色加重描边**：`FolderTreeNode` 行 + 全部笔记入口/搜索结果行的 `Folder` 图标 `text-teal-500 strokeWidth{2.25}`；`ViewToggleButton` 只留在「笔记列表页」固定头部（全部笔记/文件夹详情），首页不放；`NoteList` 加 `hideToolbar` prop（笔记首页搜索结果的纯列表不渲染「N 篇 + 视图切换」行）。桌面三栏 `NotesWorkspace` 本轮不动。tsc + build 绿灯。
- 2026-08-25 **新建笔记回到工具栏与新建文件夹并排 + 错题集挪出复习页标题区（tsc + build 绿灯）**。用户两点：①**新建笔记恢复原处**——上上轮把它移到笔记页标题右上（`NewNoteButton ghost`），用户觉得「文件夹和文件的新建位置不在一起」很怪，改回**放回 `NotesBrowser` 工具栏、与「新建文件夹」按钮并排**（`<NewNoteButton />` icon 样 + 新建文件夹，同款描边）；`notes/page.tsx` 移动端标题右上删掉 ghost「+」、恢复纯 `<h1>笔记</h1>`（桌面空状态的大按钮保留）。②**错题集挪出复习页标题区**——上轮为消「标题被按钮挤压」给复习主页标题加 `错题集` 右侧按钮 `shrink-0`，但用户发现「复习页标题区域宽度跟其它页不一样」，就是它占了标题行；改法：**标题行恢复纯 `<h1>复习</h1>`**（与其它一级页一致），把 `错题集` 链( `/review?scope=errors` )**收进「更多练习」下拉**（完形填空/故事模式之下、用 hairline 分隔，点完收起）。错题集是跨合集全局视图，故不作合集内入口、放「更多练习」这类进阶低频处，标题行不再被一颗按钮拖宽。tsc + build 通过。
- 2026-08-25 **笔记视图改单切换按钮 + 新建笔记移到标题区右上（tsc + build 绿灯）**。①**视图按钮改单个**——从「列表/网格两个小图标」改成**一个按钮点一下互切**（`ViewToggleButton`：列表态显示网格图标、网格态显示列表图标，即图示「将要切到的视图」），`NoteList` 支持受控 `view/onViewChange`：**文件夹页头部**把该按钮放在「+」左侧（受控，NoteList 不再自渲染）；**「全部笔记」tab** 及桌面中栏同样用单按钮（桌面 `md:hidden` 仍隐藏，保证桌面纯列表）。文件夹页头部为 `[返回 ‹][标题/篇数 ······][视图单按钮][+]`。②**新建笔记移到标题区**——笔记页移动端标题「笔记」右侧加 `NewNoteButton ghost`「+」（与文件夹页头部一致），`NotesBrowser` 工具栏原「新建笔记」按钮删除（避免重复），工具栏只剩「搜索框 + 新建文件夹」。
- 2026-08-25 **文件夹管理对标苹果备忘录：列表改可折叠树、文件夹页只展递归笔记、标题字号再压小（tsc + build 绿灯）**。用户三点：**①标题再小一点（手机端）**——一级页 `text-2xl`→`text-xl`(20px)，次级页 `text-lg`→`text-base`(16px)：`PageHeader` 的 size xl/lg 同步改（xl→text-xl、lg→text-base）；逐页降——notes/page、review-home、settings、materials-view（一级）；review 全部 7 个下级页、note-cards-tabs、收藏、theme-detail、material-watch、collection-detail、card-detail、文件夹页（次级）。登录页/记事内容标题/弹窗/统计数字等**非页头**保持原样。**②文件夹页头协调**——上轮标题下一行「N 篇 · M 子夹」副标题与右侧新建按钮不协调：改成 **标题（文件夹名）+ 篇数两行紧凑块居左、右侧 ghost「+」新建按钮垂直居中**，两行块高度与新按钮平衡（参考苹果备忘录「泰语 / 2个备忘录」+ 右侧菜单的协调排版）。**③文件夹套文件夹改苹果模型**——**文件夹列表**（/notes 文件夹 tab，手机端）从「永远展开的缩进树」改成**可折叠树**：顶层常显，**窄箭头 ChevronRight（无文字）点击展开/收起嵌套**，点文件夹名进它的笔记列表；计数改用递归总数 `folderNoteTotals`（含次级文件夹里的笔记）。**文件夹详情页**去掉「子文件夹」分栏，改成**只展示这个文件夹子树下所有笔记（含次级文件夹的）**，像苹果备忘录那样归属看文件夹下属关系、不再单独叠一层子夹导航。路由 `folder/[folderId]/page.tsx` 改传全部 notes 由 `MobileFolderView` 递归收拢（`descendantFolderIds` 算子树、未分类特判）。桌面端 notes-workspace 侧栏本就按 `descendantFolderIds` 递归展示所选文件夹的子树笔记，列表仍为平铺缩进（暂未改可折叠，后续可对齐）。
- 2026-08-25 **全站标题主次分明：一级页大标题保留、次级页统一 text-lg + 返回按钮；文件夹详情页头重排（tsc + build 绿灯）**。用户两点：①**标题主次**——只有一级页（笔记/复习/闪卡/语伴/设置）顶部标题统一 `text-2xl` 大字号；所有次级页标题降小、带返回按钮、主次分明。**落地**：`PageHeader` 加 `size="xl"|"lg"`（xl→text-2xl 一级页，lg→text-lg 次级页）；**次级页批量降为 text-lg**——`review/page.tsx` 全部 7 个下级页头（待加强的卡/错题集/测试/故事/完形/按note/按theme）、`note-cards-tabs` 笔记卡列表标题、`companion-favorites` 收藏标题、`theme-detail`、`material-watch`、`collection-detail`、`card-detail`（曾是 text-base，提到 text-lg + font-bold）。**返回按钮统一**：`companion-favorites` 从手写 `Link+ArrowLeft` 换成共享 `BackButton`（带浏览器历史回退），同其余次级页。逐页 scan 确认其余次级页均已带返回按钮；无遗漏的 text-xl 页头（仅 story-mode 正文里一处 `<p>` text-xl，非页头，不动）。②**文件夹详情页头重排**（解决上轮「新建笔记图标与最左标题不协调」）：导航条改为「共享 `BackButton` ‹ + 文件夹图标名（text-lg，h-5 图标作次级页规格）+ 右侧「+」新建笔记 **ghost 样式**」——`NewNoteButton` 新增 `variant="ghost"`（`rounded-lg p-1.5 text-teal-600`，与返回箭头同体量、无色块描边，不再顶一个边框盒），篇数作次级小字。③**子文件夹能收回去**：上轮折叠树默认全收起、用户看不到可折；改 `SubfolderRow` 默认 `open = depth===0`（当前文件夹的直接子夹默认展开，其下子层一眼可见、可手动收；更深层默认收起），chevron `rotate-90` 随展开态，`depth*14` 缩进，多层嵌套都能「点名字下钻 / 点箭头展开收起」。
- 2026-08-25 **文件夹详情页重构：标题降小 + 去重计数 + 新建笔记 + 子文件夹改可折叠树（tsc + build 绿灯）**。用户五点：**①标题太大**——次级页不该跟一级导航页同号（24px），改 `text-lg`(18px)，横幅一行放「返回按钮 + 图标名 + 新建笔记按钮」，**笔记计数降到次级小字**（次行 `text-xs` 居左）。**②计数重复**——页头与列表下各一个 `N 篇笔记`，删列表那个：`NoteList` 加 `showCount` prop（默认 true），`mobile-folder-view` 的普通/未分类上下文传 `showCount={false}`（视图切换两个图标仍在，`ml-auto` 推右）。**③新建笔记**——横幅右侧加 `NewNoteButton(folderId)`，未分类页传 `folderId={null}` 造无文件夹笔记。**④子文件夹改 toggle**——原来整行是 Link（`点击即下钻`），子夹一层层进去回退麻烦；改成**可折叠树**：点文件夹名字 → 进它的笔记列表页；点**小箭头 `ChevronRight`（只一个符号、无「展开/收起」文字）**→ 就地展开/收起它下面的子层。**⑤嵌套打开逻辑**——子树用 `folderTree(folders).children`（Map<parentId, Folder[]>）**递归**渲染 `SubfolderRow`，按 `depth` 缩进（`marginLeft: depth*14`），每层都能「点名字下钻 / 点箭头展开」；想跳哪层点哪层名字，回退沿返回键向上，跟 Apple 笔记「逐层下钻 + 就地折叠」一致。计数 = 该文件夹**直接所含**笔记数（`noteCountByFolder` 按 folder_id 聚一次），有子层的箭头随展开态 `rotate-90`。**未分类**：伪文件夹 id="unfiled" 不参与真实树，其下不再续子夹（`children` 查不到），只按 `showCount=false` 出笔记列表。
- 2026-08-25 **按用户反馈打磨「笔记页 + 编辑器超链接 + AI 语伴页」交互规范（tsc + build 绿灯）**。①**笔记页未分类**：图标 `?`→`Inbox`；不再「点开展开」，改成**点进去是「未分类」文件夹页**——复用 `mobile-folder-view`（路由 `/notes/folder/unfiled`，`folder/[folderId]/page.tsx` 特判 id==="unfiled" 构造伪文件夹 + 收拢所有无 folder_id 笔记，图标传 `Inbox`），与普通文件夹同视角、返回回 `/notes`。②**修笔记页标题随内容滑动**：根因是 `app-shell` 的 `main` 在移动端既给页面自身高度预留了底部导航（`calc(100dvh-5rem-safe)` 内部滚动），又外叠了一层 `pb-[calc(5rem+safe)]`——**双重预留把整页撑高出一截**，滑到列表时就整页滚、标题跟着滑走。改法：`main` 对笔记区 + 语伴页 `pb-0`（`reservesBottomEnd`），内部滚动与底部导航不再重复算。③**全部笔记视图切换**：从 SegmentedControl chip 改成**两个小图标按钮**（LayoutList/LayoutGrid，激活态 teal），仅移动端显示。④**笔记内页回原样式**：`note-editor` 吸顶头 `bg-white/70 backdrop-blur`（在绿色 `--page-bg` 上透出浅绿「绿标题区」）→ 改**实心 `bg-white`**；编辑器内容区补 `bg-white`，标题下方不再露绿色渐变色带。⑤**编辑器超链接**：`editor-extensions` 的 Link 扩展 `openOnClick:true→false`（**点/选链接不再跳转新标签**，想选链接取消地址不再被劫持）；新增 `selectionContainsLink()` 扫选区范围文本节点——**「链接+空格等混选」也能删超链接**（不再因选区一部分非链接导致 `isActive` 为 false、误走「加链接」）；工具栏 `link` 激活态随选区任一链接高亮；选中纯文字点超链接仍弹框输地址套链接。⑥**AI 语伴页**：**铺满全屏**（`app-shell` 对 `/companion` 改 `isFullBleed`，不再套 `max-w-5xl px-4 py-6`；聊天容器 `h-[calc(100dvh-5rem-safe)] md:h-dvh` + 白底）；输入框 `rows=1` 与**发送按钮同高（42px）同排**；底部工具收成一行小图标（图片 OCR / 语音 / 粘贴链接 + 语言下拉收紧 `px-1 py-0.5`）；**支持上传图片等媒体**——复用 `/api/ai/ocr` 图片识别（填进输入框）、`/api/ai/transcribe` 粘贴链接抓文字稿；右上角「收藏」改为**「收藏夹」**——是**查看收藏内容**的入口，收藏本身是**每条 AI 回复上的「收藏」图标**（并非收藏整个会话），原每条回复的 收藏/导入笔记 动作不变。
- 2026-08-24 **AI 语伴页替换底导素材 tab（阶段二完成，tsc + build 绿灯）**——接上半轮阶段一，语伴核心全部落地。①**底导第三个 tab「素材」→「语伴」**：`app-shell` `NAV_ITEMS` 改 `{ href:"/companion", label:"语伴", icon:MessageCircleHeart }`（桌面侧栏 + 移动底导同数组自动跟随），`hasStickyHeader` 同步 `/companion`、`/companion/`。**素材库独立页保留、仅顶级入口移除**（存量深链 `/materials*` 仍可达）。②**全屏对话页 `/companion`**：`companion-chat` 独立全屏卡片（标题嵌渐变头 + 可滚动消息区 + 底部输入区），文字 + 语音（`useVoiceToText` hook：MediaRecorder → `/api/ai/transcribe-audio`，识别后自动发送）；**会话持久**（localStorage `nestlingo:companion`，退出再进恢复），顶部「清空会话」一键清空（只清聊天、不动收藏）。每条 AI 回复带「收藏」+「导入笔记」两个动作。③**后端多轮**：`/api/ai/assistant` 加可选 `history`，把最近若干轮拼进 system 提示，让「全能助手」接上文。④**收藏层**：新增 `assistant_records` 表（supabase/schema.sql：kind/prompt/reply/points/media_id/note_id/favorited + RLS + updated_at trigger）；文字写 `assistant_records`，媒体链接「识别到（YouTube/音频/Spotify）→ 并收一条 `materials`」，媒体带缩略图。⑤**收藏页 `/companion/favorites`**：`listFavorites`（服务端并查 records + materials）→ `companion-favorites` 合并展示，文字卡（问/回复 + 导入笔记/复制/取消收藏）、媒体卡（缩略图 + 类型徽章 + 导入笔记/取消收藏），导入都走「新建（默认）/ 选已有」弹窗，无清空/筛选/搜索/批量。⑥**写回 helper**：新 `src/lib/supabase/assistant.ts` 集中收集/删除/`appendNodesToNote`（照 import-material 写回：读 content → 拼节点 → 更新 content+content_text / 新建）。**下一轮（延后）**：内嵌 AI 写回增强、素材中心独立页、媒体自动汇总（扫内嵌节点入库）。
- 2026-08-24 **笔记页 UI 重构 · 阶段一（完成）+ AI 语伴页替换素材 tab · 阶段二（进行中）**——本轮拆两块：先笔记页 UI + 框架统一，后 AI 语伴。**已完成·阶段一**：① 笔记移动首页标题去白底/毛玻璃，嵌页面绿色渐变（`notes/page.tsx`）；② 文件夹详情三行白条压成一条横幅（返回 + 图标名 + 篇数，嵌渐变，`mobile-folder-view` 整页改标题固定 + 下方 `flex-1 overflow-y-auto` 只有内容滚不整页滚）；③ `NoteList` 每条加 `formatNoteDate(updated_at)` 时间标识（新 `src/lib/format-note-date.ts`：今天 HH:mm / 昨天 / N 天前 / 同年 M月D日 / 更早 YYYY/M/D），并支持 **列表 / 两列网格** 切换（复用 `SegmentedControl`，LayoutList/LayoutGrid 图标，grid 卡片带摘要+时间+右上⋯）；④ 未分类笔记改为文件夹树顶部「未分类」伪入口（HelpCircle 图标 + 计数，点开展开），不再单独长列；⑤ 文件夹行/图标用品牌色 `text-teal-500` 更醒目。**框架统一**：review-home 标题 `错题集` 右侧按钮 `shrink-0`、h1 加 `min-w-0 flex-1 truncate`；materials-view h1 同为 `min-w-0 flex-1 truncate`（消除窄屏标题被按钮换行挤压）。**阶段二（AI 语伴，进行中）**：删除素材独立 tab、底导第三 tab 换成「语伴」、全屏对话（语音+文字）、会话持久可清空、每条产出可收藏（文字 `assistant_records` 表 / 媒体复用 `materials`）、收藏页并入导入笔记。**说明**：素材库独立页入口本轮移除（存量深链仍可达），媒体自动汇总入库 / 内嵌 AI 写回增强 / 素材中心独立页 归下一轮。
- 2026-08-24 **UI 精简 + iOS 设计规范 · 阶段四（底部导航毛玻璃 + 弹窗视觉收尾）**（tsc + build 通过）。①**底部导航 iOS 化**：`app-shell` 移动端 tab bar 从 `bg-white + border-zinc-200` 改成 `bg-white/80 backdrop-blur-md + border-black/5`（半透明毛玻璃 + 顶部 hairline），激活项仍品牌色。②**底部弹层顶部圆角 20px**：`add-cards-button`/`share-modal`/`review-home` 选择合集弹窗/`convert-to-cards` 四个底部弹层 `rounded-t-2xl`(16px) → `rounded-t-3xl`(20px)，桌面居中态仍 `sm:rounded-2xl`(16px)，更贴 iOS 底部抽屉观感。**至此 UI 精简 + iOS 设计规范四阶段主链路完成**（阶段 0 回退点 `d08f478` + 阶段一~四，每阶段独立 tsc+build 通过）。
- 2026-08-24 **UI 精简 + iOS 设计规范 · 阶段三（逐页精简主页面/次级页）**（tsc + build 通过）。①**闪卡页分段全宽**：`cards/page.tsx` 的「按来源/按主题」切换从左侧内联 `inline-flex` 改成**全宽铺满、两端均分**的分段控件（浅灰轨道 + 选中白胶囊），仍用 `next/link` 保留可分享 URL。②**单卡详情删除收进 ⋯**：`card-detail` 底部「删除」按钮撤掉，改为头部 `RowMenu` 里的「删除」（红字），底部只留「编辑」主按钮。③**复习主页「完形填空/故事模式」收进「更多练习」**：`review-home` 主板块从「继续背 + 测试/完形/故事」三并列改成「继续背（主）+ 测试 + 更多练习（下拉出完形填空/故事模式）」，进阶模式低频不再常驻。④**主题详情「移出」收进卡 ⋯**：`CardTile` 新增可选 `menuItems` prop（传了则替换默认「编辑/删除」菜单项），`theme-detail` 从每卡底部常驻「移出」文字改成卡右上 ⋯ 菜单里的「移出」，卡片更干净。⑤**设置页「识别规则」默认收起**：`settings` 的「闪卡识别规则」从常驻 Section 改成原生 `<details>` 折叠面板（标「高级」+ 旋转 chevron，默认收起），进阶低频项不再一开始塞满设置页。
- 2026-08-24 **UI 精简 + iOS 设计规范 · 阶段二（共享组件 + 语言列表统一）**（tsc + build 通过）。①**新增 `segmented-control.tsx`**——iOS 分段控件（浅灰轨道 + 选中白胶囊），`w-full` 全宽铺满、两端均分，受控 `value`/`onChange`，用于视图/标签切换，替代各页 ad-hoc toggle 链接。②**新增 `filter-sheet.tsx`**——统一「筛选」chip 按钮（带选中数徽标）→ 下拉（桌面）/底部弹层（移动），多组单选框组，替代各页行内散落语言 chips 与自写筛选下拉。③**语言列表统一**——`cards-view`/`group-browser`/`test-errors`/`review/page` 四处的本地 `LANG_ORDER`（漏了 `english`，只有 5 语言）删掉，统一 import `lang-detect.ts` 的共享 6 语言 `LANG_ORDER`（含 `english`），英语卡不再漏筛。两个新组件尚未接入页面，阶段三逐页替换时使用。
- 2026-08-24 **UI 精简 + iOS 设计规范 · 阶段一（设计令牌 + 统一页面框架）**（tsc + build 通过，回退点 `d08f478`）。目标：全站清爽、iOS 苹果 App 观感（鼠尾草薄荷绿品牌色 + 系统 SF Pro 字体 + 仅浅色 + 圆角 12–16px + 背景品牌色渐变）。**阶段一落地**：①**字体换系统栈**——`layout.tsx` 删 `next/font/google` 的 Geist/Geist_Mono（顺带消除本地 build 因 Google Fonts 超时的依赖），`globals.css` 的 `--font-sans`/`--font-mono` 与 `body` 改用 `-apple-system,BlinkMacSystemFont,"SF Pro Text","SF Pro Display","Helvetica Neue","Segoe UI","PingFang SC",…` 系统栈（等宽 `ui-monospace,"SF Mono",…`）。②**圆角统一 12–16px**——`@theme` 里重定义 `--radius-sm/md/lg/xl/2xl/3xl`（8/10/12/14/16/20px），全站 `rounded-lg`→12px、`rounded-2xl`→16px、`rounded-3xl`→20px 自动生效（`.btn-brand` 14px、`.card-soft` 16px）；同步把 globals.css 写死的 `border-radius` 对齐（code 4→6、img/columns/callout 8→10、tiptap callout 10→12、callout-action 5→8）。③**背景渐变柔和化**——`--page-bg` 改成顶部居中淡绿径向 + 右上角副淡绿（更「空气感」）。④**新增 `page-header.tsx`**——统一「返回 ← + 标题 + 右侧动作」吸顶头（`page-header` 渐变底 + 底部 hairline + 标题嵌渐变），先在闪卡页落地作样板，其余页阶段三逐个替换。⑤**笔记区改渐变底**——`app-shell` 笔记区容器去掉 `bg-white`（透出 body 渐变）、`note-editor` 外层去 `bg-white`、其吸顶头与 `notes/page.tsx` 手机头从 `bg-white/95` 改 `bg-white/70 backdrop-blur-md`（标题嵌渐变、内容在下方白色编辑器面板上切分）。
- 2026-08-24 **三处编辑器小改：加假名去气泡 + 工具栏去滑条并加宽文本区 + 超链接加/去按钮**（tsc + build 通过）。①**加假名后显示「去假名」**：`addFuriganaToCallout` 本已用 `replaceWith` 重建节点让 renderHTML 重算标签（加完即变「去假名」），本轮修掉它重建时把 `attrs` 写成 `{ kind:"article" }` 而**丢掉 source 来源**的 bug——改成 `attrs: calloutNode.attrs`（与 `removeFuriganaFromCallout` 一致，来源链接不再丢）。同时**去掉该按钮的「…中」气泡反馈**：`rich-text-editor` 的 `setCalloutActionHandler` 里 `toggleFurigana` 直接 `runCalloutAction`、不 `setCalloutBusy`（加/去假名是本地即时标注，非长 AI 请求，无需反馈；翻译/精读笔记仍保留气泡），并删掉随之不再用的 `calloutHasFurigana` 导入。②**工具栏不再出滑条 + 文本区加宽**：工具栏容器原来 `md:flex-nowrap md:overflow-x-auto` 在宽屏按钮溢出时出横向滚动条，改成**始终 `flex-wrap` 自然换行**（不横向滚动）；正文容器 `max-w-3xl` 放宽为 `max-w-3xl md:max-w-4xl xl:max-w-5xl`，宽屏自适应加宽、工具栏有更多空间少换行。③**超链接加/去按钮**：`rich-text-editor` 工具栏「引用」旁加 `Link2` 图标按钮——选中文字里已有链接（光标在内也算）→ `unsetLink()` 去掉超链接；否则选中文字 → `window.prompt` 输入地址 → `setLink({href})`；无选中时 `alert("先选中要加链接的文字")`。`toolbarActive` 加 `link` 激活态（有链接时按钮高亮）。
- 2026-08-23 **诊断 Gemini 翻译失败：非模型/网络，是配额 429 用尽 + 上游错误改成中文可读提示**（tsc 通过）。用户问「gemini 无法调用了吗？刚用它翻译没成功」。**结论**：能调用，是你的 Gemini API Key **配额用尽（429 quota exceeded）**。排掉了两个假因——①**网络通**：`serverFetch` 走 `.env.local` 的代理能访问 Gemini（Google 秒回、非超时），故不是墙/GFW；②**模型名对**：`GEMINI_MODEL` 未设时默认 `gemini-3.6-flash` 是官方当前推荐模型（Google 404 报错里点名让用 `models/gemini-3.6-flash`），我本来还怀疑这名字是编的，实测 2.0/2.5 系列反而已下线（404「no longer available」）。**真正失败点**：`gemini-3.6-flash` 返回 `429 You exceeded your current quota, please check your plan and billing details`——key 有效、模型有效，就差配额/计费。**代码改善**：`ai-client.ts` 的 `openaiCompatChat` 原来把任何非 ok 都包成 `AI 调用失败（Gemini 502）：<英文原文>`，用户只见一堆英文误以为「没成功」；本轮加 `friendlyProviderError()` 解析上游错误体里的 `error.code`（能拿到更准的 429/401/404 而非外层 502），按码给中文提示——429→「配额上限，去 Google AI Studio / 服务商控制台查配额或开通计费」、402→充值、401/403→Key 无效、404→模型下线、400→请求原文、其余→带状态码的通用提示，并把 `AiError.status` 改为透传上游真实状态码（不再是固定 502）。DeepSeek/OpenAI/Groq/Gemini 四个 OpenAI 兼容提供商通用受益。**需人工**：到 Google AI Studio 检查该 `GEMINI_API_KEY` 的项目配额/是否绑定计费，充值或升级后 Gemini 文字（AI 精读/翻译/词群整理/分类补词）即恢复。
- 2026-08-23 **「原文」callout 三按钮回归实时 + 图标 + 点击反馈 + 恢复学伴「翻译」气泡（翻译器）**（tsc + build 通过）。上一轮把按钮从 `Decoration.widget`（每次编辑都重算 → 假名能实时出现，但渲染在块之前）改成 `renderHTML`（位置贴进 `.callout-label` 对了，但**只画一次** → 直接粘贴/打字不重建节点，`hasKana` 判定过期，「加假名」按钮不出现）。用户四点：①**假名按钮实时出现**——`callout-extension.ts` 的 `renderHTML` 对原文块固定输出三个按钮（翻译/精读笔记/加假名）；「是否显示加假名」「翻译是否变已译色」改由 `callout-actions.ts` 插件的 `Decoration.node` 给 callout 加 `no-kana`/`is-translated` 类、`globals.css` 控制——装饰**每笔交易重算**，粘贴日语即去掉 `no-kana`、假名按钮自动出现，且无手动改 contenteditable 的死循环。②**图标**：按钮前加符号（`⇄` 翻译 / `✎` 精读笔记 / `あ` 加假名）——`renderHTML` 里无法安全产出 lucide SVG（PM 用 `document.createElement` 建元素，SVG 标签会变 HTMLUnknownElement），用等宽 unicode 符号。③**点击反馈**：点按钮时抓取 `getBoundingClientRect()` 传给 `setCalloutActionHandler`，`rich-text-editor` 在按钮旁弹「翻译中…/精读中…/加假名中…」小气泡（复刻「✨ 解释」气泡的 `createPortal`）；加 busy 令牌防止先发动作完成时误清后发动作的忙碌态；翻译完成后节点重建带 `translation` mark → `is-translated` 类 → 按钮上已译色。④**恢复学伴「翻译」气泡（翻译器）**：用户澄清要的**不是**「翻译原文」（那种把全篇原文 callout 翻成中文的），而是**通用「翻译」模式气泡**——当作翻译器，把用户粘贴的任意内容翻成所选目标语言、地道自然。`ai-assistant-panel` 去掉「翻译原文」快速按钮（连同 `translateAll`/`translating`/`translateAllArticleCallouts` 一并删除，`translate-apply.ts` 移除该死导出），改为加回「翻译」气泡（`fillTranslate` 用 `setInput` 在输入框前缀「请把下面这段翻译成{目标语言}，翻得地道自然、符合母语者表达：」+ 现有输入，或空框时仅给指令、粘贴正文在其下；气泡行加 `flex-wrap` 防窄屏溢出）。⑤`removeFuriganaFromCallout` 改为摘完 mark 后重建节点，让「加/去假名」标签与装饰类随操作刷新。**验证**：headless Chrome 打英文「空原文块 + 已含日语原文块」编辑器——空块显示 `no-kana` 藏掉加假名，往空块输入日语后 `no-kana` 即时消失、加假名按钮出现；`["⇄翻译","✎精读笔记","あ加假名"]` 落在标签内；点精读/翻译各弹「精读中/翻译中」气泡并各发一次 `/api/ai/analyze`、`/api/ai/translate`。**注意**：本条目**取代**上一条的 renderHTML 静态按钮方案——renderHTML 只负责「把按钮摆在标签里」，按钮的显隐/已译态交给装饰器按内容实时驱动。
- 2026-08-23 **「原文」callout 操作按钮回到标签内 + 新增「翻译」按钮（替换 AI 助手「翻译原文」）**（tsc + build 通过）。用户反馈：①「精读笔记」按钮现在出现在 callout 块**上方**（widget 装饰 `side:-1` 让按钮跑到块之前，不再贴原文标题）；②AI 助手单独的「翻译原文」改放进 callout 里。**改法**：按钮不再用 widget 装饰，改由 `callout-extension.ts` 的 `renderHTML` 直接把按钮（翻译 / 精读笔记 / 加假名）写进 `.callout-label`——按钮是节点 DOM 的一部分，由 PM 亲自产出，**不再手动改 contenteditable -> 无解析死循环**；`callout-actions.ts` 从 widget 装饰改成一枚插件，在编辑器根 `view.dom` 挂一份委托 `click` + `mousedown`（`mousedown` 阻止按钮把焦点带进正文），从点击按钮反查所属 callout 的 `pos`（`posAtDOM`）交回 `runCalloutAction`。**新增「翻译」动作**：`runCalloutAction` 加 `translate` 分支，调 `translate-apply.ts` 新导出的 `translateCallout(editor, pos)`——先整理原文（`tidyText` 折叠多余空白/换行、丢空段、旧译文段丢弃），再逐段翻成中文以 `translation` mark 插回原段下方，形成干净的「上下双语对照」；替换原 AI 助手调用的 `translateArticleCallouts`（已删，AI 面板「翻译原文」按钮与 `translateOriginal`/`translating`/`Languages` 一并移除）。**分享图/阅读模式**：按钮由 renderHTML 一并输出，但 `globals.css` 加 `.share-content .callout-actions,.share-content .callout-action{display:none}` 隐藏（编辑器专属）；`rich-text-editor` 的 `setCalloutActionHandler` 转发逻辑不变。**验证**：headless Chrome 打开含 article callout 的编辑器，`.ProseMirror` 秒出、按钮 `["翻译","精读笔记","加假名"]` 落在 `.callout-label` 内（`allInsideLabel:true`、`strayActions:0`）、主线程无阻塞（n=1 与 n=100 均正常）；word/example/grammar callout 按钮数为 0。**注意**：本条目**取代**上面那条「用 widget 装饰修卡死」——widget 每次装饰都随事务重算，位置仍是块前； renderHTML 产出即贴标签，且同样绕开 mutation 循环。同时**推翻**更早「把翻译按钮从 callout 挪到 AI 助手」的决策——现在翻译按钮回到 callout、助手只留模板气泡。
- 2026-08-23 **修笔记打开无响应：所有含「原文」callout 的笔记一打开就卡死**（tsc + build 通过）。**根因**：`callout-actions.ts` 的 ProseMirror 插件在 `view()`/`update()` 钩子里往 `.callout-label` 手动 `appendChild`——这是在 ProseMirror 的 contenteditable DOM 里改内容，触发 MutationObserver → 解析 → dispatch → `update()` → 再 append 的死循环，主线程被占死、浏览器报「页面无响应」；网页端 + 手机端那些含 `kind="article"` 原文块（精读笔记）的笔记都打不开。**改法**：`callout-actions.ts` 弃用「钩子里改 DOM」，改成 **ProseMirror widget 装饰**（`Decoration.widget` 挂在原文块起始位、`side:-1`、`key` 固定）——按钮节点由 PM 渲染并管理，不碰正文 DOM、无循环；按钮仍是 `contenteditable=false`、只在编辑器出现（renderHTML 不产出、分享图/阅读模式不出现），点击仍经 `setCalloutActionHandler` → `runCalloutAction` 执行。「✨ 精读笔记」始终显示、日语原文加「あ 加假名/去假名」逻辑不变。**验证**：headless Chrome（桌面 + 移动视口）加载含原文块的真实结构笔记，`.ProseMirror` 秒出、按钮 `["✨ 精读笔记あ 加假名"]` 渲染、主线程无阻塞；纯文本 / 生词 / 例句 / 语法 / 媒体 / 分列 / 假名 / 译文各节点均正常渲染。
- 2026-08-23 **安卓手机发音修复 + AI 模型下拉精简 + 部署 Vercel**（tsc + build 通过，已部署生产）。① **安卓 TTS 无声**：`speak-button` 挂载时除 `voiceschanged` 外再按 100/400/1000ms 定时重拉 `getVoices()`（安卓首次为空且事件未必触发）；点击手势里现抓 `synth.getVoices()` 选音色（拿不到退回 state 缓存）；`cancel` 后改 40ms `setTimeout` 再 `speak` + `paused` 则 `resume`（安卓/桌面 Chrome「cancel 后立刻 speak」会被吞掉）。保留系统 TTS，暂不上 Azure。② **AI 模型下拉精简**：`ai-model-form` 去括号说明，标签只留 Claude/DeepSeek/OpenAI/Groq/Gemini，任务标题「文字/语音转录/图片识别」，`settings` 描述收敛成一句。③ **部署**：生产补 `GEMINI_API_KEY`（敏感），`vercel --prod` 首传 `fetch failed` 网络抖动、重试成功，`readyState: READY`，域名 https://nestlingo-psi.vercel.app。
- 2026-08-23 **闪卡发音按语言标签读（修日语纯汉字卡被读成中文）**（tsc 通过）。`SpeakButton` 加 `lang?: Lang` prop，`cleanForSpeech(text, lang?)` 用显式语言替代 `detectLang` 兜底；所有卡面发音点（复习背卡 `review-session`、卡面 `card-tile`、单卡详情 `card-detail`、错题集 `test-errors`、测试 `test-session`×2、待加强 `review-home`）都传入 `lang={cardLang(card)}`——纯汉字日语卡（如「勉強」）靠 `card.lang` 标签判成 ja-JP，不再被 Unicode 兜底判成中文。完形填空句（无 lang 字段）仍按句子自动识别，不动。**手机端无声音**：已修 iOS 两个已知坑——`SpeakButton` 用 `useRef` 强引用 utterance（防 iOS GC 提前回收、播到一半停）+ `speak` 后 `paused` 则 `resume()`（防「cancel 后卡 paused 不出声」）；`speech.ts` 的 `speakParagraph` 同样持住分段 utterance。**更真人化音色**：speechSynthesis 是系统音色、无法提升，仍待确认是否换云端 TTS（Groq TTS 仅英/阿拉伯语、不覆盖五种语言），见「待确认」。
- 2026-08-23 **撤回 AssemblyAI + AI 模型下拉只显示已配 Key 的提供商**（tsc 通过）。① AssemblyAI 一次性额度不值、用户决定不要——`transcribe.ts` 还原成 Groq/OpenAI 两个 Whisper 兼容提供商（删 `assemblyaiTranscribe` 三段式），`ai-model-form.tsx`「语音」下拉删 AssemblyAI 项、`AiKeys` 删 `assemblyai`，`settings/page.tsx` 不再检测该 Key。② **下拉只显示已配 Key 的选项**：`ai-model-form.tsx` 渲染改成 `t.options.filter(o => o.key === null || keys[o.key])`，未配 Key 的 provider 不再出现在下拉里（原来「未配 Key」的提醒也删掉）；已保存值若不在可见项里则回退「默认」，防止 select 空白。
- 2026-08-23 **接入 Groq / Gemini 文字模型 + AssemblyAI 语音转录**（tsc + build 通过）。① **Groq / Gemini 文字**：`ai-client.ts` 把 `deepseekChat`/`openaiChat` 抽成共享 `openaiCompatChat({url,model,key,label})`，新增 `groqChat`（`GROQ_MODEL` 默认 `llama-3.3-70b-versatile`）与 `geminiChat`（Gemini 走 OpenAI 兼容端点 `generativelanguage.googleapis.com/v1beta/openai/chat/completions`、`GEMINI_MODEL` 默认 `gemini-3.6-flash`）；`AiProviderText` 加 `"groq"|"gemini"`，`aiChat` 路由与 `userTextProvider` 映射补齐。② **AssemblyAI 转录**：`transcribe.ts` 重构为「提供商名 → 分派」——Groq/OpenAI 仍走 Whisper 兼容表单（`whisperTranscribe`），新增 `assemblyaiTranscribe`（上传 → 提交 → 轮询三段式，`ASSEMBLYAI_API_KEY`）；`SpeechProvider` 加 `"assemblyai"`，自动回退链 Groq → AssemblyAI → OpenAI。③ **设置下拉**：`ai-model-form.tsx`「文字」加 Groq/Gemini 两项、「语音」加 AssemblyAI 一项，`AiKeys` 补 `gemini`/`assemblyai`；`settings/page.tsx` 同步检测这两个 Key。均无 Key 时优雅报中文错、不影响未配置环境。**需人工**：到 AI Studio 拿 `GEMINI_API_KEY`、AssemblyAI 拿 `ASSEMBLYAI_API_KEY` 填 `.env.local`（Groq 的 key 已有，文字任务直接可用）。公有云免费层（AWS/Azure）未接入，见下方「待确认」的评估结论。
- 2026-08-23 **原文 callout 只留「精读笔记 + 加假名」两按钮（撤翻译按钮）+ 双语翻译挪到 AI 助手「翻译原文」按钮 + 接入 OpenAI 文字模型**（tsc + build 通过）。用户澄清三点：① **原文 callout 撤掉「⇄ 翻译」按钮**，只恢复之前丢失的两个：「✨ 精读笔记」（始终显示，调 `/api/ai/analyze` 整理原文 + 逐段翻译 + 生词/例句/语法解释，原文里做生词高亮/语法下划线/日语加假名）与「あ 加假名/去假名」（仅日语原文 `hasKana` 显示、已标变去假名）；`callout-actions.ts` 删掉 translate 动作与按钮注入，`CalloutAction` 收窄成 `analyze | toggleFurigana`。② **双语翻译挪到 AI 助手**：新增 `src/lib/translate-apply.ts` 的 `translateArticleCallouts(editor)`（从后往前翻译每个 article callout，译文以 translation mark 插原段下方、旧译文丢弃重建、`attrs` 保留 source），学伴面板加「翻译原文」直接动作按钮（teal 胶囊 + Languages 图标，busy 转圈 / done / error），点即翻译当前笔记所有原文 callout 成上下双语对照；**同时删掉「翻译」气泡**（中文→外语直接打字 + 语言下拉即可，且避免与「翻译原文」名字混淆，气泡总数不增、不换行溢出）。③ **接入 OpenAI/ChatGPT 文字**：`ai-client.ts` 加 `openaiChat`（OpenAI 兼容格式、`OPENAI_MODEL` 默认 `gpt-4o-mini`、用现有 `OPENAI_API_KEY` 无需新 key），`AiProviderText` 加 `"openai"`、`aiChat` 路由与 `userTextProvider` 映射补 openai；`ai-model-form.tsx`「文字」下拉加「OpenAI（ChatGPT）」选项。模型选择仍放设置页（全局偏好，一次设置即固定），对话窗不加选择器。
- 2026-08-23 **例句补全再简化成纯翻译 + 恢复原文 callout 的「AI 精读/加假名」按钮 + 新增「翻译」双语对照 + 分享图按条切页行距收紧**（tsc + build 通过）。用户四点：① **例句补全去生词解释、只翻译**——「算了，例句不要生词解释了，直接翻译句子意思就行」：`/api/ai/assistant` 的 `notePointsHint` 例句规则改回 front 原句一字不改、back 只给准确中文翻译、extra 留空（推翻上一轮「生词解释落到例句背面」）；气泡「补全笔记」模板同步简化成「补全这篇笔记里的生词、例句、语法：补充释义、翻译和说明，给生词、语法各加一个例句。」。② **恢复原文 callout 的「AI 精读」标识**——上一轮为修 React 19 `flushSync` 冲突把 callout 从 ReactNodeView 改成 renderHTML，交互按钮随之丢失。本次新增 `src/lib/callout-actions.ts`：一个 ProseMirror 插件（TipTap `CalloutActions` Extension 包裹，已注册进 `editor-extensions`）在 `view.update` 阶段给每个 article callout 的 `.callout-label` 注入纯 DOM 按钮「✨ 精读 / ⇄ 翻译 / あ 加假名」，点击经模块级 `setCalloutActionHandler` 回调到 `rich-text-editor` 里的 `runCalloutAction(editor, action, pos)`；「加假名」仅在日语原文（`hasKana`）时显示、已标假名时变「去假名」（复用 `addFuriganaToCallout`/`removeFuriganaFromCallout`）；按钮只在编辑器出现（renderHTML 不产出、分享图/阅读模式不出现）。③ **新增「翻译」做双语对照**——`src/app/api/ai/translate/route.ts`（鉴权 + `aiChat` 逐段翻成简体中文、`\n` 分隔段数对齐），`addTranslationToCallout`（callout-actions.ts 内）把原文 callout 每段翻成中文、以 `translation` mark 插在原段下方（丢弃旧译文重建、重复点不叠、`attrs` 保留 source），由「⇄ 翻译」按钮触发。④ **分享图「按条切页」行距收紧**——`share-modal` 按条切页容器加 `share-flat` 类，`globals.css` 加 `.share-flat` 规则（`line-height` 1.7→1.5、`p/ul/ol/li` margin 收紧，每条不再分太开）。
- 2026-08-23 **学伴「补全」例句修正：生词解释落到例句背面、不再拆成词卡 + 工具栏撤销/重做/AI 回到一行**（tsc 通过）。用户两次反馈推翻了上一轮「例句拆生词做词卡」的走向：① 例句补全**不再**为每个生词额外输出一条 `kind="word"` 点、也不进笔记「生词」区、不成词卡——`/api/ai/assistant` 的 `notePointsHint` 例句规则改为：front 原句一字不改、back 先给准确中文翻译、换行逐条列出值得解析的生词解释（每条「词：中文释义」，可附句中例句），没有值得解释的生词就只给翻译、extra 留空；「生词的解释还是放在例句卡的背面、翻译的下面」。`ai-note.ts` 的 `item()` 加 `back.replace(/\n/g,"  ")`——例句多行背面（翻译 + 逐词解释）经 callout 单行存储、再由 `parseCards` 按双空格拆回换行，正面/背面显示与「转成闪卡」都保持逐行；学伴面板知识点 back 加 `whitespace-pre-wrap` 显示多行。气泡「补全笔记」模板同步改成「例句把翻译写清楚、下面逐条列出值得解析的生词释义」。② **工具栏撤销/重做/AI 学伴不再独立成组换行**：`rich-text-editor` 工具栏容器 `flex flex-wrap` 加 `md:flex-nowrap md:overflow-x-auto`（桌面一条、放不下内部横滑，手机仍自动换行自适应），撤销/重做/AI 学伴那组 `flex shrink-0` 跟在一行编辑功能后面、不再单独折到下一行。
- 2026-08-23 **学伴「补全」例句改为拆生词做词卡（不再随意补例句）**（tsc 通过）。`/api/ai/assistant` 的 `notePointsHint` 把「补全」指令改成按类型分流：生词/语法照旧（back 丰富释义/说明 + extra 例句），**例句（example）不再额外补一个例句**——front 原句一字不改、back 只给准确翻译（可补结构/用法），而是挑出例句里值得背的生词，为每个额外输出一条 `kind="word"` 的点（front=生词、back=中文释义、extra=原句语境下的例句）。这样例句「补全」得到的是「词：释义  例句」词卡，落到笔记「生词」区。气泡「补全笔记」模板同步改成「给生词、语法加例句；例句挑出生词做成词卡」。（⚠️ 此条已被上一条**推翻**——用户反馈生词解释应放例句卡背面、不做词卡。）
- 2026-08-23 **学伴「补全」扩到生词/例句/语法 + 自动去重 + 按钮改「覆盖原始笔记」**（tsc 通过）。① **气泡改名「补全笔记」**：`MODE_BUBBLES` 加回补全项，改名「补全笔记」、模板覆盖生词/例句/语法（`补全这篇笔记里的生词、例句、语法：把释义/翻译/说明补充得更完整，并给每一条加一个例句。`）。② **上下文按类型抽 + 去重**：`ai-assistant-panel` 的 `noteWordsOf` 改为 `notePointsOf`，从 `parseNote` 抽 word/example/grammar 三类卡（各带 kind），并用 `alreadyEnriched(back)`（背面多行「释义\n例句」即视为已富化）过滤掉已补全的，只把薄卡发给 AI——新增内容再补全时自动跳过已富化的，不重复补全。③ **路由字段改名 `noteWords`→`notePoints` 并泛化**：`/api/ai/assistant` 的 `noteWordsHint` 改为 `notePointsHint`，context 行带 `[生词/例句/语法]` 前缀，system 提示让 AI 按原类型回点（生词→word、例句→example、语法→grammar）、front 原词不变、back 补全释义、extra 例句；body 解析保留 kind。④ **覆盖写回改「覆盖原始笔记」+ 按类型合并**：`replaceWords` 改为 `replaceOriginal`，`selectedPoints` 过滤 `!== article`；现有笔记里三类卡先按 front 归并，AI 富化点覆盖薄卡、其余已富化/未命中卡原样保留（back 换行 `\n` 回写成双空格再经 `pointsToNoteContent` 重建 callout，转成闪卡时拆回），替换对应 word/example/grammar callout；按钮文案「覆盖到笔记『生词』区」改「覆盖原始笔记」、条件从 `kind==="word"` 放宽到 `kind!=="article"`。
- 2026-08-23 **AI 补全 / 学伴四项收尾**（tsc 通过）。① **转成闪卡列表页恢复「AI 补全」并改为富化+覆盖**：`convert-to-cards` 重新加回 `Sparkles`/`Loader2` 导入、`aiBusy`/`aiError`/`aiFilled` 状态与「AI 补全」按钮（语言行下方、无 ✨ emoji、只用 Sparkles 图标）；`fillAll()` 逐张调 `/api/ai/card-explain`（带 kind），把每张新卡背面统一覆盖成返回的更完整解释——**不再要求背面为空**，已有释义也会被富化覆盖；补全过的卡正面右侧加「AI」小徽章（`ai` prop）。② **学伴去「补全生词」气泡**：`ai-assistant-panel` 删掉 `MODE_BUBBLES` 里的「补全生词」项——用户直接发「丰富当前笔记页面的生词含义给出例句」即可，`noteWordsOf(editor)` 上下文随每句自动注入，无需模板气泡。③ **学伴加「清除会话」**：面板头部关闭钮旁新增「清除会话」按钮，`clearSession()` 二次确认后清空消息/知识点/勾选并 `localStorage.removeItem(storageKey)`（按笔记隔离的历史可手动清空）。④ **AI 学伴按钮回到撤销/重做同组**：`rich-text-editor` 工具栏把撤销/重做/AI 学伴包进一个 `flex shrink-0 items-center gap-0.5` 小组，`flex-wrap` 换行时三者整体走、不再把 AI 学伴单独挤到下一行。
- 2026-08-23 **AI 学伴支持识别当前笔记并「补全生词」：读笔记生词作上下文 + 富化后覆盖写回**（tsc + build 通过）。① **注入笔记生词上下文**：`ai-assistant-panel` 发消息时用 `noteWordsOf(editor)`（`docToText(editor.getJSON())` → `parseNote` 抽出生词 sections 的 front+back）随 `message` 一起传给 `/api/ai/assistant`（新字段 `noteWords`，路由最多收 60 个、只留 front+back 防超长）。② **路由识别补全意图**：assistant 路由新增 `noteWordsHint`，把生词清单注入 system prompt——用户说「补全/丰富/完善这些生词释义、加例句」时，针对这些词输出 word 点（front 原词不变、back 更完整中文释义、extra 原语言例句），仍是「词：释义 例句」格式、转成闪卡可识别。③ **覆盖写回（替换原生词区）**：面板新增「补全生词」模式气泡 + 知识点列表下方「覆盖到笔记『生词』区」按钮——`replaceWords()` 用 `pointsToNoteContent(words)[0]` 生成富化后的生词 callout，遍历 `editor.getJSON().content` 把原有 `callout[data-kind=word]` 全部替换成这一个（多个合并、没有则末尾新建），`editor.commands.setContent` 写回并触发 onUpdate → 自动保存。与「加入笔记」（追加）区分开，不产生重复词。历史仍按 noteId 存 localStorage 隔离。
- 2026-08-23 **编辑器工具栏还原成 `flex flex-wrap` 自适应换行（撤销上一轮「一行横滑」方案），只保留 Chrome 键盘弹起吸顶修复**（tsc + build 通过）。用户改用「适应屏幕换行」而非「单行横滑」，故把工具栏从「外层 `flex items-stretch` + 左侧 `overflow-x-auto` + `w-max` 内层 + 右侧 `shrink-0` 固定组」还原成**单个 `flex flex-wrap items-center gap-0.5` 容器**——所有按钮（含撤销/重做/AI 学伴，去掉原来独立固定组与 `border-l` 分隔）回到同一行、窄屏自动换行，页面/工具栏都不再横向滚动；保留 `blockquote` 引用按钮与表格操作工具栏（用户确认「有删表工具栏就不用动」）。Chrome 键盘弹起吸顶修复（`layout.tsx` 的 `interactiveWidget: "resizes-visual"`）不受影响、继续生效。
- 2026-08-23 **修三处移动端 bug：工具栏只内部横滑不带动页面、表格可删、素材筛选标签不再跳 Tab 下方溢出**（tsc + build 通过）。① **工具栏只内部横滑（页面不再左右滑动）**：上一轮把功能按钮区做成 `overflow-x-auto`，但没加 `w-max` 内层包裹，按钮仍按 flex 收缩、撑得整页横向滚动。改法：可横滑区 `no-scrollbar min-w-0 flex-1 overflow-x-auto` 内再包一层 `flex w-max items-center`（`width:max-content` 让按钮按自然宽度排、超出只在区内滚），外层 `min-w-0 flex-1` 收紧宽度、`w-max` 让内层溢出不外传——只有工具栏区能左右滑，页面不再横向滚动；撤销/重做/AI 学伴仍固定右侧 `shrink-0` 不随滚动。② **表格可删**：表格操作工具栏（插删行列、标题行、删表）原来放在 sticky 容器外、光标一旦离开表格即消失，手机上不好点。改法：把这行嵌进 sticky 工具栏容器内、加 `no-scrollbar overflow-x-auto` 可横滑，光标在表格里时跟随吸顶常驻，「删表」始终可达；根因确认 `deleteTable` 命令本就在（`insertNormalTable` 无 header、`deleteTable` 删整表）。③ **素材筛选标签不再跳位/溢出**：`materials-view` 原来「批量操作 + 筛选开关」与已激活标签混在同一 `flex flex-wrap`，点中「日语/视频」等具体标签时激活标签增多、`flex-wrap` 把整组挤到 Tab 下方再溢出屏幕。改法：右组改成 `flex items-center`（只放批量操作 + 筛选开关，宽度稳定不换行）；已激活标签（语言/类型/状态 + 清空）抽成**单独一行** `mt-2 flex flex-wrap`，出现/消失只增删这一行、不影响上面的 Tab/筛选行，不再跳位溢出。
- 2026-08-23 **编辑器工具栏：键盘弹起不再顶上去 + 窄屏横向滑动 + 撤销/重做/AI 固定 + 新增引用按钮**（tsc + build 通过）。① **键盘弹起工具栏不被顶走**：根因是 Chrome Android 默认 `interactive-widget=resizes-content`——键盘弹起时缩放「布局视口」、`position: sticky` 的工具栏跟着重排被顶出可视区（iOS Safari 等默认不缩布局视口、故正常）。`layout.tsx` 的 viewport 加 `interactiveWidget: "resizes-visual"`（只缩可视视口、布局视口不变），sticky 工具栏保持钉在顶部。② **工具栏一行放不下改横向滑动**：`rich-text-editor` 工具栏从 `flex flex-wrap`（按钮换行）改成「外层 `flex items-stretch` + 左侧 `no-scrollbar overflow-x-auto` 可横滑区 + 右侧固定区」——参考 Notion / 备忘录，窄屏时功能按钮区左右滑动、不换行。③ **撤销/重做/AI 学伴固定**：右侧固定区（`shrink-0` + `border-l` 分隔）只放撤销、重做、AI 学伴三个，不随横向滚动、始终可见。④ **新增引用按钮**：格式区列表后加 `Quote` 图标按钮，`toggleBlockquote()`（StarterKit 自带 blockquote，`.tiptap blockquote` 样式已有），`toolbarActive` 补 `blockquote` 激活态（高亮可开可关）。
- 2026-08-23 **标题栏背景与页面融合（修正上一轮）+ 验证 callout 正常显示**（tsc + build 通过，已部署生产）。① **修正标题栏背景**：上一轮把 body 渐变删成纯 paper 是误改——用户要的是「标题栏不单独做一个吸顶区域、跟原背景融合，只是底下内容跟它切分；标题在渐变背景上就仍是渐变背景」。改法：`:root` 新增共享变量 `--page-bg`（顶部两角淡绿 radial 渐变 + paper 底色），body 恢复 `background: var(--page-bg)` + `background-attachment: fixed`（渐变保留）；`.page-header` 移动端 `background` 从纯 `var(--paper)` 改成同样的 `var(--page-bg)` + `background-attachment: fixed`——标题栏与 body 同一条渐变（同为 viewport 定位、视觉无缝融合），且 paper 底色不透明、下滑时下方内容被切掉不渗入。桌面端 `md:` 仍复位 `background: transparent`。② **callout 移动端验证**：headless 跑 `Editor` + renderHTML 版 Callout 插入 4 种 kind（word/example/grammar/article），`getHTML()` 均输出 `<div data-kind=… class="callout"><div class="callout-label" contenteditable="false">标签</div><div class="callout-body"><p>…</p></div></div>`，标签/正文/属性齐全、CSS（`.tiptap .callout` 彩底 + 标签着色）未受本轮改动影响——点击后在光标处直接展示 callout 区域，正常。已 `vercel --prod` 部署（首传 `fetch failed` 网络抖动、重试成功，readyState: READY）。
- 2026-08-23 **全部页面标题吸顶 + 标题栏背景与页面一致 + 去滑动绿幕 + 防 iPhone 缩放**（tsc + build 通过）。① **次级页面标题吸顶**：新增共享 `.page-header` 工具类（globals.css：移动端 `sticky top-0` + `-mt-6 -mx-4` 抵消 `max-w-5xl px-4 py-6` 留白贴满整宽 + 安全区顶边距 + paper 底色 + 细 hairline；`md:` 回到普通块）。套到复习全部子页（待加强/错题集/测试/故事/完形填空/背诵单文件/背诵主题 7 处 header）、单卡详情 `card-detail`、主题详情 `theme-detail`、设置 `settings`、素材详情 `material-watch`、合集详情 `collection-detail`、笔记闪卡页 `notes/[id]/cards`；笔记区 `mobile-folder-view` 用白底吸顶（笔记区是白底、非 paper）。三个一级页（复习主页 `review-home`、素材 `materials-view`、闪卡 `cards/page`）的 inline 吸顶类也统一收敛成 `.page-header`。`app-shell` 的 `hasStickyHeader` 从「只豁免复习主页」扩成覆盖所有带吸顶头的路径（`/review` 全子页、`/cards/*`、`/materials/*`、`/settings`、`/groups/*`、`/notes/[id]/cards`），main 不再重复叠顶部安全区留白。② **标题栏背景与页面一致**：body 背景删掉鼠尾草绿的 radial 渐变、改成纯 `var(--paper)`（`background-attachment: fixed` 一并移除，iOS 下它也是滚动闪绿来源之一）；`.page-header` 用 `var(--paper)` 替代原来的 `bg-white`，标题栏与页面同色、不再有「白条」区分感。③ **去滑动绿幕**：`layout.tsx` 的 `themeColor` 从 `#4c8c7c`（绿）改 `#fafbfa`（纸色），`manifest.ts` 的 `theme_color`/`background_color` 同步从 `#0d9488`/白 改 `#fafbfa`——iPhone 橡皮筋 overscroll / 状态栏不再露绿。④ **防缩放**：viewport 加 `maximumScale: 1, userScalable: false`；`html` 加 `-webkit-text-size-adjust: 100%`；`body` 加 `touch-action: manipulation`（防双击放大）；移动端 `input/select/textarea` 统一 16px（防 iOS 聚焦自动放大）。注：iOS Safari 为无障碍仍允许捏合缩放，`user-scalable=no` 对其无效，此为主流「尽力而为」组合（防双击/聚焦放大为主）。
- 2026-08-23 **背诵/测试会话页隐藏移动端底部导航 + 修 callout（生词/例句/语法/原文）点击无反应**（tsc + build 通过）。① **隐藏底部导航**：`app-shell` 新增 `isReviewSession = pathname === "/review" && searchParams.size > 0`——复习主页（无参数）保留底部导航，所有背诵/测试子页（`?mode=test`、`?mode=cloze`、`?mode=story`、`?scope=weak`、`?scope=errors`、`?note=…`、`?theme=…`）沉浸式专注背诵、移动端底部导航一并隐藏，`main` 同步 `pb-0`（其余页面仍 `pb-[calc(5rem+env(safe-area-inset-bottom))]` 给导航留位）。② **callout 点击无反应**：根因是 callout 用非原子 ReactNodeView（`callout-nodeview`），React 19 下 TipTap 同步 dispatch 触发的 `flushSync` 与 React 渲染冲突，插入被静默吞掉、区块不出现。改法：`callout-extension` 去掉 `addNodeView()`，改走 `renderHTML`（与 columns/Table 一致、已证实可用），标签 `contenteditable=false` 防删、正文由内容洞承接，分享图/转成闪卡共用同一份 renderHTML；`globals.css` 补 `.tiptap .callout[data-kind=…] > .callout-label` 按类型着色（生词金 #9a7328 / 例句绿 #2b795c / 语法紫 #5a4ac0 / 原文灰 #a1a1aa）对齐原 NodeView 配色。「原文」块的「来源」链接在 renderHTML 保留；原 NodeView 里的「加假名/精读笔记」交互（仅 article 类型）随 NodeView 移除而放弃，`callout-nodeview.tsx` 文件保留但不再被引用。
- 2026-08-23 **修喇叭无声音（移除失效 Google TTS）+ 背诵闪卡手机端高度拉伸 + 笔记粘贴链接回车自动变可打开链接**（tsc + build 通过）。① **喇叭没声音**：根因是发音优先走 Google 翻译在线 TTS（`translate_tts?client=tw-ob`），该免费端点已失效且国内被墙，`new Audio().play()` 必失败；失败后 2.5s `setTimeout` 才退回 speechSynthesis，此时已脱离用户点击手势、iOS 拒绝出声，且移动端 `getVoices()` 首次为空。改法：`speak-button` 删掉整个 Google TTS 分支，纯用浏览器 speechSynthesis；挂载时预热音色列表并监听 `voiceschanged`（音色异步加载），点击时在用户手势里同步 `speak()`，选不到音色就用该语言默认音色读；`speech.ts` 的 `pickVoice` 加可选 `voices` 参数（避免重复 getVoices）。② **背诵闪卡高度拉伸**：`review-session` 卡片 `min-h-[280px]` 改成 `min-h-[60vh] sm:min-h-[320px]`——手机端占 60% 视口高、明显更高，桌面 320px（略增）。③ **粘贴链接变可打开链接**：`editor-extensions` 加 `@tiptap/extension-link`（`openOnClick:true + autolink:true + linkOnPaste:true`，target=_blank），粘贴 URL 后跟空格/回车即自动转成可点击链接、点击开新标签；`globals.css` 加 `.tiptap-link` 样式（teal 下划线）。此前「放弃超链接」的 `/api/link-title` 路由未恢复——本次只做「粘贴纯 URL 自动成链接」，不做「抓标题换文字」。
- 2026-08-23 **修手机端编辑器工具栏真正生效（touch 事件）+ 素材筛选按钮不再换行溢出 + sw.js 静态资源改 stale-while-revalidate**（tsc + build 通过）。① **移动端工具栏**：上一轮只加 `onMouseDown preventDefault`，但移动端触摸序列里 `touchstart` 就先取消文字选区并触发 ProseMirror 同步折叠、`mousedown` 阶段已太晚，故选中文字点 B/下划线/高亮仍不生效、callout 仍插到折叠后的错位处。本次在 `ToolButton`、4 个 callout 按钮、AI 学伴按钮统一加 `onTouchStart={(e) => e.preventDefault()}`（阻止系统取消选区，并顺带阻止后续合成 click）+ `onTouchEnd` 里手动执行命令——移动端只走 touchend、桌面仍走 onClick，互不重复。② **素材筛选按钮**：`materials-view` 筛选按钮文字从「筛选/收起筛选」动态切换改成**固定「筛选」**（打开态用 `filterOpen` 加 teal 实心高亮，与有激活条件同款）——此前点开后文字变「收起筛选」加宽，`flex-wrap` 把「批量操作」和筛选按钮挤到 Tab 下方换行，筛选按钮跑到左侧导致 dropdown（`right-0` 向左展开）溢出屏幕左边缘；文字固定后宽度稳定不再换行、下拉右对齐不溢出。③ **sw.js 静态资源缓存**：`/_next/static`、`/icons/`、`/icon.` 从缓存优先改成 **stale-while-revalidate**——有缓存先返回（秒开），同时后台 `fetch` 拉新覆盖缓存、下次访问即拿新资源，部署后无需手动清缓存；断网且无缓存时回退拉取（`.catch(() => m)`）。
- 2026-08-23 **修手机端笔记编辑器工具栏：选中文字可加粗/下划线、高亮可开可关、callout 按钮有反应**（tsc + build 通过）。根因有两个：① 工具栏按钮点下去先把编辑器焦点抢走（blur），ProseMirror 选区被折叠，`.focus().toggleBold()` 变成只对「之后输入」生效，插入类（callout）也插到了折叠后的错位处、看起来像没反应；② 激活态高亮 `editor.isActive(...)` 直接在 JSX 里算，组件不随选区变化重渲染，导致「再点取消 B」高亮不变。改法：① `ToolButton` 与 4 个 callout 按钮、AI 学伴按钮都加 `onMouseDown={(e) => e.preventDefault()}`，阻止按钮抢焦点、保住选区/光标（与已有「✨ 解释」气泡按钮同款写法）；② 用 `useEditorState` 订阅 transaction/update，把 bold/italic/underline/highlight/bulletList/table 的激活态抽出来实时刷新，替换原来 6 处 `editor.isActive(...)`。标题下拉（blockType）本就有 selectionUpdate/transaction 订阅、无需改。**桌面对齐补强**：`ToolButton` 加 `disabled` 属性（`cursor-not-allowed opacity-40`），撤销/重做按钮在不可用时置灰——用 `useEditorState` 里新增的 `canUndo`/`canRedo`（`editor.can().undo()/redo()`）驱动，跟桌面编辑器「无可撤销时置灰」一致。
- 2026-08-23 **统一各主页 sticky 标题：复习/闪卡/素材标题吸顶，与笔记一致**（tsc + build 通过）。定案：吸顶薄标题栏（`top-0 + bg-white` 不透明 + `-mx-4 -mt-6` 贴满容器 + `pb-3 pt-[calc(env(safe-area-inset-top)+0.75rem)]` 顶到状态栏）比「标题随内容滚走」更利于手机端导航，统一采用。① **复习页留白修正**：`review-home.tsx` sticky 头从 `top-[max(1rem,env(safe-area-inset-top))] + bg-paper/95 backdrop-blur + py-3`（上方多 1rem 空隙、半透明渗内容）改成与笔记页一致的 `top-0 + bg-white + pb-3 pt-[calc(env(safe-area-inset-top)+0.75rem)]`。② **闪卡/素材标题吸顶**：`cards/page.tsx` 页头、`materials-view.tsx` 标题+工具栏行都套上同款 sticky 头（原来只是普通背景里的一行，下滑即滚走）。③ **main 安全区留白豁免扩展**：`app-shell.tsx` 新增 `hasStickyHeader`（`isNotes` ∨ `/cards` ∨ `/materials` ∨ review 主页），有吸顶头的页面 `main` 不再叠 `pt-[max(1rem,env(safe-area-inset-top))]`（否则吸顶头上方多空隙）；**review 主页用 `useSearchParams` 区分**——`pathname==="/review" && searchParams.size===0` 才算主页（scope/mode/note 等子页是带返回键的普通头，仍靠 main 留白避状态栏）；`/cards/[id]`、`/materials/[id]` 详情页因 `pathname !== "/cards"/"/materials"` 精确匹配、保持原 main 留白。
- 2026-08-23 **手机/桌面 UI 修复一批：sticky 标题留白 + 素材筛选下拉紧贴 + 素材卡片标签/菜单换位 + 合集封面 + 批量按钮统一 toggle**（tsc + build 通过）。① **sticky 标题留白/内容渗入**：`app-shell` 的 `main` 顶部安全区 padding 对 `isNotes`（笔记区）也豁免（原来只豁免 `isNoteDetail`，`/notes` 列表页顶部多出 `1rem+安全区` 空隙，滚动内容从那里渗进来）；`notes/page.tsx` 手机 sticky 头从 `top-[max(1rem,env(safe-area-inset-top))] + bg-white/95 backdrop-blur`（半透明、上方留 1rem 空隙）改成 `top-0 + bg-white` 不透明 + `pt-[calc(env(safe-area-inset-top)+0.75rem)]`（背景顶到状态栏、内容被完全挡住）。② **素材筛选下拉紧贴按钮**：`materials-view` 的「筛选」按钮包进 `relative` 容器，展开面板从「inline 占一整行（`mt-3 border-t pt-3`）」改成**绝对定位 dropdown**（`absolute right-0 top-full mt-2 z-40`，含 `fixed inset-0 z-30` 点外关闭遮罩），紧贴按钮向下弹、不再"换一个地方"。③ **素材单条卡片标签/菜单换位**：语言/类型/状态三个徽章从标题行右上角**移到下面单独一行**，`RowMenu`（⋯）从下面单独一行**移到标题行右上角**（原标签位置），卡片最右侧不再有突兀的单独菜单行。④ **合集封面**：无缩略图占位从 `bg-zinc-50` 灰底白图标改成 **teal→cyan 渐变 + 图标 + 合集名首字**，更有质感。⑤ **批量/选择按钮统一原地 toggle（点开即退出、不另找退出）**：`cards-view`（按来源）、`group-browser`（按主题）两处「批量操作」从「点了就消失、退出藏在下方操作栏『完成』」改成**始终显示 + 文字 `批量操作↔退出批量` 原地切换**；`note-cards` 的「选择」改成**始终显示 + 文字 `选择↔完成` 原地切换**（teal 实心高亮），全选/删除仅在激活态展开。与 `materials-view` 已有 toggle 模式对齐。
- 2026-08-23 **Vercel 重新部署最新代码（含 lint 清理 + 此前全部改动）**（build 绿、readyState: READY）。正式域名 https://nestlingo-psi.vercel.app；首次 CLI 上传遇一次 `fetch failed` 网络抖动、重试即成功，云端 build 14s、路由表完整（32 路由 + 20 API）。本地 `curl` 直连该域名超时（无代理、`-4` 也超时）——非部署失败，疑似 Deployment Protection 或本地到 Vercel edge 网络问题，需浏览器实测确认。「待人工」3 项仍未落地（见「进行中」顶部）：Supabase Auth URL、关 Deployment Protection、生产库跑 schema.sql。
- 2026-08-23 **代码质量清理（lint）：修 6 处 `no-explicit-any` + 4 处 `prefer-const` + 12 处未使用变量/导入 + 1 处无效 eslint-disable**（tsc + build 通过）。`no-explicit-any` 六处（analyze/cloze-examples/cloze-session 的 AI JSON 防御解析 + ai-note 的 marks attrs）改 `Record<string, unknown>`（cloze-session 的 map 回调用 `String()`/`typeof === "string"` 兜底 word/sentence/translation）；`prefer-const` 四处 `let→const`（extract 的 doc、cloze 的 i/j、parse-cards 的 front）；未使用项（Plus/ReactNode/MATERIAL_TYPE_LABEL 三个 import、busyId/aiError/setMaterial 解构、catch(e)/setSelected(prev) 参数、input 常量、title prop、review-home 无效 eslint-disable）逐一删除或改 `[, setter]` 形式。剩 13 个 React Compiler 严格规则告警（set-state-in-effect ×10、refs-during-render ×2、impure-during-render ×1）**未动**——非运行时 bug、不阻塞 build/部署（Next 16 build 不跑 eslint），重构有风险暂缓；另有 6 个 warning（5 个 `<img>` 建议 next/image + 1 个 useCallback 依赖）同暂缓。
- 2026-08-23 **选中文件夹 → 点笔记 → 文件夹栏收起（两栏）**（tsc 通过）。`notes-workspace` 的「进笔记页自动收起文件夹栏」effect 依赖从 `[isNote]` 改成 `[pathname]`——之前只在 `/notes`（列表）↔ `/notes/[id]`（笔记）之间切换时收起，若已停在某篇笔记页、展开文件夹栏后再选文件夹点另一篇，`isNote` 不变、effect 不触发、栏会一直开着；改依赖 pathname 后，任何一次跳到笔记页（含笔记间跳转）都会重新收起，始终回到「笔记列表栏 + 笔记内页」两栏。列表页 `/notes` 仍不收起（选文件夹只是筛选列表、不跳转）。
- 2026-08-23 **笔记列表高亮「正在看的这篇」 + 复查近期改动手机端自适应**（tsc 通过）。`NoteList` 增加 `activeNoteId`（仅 `/notes/[id]`，列表页/文件夹页/子页不算），打开某篇笔记时左侧列表该篇用 `bg-teal-50 border-teal-300` + 标题 `text-teal-700` 高亮，与文件夹选中态（`selectedId` 高亮）呼应——点文件夹高亮文件夹、点笔记高亮笔记，当前所在位置一眼可见（桌面三栏；手机端正文页全屏、不渲染 NoteList，故只在桌面体现）。顺带复查上一批改动手机端：AI 生成面板的合集选择已改为「语言下拉 + 合集下拉(flex-1 min-w-0) + 『＋ 新建』按钮(shrink-0)」纵向堆叠、`showNewCol` 输入框单独一行，窄屏不挤压；`material-watch` 操作区/提取行均 `flex-wrap`、嵌套文件夹缩进与「⋯」菜单均为 `right-0` 定位不越界——均无功能板块或按钮挤压问题，无需再改。
- 2026-08-23 **AI 生成素材面板补「新建合集」按钮 + 笔记左侧列表跟随选中文件夹**（用户两点，tsc 通过）。① **AI 生成面板可新建合集**：`ai-generate-panel` 的合集选择原来是「下拉 + 一个不起眼的『或新建一个合集』文本输入」，且从合集详情打开（`defaultCollectionId` 有值）时整个选择器被换成「归入合集「X」」静态文字、根本没法新建。现改成**始终显示「合集下拉 + 右侧『＋ 新建』按钮」**（点「新建」展开输入框），不再因 `defaultCollectionId` 隐藏选择器（从合集详情打开也默认选该合集、但可改或新建）；`generate()` 的建合集逻辑从「没选合集才用新名」改成「**填了新名就新建**（覆盖下拉里已选的）」，避免选了合集又填了新名时新名被无视。② **笔记左侧列表跟着文件夹走**：`notes-workspace` 之前点开某篇笔记（`isNote`）后，左侧笔记列表无条件切回 `noteMatches`（全部笔记、忽略文件夹）；现删掉 `noteMatches`，列表恒用 `filtered`（跟随 `selectedId`）——从某个文件夹点进笔记，左侧仍只显示该文件夹（含子文件夹）下的笔记，不再跳回「全部笔记」；标题/「文件夹标签」/「新建笔记归处」也随之跟随 `selectedId`（标题显示文件夹名、仅「全部」时给笔记挂文件夹名、新建笔记归入选中文件夹）。
- 2026-08-23 **笔记文件夹支持嵌套（大文件夹 → 子文件夹 → 笔记，任意层级）**（tsc 通过）。schema 的 `folders.parent_id`（on delete cascade）与 `notes.folder_id`（on delete set null）本就存在，本次只做 UI 层。① **共享树工具 `src/lib/folders.ts`**：`folderTree(folders)`（roots + children Map）、`flattenFolderTree(folders)`（先序遍历压平成 `{folder, depth}` 供按缩进渲染）、`descendantFolderIds(folders, id)`（某文件夹 + 全部后代的 id 集）、`folderNoteTotals(folders, direct)`（每个文件夹「含子文件夹」的笔记总数）。② **新建子文件夹**：桌面侧栏（notes-workspace）与手机（notes-browser）两处 `createFolder` 都改插 `parent_id`（新增 `newFolderParentId` state）；文件夹「⋯」菜单加「新建子文件夹」项（先设父级再展开输入框，placeholder 随之为「子文件夹名」），顶栏「新建文件夹」始终重置父级为 null。③ **缩进树渲染**：桌面侧栏与手机「文件夹」tab 都改用 `flattenFolderTree` 按深度缩进（`marginLeft: depth*12/16`）——不再平铺；手机搜索命中时平铺显示（depth 归 0）不保留缩进。④ **选中父文件夹包含子文件夹笔记**：notes-workspace 的 `filtered` 用 `descendantFolderIds` 过滤，选大文件夹能看到其下所有子文件夹里的笔记；侧栏计数改 `folderNoteTotals`（父文件夹显示总篇数、不再 0）。⑤ **笔记「移动」菜单缩进**：`NoteList` 的「移动到」列表用 `flattenFolderTree` 缩进显示、前面加文件夹小图标。⑥ **手机文件夹详情页列子文件夹**：`mobile-folder-view` 顶部加「子文件夹」列表（点子文件夹继续下钻），返回链接有父级时回「上一级」、否则回「返回笔记」。⑦ **删除提示**：两处 `removeFolder` 确认文案改为「它的子文件夹会一起删除，里面的笔记都会保留（变成无文件夹）」。⚠️ 旧库无需迁移（parent_id 列已存在）；删除父文件夹会级联删子文件夹（子文件夹内笔记 folder_id 置 null 保留）。
- 2026-08-23 **素材库四个顶栏按钮改弹窗 + 文章原文采集区去多余白框 + 复习页合集选择器随闪卡页同步删除**（用户三点，tsc 通过）。① **文章采集区去白框**：`material-watch` 的文章(link)分支原来包在外层「观看区」`rounded-2xl border bg-white` 白框里、再套一层 `p-5`，与本就自带边框的 textarea 重复；现把 link 分支提到白框之外单独渲染（只留 `mb-4`、无外框），白框只包 generated/file/podcast/视频 四种。② **四个顶栏按钮改弹窗**：新建合集（内联表单）、收藏素材(`AddMaterialPanel`)、上传文件(`FileUploadPanel`)、AI 生成(`AiGeneratePanel`) 全部从内联面板改成居中遮罩弹窗（`fixed inset-0 z-50 bg-black/40` 点背景关闭 + 内层 `max-h-[85vh] max-w-lg overflow-y-auto rounded-2xl bg-white p-5 shadow-xl` `stopPropagation`）；三个 Panel 根节点原地从 `card-soft mb-5 p-4` 改为弹窗外层+内层，`collection-detail` 里同款面板一并受益。③ **复习页「选择合集」随闪卡页同步**：`review-home` 抽出 `defaultCollection` 兜底函数，新增挂载 `router.refresh()` 刷新一次（击穿客户端路由缓存、避免在闪卡页删合集后返回仍显示旧列表）+ 一个 `useEffect` 监听 `collections` 变化——若「正在背」的合集已不在列表里，回退到上次背过的 / 默认合集，不再指向已删合集。
- 2026-08-23 **文章原文采集区加「保存」按钮**（用户：手动改动怕丢，要显式保存；tsc 通过）。`material-watch` 文章分支在正文 textarea 下方右侧加一个「保存」按钮（`Save` 图标 + 文字）——点击 `saveContent()` 立即把 `articleTextRef.current` 写回 `materials.content`（不等 800ms debounce），成功短暂显示绿色「已保存」、失败落进顶部 `error`；新增 `saveBusy`/`saved` 两个 state。原有的 debounce 静默保存 + 离开页面尽力保存仍保留（双保险）。
- 2026-08-23 **素材观看页「打开原文/音频」恢复成箭头+文字链接、放内容框上方右上角（去掉按钮与白背景）**（用户反馈，tsc 通过）。① 打开入口从「圆角边框按钮」改回最初的「箭头 ExternalLink + 文字」纯链接形式（`text-xs text-zinc-500 hover:text-teal-600`，无边框无背景），放在内容框（观看区）上方、右对齐（`mb-1 flex justify-end`）——即最初「在原始页面打开」外链的样式，只是从右下角挪到右上角；② 视频/音乐黑色板块只留 iframe（去掉了上一轮塞进黑板块/白底头部条的按钮），文章分支删掉顶部那段灰色解释文字、只留「自动提取正文」按钮 + 文本框；③ 播客「打开音频」也从单集列表内部移到这条上方的统一链接行。条件仍为 视频(youtube)/文章(link)→「打开原文」、播客(podcast)→「打开音频」。
- 2026-08-23 **素材观看页按钮位置三调：视频「打开原文」移出黑色板块、文章「提取正文」与「打开原文」同一行**（用户反馈，tsc 通过）。① 视频/音乐：黑色板块只保留 iframe（`aspect-video` 只适应视频画幅），「打开原文」按钮移到黑色板块上方的白底头部条（`px-3 py-1.5` 右对齐），不再塞进黑板块里；② 文章：「自动提取正文」按钮与「打开原文」合并到同一行（`justify-between`，左提取、右打开原文），textarea 紧跟其下 `mt-3`——消除上一轮两者分两行导致「提取正文」被顶上去的问题。
- 2026-08-23 **素材观看页「打开原文/音频」按钮位置再修正：视频放画面上方、文章贴文本框右上角**（用户截图反馈，tsc 通过）。上一轮把按钮绝对定位浮在内容框右上角，导致视频里按钮盖在画面上、文章里不贴文本框。改为各分支内就地放置：① 视频/音乐(embed)在 iframe 上方加一条 `bg-zinc-900` 头部条、右侧「打开原文」（不再浮进画面）；② 文章(link)在 textarea 上方右对齐放「打开原文」、`mt-1.5` 紧贴文本框；③ 播客(podcast)在单集列表顶部说明行右侧放「打开音频」（指向订阅页）；④ 内容框去掉 `relative`、删掉那个跨分支的绝对定位按钮。
- 2026-08-23 **素材观看页「打开原文/音频」按钮贴内容框右上角 + 素材库顶栏清爽化（批量操作挪 Tab 行、次要操作收「⋯」菜单）**（用户两点，tsc 通过）。① **打开按钮位置修正**：`material-watch` 删掉标题栏标签区的「打开原文」按钮（上一轮误放标签区），改在内容框（观看区）右上角 `absolute right-2 top-2` 浮一个「打开原文/打开音频」小按钮——视频(youtube)/文章(link)显示「打开原文」、播客(podcast)显示「打开音频」（音频 audio 的「原始链接」按钮本就在播放器右上角、不重复，spotify/file/generated 不显示）；标题栏恢复只留语言/类型/状态三个徽章。② **素材库顶栏不冗长**：`materials-view` 把顶栏「批量操作/新建合集/上传文件/AI 生成」四个次要按钮收进一个「⋯」下拉菜单（桌面+移动统一，删掉原来桌面平铺一行 + 移动「＋」两套并存），顶栏只留「收藏素材」主操作 + 「⋯」两个；「批量操作」入口改放到 Tab+筛选行、筛选按钮左侧一个 `text-xs text-zinc-400 hover:text-teal-600` 低调小文字（batchMode 时 teal 高亮），批量操作栏（全选/移到合集/删除选中）仍在该行下方展开。
- 2026-08-23 **素材观看页「打开音频」去重 + 按主题卡片可「删除分类」+ 「新建分类」改成居中弹窗**（用户三点，tsc 通过）。① **音频按钮去重**：`material-watch` 顶栏标题右侧的外链按钮原按「非 generated 且非 file」显示「打开音频/打开原文」，现收紧为**只有 `material.type === "link"`（网页文章）才显示**、文案固定「打开原文」——音频的「打开原始链接」按钮本就在采集区/播放器右上角（`PodcastAudioPlayer` 的 `openHref`），不再在顶栏标签区重复出现。② **按主题卡片支持真正「删除」**：`group-browser` 的 `ThemeCardMenu` 菜单项统一成「删除分类」（不再是「隐藏」），内置主题 → 加入 `hidden_themes`（可再恢复）、自定义主题 → 先清空其下 `cards.theme` 再删 `word_themes`（词回到「未分类」不被删）；删除确认文案改为「删除分类「label」？它会在闪卡页里消失，里面的词回到「未分类」，词不会被删除。」。③ **新建分类改弹窗**：`NewThemeForm` 从内联 `card-soft` 卡片改成居中遮罩弹窗（`fixed inset-0 z-50 bg-black/40` 点背景关闭 + 内层 `max-h-[85vh] max-w-lg overflow-y-auto` 白面板 `stopPropagation`），标题改 `text-base text-zinc-900`、关闭钮改 `p-1`/`h-5 w-5`，点「新建分类」按钮弹窗紧贴按钮出现、不再隔着整块区域离得太远。
- 2026-08-23 **闪卡两视图对齐：按主题卡片加「⋯」菜单+导出；按来源加批量操作；批量入口移到语言标签行右侧低调位**（用户三点：①按主题卡片也跟按来源一样一个隐藏菜单一个导出；②按来源也支持批量操作；③两个 tab 的批量操作换不显眼位置；tsc 通过）。① **按主题卡片补底部操作区**：`group-browser.tsx` 主题卡片底部加 `border-t` 操作区——左「⋯」菜单（内置主题「隐藏分类」、自定义分类「删除分类」，新增 `deleteTheme` + `ThemeCardMenu` 组件）、右「导出」（新增 `ThemeExportButton`，直接导出内存中该主题已分组卡为 CSV，`exportCardsCsv` 复用）；整卡点击/链接不变，批量模式下操作区隐藏。② **按来源加批量操作**：`cards-view.tsx` 新增 `batchMode`/`selected`（按 noteId）与 `toggleSelect/toggleSelectAll/toggleBatch/batchDelete`；批量模式把合集卡片整卡改成可勾选 button（`CollectionCard` 加 `batchMode/selected/onToggle`，主体抽成 `body` 复用），独立闪卡块批量时隐藏；批量删除复用单卡删除语义（闪卡文件连笔记删、普通笔记只删卡正文保留）。③ **批量入口低调化**：两个视图把工具栏里的「批量操作」按钮去掉，改成语言标签行最右侧一个 `ml-auto text-xs text-zinc-400` 的小文字入口（GroupBrowser 原工具栏按钮删除、CardsView 新增入口同款），批量操作栏（全选/删除所选/完成）仍在语言标签行下方出现。
- 2026-08-23 **一级导航收起后不再随离开笔记区自动展开**（用户：点笔记自动缩回一级导航后，换到别的页面也不用再返回，可手动展示导航栏；tsc 通过）。`app-shell.tsx` 的 `useEffect` 从 `setCollapsed(isNotes)` 改为 `if (isNotes) setCollapsed(true)`——进入笔记区仍自动收起，离开后保持收起状态、不再强制恢复展开；用户点侧栏顶部「»」手动展开（该按钮本就有）。
- 2026-08-23 **按主题视图的语言分类样式与按来源对齐：非活跃按各自语言色显示**（用户：按主题下面的语言分类样式也跟按来源一样、不同语言不同颜色；tsc 通过）。`group-browser.tsx` 语言 chips 原先是「选中态才显示语言色、非活跃灰边框」，现改成与 `cards-view.tsx` 一致——**非活跃 chip 按各自语言色显示**（`LANG_COLOR[l] border-transparent`）、选中态统一 `bg-teal-600 text-white`；「全部语言」两处本已一致未动。
- 2026-08-23 **闪卡页「按来源/按主题」切换改回标题下方左对齐**（用户：tab 不该跟标题同行、要像素材页一样放左侧；tsc 通过）。`cards/page.tsx` 页头只留 `<h1>闪卡</h1>` 一行，`ViewSwitch`（按来源/按主题）单独一行、`mb-5` 左对齐放在标题下方——与素材页 Tab（单条素材/合集）同侧同位。撤销上一轮「标题 + 视图切换同行 `justify-between`」的排法。搜索框+操作按钮（CardsView/GroupBrowser 内 `sm:flex-row`，手机端上下堆叠）与语言 chips 布局未动。
- 2026-08-23 **素材页布局再压缩：标题与工具栏同行 + Tab 与筛选同行**（用户反馈「标题跟下面的筛选空格很大」，tsc 通过）。① **标题并入工具栏行**：`materials/page.tsx` 删掉独立 header（只留错误提示 + MaterialsView），把 `<h1>素材</h1>` 挪进 `materials-view` 顶部、与操作按钮（批量/新建合集/上传/AI/收藏素材）同一行 `justify-between`——标题左、按钮右，消除原来「标题一行 + 工具栏右对齐一行」两行间的空白。② **Tab 与筛选同行**：原来「单条素材/合集」Tab 切换单独占一行 `mb-4`，现并入筛选卡片 `justify-between` 同行——左 Tab、右「筛选」按钮（激活标签/清空跟随）；展开的筛选面板与批量栏仍留在卡片内 Tab 行下方。桌面端由「标题/工具栏/筛选/Tab」四行压成「标题+按钮、Tab+筛选」两行，手机端主要内容大幅上移。
- 2026-08-23 **素材库/闪卡页清爽化：删冗余文字 + 素材页删搜索框 + 原文入口挪右上角**（用户觉得素材库设计冗杂、要清爽、手机端主要内容占最多；tsc 通过，build 仍因离线拉不到 Google Fonts 失败、与改动无关）。① **「在原始页面打开」挪到标题栏右上角**：`material-watch` 原来观看区下方右下角的「在原始页面打开/打开音频」外链，挪到标题栏右侧三个徽章旁，做成同款圆角「打开原文/打开音频」小按钮（`ExternalLink`），点新标签直接看原文；删掉下方那行独立的 `mb-4 -mt-2 flex justify-end` 块。② **删素材页计数**：`materials-view` 工具栏左侧「0 个合集 · N 条单条 · 共 N 条素材」删掉，外层 `justify-between` 改 `justify-end`（按钮右对齐）。③ **删两处标题解释文字**：素材页（`materials/page.tsx`）「粘贴链接收藏视频/音频/播客/文章，导入笔记精读转卡。」与闪卡页（`cards/page.tsx`）「按来源或按主题浏览、管理你的闪卡。」均删，页头只留标题。④ **素材页删搜索框**（用户定）：`materials-view` 删「搜索合集名/素材标题/来源/链接」输入框与 `Search` 图标、`query` state、`q` 变量及 visibleCols/visibleMats 里的 `q` 过滤——素材是收件箱中转站、数量少、语言/类型/状态筛选已够，删了省一整块空间。⑤ **闪卡页搜索框保留**（用户定）：闪卡是长期积累的核心资产、找卡是刚需，cards-view/group-browser 的搜索框不动。
- 2026-08-23 **素材库：文章改为大屏原文采集 + 去掉内嵌兜底提示 + 筛选项只列实际出现的值**（用户三点：文章预览进去还是窄 iframe、应占更大屏幕更好预览；「若此页显示拒绝连接」那行兜底文字占屏幕、删掉；筛选「有的再出现、没有的别多预设」——tsc 通过；`npm run build` 因离线拉不到 Google Fonts（Geist）失败、与本次改动无关）。① **文章改为大屏采集**：`material-watch` 的 `link` 分支去掉「站内预览 iframe ↔ 原文采集」切换，改为**点进文章直接进原文采集区**——删掉 `captureOpen` state 及其 iframe 预览、切换按钮，文本框从 `rows={10}` 加大到 `min-h-[55vh]` 占满大半屏高，可「自动提取正文」或直接粘贴；「导入到笔记」逻辑不变（无正文仍禁用、提示先采集）。② **删掉兜底提示**：iframe 预览下方那行「若此页显示拒绝连接或空白，是网站禁止内嵌——点上原文采集粘贴正文即可」随预览一并移除（文章不再内嵌，提示失去意义）。③ **筛选项动态化**：`materials-view` 新增三个 `useMemo` 聚合实际出现过的语言/类型/状态——单条按 `materialLang(m)`/`m.type`/`m.status`，合集按自己定义的 `lang`/`type` 加 `colMeta` 里素材聚合的 `langs`/`types`/`statuses`；`LangChips`/`TypeChips`/`StatusChips` 只渲染聚合集合里存在的值（语言沿 `LANG_ORDER`、类型沿 `MATERIAL_TYPE_LABEL`、状态沿 pending/imported 顺序），不再预置空的；「全部语言/类型/状态」清空按钮保留。
- 2026-08-23 **手机端四处 UI 修复 + 网页端进入笔记区一级导航自动收起**（用户：「笔记/复习页往下滑标题跟着动、笔记页顶部有空隙、复习页三个按钮挤一行、笔记编辑页底部导航挡编辑；网页端进入笔记区一级导航能自动缩进去」——tsc + build 通过）。① **标题吸顶**：笔记列表页（`notes/page.tsx` 手机块）与复习主页（`review-home.tsx` 头部）都改成 `sticky top-[max(1rem,env(safe-area-inset-top))]`——吸顶偏移与全局 `main` 的顶部安全区一致，滚下来标题钉在状态栏下方、不再跟内容一起走；桌面端（`md:`）复位回普通头部。② **笔记页顶部空隙**：手机块去掉外层 `py-6`，标题改吸顶头（自带 `px-4 py-3` + 白底 backdrop-blur），正文挪进 `px-4 py-4` 容器，消除 24px 多余空隙。③ **复习三按钮适配手机**：`review-home` 的「测试/完形填空/故事」网格 `grid-cols-3` → `grid-cols-1 sm:grid-cols-3`，手机端竖排全宽、不再挤一行，`sm+` 仍三列。④ **笔记编辑页隐藏底部导航**：`app-shell` 底部 `<nav>` 用 `!isNoteDetail` 包裹（编辑页顶部已有返回键，不再叠底部导航），并给 `main` 在 `isNoteDetail` 时 `pb-0`（去掉原来留给底部导航的 5rem 空位）、`note-editor` 右下角悬浮「闪卡/转成闪卡」按钮从 `bottom-20` 下调到 `bottom-[calc(1rem+env(safe-area-inset-bottom))]`。⑤ **网页端进入笔记区自动收起一级导航**：`app-shell` 加 `useEffect(() => setCollapsed(isNotes), [isNotes])`——进入 `/notes`、`/notes/[id]`、`/notes/folder/[id]` 时左侧一级导航自动收成 56px 窄条（只留图标），宽度让给笔记三栏（文件夹/列表/正文），离开笔记区自动恢复展开。
- 2026-08-23 **为部署到 Vercel 做准备：给「依赖本机 yt-dlp」的三项功能加功能开关，禁用时灰化 + 提示（tsc + build 通过）**。三项：YouTube 抓字幕、无字幕「听声转录」、合辑一键导入——都靠 `python3 -m yt_dlp` + 本机 Chrome 登录态 + 临时文件，函数即服务（Vercel）给不了。方案：① 新增 `src/lib/feature-flags.ts`：`YTDLP_DISABLED = NEXT_PUBLIC_DISABLE_YTDLP === "1"`（带前缀，客户端构建时内联、服务端按环境变量读，两端一致）。② **路由兜底**：`/api/ai/transcribe` 的 YouTube 分支、`/api/materials/playlist` 的 POST 开头，`YTDLP_DISABLED` 时返回 `503 {"error":"此功能暂未启用"}`（任何散调都优雅降级，不白屏）。③ **UI 灰化**：`media-embed-nodeview` 的「生成文字稿」按钮在 `kind==="youtube"` 时 `disabled` + `cursor-not-allowed` + `opacity-40` + `title/aria`「此功能暂未启用」（**音频直链转录走云 STT，不受影响，仍可点**）；`add-material-panel` 贴到纯合辑链接时直接提示不调端点；`ai-assistant-panel` 的「链接」抓文字稿对 YouTube 链接同样拦截。后期买服务器（真实 Node + yt-dlp）**不设这个变量即可自动恢复，代码零改动**。
- 2026-08-22 **手机端顶栏整合：拿掉移动端全局顶栏，设置页顶部加「账号」卡片（头像+邮箱+退出登录）**（用户：「手机端最顶部 logo 和设置能不能挪到设置页，加一个头像登录的样式」——tsc + build 通过）。移动端底部导航本就含「设置」标签，顶部再放一个设置按钮属重复；logo 只剩品牌装饰、意义不大，故**整条 `md:hidden` 全局顶栏移除**，而非只搬两个元素。① **app-shell**：删移动端顶部 `<header>`；主内容区移动端顶部改由 `main` 自身 `pt-[max(1rem,env(safe-area-inset-top))]` 撑开状态栏安全区（单篇笔记页豁免——note-editor 已有自带头部带 safe-area 顶边距，双叠会加出空隙）。② **settings/page**：顶部加账号卡片——圆形头像（取邮箱首字母）+ 邮箱 + 「语巢 · Nestlingo」，卡片内底部放退出登录按钮（顺带把原来独立放在页面最底部的 `LogoutButton` 收进卡片、删除那一段）；`settings/page` 顶部 `getSession` 取 `userEmail`。桌面端侧栏布局不动。tsc + build 通过。
- 2026-08-22 **自检 DEbug 全量修复（5 个子代理并行审计 → 合并去重优先级清单 → 分批修复，tsc + build 通过）**。按严重度：
  - **高（数据/安全）** ① **删合集误删其内素材**：`materials.collection_id` 原 `on delete cascade`，与表注释/UI「素材退到单条」矛盾 → schema 改为 `on delete set null`（新装走 create table；已有库补幂等 `drop constraint if exists materials_collection_id_fkey` + 重建为 set null）。② **素材存储桶缺 UPDATE/DELETE 策略**：用户改/删自己上传文件会被拒 → 补 `materials_bucket_update` / `materials_bucket_delete` 两条 policy（桶保持 public 读：上传 URL 被 `<img>/<audio>` 直接引用、路径含 uid+随机uuid 难猜，改私有会破坏自身展示；长期可换签名 URL）。③ **SSRF**：`extract/meta/transcribe/rss` 服务端追随任意用户 URL 抓取无防护 → 在唯一出网交汇点 `serverFetch` 加 `assertSafeUrl` 守卫，只放行 http(s)、拦截 环回(127/::1)/RFC1918(10/172.16-31/192.168)/链路本地·云元数据(169.254.*)/0.*/.local；四个路由的 serverFetch 均在 try/catch 内，守卫抛错会落成友好 JSON。
  - **中（正确性/鉴权/健壮性）** ④ 复习顶层 `stats.due` 漏计无 review_state 的卡、与合集/`listReviewCards` 口径不一 → 无状态卡也计 due。⑤ 删除自定义主题后卡片仍挂已删 theme 出「裸 UUID」鬼分组 → `group-browser.batchDelete` 与 `theme-detail.deleteTheme` 删除前先把该主题下 `cards.theme` 置 null。⑥ `note-cards.deleteSelected` 会删「被搜索隐藏但仍选中」的卡 → 只删「可见 ∧ 选中」。⑦ `cards-view` 独立卡「重命名/删除」作用于**所有**孤儿卡、不顾当前语言/搜索过滤 → `OrphanMenu` 改收可见孤儿 id 数组，只作用可见子集。⑧ `transcribe` 对「被 YouTube 挡/代理/超 25MB/无音轨」返回 404 → 改 502。⑨ `card-explain` / `cloze-examples` 缺 `getUser()` 重校验（全靠 proxy 本地 JWT 解码）→ 补 `createClient().auth.getUser()` 401 门禁。⑩ `add-material-panel` 解析后改 URL 会拿旧 `meta.audioUrl` 保存 → URL 一改即清空 meta/pl 强制重解析；且 `&list=` 单视频被当作整个播放列表 → 只有「纯合辑链（无视频 id）」才走合辑端点。⑪ 上游 provider 调用无超时 → ai-client 4 处 + transcribe.ts 加 `AbortSignal.timeout(60_000)`；OCR 的 base64 限 ≤14MB、transcribe-audio/file 在读内存前先按 `file.size > 25MB` 拦截。
  - **移动端** ⑫ `viewport-fit:cover` 但底栏/顶栏无 `env(safe-area-inset-*)` → 底部导航 `pb-[env(safe-area-inset-bottom)]`、顶栏 `pt-[max(0.75rem,env(safe-area-inset-top))]`、main 底部 `pb-[calc(5rem+env(safe-area-inset-bottom))]`；单篇笔记页（自带返回/保存头部）隐藏全局顶栏消除两 sticky 头重叠，并给笔记头补 safe-area 顶边距（顺带解决其下编辑器工具栏 `top-12` 偏移被全局顶栏顶偏的问题）；`import-material` / `edit-material` / `edit-collection` 三个弹窗加 `max-h-[85vh] overflow-y-auto`，短屏可滚。
  - ⚠️ **需人工落地**：a) 跑一次 schema（`supabase db push` 或重跑 `schema.sql`）应用 ① 的 FK 迁移与 ② 的两条存储桶 policy（脚本幂等可重复）。b) 端到端点一遍本轮修复关键路径（删合集素材保留、改/删自己上传文件、贴内网 URL 被拒、复习首页与各合集 due 一致、删主题后按主题视图无裸UUID、搜索过滤下删卡/独立卡重命名只作用可见、`&list=` 视频只存单条、解析后改 URL 需重解析）。tsc + build 通过。
- 2026-08-22 **修「删笔记后出现404」真正根因：桌面端边栏列表删笔记时，当前路由仍是已删笔记自己的页**（用户澄清：没改内容，只是打开了笔记然后在列表里点删除，删完应回初始笔记页。此前把根因误判成 autosave 刷新撞已删笔记，已更正）。真正根因：桌面端「文件夹 + 笔记列表 + 正文」是三栏布局，边栏列表的删除按钮在 `NoteList.removeNote`（`notes-browser`）里只做 `router.refresh()`；当你**正打开着这篇笔记**（当前 URL = `/notes/{id}`）时从边栏删它，`router.refresh()` 重拉的就是这条 `/{notes/{id}}` → 笔记已删、`getNote` 返回 null → `notFound()` → 404。修法：`removeNote` 删除后用 `usePathname` 判断——若当前正停在 `/notes/{note.id}` 就先 `router.replace("/notes")`（跳回笔记首页，列表一并刷新）；否则（已在 `/notes`）才原地 `router.refresh()`。`note-editor` 的 `deleteNote` 本就走 `router.replace("/notes")`，并额外在删除开头清掉挂起的 1s autosave timer + `dirtyRef`（防「刚改过就删」时定时器对已删笔记再写/再刷新），保留不动。tsc 通过。
- 2026-08-22 **修素材库正文文本框改完不保存**（用户：提取完的正文是可编辑文本框，减删后换页再回来又回到原文本）。根因：`material-watch` 只在点「自动提取正文」时才 `update materials.content`，文本框手动改动从不落库，离开页面后重新打开 `articleText` 从 `initial.content` 重新初始化 → 改动丢失。修法：改动静默写回 `materials.content`——`onChange` 触发 800ms debounce 的 `scheduleContentSave`（读 `articleTextRef` 最新值），另加一个卸载 `useEffect`：离开页面时 debounce 未触发的话尽力存一次当前正文（与 `initial.content` 不同才写）。「导入到笔记」用的 `articleText` 仍是实时值，不受影响。tsc + build 通过。
- 2026-08-22 **笔记返回键固定回「笔记」主页 + 恢复顶部「来自素材」绿chip（支持多素材）**（用户两点，tsc + build 通过）。① **返回键按笔记自己的归处**：笔记从素材库导入进来后，点返回箭头会走浏览器后退（`router.back()`）回到素材页，不符合预期。`BackButton` 新增 `forceFallback` 开关——`true` 时永远 `router.push(fallback)` 不看历史；`note-editor` 的返回箭头传 `forceFallback`，始终回 `/notes`（backHref）。原「从 A 点进背诵 → 返回不绕圈」的历史回退逻辑其它调用方不受影响（默认 `forceFallback=false`）。② **恢复顶部「来自素材」绿chip**：上一轮删掉了，但用户意识到**一篇笔记可导入多条素材**——只用原文块里的「来源」不够：视频/音频/播客这些**媒体导入没有原文块、就没有来源标识**，只有顶部 chip 能溯源；且文章导入时 chip（→素材库那条记录 `/materials/{id}`）与原文块「来源」（→原始 URL）指向不同去处，各有价值。故恢复：`types.ts` 补回 `SourceMaterial`、`queries.ts` 补回 `listSourceMaterials`、`note-editor` 补回 `sourceMaterials` prop 与其渲染（多条素材 → 多个 chip 自动换行）、`notes/[id]` 补回查询与传参。⚠️ 上一轮"只剩原文块来源"已撤回，现为「顶部 chip（每素材一个，链素材库）+ 原文块来源（链原始 URL）」并存。tsc + build 通过。
- 2026-08-22 **笔记去掉顶部「来自素材」绿chip + 修「精读笔记后来源消失」 + 文章已存正文时直接进采集区**（用户三点，tsc + build 通过）。① **取消顶部绿色「来自素材」链接**：原文块里已有「来源」标识（回原始链），顶部再挂一个绿色 chip 属重复，干脆删掉——`note-editor` 去掉 `sourceMaterials` prop 与其渲染块（连同 `Link`/`ExternalLink` import），`notes/[id]` 不再 `listSourceMaterials`，`types.ts` 的 `SourceMaterial`、`queries.ts` 的 `listSourceMaterials` 一并清理（`detachMaterialsFromNote` 保留，删笔记解绑仍用）。⚠️ 唯一损失是「从笔记一键跳回素材库记录」那条路由，来源只剩原文块的「来源」→原始 URL；需要管理素材库里的那条记录可走素材库页。② **修「精读笔记后来源标识消失」**（bug）：`callout-nodeview` 点「精读笔记」会 `deleteRange(整个原文callout)` 再 `insertContentAt(AI 内容)`，而 `analysisToNoteContent` 重建「原文」块时**没传 source**，来源链接就丢了。修法：`analysisToNoteContent(a, transcript, source?)` 增加可选 `source`，重建的原文 callout 用 `callout("article", paras, source)` 带上它；`onAnalyze` 调用时把 `node.attrs.source` 传入。⚠️ 已触发过精读的那篇笔记里，来源已经在数据库里丢了，这只能让**下次**精读不再丢；已丢的要重新导入才有。③ **文章已存正文时别显示空白 iframe**：`material-watch` 的 `captureOpen` 初值从「仅微信」扩为「微信 **或** `materials.content` 非空」——正文已抓/已存（提取或导入过）就直接进「原文采集」区先看到已有正文，不再对着一个空白/拒绝内嵌的 iframe + 兜底提示。tsc + build 通过。
- 2026-08-22 **修素材库合集卡片：缩略图更清晰 + 「⋯」菜单贴近文字**（用户两点：预览图很模糊、底下隐藏菜单跟上面文字隔得有点远。tsc + build 通过）。① **缩略图更清晰**：合集卡 / 单条行两处 `<img>` 都换成共享 `MaterialThumb`——渲染时把 YouTube 的 `hqdefault.jpg`（480×360、压缩软，放大到大卡片就糊）统一**升级成 `maxresdefault.jpg`**（1280×720 高清）；个别视频没有 maxres（会返回 120×90 灰占位图或 404），用 `onLoad`（`naturalWidth <= 121` 判灰图）与 `onError` **自动回退到 `hqdefault`**，既修复库里已存的老记录（`ytMaxRes` 只改 src 不动库，属渲染层修复、无需迁移），又不出现升级后反而冒灰块的情况；无缩略图仍走原图标占位。② **菜单贴近文字**：合集卡底部 `⋯` 菜单原 `mt-3 + pt-2 + border-t`（约 20px 间距 + 分割线），收紧成 `mt-2`、去掉分割线，跟上方标签更近。tsc + build 通过。
- 2026-08-22 **素材库再收尾：手机端顶栏折叠成「＋」菜单 + 微信文章内嵌兜底 + 单条素材 tab 前移**（用户三点，tsc + build 通过）。① **手机端顶栏折叠成「＋」菜单**：`materials-view` 顶栏四个次要按钮（批量/新建合集/上传/AI）在移动端收进一个「＋」下拉菜单（`sm:hidden`，主操作「收藏素材」始终可见）；桌面端（sm+）仍平铺一行。菜单点外部自动收起、再点变「✕」关闭。② **微信文章内嵌兜底**：`material-watch` 的 `link` 分支——微信官方禁止被 iframe 内嵌（`X-Frame-Options`/CSP），内嵌必「拒绝连接」，故 `captureOpen` 默认值改为 `mp.weixin.qq.com` 命中即直接进「原文采集」（粘贴/提取正文），不再空撞一次死链接；并在站内预览 iframe 底部叠一句兜底提示「若显示拒绝连接或空白，是网站禁止内嵌——点原文采集粘贴正文即可」，覆盖其它无法内嵌的网页。③ **单条素材 tab 前移**：顶部 Tab 顺序改成「单条素材 | 合集」，默认也落在单条素材。tsc + build 通过。
- 2026-08-22 **素材库详情页/主页布局重排 + 修复行部下拉超出屏幕 + 筛选折叠成下拉（手机端适配）**（用户五点：①合集/单条最下方素材菜单弹出的菜单「跳出屏幕」；②筛选导航语言/类型/状态要不要做成一个筛选符号口点开下拉；③搜索框若能同时搜合集和单条能不能放 tab 上方；④顶部导航按钮点开的弹窗出现在 tab 下面、逻辑不对；⑤整个页面布局怎么优化更合理、保证手机端适应——全部落地，tsc + build 通过）。① **修行部下拉越界**：`RowMenu` 菜单改**portal 挂到 body + fixed 定位**，打开时量触发钮/菜单尺寸、把菜单**夹在视口内**——底部越界自动向上翻、左右贴边防越界；并且用 portal **逃出合集卡的 `overflow-hidden`**（此前正是被卡裁掉才显得「跳出/被切」），滚/缩窗口时跟着按钮走不悬空。② **筛选折叠成「筛选」下拉**：语言/类型/状态三组 chips 收进一个「筛选 ▾」按钮（带已激活数量角标），展开才显示；已激活项以**可清除的彩色小标签**直接排在按钮旁边（点了就单独清掉），另有「清空」。③ **搜索框移到 tab 上方**：本就同时过滤合集与单条，现置于顶部共享，与筛选同卡片。④ **面板挪回 tabs 之上**：新建合集 / 收藏素材 / 上传 / AI 生成四个面板从「tab 之后」上移到**顶栏按钮正下方（tab 之前）**，符合「紧贴按钮」初衷。⑤ **整体布局 + 手机端**：新顺序 = 工具栏 → 面板 → 搜索+筛选 → tab → 列表；顶部工具栏按钮手机端排成 **2 列网格**（sm+ 仍为一行 flex），避免纵向铺成长条；顺带清掉未用的 `Pencil` import。tsc + build 通过。
- 2026-08-22 **素材库再优化（防重复收藏 + 笔记回标来源）**（用户在「素材库还有什么需要优化的？」里多选落地这两条，tsc + build 通过）。① **防重复收藏**：`add-material-panel` 保存前用「稳定键」去重——单集播客/音频用解析出的音频直链、YouTube 归一成唯一 videoId 的 watch 链、Spotify 去 query 只留 origin+path、其它原样 trim；库里已有同一条（`url` 撞库）就琥珀色提示「已收藏过「标题」」并停止、不新增、也不会为它新建合集；保存时 `url` 就用这个稳定键（同视频不同贴法日后也归一到同串，链接直接复用 `material.url` 打开/导入不受影响）。② **笔记回标来源**：从素材导入的笔记，打开时在标题上方显示一个「来自素材：title ↗」小chip，点击跳回 `/materials/{id}`；多素材导入同一篇会在顶部列出多个来源。实现为**反向查询**（`materials.note_id → 该笔记`），无需改 schema、也天然支持多来源——新增 `SourceMaterial` 类型 + `listSourceMaterials(noteId)`（queries.ts，失败返回空数组不打断打开笔记），`notes/[id]` server 页取好传给 `NoteEditor`（多一个非必填 prop，渲染在标题上方一行小chip，`ExternalLink` 图标）。⚠️ 反向回标只在 `materials.note_id` 仍指向该笔记时生效，与上一轮「删笔记解绑素材」配合，故删除笔记后不会出现指向已删素材的残迹。
- 2026-08-22 **YouTube「无字幕 → 听声转录」回退落地（`/api/ai/transcribe`）**（用户：「原文段分段发音/发音方案先不做，把无字幕视频听声转录先做了」——之前对没有字幕轨的视频只能报「没能抓到字幕」，现改为自动切到「听声转录」：yt-dlp 下该视频纯音频（`-f bestaudio`，原生格式不转码、免 ffmpeg）→ 交共享 `transcribeAudio`（Groq/OpenAI Whisper）转文字稿。`route.ts`：原 `fetchYouTubeTranscript` 拆成 ① `fetchYouTubeSubtitles`——有字幕（含自动）返回文字，**操作成功但真没字幕 → 返回 `null`**（不再抛错），仅 yt-dlp 被拦/报错才抛中文提示；② 新增 `fetchYouTubeAudioTranscript`——下音频、>25MB 给友好错误（视频太长）、`transcribeAudio` 转写。POST 的 YouTube 分支改为「无字幕 → 听声转录」，成功响应带 `via:"audio"`。前端「生成文字稿」按钮无需改。⚠️ 依赖本机 yt-dlp 能过 YouTube 的 n-sig/po-token 挑战（带 Chrome 登录 cookie）；仍 403 则给中文错误。tsc 通过）。
- 2026-08-22 **修素材库撞 404：删除笔记后，指向它的素材解绑回「待处理」**（用户：「素材库导入的笔记，删除后好像会出现 404」）。根因：素材库导入会写 `materials.note_id`（FK `on delete set null`），但删笔记后 `status` 仍留在 `'imported'`；某些跳转留下一条指向已删笔记的 `/notes/{id}` 链接（尤其浏览器后退回素材观看页时，组件内存旧 `note_id` 还在），点到就 404。修法：新增 `detachMaterialsFromNote(noteId)`（`src/lib/supabase/client.ts`），**在删笔记之前**把 `materials` 里指向它的行 `update({ status:"pending", note_id:null })`——素材回退成「待处理 + 导入到笔记」，badge/按钮不再自相矛盾，任何重新抓取都指向不到已删笔记。接入三处：`note-editor.deleteNote`、`notes-browser.removeNote`、`cards-view.doDelete`（删「闪卡文件」笔记时）。tsc 通过）。
- 2026-08-22 **笔记内嵌音频补封面 + 播放器加「打开原始链接」 + 放弃笔记超链接（网页文章改「原文+来源」） + 笔记右上角「完成」改编辑/阅读模式切换**（用户四点：①播客转到笔记里封面怎么不显示了？②播客风组件要不要加个「打开原始链接音频」、手机端可直接打开原始 App？③算了放弃笔记超链接形式、网页文章就别导入链接了、还是原文粘贴加来源标识；④笔记右上角「完成」一点就跳出笔记、逻辑不对，应该是从编辑模式保存变成阅读模式）。① **封面补上**：`mediaEmbed` 节点加 `cover`/`original` 两个属性（默认 null）；`media-embed-nodeview` 把 `cover` 传给 `PodcastAudioPlayer`（此前没传，内嵌的播客单集/音频只能显示音乐图标占位）；`import-material-modal` 的 mediaEmbed 节点带上 `cover=material.thumbnail`——导入的播客单集 / 音频现在露出封面。② **播放器加「原始链接」**：`PodcastAudioPlayer` 加可选 `openHref`，在封面/标题右上方渲染一个「原始链接」外链小按钮；`media-embed-nodeview` 取 `attrs.original`，`material-watch` 的音频与文件音频用 `material.url`；mediaEmbed 的 `original` 导入时 = 素材 `url`（真实外链才带，AI 生成空链不带），手机上点它可直接跳去原始播客 App / 原文。③ **放弃笔记超链接（收回上一轮的粘贴转标题 + 工具栏「链接」按钮）**：`rich-text-editor` 移除超链接逻辑（`handlePaste` 转标题、`applyLink`、工具栏 `Link2` 按钮、`looksLikeUrl`/`fetchTitle`）；`editor-extensions` 的 `StarterKit` 回到默认（去掉 `link:{openOnClick:false}`）；删除 `/api/link-title` 路由。随之网页文章**不再导入成「标题+URL」链接**——`import-material-modal` 的 `link` 分支改为统一写「原文」callout + 来源标识（正文取 `material.content`，没有就标题占位）；`material-watch` 的「导入到笔记」在无正文时禁用（提示先「原文采集」粘贴/自动提取），去掉「导入为链接」。功能移除后不影响转成闪卡（`link` 本来就只是 mark，不进卡片面、不单独成卡）。④ **「完成」只保存、不跳离（阅读模式做了又撤掉）**：`note-editor` 的「完成」原 `await save(); router.push(backHref)` 跳出笔记，先改成「保存 + `generateHTML` 切只读阅读模式」，但**实测发现问题又撤了**——`generateHTML` 只把内嵌媒体渲染成静态占位（`▶ 视频 / 音频 / ♪ 音乐`），点「完成」后内嵌的播客/音频就没了；于是放弃阅读模式，改回「完成」=`await save()` 留在本页（不再 `router.push`），要离开用左上角返回。编辑器始终挂着、不因按钮切换而重挂，普通笔记/媒体内容都原样保留。tsc + build 通过，`/api/link-title` 已撤离路由表。
- 2026-08-22 **素材库把合集与单条素材改成顶部两个 Tab 分开 + 笔记超链接（粘贴转标题 + 工具栏链接按钮）**（用户：「合集永远置顶吗？合集变多了怎么办？要不要做 tab 页把单集和合集分开放？」选了「做（推荐）」；「笔记这块如有来源不放素材库能不能点开原链接，网页文章就不嵌入了，超链接能否像 Notion 一样粘贴链接转成可点击网页标题」，并确认不破坏转闪卡后采纳）。① **素材库 Tab 化（覆盖旧「合集+单条同排」）**：`materials-view` 顶部加「合集 | 单条素材」两块切换（各自带数量），**合集 tab 是响应式卡片网格**（缩略图/图标 + 名称 + 条数 + 合集/语言/类型徽章，编辑/删除收进卡片底部「⋯」）、**单条 tab 是列表**；搜索 + 语言/类型/状态筛选仍作用于**当前 tab**；批量操作的全选/全不选按当前 tab 的可见项走。合集再多也是网格、不挤占单条列表，每块各占整屏。② **笔记超链接（Notion 式）**：① 贴 URL 转可点击标题——`rich-text-editor` 的 `handlePaste` 拦截纯 URL 开头一次，先插入带 link mark 的 URL，再异步 `POST /api/link-title` 抓 `og:title` 把文字换回标题（失败回退成裸 URL 链接；`AbortSignal.timeout(12_000)`）；② 工具栏「链接」按钮——选中文字点它弹内联输入，自动补 `https://`，`setLink/unsetLink`，`editor.isActive("link")` 高亮；③ `StarterKit.configure({ link: { openOnClick: false } })`——编辑时点正文里的链接不跳走，Ctrl/⌘+点击才打开。**新路由 `/api/link-title`**（POST `{url}`，`serverFetch` + `extractOgMeta`，只抓标题不写库、无密钥下发、失败 502 前端回退裸链接）。**已确认不破坏转闪卡**：`link` 是 mark 不是新行——① 不会单独成卡（是内联标记）；② URL 从不进卡片面（`docToText` 去掉 marks 只留显示文字）；③ 独立「标题+链接」段不会被识别转卡（callout 外的裸段落 `bareToCard:false`）；④ 「原文」区整体跳过。唯一注意点：链接若放进 生词/例句/语法 卡片行内，其显示文字（标题）会并入该行内容，但无 URL、也不产生第二张卡。tsc + build 通过。
- 2026-08-22 **笔记内嵌音频也用播客风播放器 + 文章原文块带「来源」链接回原始 URL + 播放器抽成共享组件**（用户两个新问题：「既然播客风可行，嵌入笔记里的播客单集是不是也能支持？」「网页文章粘贴原文导入笔记后，能不能在笔记里标一个来源、点击看原始链接？你觉得哪个好？」并要整体逻辑再优化）。① **播放器抽共享组件**：把 `material-watch` 内联的 `PodcastAudioPlayer` 抽成独立 `src/components/podcast-audio-player.tsx`，素材观看页（媒体音频 / 文件音频）与**笔记内嵌音频**共用一套，观感一致——`media-embed-nodeview.tsx` 的 `<audio controls>`（原黑底原生控件）换成 `<PodcastAudioPlayer src title />`，笔记里内嵌的播客单集 / 音频直链也得到 封面 + 进度条 + 播放/暂停 + ±15/30s + 倍速 + 静音；`material-watch.tsx` 删掉本地定义、只 import（顺带清掉 `useRef/Pause/RotateCcw/RotateCw/Volume2/VolumeX` 冗余 import）。② **「来源」标识回原始链接**（采纳「点开看原始链接」而非「跳素材库」，理由：读原文一步到位，素材库观看页本就保留入口）：`callout` 节点加 `source` 属性（`data-source`，默认 null）；`callout-nodeview` 在「原文」标签旁渲染一个小「来源 ↗」（`ExternalLink` 图标，`target=_blank` 开原始链接）；`renderHTML`（分享图/闪卡）同步输出。`transcriptCallout(text, source?)` 与 `callout()` 支持传 source；`import-material-modal` 的 文章提取(`extra.articleText`) 与 旧 `type=generated` 两条在包「原文」callout 时把 `material.url` 传作来源。③ **整体逻辑闭环**：素材库(文章) → 导入 → 笔记「原文」块带「来源」点击开原始链接；素材库(音频/播客单集) → 导入 → 笔记内嵌走与观看页同一套播客播放器。⚠️ 旧笔记里已导入的「原文」块无 `source`，不显示「来源」，只有重新导入才有。tsc + build 通过。
- 2026-08-22 **音频/播客观看页换成播客风播放器（封面 + 进度 + 播放操作），去掉原生黑底 `<audio>`**（用户：播客的播放页面一个很丑的音频播放，能不能参考播客播放组建、展示封面加播放操作）。新增 `src/components/material-watch.tsx` 内联组件 **`PodcastAudioPlayer`**：封面（`material.thumbnail`，无则音乐图标占位）+ 标题/来源 + 可拖进度条（当前/总时长，渐进式音频到 `loadedmetadata` 才有总长，之前禁拖）+ 控制行——倍速（1 / 1.25 / 1.5 / 2 循环）+ 回退 15s + 播放/暂停（大圆钮）+ 快进 30s + 静音；深色 `bg-zinc-900` 底、zinc 控制色、teal 主钮。替换两处：① 媒体分支 `embedKind==="audio"`（单集播客 / 音频直链）；② `FileMaterial` 的 `file_kind==="audio"`（文件素材）。`<audio>` 保留为隐藏元素承接事件（play/pause/timeupdate/loadedmetadata/durationchange）。tsc + build 通过。
- 2026-08-22 **文章观看页再调：能内嵌的照样内嵌 + 「原文采集」改成切换按钮 + 去掉重复「打开原文」**（用户反馈：不是所有网页都不能嵌、能嵌的应该照旧嵌、只保留一个文本框按钮让我复制原文；且采集框右上角「打开原文」跟右下角「在原始页面打开」重复）。改法：`link` 默认**内嵌 iframe**（能嵌的网页照旧预览），观赏区顶部加一条切换栏「原文采集（粘贴正文）↔ 返回预览」——点「原文采集」才切到文本框（自动提取 + 粘贴），采集框**去掉重复的「打开原文」**（只用右下角那份）；「导入到笔记」按有无正文自适应：有正文→写「原文」块、无正文→「导入为链接」（标题+URL 段落，title 提示说明）。tsc + build 通过。
- 2026-08-22 **修 Apple「单集」链接导入失败：`?i=` 是 Apple 单集 id（trackId），不是 RSS 的 guid**（用户贴 `podcasts.apple.com/.../id751574016?i=1000782885945` 报「没找到这一集」）。根因：单集解析 `appleSingleEpisode` 原先解析 RSS 订阅源后按 `<guid>` 匹配 `?i=`，但多数订阅源根本不把这串数字写进 guid，匹配必失败。改用 **Apple lookup 的 `entity=podcastEpisode`**（公开无 Key）：`lookup?id=<节目id>&entity=podcastEpisode&limit=200` 直接列出该节目全部单集，`trackId` 即 `?i=` 的值，命中后取 `episodeUrl/ trackName/collectionName/artworkUrl600` 当 音频直链/标题/来源/封面；找不到（这集太早超出可查范围）给改友好的错误。顺带删掉 meta 路由里不再用的 `itunesFeedUrl/episodeAudio/xmlImage/xmlUrl/AUDIO_EXT`。实测该 URL 命中「Do You Ever Question Your Questions?」并取到直链 `<https://traffic.megaphone.fm/ALLE8170345651.mp3>`。tsc + build 通过。
- 2026-08-22 **观看页文章改成「原文采集区」：自动提取（失败撤按钮）或手动粘贴，导入才有意义**（用户反馈链：提取能不能选内容/没文本时导入是不是空洞/能不能自己贴正文再导入成原文块，并要「不同类型不同查看处理」）。`material-watch` 的 `link` 分支淘汰「内嵌原网页 iframe（X-Frame-Options 黑屏）+ 提取成功才显示正文」的旧逻辑，改成**原文采集区**：① 一个大文本框（初值取 `materials.content`，占位提示「把正文贴到这里…」）+「自动提取正文」按钮；② 自动提取**成功**填进文本框并 `update materials.content` 持久化（刷新/重进不丢），按钮变「重新提取正文」；**失败**撤掉该按钮（不再让用户反复点注定失败的提取）、只留粘贴盒、显示一次友好说明（可能要登录/付费/动态渲染）；③「导入到笔记」改用文本框内容作为 `articleText`，**无文本时禁用**（title 提示「请先粘贴正文或点自动提取」），不再产出「标题+链接」这种空洞结果；④ 顶部保留「打开原文」小链接。即「想只拿一段就只贴那段」＝手动粘贴取代了「选区域提取」这种重功能。其余类型（媒体/播客/文件/AI）本就各自走播放内嵌/原文块，未动。导出 `Globe` 图标冗余已清。tsc + build 通过（`/materials/[id]` 仍在路由表）。
- 2026-08-22 **素材库 Round 4：筛合集 + 单集播客只导单集 + 文章内嵌回退 + 合集标签 + 表单上移**（用户一批反馈：筛选也要筛合集、粘贴单集播客链接只导那一集/整订阅限上限/最新在前、网页文章无法内嵌别黑屏、合集支持类型/语言标签并对合集与单条做区分标识、顶栏按钮表单离得太远）。① **筛选也能筛合集**：`materials-view` 的 语言/类型/状态 三组筛选对合集行生效——合集自己定义的 `lang`/`type` 标签优先、没定义才按里面素材兜底（`colMeta` 聚合 + `collectionLang`/`collectionType` 取标签）。② **单集播客只导单集**：`/api/materials/meta` 检测到 Apple `podcasts.apple.com/...?i=<id>` 单集链时调 `appleSingleEpisode`（itunes lookup 拿 feed → 按 guid 定位那集）只解析出该集的音频直链/标题/来源/封面，`kind="audio"` + 响应加 `audioUrl`，收藏保存用 `audioUrl` 当素材地址，不再列整档；只有整个订阅/主页链接才走 `podcastMeta`（播客全集）。③ **订阅单集上限 + 最新在前**：`/api/rss` 每集取发布日期时间戳（pubDate/updated/dc:date），`sort` 最新在前 + `slice(0, 30)` 上限，响应加 `count`/`truncated`；观看页播客播放器显示「（共 N 集，只列最近 30 集）」。④ **文章内嵌回退**：`material-watch` 对 `type===link` 且未提取正文时**不再内嵌原网页 iframe**（会 X-Frame-Options 黑屏），改成站内提示「这个网页无法直接在站内预览」+ 「提取正文」「打开原文」两个按钮；提取出正文才渲染正文区。⑤ **合集标签 + 区分标识**：合集行加「合集」小徽章 + 语言/类型徽章（合集定义的 `lang`/`type` 优先、按素材兜底）；「新建合集」表单加 类型/语言 下拉；新增 `edit-collection-modal.tsx`（改名 + 定义 类型/语言，留空则按素材兜底）接入合集行「⋯」菜单的「编辑」。⑥ **表单上移**：顶栏「新建合集 / 上传文件 / AI 生成 / 收藏素材」点开后的表单/面板从筛选导航下方**上移到工具栏正下方**（紧贴按钮，不再隔着筛选区太远）。⚠️ schema：给 `material_collections` 补 `lang`/`type` 两列（`alter add column if not exists` 幂等，见「进行中」）。tsc + build 通过（`/api/rss`、`/api/materials/meta`、`/materials/[id]` 仍在路由表）。
- 2026-08-22 **素材库再改：合集与单条合一板块 + 恢复筛选导航 + 批量全选 + 「⋯」菜单收编辑/删除**（用户一批反馈：同一板块共存、批量要有全选、删除按钮太明显改隐藏菜单、语言/类型等筛选导航不见了请恢复）。① **统一板块**：素材库主页重写成**一个列表**，合集行（文件夹图标 + 名称 + 条数 + 最新缩略图）与单条素材行同排展示，不再分「合集 / 单条素材」两个标题段；点合集进详情、点素材标题进观看页；顶部加回**检索框 + 语言 / 类型 / 状态 三组筛选 chips**（恢复最初的筛选导航，合集按「任一条匹配」参与语言/类型/状态筛选、单条按自身字段筛）。② **批量操作（含全选）**：工具栏「批量操作」开关，批量栏有「全选 / 全不选」+「移到合集…」（只作用于选中的单条）+「删除选中（N）」（选中的单条硬删、选中的合集删除并把其素材退成单条）；行面在批量态可整行点选。③ **「⋯」菜单收编辑/删除**：新增 `src/components/row-menu.tsx`（`RowMenu`，点开下拉、点外面收起）；每行素材的面板主操作（「导入到笔记」在合集详情保留为主按钮）之外的 编辑/导入/删除 收进「⋯」；合集行「⋯」只有「删除」。④ **编辑素材弹窗**：新增 `src/components/edit-material-modal.tsx`（改 标题/来源/语言/类型/归属合集，从单条到任意合集、或移出成单条），行内即改即存；用于素材主页与合集详情的「编辑」。⑤ **收藏单条不必选集类**：此前「收藏素材」强制必选集类、否则提示「不显示」，现单条也展示 → 改为**不选合集就存成单条**（下拉加「不归合集（单条）」项），与统一板块一致。⚠️ 两条流：素材主页「新建合集」按钮（内联表单）替代原「合集」管理面板；`collection-detail` 每行原「移到合集/删除」按钮换成「⋯」（编辑/删除），批量栏（全选/移到其它合集/删除选中）保留。tsc + build 通过（`row-menu`、`edit-material-modal` 新增）。
- 2026-08-22 **素材库结构反馈四项：恢复单条展示 + 批量操作 + 导入合集统一语言 + 预览改回退**。① **单条素材与合集并列展示**（用户：把早前「隐藏未归类」翻回去）。`materials-view`（素材库主页）改为「合集网格 + 单条素材」两段——未归入合集的素材不再隐藏、也**不标「未归类」**，点标题即进观看页，或在行内「移到合集…」下拉归入某个合集（先到「合集」面板新建或直接在单条列表批量移）；合集网格 / 单条列表各自有空态引导。② **批量操作**（合集可批量删除 + 素材可批量操作）：`materials-view` 的「合集」管理面板每行加勾选框 + 「删除选中（N）」批量删（`collection_id on delete cascade` 让素材退单条继续展示）；「单条素材」段加「批量操作」开关——勾选（整行可点选中）后出现「移到合集…」下拉 + 「删除选中（N）」；`collection-detail`（合集详情）同样加「批量操作」：全选/全不选 + 「移到其它合集…」 + 「删除选中（N）」，行内 `onClick` 批量态选中单个、标题点击 `stopPropagation` 不误触发选择。③ **导入合集统一语言**：`add-material-panel` 的合辑（playlist）分支在标题下加「语言」下拉（默认「按内容自动判断」，选一种则整批导入都用它，避免同一合集语言混杂），`importPlaylist` 写 `lang` 时用「plLang==='auto' ? detectLang(e.title) : plLang`。④ **去掉预览按钮 + 观看页返回原处**：`collection-detail` 每行删掉「预览」链接（点标题就直接进观看页），并把行内合集 select 加「（移出合集）」项；`material-watch` 顶栏返回从硬编码 `/materials` 的 Link 换成 `BackButton`（优先 `router.back()` 回到刚进入的那个列表，历史不足才回落 `/materials`），观看页删除后也保持一致回退。tsc + build 通过。
- 2026-08-22 **素材库四能力：播客选单集听/导入 + AI 生成素材 + 文章提取正文 + 上传文件**（用户四组方案的落地）。① **播客选单集听**：素材观看页 `material-watch` 对 `type===podcast` 不再内嵌原网页（会被 X-Frame-Options 拦），改成 `PodcastPlayerMaterial`——进页 `POST /api/rss` 拉订阅单集列表，每集「听」按钮点开 `<audio>` 播放，「导入」选定该集；导入复用 `MaterialImportExtra`（`audioUrl/audioTitle`）把该集音频以 `mediaEmbed` 内嵌进笔记。② **AI 生成素材**：新增 `type=generated` + `materials.content` 列；`/api/materials/generate`（POST `{topic, lang}`，走 `aiChat`）按「主题+语言」让 AI 整理成可学习正文（如「日语 N3 全部语法知识点」）；新组件 `AiGeneratePanel`（题材 + 语言下拉 + 必选集类，可内联新建合集），生成后 `insert` 一条 `type=generated` 素材并跳观看页——观看页渲染 `content` 正文，导入时包成「原文」callout（可 AI 精读/转卡时跳过）。③ **文章提取正文**：`/api/materials/extract`（出网走 `serverFetch` + 正则抽读 readability）返回 `{title, text}`；`material-watch` 对 `type===link` 加「提取正文/重新提取正文」按钮，抽出的正文渲染在观看区，`MaterialImportExtra.articleText` 导入时包成「原文」callout，并支持以抽出页标题新建笔记。④ **上传文件**：`type=file` + `file_kind`（audio/image/doc）；新建 Supabase 存储桶 `materials`（公开读 + 按 `user_id` 前缀文件夹的 RLS 读写 policy）；`FileUploadPanel` 选本地音频/图片/文档 → 上传 `materials/{uid}/{uuid}.{ext}` → 取公开 URL 存成 `type=file` 素材 → 观看页按 `file_kind` 渲染（音频 `<audio>` / 图片 `<img>` / 文档下载链接）；导入时音频→`mediaEmbed`、图片→`image` 节点、文档→链接段落。**入口**：素材主页与合集详情工具栏各加「上传文件 / AI 生成」按钮（与「收藏素材」同排，同一时间只开一个面板）。⚙️ 需应用一次 schema（`materials` 补 `content`/`file_kind` 列 + 建 `materials` 存储桶）。⚠️ AI 生成走用户文字/设置里的 provider；播客单集解析、AI 生成、文章提取都涉及出网，Vercel 部署时按环境预留。tsc + build 通过（`/api/materials/generate`、`/api/materials/extract` 已进路由表）。
- 2026-08-22 **素材库改「合集嵌套视图」+ 英语语言 + 选择性导入 + 修 404**（用户一批反馈）。① **修 404**：`/materials/[id]` 详情页此前同步读 `params`（本版 Next 的 `params` 是 `Promise`，须 `await`），导致 `params.id` 为 undefined → getMaterial(undefined)→null→notFound 全 404；改为 `const { id } = await params`（对齐 notes/cards 详情页写法）。② **英语语言**：`Lang` 联盟加 `english`，`detectLang` 拉丁/希腊字母→英语（放在泰/韩/假名/汉字判断之后、其它之前），补 `LANG_ORDER/LANG_LABEL/LANG_COLOR/speechLang/cleanForSpeech` 各分支；`cleanForSpeech` 的 `lang==="other"` 原样返回不变、英语改为保留拉丁/希腊字母。⚠️ 会影响卡片/词群里「纯拉丁文本」从「其他」变为「英语」的标注。③ **合集嵌套视图（隐藏未归类）**：素材库主页 `/materials` 改为**只展示合集网格**（`materials-view.tsx` 重写：每合集一张卡 = 缩略图/类型图标 + 名称 + 条数，点进 `/materials/collection/[id]`；没合集给空态引导先建合集；**未归类素材不再展示**）；新增 `/materials/collection/[id]` 详情页（`page.tsx` + `collection-detail.tsx`：返回/合集名/条数 + 搜索 + 逐条 预览(→观看页)/导入到笔记/查看笔记/删除/移到其它合集）；`getMaterialCollection` + `listMaterialsByCollection` 新增。④ **单条素材必选集类**：收藏单条时必须归入一个合集（默认用当前合集，否则下拉选或内联新建；未选就提示「素材必须归入合集，否则不显示」并禁保存），避免收进库里却因「不显示未归类」而丢失。⑤ **合辑选择性导入**：`add-material-panel` 合辑分支每集可勾选（默认全选，含 全选/清空），按钮改「导入所选（N）」，只把勾选的每集存成素材；仍归入同名合集（指定合集则归入该合集）；重复导入跳过已收录 url。⚠️ 合辑解析仍靠本机 yt-dlp，Vercel 不可用。tsc + build 通过（`/materials/collection/[id]` 已进路由表）。
- 2026-08-22 **素材库增强：YouTube 合辑一键导入 + 站内观看页替换预览弹窗**（用户两项需求）。① **合辑导入**：`media.ts` 加 `parseYoutubePlaylist`（识别 `youtube.com/playlist?list=` 及 watch/youtu.be 带 list 的链接）；新增 `/api/materials/playlist`（runtime=nodejs，`yt-dlp --flat-playlist --dump-single-json` 取合辑标题 + 每集 id/标题，拼观看地址与 `i.ytimg.com` 缩略图，上限 300 集；被 bot 拦截给友好报错）；`add-material-panel` 解析到合辑链接走独立分支——列出每集（滚动 + 缩略图）与「共 N 集（只取前 N 集）」，点「导入全部」把每集各自存成一条素材（type=youtube、source=合辑名、lang=detectLang(title)），并**按合辑名自动建同名素材合集归类**（已收录 url 自动跳过，重复导入不产生重复素材）。② **观看页**：把「预览」弹窗换成真正的站内页面 `/materials/[id]`（`page.tsx` server 取 `getMaterial` + `material-watch.tsx` client）——视频/音乐 iframe 内嵌、音频 `<audio>`、文章/播客内嵌原网页（`sandbox`；被 X-Frame-Options 拦时给「打不开就在新标签打开原文」回落），页头返回/标题/来源/语言/类型/状态徽章 + 「导入到笔记（或查看笔记）」/「删除」主操作；`materials-view` 的「预览」按钮与标题都改成链接到该页；删除了 `material-preview-modal.tsx`。⚠️ 合辑导入依赖本机 yt-dlp（转录已用），Vercel 环境不可用。tsc + build 通过（`/api/materials/playlist`、`/materials/[id]` 已进路由表）。
- 2026-08-22 **素材库（收件箱，待办 5）**：囤积学习素材的视频/音频/播客/文章「收件箱」，与笔记互补——收藏 → 抓元数据 → 列表筛选 → 预览 → 导入笔记 → 精读/转卡。① **数据层**（schema.sql 追加）：`material_collections`（id/user_id/name/created_at，照 word_themes）+ `materials`（url/type/title/source/thumbnail/lang/status 默认 'pending'/note_id 引用 notes on delete set null/collection_id 引用 material_collections on delete cascade/created_at/updated_at），各带 user_id 索引 + RLS `*_own`，materials 加 `materials_set_updated_at` trigger。`type ∈ youtube|audio|spotify|link|podcast`；`status ∈ pending|imported`；删笔记只孤立标记不删素材，删合集退「未归类」不删素材。② **types.ts** 加 `MaterialStatus`/`MaterialType`/`MaterialCollection`/`Material`/`MaterialWithNote` + `MATERIAL_TYPE_LABEL`。③ **queries.ts** 加 `listMaterialCollections`/`listMaterials`（order created_at desc + titleMap 合并 note_title，照 listWordCards）/`getMaterial`。④ **`/api/materials/meta`**（runtime=nodejs，出网全走 serverFetch）：按序 `parseMediaUrl`→YouTube oEmbed/Spotify oEmbed/音频文件名 → `resolvePodcastFeed`+XMLParser 取节目名/封面（Apple/Google/主页）→ 通用文章 `serverFetch`+新 `lib/og-meta.ts` 的 `extractOgMeta` 抓 OG/Twitter/`<title>`；`detectLang` 猜语言、hostname 兜底 source。⑤ **页面与组件**：`/materials` server 页 + `app-shell` 加「素材」导航（Inbox 图标）；`materials-view.tsx`（搜索 + 语言/类型/状态筛选 + 合集归类 + 每条 预览/导入到笔记/查看笔记/删除 + 空态 EmptyState）；`add-material-panel`（贴链接→解析→可改标题/来源/类型/语言/合集→入库）；`material-preview-modal`（视频/音乐 iframe、音频 audio、文章/播客打开原文）；`import-material-modal` 含 `importMaterialToNote`（媒体→内嵌 mediaEmbed 节点 `src=embedUrl`，链接/播客→纯文本段落「标题 URL」（编辑器无 Link mark）；新建笔记或选已有，写 doc 形状 content + docToText，标 status='imported'+note_id，完成后跳笔记）；合集 create/delete。**决策**：库内不做转写（转写/AI 精读全在导入后的笔记里用 mediaEmbed 自带按钮）；materials 无 transcript 列；客户端组件只从 queries.ts import 类型不 import 运行函数（避免把 next/headers 拉进客户端 bundle）。⚠️ 需应用一次 schema 建 `materials`/`material_collections` 两表。tsc + build 通过。

（待办 3，用户选的「快」项）。`/api/ai/ocr`（视觉 OCR，只读文字、不翻译）此前已存在（AI 学伴「图片输入」在用），本次给笔记编辑器加入口：**媒体弹窗**里新增一行「上传图片 → 识别文字（OCR）」按钮（`ScanText` 图标，与「上传本地音频 → 转文字稿」同位同款）；复用助手面板的 文件→读 base64→POST`/api/ai/ocr` 流程（`AbortSignal.timeout(90_000)`），识别出的文字**按空行切成段落、插入光标处**（不是插入图片，方便接着编辑）；忙时按钮转「识别中…」、出错红字（超时 / 图片无文字 / 接口失败）。tsc + build 通过。
- 2026-08-22 **转成闪卡入库防卡顿/重复 + 完形填空出题质量 + 省 token**。① **转成闪卡入库可卡顿/重复**（`convert-to-cards` `save()`）：`await fillReadings(rows)` 原在 `setSaving(true)` 之前跑，读音接口一卡就卡好几秒、确认按钮仍可点，用户很容易点两次 → 同一张卡入库两次。改成**先 `setSaving(true)` 再走网络**（`fillReadings` 在 `saving` 保护下，`try/finally` 确保结束复位），并加 `if (saving) return` 防连点。② **入库完成文案**：done 屏「去『卡片』页或这篇笔记的闪卡里看看」→「去『闪卡』页…」（页面已改名闪卡）。③ **完形填空别把释义和例句混在一起**（`cloze.ts`）：生词背面往往是「释义 例：例句」，原先整段背面进句子池被当成语境句挖空 → 显示成「巡游，周游 例：名所を____（巡游名胜）」。新增 `splitWordBack(back)` 拆成 `{meaning, example}`——句子池里只用**例句**当外语语境句、`词义`（答前提示）只取**释义**部分，两者不再混排；没拆出例句的背面（纯译文/纯释义）维持原样。④ **完形填空进页不自动调 AI**（`cloze-session.tsx`）：原来一进会话只要有缺例句的生词就自动调 `/api/ai/cloze-examples`。为省 token 改为**先进页只用有现成语境句的题**，缺例句的生词记进 `missingWords`，题头上方显示「有 N 个生词没有现成例句 + 用 AI 造例句」按钮，**点按钮才调 AI**，造完追加进队列、按钮收起；AI 出错红字在按钮下方单独展示。⑤ **显示答案后也要判分**（`cloze-session` `check()`）：原「已看过答案 → 直接进整句、不判分」，改成**始终按输入比对**——显示答案后没打字 / 打错再点「核对」也判 ✗，不再静默跳整句（用户反馈）。tsc + build 通过。
- 2026-08-22 **复习页返回路径改「回退」+ 卡片背面标签排版 + 日语语言色去红**。① **返回回环**：从卡片页点「开始背诵」进复习，点「←」回卡片页时用 Link 推一个卡片页进历史，再按浏览器后退又绕回复习页。新增 `src/components/back-button.tsx`（`BackButton`，`useRouter`）：优先 `router.back()`（回到进入本页的那个原处，不再把原处推进历史造成回环），历史不足后退（直接打开链接/新标签）才回退 `push(fallback)`；复习页 7 个返回箭头（scope=weak/errors、测试、故事、完形、按 note 背诵、按 theme 背诵）、`notes/[id]/cards`「原路返回」、`card-detail`/`theme-detail`/`note-editor` 顶栏返回、测试结束的「返回」CTA（test-session 2 处）统一替换。② **卡片背面标签排版**（`card-back.tsx`）：结构化行原先把标签右对齐在一列宽里，导致「例」这类标签被缩进、与上一行正文（如释义）左端不对齐、看着突兀；改成**左对齐小方章**（`rounded-md bg-teal-50 px-1.5 text-[11px] font-semibold text-teal-700`）贴在内容前——标签左缘与正文左缘对齐，内容紧随其后，不再有悬浮空隙。③ **日语语言色**（`lang-detect.ts` `LANG_COLOR`）：日语从 `rose`（红粉）改成 `violet-50/violet-700`，避免红色违和。tsc + build 通过。
- 2026-08-22 **转成闪卡的卡片正面加假名（日语生词汉字上方标读音）**（用户：背诵时很多汉字怎么读不清楚）。① **数据层**：`cards` 表幂等补 `reading` 列（schema.sql）、`Card` 类型加 `reading: string | null`。② **新建 `src/components/card-front.tsx`**（`CardFront`）：把 front + reading（JSON 字符串 `FuriganaSegment[]`）渲染成可能带 `<ruby>` 上标的控件——汉字段套 `ruby.furigana` + `<rt>` 假名、其余原样；reading 解析失败/为空就纯文本（不影响正面文字 / 搜索 / 转卡）。**CSS**：`.tiptap`/`.share-content` 之外补全局 `ruby.furigana { ruby-align:center }` + `ruby.furigana rt { font-size:.62em; color:#9ca3af; line-height:1 }`（与笔记内 furigana 一致，卡片面也能标）。③ **转成闪卡时批量生成 reading**：`convert-to-cards` 的 `save()` 走 `fillReadings(rows)`——只处理日语（`lang==="japanese"`）且正面含汉字的**新卡**，把正面拼成 `\n` 一次调 `/api/ai/furigana`，再按 `\n` 段切回每张写 `reading=JSON.stringify(segments)`；失败/数量对不上静默跳过（reading 留空，卡片只显示纯汉字）。④ **研究/浏览面一律接 `CardFront`**：`review-session`（主翻面 + 列表 ReviewListCard）、`card-tile`（卡片页按来源/按主题翻面）、`card-detail`（单卡详情翻面）、`card-sidebar`（笔记侧栏列表）、`review-home`（待加强的卡）、`test-errors`（错题列表）——front 文本全换成 `<CardFront text={x.front} reading={x.reading} />`；`note-cards` 复用 CardTile 自动覆盖。旧卡 reading 为 null 走纯文本；新卡由转卡入库时自动补。tsc + build 通过。
- 2026-08-22 **闪卡加 `lang` 语言字段 + 主题语言筛选一致性 + 背诵返回原处 + 「正在背的合集」记进度**（一揽子）。① **数据层**：`cards` 表幂等补 `lang` 列（schema.sql）、`Card` 类型加 `lang`；`lang-detect.ts` 新增 `cardLang(card)`（**存储的 `card.lang` 优先**，没存才回退 `detectCardLang`）并导出共享 `LANG_ORDER`；`queries.ts` 的 `dominantLang` 改收卡片对象、逐张 `cardLang` 投票（保留 content_text 对中日 tiebreaker），集合语言与单卡一致。② **建卡写入**：7 个建卡点（convert-to-cards / add-cards-button / ai-assistant-panel / rich-text-editor / note-cards / card-sidebar / collect-words-modal）insert 都带 `lang: detectCardLang({front, back})`；存量 null 卡走 `cardLang` 回退无感。③ **转换预览语言下拉**：convert-to-cards 预览每张卡旁可改语言，`save()` 用预览里可改的 `c.lang`；「AI 补全」只补 back 时对受影响卡**一并重算 lang**（正面纯汉字+AI 补了含假名背面 → 语言自动更正为日语）。④ **四处编辑态语言下拉**：card-tile / card-detail / card-sidebar / review-session 的 ReviewListCard 编辑位各加语言下拉，保存写 `lang`（把检测值/用户改值回填）。⑤ **显示/筛选统一 cardLang**：card-tile 徽章、cards-view 孤儿卡、group-browser（词数+过滤）、review 测试语言筛选、test-errors 全改读 `cardLang`（用户改过语言立即生效）。⑥ **主题语言筛选一致性**：词群页选「日语」→ 主题卡链接带 `?lang=` → `groups/[key]` 按 `cardLang` 过滤 `themeCards`、传给 ThemeDetail → 详情页只显示该语言；背/测（theme-detail + review 主题分支）也按 `cardLang` 过滤，进入背诵/测试只含该语言。⑦ **背诵返回原处**：review 页 `searchParams` 加 `back`，note/theme/test 分支返回按钮 `href={back ?? 默认}`；入口设置——`notes/[id]/cards`「开始背诵」带 `&back=/notes/<id>/cards`、theme-detail「背/测」带 `&back=/groups/<key>[?lang=]`。⑧ **「正在背的合集」记进度**：review-session 在 `rate()` 时把当前合集 `key`（`note_id::kind`）写进 localStorage（`canRememberCollection` 默认 true；**主题背诵置 false**，不属于合集语义不改写）；review-home `current` 挂载后读 localStorage 优先切到它，没存过按最近复习/到期最多/第一个兜底（SSR 用确定性兜底避免 hydration 不一致）。**⑨ 转换预览「整批改语言」**（2026-08-22 跟进）：原预览只能逐张改语言，加「语言：」下拉——通常整篇同一种语言，一键设为该语言（或「按内容自动判断」逐张重判），再逐张微调；下拉值不覆盖 AI 补全对已改卡的保护（手动改过的保留）。tsc + build 通过。
- 2026-08-22 **复习页字体层级 + 「纯汉字日语词被标成中文」单卡语言判断改善**。① **复习卡字体**：`review-session` 主翻面卡原本正反面都用 `text-2xl font-semibold`，背面是定义+整句例句被撑到 24px、且与 `card-back` 里 `text-xs` 的标签列形成突兀对比。改成**正面 `text-3xl font-semibold`（要记的词/句）、背面 `text-lg font-medium`（可读的定义+例句）**——背面内容仍是 `CardBack` 渲染，层级清楚。② **单卡语言判断**：卡片语言徽章/分组一直用 `detectLang(front)`，纯汉字日语词（運行/電車/先生，无假名）被误标中文；`detectLang` 顺序修复只救「含假名」的词，纯汉字词救不了。新增 **`detectCardLang(card)`**（`lang-detect.ts`）：正面能确定脚本（泰/韩/含假名日语）→ 用正面；正面是纯汉字(中文)或无脚本(其他)时，**看背面**——日语卡背面几乎必带假名例句（`電車が運行する` 的 が/する），靠它把「運行」这类纯汉字日语词从中文里拽出来；背面仍无确定脚本则保留正面判断（纯汉字兜底中文）。调用点从 `detectLang(x.front)` 换成 `detectCardLang(x)`：`card-tile`（徽章）、`group-browser`（按主题语言标签）、`review/page`（测试语言筛选）、`test-errors`（错题语言筛选）、`cards-view`（孤儿卡）。纯汉字+纯中文释义且背面无假名的卡（如词本只有汉字+中文注解）仍归中文——无字典信号时字符层无法区分，属固有边界。tsc + build 通过（8 组样例符合预期）。
- 2026-08-22 **修复「开始背诵」新合集误显示「都复习完了」+ 全站「卡片→闪卡」文案统一**。① **背不上的 bug**：`/notes/[id]/cards` 的「开始背诵」跳 `/review?note=<id>`（不带 kind），落到 review 页「背诵某一个文件」的 `listReviewCards(noteId, undefined)`，而 `kindMatches` 当 kind 为空时只放行 `!c.kind`（普通卡）——可笔记用「转成闪卡」生成的卡全是 word/example/grammar 类型，一张都不匹配 → `items.length===0` → 误提示「都复习完了」。改成 **kind 为空就复习这个合集的全部卡**（`kind ? c.kind===kind : true`，`queries.ts`），点「开始背诵」把整篇笔记的普通/生词/例句/语法一次过完；指定 kind 时（复习主页「继续背/测试/完形」按 kind 传）仍只看那一种。空态文案同步改清晰：`这个文件都复习完了/回文件列表` → `这个合集都复习完了/回闪卡列表`（该字样只在真正没到期卡时才出现）。② **「卡片→闪卡」遍历统一**（页面已改名闪卡，把其它用户可见的「卡片」串一起换掉，含指向页面的「卡片页/词群页」）：`独立卡片`→`独立闪卡`（review 页 4 处、cards-view 块标签、queries.ts 列表兜底标题）、`卡片文件`→`闪卡文件`（add-cards-button、cards-view）、`未命名卡片集`→`未命名闪卡集`、`创建卡片文件失败`→`创建闪卡文件失败`、`删除这张卡片？`→`删除这张闪卡？`（card-tile/card-detail/card-sidebar/review-session）、`共复习 N 张卡片`/`删除选中的 N 张卡片`/`没有可入库的卡片`/`没有可测试的卡片`/`需要卡片同时有正面和背面`/`没有新的卡片可入库`/`没识别出卡片`/`没有匹配「x」的卡片`→对应闪卡、`去卡片页`→`去闪卡页`（note-cards）、`回词群页`→`回闪卡页`（review 主题空态）、theme-detail 删除分类提示里的 `词群页`→`闪卡页`。导航「卡片」与 `/cards` 页 h1「闪卡」此前已是「闪卡」。tsc + build 通过。
- 2026-08-22 **修复日语笔记转成闪卡后被分组到「中文」**：根因是 `src/lib/lang-detect.ts` 的 `detectLang` 把**汉字区排在假名区前面**——中文与日语共用汉字，于是 `食べる`/`お寿司`/`先生（せんせい）` 这类**汉字+假名混合**日语词先被汉字命中 `[一-鿿]` 判成「中文」（假名分支对任何混合文本永不触发），纯汉字的日语笔记卡片（日本語/勉強/学生）也一并落进中文，语言筛选/词群/复习里整篇被标成中文。① **`detectLang` 调顺序**：假名 `[぀-ヿ]` 提到汉字 `[一-鿿]` 之前——假名只存在于日文，是最可靠信号；混合假名的日语词立刻判成日语，只有**纯汉字**文本（字符层与中文无法区分）仍归中文兜底。② **`dominantLang`（`src/lib/supabase/queries.ts`）加正文二次确认**：卡片页「按来源」集合语言是逐卡正面的多数表决（`listCardGroups` → `dominantLang`），纯汉字日语词会被表决判成中文；于是 `listCardGroups` 的 notes 查询多取 `content_text`，`dominantLang` 在**正面表决=中文**这种歧义时用整篇正文（含大量假名/助词/句子，比单个纯汉字词可靠）二次确认**若其实是日语则判日语**；泰/韩/日（带假名）脚本唯一、正面表决即准不改写，避免把外来文本带偏；正面全测不出时用正文兜底。tsc + build 通过；`detectLang` 12 组样例验证符合预期（混合假名的日语词、纯假名、纯汉字中日各自归位）。
- 2026-08-22 **本轮三处微调**：① **释义精简**——`/api/ai/card-explain` 生词卡的释义要求从「更完整的释义」改成「按词典风格、先词性 + 2~4 个最常用意思（分号/顿号隔开）」，精炼但该记的不缺；搭配/相关词/例句标了「可缺省」，不强求全给。② **转卡弹窗按钮改名**——「✨ 一键 AI 补全」→「✨ AI 补全」，下方说明同步精简为「给新卡补上读音 / 释义 / 例句，逐张可改。」。③ **修复笔记闪卡侧栏漏掉「语法」分组**——`CardSidebar` 的类型过滤 tab 原本只有「全部 / 生词 / 例句」（`KIND_LABEL` 和 `kindFilter` 类型都只写了 word/example），导致语法卡虽然在（「全部」里可见、卡片页合集也分组了），但侧栏既没有「语法」tab 也按类型过滤时被漏掉。改成 `KIND_LABEL` 加 grammar、`kindFilter` 类型扩到 grammar、用 `kindCounts` 统一计数、tab 列表按 `presentKinds`（有哪类显示哪类）生成。tsc + build 通过。
- 2026-08-22 **闪卡背面「标签→左轴」渲染 + AI 解释按类型给内容 + 转卡弹窗「一键 AI 补全」**：① **`src/components/card-back.tsx`（新）**——统一样式的背面渲染器，规则是「只把**已知标签**（读音/发音/释义/意思/词性/搭配/常用搭配/相关词/例句/例/译文/翻译/解析/长难句/生词/词义/用法/注意/说明/接续/接续规则/规则…）开头的行渲染成**左轴排版**（标签在右对齐列 + 内容在右），其余行一律原样 `whitespace-pre-wrap`。这解决了「所有闪卡统一样式 vs 保留自由度」的矛盾：AI 生成的结构化背面统一好看，而用户手写的自由内容（一句话释义之类）不被强行拆。另导出 `isStructuredBack` 备用。② **三处展示位都接 `CardBack`**——`card-tile.tsx`（翻面）、`card-detail.tsx`（详情翻面）、`review-session.tsx`（主翻面 + 列表翻面），保证卡片/详情/复习到处一致。③ **`/api/ai/card-explain` 按 kind 分流**：`KIND_PROMPTS` 给 生词 / 例句 / 语法 三套不同补全内容（生词=读音/词性/释义/搭配/相关词/例句；例句=整句翻译/长难句解析/句子生词/用法；语法=语法说明/接续规则/例句/用法注意），输出统一「标签：内容」分行，交给 `CardBack` 左轴渲染；其余逻辑（纯文本剥离、AiError、语言标注）不变。④ **转卡弹窗加「✨ 一键 AI 补全」**（`convert-to-cards.tsx`）：在「收录类型」行下加一个 violet 描边按钮，对识别出的每张**新卡**串行调 `/api/ai/card-explain`（kind 传所属组），把返回的 `explanation` 填进预览 back（**只在原 back 为空时覆盖**，非空保留用户手写内容），补过的每张 `CardDraft` 盖个小「✨ AI」徽标，失败即停并红字提示；「确认入库」前逐张仍可改。⚠️ 只做「转卡弹窗」这一层（用户选的），不碰已存在旧卡的批量补全（旧卡要补走单卡「AI 解释」）。此前草稿用的临时文件 `poc-back-style.html` 已删除。tsc + build 通过。
- 2026-08-22 **「按来源 / 按主题」两张卡片视图统一成一套**：用户指出两处卡片形态不一致（来源=翻面+就地编辑、主题=正反面同摊+点卡跳独立详情页），且「✨ AI 解释」只在主题的详情页有、按来源的编辑位却没有。统一为同一套可复用组件 `src/components/card-tile.tsx`——翻面展示 + 就地编辑（正面/背面输入 + 「✨ AI 解释」按钮）+ 右上「⋯」菜单（编辑/删除），两处渲染它，样式、翻面、编辑、AI 解释完全一致。① **`card-detail.tsx` 的编辑逻辑原封不动保留**（/cards/[id] 单卡详情页还被复习 / 测试错题用着，不该删），`card-tile.tsx` 里的 `fillBack`/`saveEdit`/`deleteCard` 与它同源。② **`note-cards.tsx`**：删除内联的 `CardItem` 组件，改用 `CardTile`（`selecting`/`checked`/`onToggleSelect` 传选择态）——于是按来源的卡片列表也拿到「✨ AI 解释」。③ **`theme-detail.tsx`**：弃用「正反面同摊 + 点卡跳 /cards/[id]」的旧卡片，改用 `CardTile`（`menu={false}` 关掉「⋯」菜单，`footer` 放「移出」按钮）——主题卡不再跳详情页，变成翻面 + 就地编辑，且有 AI 解释。跳转关系仅在主题视图移除，`/cards/[id]` 页面本身未删除。**数据一致性**（用户问：主题里 AI 补全，来源下对应卡会跟着变吗？答：会）——主题与来源是同一张 `cards` 记录的两种展示（主题=打 `theme` 标签、来源=按 `note_id` 分组），改 `cards.back` 后两处重新拉取即同步；但 AI 解释只填进输入框，要点「保存」才写库。tsc + build 通过。
- 2026-08-22 **单卡「✨ AI 解释」（原「闪卡背面 AI 补全」，按用户反馈改design）**：不要固定「读音/词性/释义/搭配/相关词/例句」六桶模板，改成像精读那样**自由地把背面完善成一份完整清晰的中文解释**，名字叫「AI 解释」不叫「补全背面」。① 新建 `src/app/api/ai/card-explain/route.ts`（`POST { front, kind, back }`，文字任务走 `aiChat` 尊重用户设置里的文字提供商）——`systemPrompt` 给语言（`detectLang` + `LANG_LABEL`）让模型按语言给读音建议（泰/韩→拉丁转写、日→假名+罗马字、中→拼音、英→音标），要求**保留背面已有正确内容、只补缺失、按「读音：… / 释义：… / 搭配：… / 例：…」分行输出且不重复已有项**；返回纯文本 `{ explanation }`（剥代码块围栏）。② `card-detail.tsx` 编辑态在「背面」输入框下加「✨ AI 解释」按钮（`Sparkles` 图标、violet 描边）：点它用当前 `front/back/kind` 调接口，把 `explanation` 填进背面输入框（用户看过可再改再保存），忙碌转「AI 解释中…」、出错红字、正面为空时禁用。tsc 通过。⚠️ 只做单卡按需，不塞进「一键转卡」。此前误建的 `/api/ai/card-fill` 已删除。
- 2026-08-22 **AI 模型路由 + 设置页「AI 模型」板块（待办 7 最后一项）**：按任务分流、设置页可换模型，改动集中、各 AI 路由零改动。① **schema**：`user_settings` 加三列 `ai_text_provider` / `ai_speech_provider` / `ai_vision_provider`（text，null=用环境默认）；`types.ts` 的 `UserSettings` 同步三字段。② **`queries.ts`**：`getUserSettings` 多选三列并返回；新增轻量 `getUserAiProviders() -> {text,speech,vision}`（只读三列，出错返回全 null）。③ **模型路由集中在 AI 封装（各路由不用动）**：`ai-client.ts` 的 `aiChat` 加 `provider` 选项、路由优先级 = 显式 provider → 用户设置 → `.env AI_PROVIDER`；`aiVision` 拆成 `anthropicVision`/`openaiVision`（新增 OpenAI 视觉 `gpt-4o-mini` 那条）、路由 = 显式 → 用户设置 → Claude；`transcribe.ts` 的 `pickProvider(prefer)` 支持 Groq/OpenAI 二选一（缺 Key 退回 G→O 自动优先级）、`transcribeAudio` 加 `prefer` 参数。三者内部都调 `getUserAiProviders` 落用户设置（服务端读 cookie，无则回环境默认）。④ **设置页**：`SettingsForm` 加「AI 模型」板块——文字 / 语音转录 / 图片识别三个下拉（只给预设选项，「默认」=跟随环境；Claude质量优先 / DeepSeek省钱；Groq免费额度大 / OpenAI Whisper），随「保存设置」一起 upsert。⚠️ 需应用一次 schema（本地 `supabase db push` 或重跑 `schema.sql`；表已存在则靠幂等 `alter add column if not exists` 补列）。tsc + build 通过。**版面调整**：把「AI 模型」从 `SettingsForm` 里抽出来，做成独立 `src/components/ai-model-form.tsx`（自带「保存 AI 模型」按钮，样式同识别规则表单），以 `<Section title="AI 模型">` 放在「闪卡识别规则」之后、「数据备份」之前；设置页**去掉底部「AI 功能状态」板块**（连同其 `provider/hasAiKey/aiLabel/aiKeyName/hasOpenAI` 这些只在那处用的变量一并删掉）。再给「AI 模型」加「未配 Key」小标识：设置页按 `.env.local` 算 `aiKeys`（anthropic/deepseek/groq/openai 四布尔，只显示有没有不显示值）传给 `AiModelForm`，选项里缺对应 Key 的标「（未配 Key）」、当前选择缺 Key 时在该项下用 amber 小字提示「会回退/不可用」及缺哪个 Key。
- 2026-08-22 **完形填空打磨：没语境句的生词让 AI 现造例句 + 揭晓后去掉记忆曲线打分**（改上次的 `mode=cloze`）。① **出题器 `src/lib/cloze.ts`**：`buildClozeItems` 从「返回 `ClozeItem[]`」改成返回 `{ items, missing }`——有现成语境句的进 `items`，**没语境句的生词进 `missing`**（不再跳过）；`ClozeItem` 去掉不再用的 `state` 字段、加 `aiGenerated`。新增 `parseAiCloze(raw, missing)`：把 AI 例句（目标词用 `⟦ ⟧` 包裹）拆成一条完形题（优先用 ⟦⟧ 定位、无则退回 `matchIndex`、都定位不到返回 null）。② **新增 `/api/ai/cloze-examples`**：POST `{ items: [{word, lang}] }` → 用 `aiChat`（Claude/DeepSeek 共用）给每个词按其语言造例句、目标词写进 `⟦⟧`，返回 `{ examples: [{word, sentence}] }`，只留服务端、Key 不外泄。③ **`src/components/cloze-session.tsx`**：进会话时若 `items` 不足 `dailyGoal` 且 `missing` 非空，调 `/api/ai/cloze-examples` 补例句（等待时转圈「正在用 AI 给没有例句的生词补句子…」，失败降级为只出有语境句的题）；**揭示答案后不再显示「忘记/困难/一般/简单」SM-2 打分**（删掉 `scheduleReview`/`dueAtFrom`/`review_state` 写入），改成**直接「下一题」**推进——完形变成纯练习、不改到期；AI 造的例句在题头带一抹极小 `Sparkles + AI` 灰字徽标；「本轮完成」不再累计记忆曲线。tsc + build 通过（`/api/ai/cloze-examples` 已进路由表）。**后续微调**：①「显示答案」只把空填上（`＿＿＿＿` 原地高亮答案），仍停答题态、可照抄再点「核对」，不直接切整句、不判对错——`cloze-session` 加 `shown` 态 + `showAnswer()`，点它聚焦输入框、隐藏「显示答案」按钮、核对时 `shown` 分支直接进整句；② 整句展示**不再显示「已显示答案」**，而是**显示例句的整句中文翻译**——`/api/ai/cloze-examples` 的 prompt/parse 都加 `translation`（整句译，不只译词），`parseAiCloze` 多接收 `translation`，答后信息区新增「整句：…」一行（原有语境句的 `translation` 也照常显示）。**再调**：③「显示答案」改成**开关**——`toggleAnswer()` 点一下把空填上、再点一下隐藏回空白，按钮在「显示答案 ↔ 隐藏答案」间切换（显示态用 teal 描边高亮）；显示态下照常输入 + 「核对」直接进整句（不判分），隐藏态下输入 + 核对正常判对错；④ 译文/词义等提示改成**统一的柔和信息条**——答前与答后都用 `rounded-xl bg-zinc-50` 软底 + 小白色「译 / 整句 / 词义」标签 chip + 内容，`flex-wrap` 自动换行，语法提示题另以 teal 小字单独一行。**又调**：⑤ 译文**紧跟挖空句下方**——`current.translation` 从柔和小框里挪到句子 `<p>` 正下方（答前、答后都在，双语对照那层关系），前面配小「译」标签 chip；「词义」提示保留为下面那个柔和框（teal「词义」标签）。
- 2026-08-22 **完形填空复习题型（mode=cloze）**：给复习加一种「填词」题型——把某个**生词**挖进一句语境里，你凭语境填空。① **出题器 `src/lib/cloze.ts`** 的 `buildClozeItems(items) -> ClozeItem[]`：只给「生词卡」（kind=word，或未分类且正面是单 token）出题；语境句来自本组卡片的 例句/语法正反面、各卡背面（可能自带例句），优先**外语例句**，用 `matchIndex` 在句子里找到该词后挖空成 `＿＿＿＿`（纯拉丁词加词语边界检查，避免把 category 里的 cat 误挖）；找不到语境的卡**自动跳过**（完形必须有语境）。`normalizeAnswer`（忽略大小写/多余空格）供核对。每个 ClozeItem 带 `translation`（语境句的译文）与 `meaning`（空格词的释义）。② **`src/components/cloze-session.tsx`**：显示挖空句 + **答前提示**（整句译文 + 「空格＝其释义」两个标签，知道要填什么）+ 输入框（可打字，回车核对，给出 ✓/✗）+「显示答案」；揭示后展示完整句（答案词高亮）+ 释义 + 语法卡正面；之后照常「忘记/困难/一般/简单」打分计入 SM-2（复用 `scheduleReview`/`dueAtFrom`），遗忘的排到队尾再背；今日目标截断与问答一致；没有可挖空的卡显示引导（补例句或用问答）。③ **`/review` 页面加 `mode=cloze` 分支**（支持 note/kind，也支持 theme），加载同一组待复习卡交给 `ClozeSession`（带 key 强制重挂载）。④ **复习主页「正在背的合集」操作区**从 2 列改 3 列：测试 / **完形填空** / 故事模式，三者同排。tsc + build 通过。
- 2026-08-22 **闪卡页 搜索/操作/语言 三处排版统一（桌面同排 + 按钮样式一套）**：① **桌面端搜索 + 操作按钮同一行**——两 Tab 的「搜索框（左）+ 操作按钮（右）」从「各占一行」改为 `flex-col gap-3 sm:flex-row sm:justify-between`：手机端上下堆叠（搜索整行、按钮换行），`sm` 起同一行（搜索左、按钮右），搜索框仍 `sm:max-w-md` 不撑满。② **按钮样式统一 + 放最右**：统一成单一形状 `rounded-lg`（按主题原来 `rounded-full` 改掉）；「添加闪卡 / 新建分类」这类**新建/添加**用绿色主按钮（`bg-teal-600` 实心），排在该行**最右侧**；其余保留默认 outline（灰边、`hover:bg-zinc-50`）——于是 按来源=`[导出全部][添加闪卡(绿)]`、按主题=`[AI 智能整理][批量操作][新建分类(绿)]`。③ **语言筛选统一**：按主题 chip 改成与按来源**完全一致**——`text-xs / rounded-full px-3 py-1`、活跃语言按 `LANG_COLOR` 显示、选中语言用各自颜色、「全部语言」用 teal（原来是 `text-sm px-3.5 py-1.5`、一律 teal、带计数；本次去掉计数、与按来源对齐）。`AddCardsButton` 主按钮也补 `inline-flex items-center gap-1` 与「新建分类」对齐。tsc + build 通过。
- 2026-08-22 **闪卡页「导出全部 / 添加闪卡」挪进按来源视图 + 两个 Tab 布局统一**：顶部两个按钮不再放页头（页头只留标题+副标题），而是作为「视图级操作」搬到**按来源** Tab 内——搜索框下面、语言标签上面，右对齐；**按主题** Tab 那排「AI 智能整理 / 新建分类 / 批量操作」也同步从语言标签下面**提到搜索框下面**，于是两个 Tab 的纵向结构完全一致：`搜索行 → 操作行(右对齐) → 语言标签行 → 内容`。实现：`cards-view` 加 `actions?: ReactNode` 属性（`CardsView` 接收并渲染在搜索与语言之间）；`group-browser` 把 `!batchMode` 工具栏一块上移；`cards/page.tsx` 页头只留标题，把 `<ExportCardsButton/><AddCardsButton/>` 以 `actions` 传进 `CardsView`。**空态也没有按钮**：`totalNotes===0 && orphans.length===0` 时原来直接渲染 `EmptyState`（会提前 return、够不到 `CardsView` 的 actions），故在空态分支里也渲染同一排 `导出全部/添加闪卡`，保证「最需要加卡的时机」按钮仍在。tsc + build 通过。
- 2026-08-22 **卡片页（/cards）移动端适配 + 按主题加搜索**：① 导航标签「卡片」→「**闪卡**」（`app-shell` nav，页内 h1 已是闪卡）。② 顶部「导出全部 / 添加闪卡」两个按钮在手机被挤成两行——header 改成 `flex-col gap-3 sm:flex-row sm:justify-between`，手机端标题一行、按钮一行（同排不换行）；按钮行加 `sm:shrink-0`。③ 搜索框适配：`cards-view`（按来源）搜索框与语言标签原来是**同行**、手机被挤，改成**搜索框单独占一行**（`w-full sm:max-w-md`），语言标签另起一行。④ **按主题（`group-browser`）同步加搜索**：顶部单独一行搜索框「搜索主题 / 词…」，`searchedGroups` 按主题名 / 卡面文字过滤（命中主题名→保留整组，否则看卡里文字），空态提示「没有匹配的主题或词」。tsc + build 通过。
- 2026-08-22 **去掉媒体内嵌节点的「AI 精读」按钮（去重）**：媒体节点动作栏的「AI 精读」和其下方「原文」callout 的「精读笔记」功能重复——文字稿必定落在「原文」callout 里，callout 的精读（读自身 `textContent` → `/api/ai/analyze` → 换成 原文高亮+生词/例句/语法）已完全覆盖。故去掉媒体节点的「AI 精读」按钮及 `analyze`/`followingText`，动作栏只剩「悬浮播放 / 生成文字稿 / 删除」；精读一律走「原文」callout 的「精读笔记」。tsc 通过。
- 2026-08-22 **播客链接多平台支持（Apple Podcasts / Google 播客 / 播客主页，待办 1 的 Apple 单集）**：新建 `src/lib/podcast-resolve.ts`——`resolvePodcastFeed(url)` 把「用户粘贴的链接」统一解析成可抓取的 RSS/Atom 源地址，按优先级 ①已是 `.xml/.rss` 或链里含 `feed` 的源 → 原样；② **Apple Podcasts / iTunes**（`/id<数字>`）→ `itunes.apple.com/lookup?id=<id>` 拿 `feedUrl`（公开无 Key）；③ **Google 播客**（`podcasts.google.com/feed/<base64>`）→ base64url 解回源地址；④ 其他播客托管页（PodBean / Buzzsprout / 自建站…）→ 抓页面找 `<link rel="alternate" type="application/rss+xml">`（或 atom），相对地址用 `new URL(href, page)` 补齐。`/api/rss` 开头先走 `resolvePodcastFeed`（Apple / Google / 主页都能挑集了），再抓源、列每集 → 挑一集以音频节点插入 → 「生成文字稿」走 Groq 转录。`rich-text-editor` 的媒体弹窗：非内嵌媒体的 http(s) 链接一律交给 `/api/rss` 解析（不再只认 RSS 源），提示文案更新，`isRssUrl` 该分支移除。Spotify 维持只内嵌播放（转录做不了）。**验证**：Apple lookup 实测返回 `feedUrl`（The Daily → feeds.simplecast.com）；Google base64 解回正常；`<link alternate>` 正则对多种属性顺序、绝对/相对 href 均正确、且不误抓 atom。tsc + build 通过（`/api/rss` 仍在路由表）。
- 2026-08-22 **转录多端：Groq 云 STT + 共享端点「转任意音频文件」（待办 1 的云端方案敲定）**：采纳 Deepseek 三层方案里的「云端兜底」，但**用 Groq 替代 Deepgram**做云 STT 首选（免费额度更合适、OpenAI 兼容、`api.groq.com/openai/v1/audio/transcriptions` + `whisper-large-v3`，只要换 URL 和 Key 就能用，接入比 Deepgram 简单）；**本地 Whisper 保留**不动，FunASR 自托管、手机 Web Speech 短语音两者记作「可选二期增强」（FunASR 上不了 Vercel、手机外网连不到，Web Speech 仅 Chrome 且发到 Google，都不是「手机随时转长文件」的解——那个只能靠云 STT）。落地：① 新建 `src/lib/transcribe.ts`——服务端「音频→文字」**唯一入口**，`GROQ_API_KEY`(推荐，免费)→`OPENAI_API_KEY` 自动回退，50 行内统一模型/Key/错误提示，保证多端一致；② 新建 **`/api/ai/transcribe-file`**——收「任意客户端上传的音频文件」，回 `{ text }`，是 Web/手机/未来原生 App 共用的那个端点（只转录、不存音频，Key 从不离开服务端）；③ `/api/ai/transcribe-audio`（浏览器录音/学伴语音）与 `/api/ai/transcribe`（音频直链）**都改成走 `transcribe.ts`**，于是浏览器录音、粘贴音频 URL 全部转为 Groq 免费额度（YouTube 抓字幕那条无需 Key 不变）；④ `rich-text-editor` 媒体弹窗加「**上传本地音频 → 转文字稿**」：选文件 → POST 给 `/transcribe-file` → 把文字稿以「原文」区块插到光标处（转卡自动跳过），手机端 / 未来 App 同一个端点。⚠️ **仍待做**：「无字幕视频 → 听声转录」那半条（yt-dlp 下音频 403 / 需 deno+ffmpeg+本地代理、上不了 Vercel）没动。需要用户往 `.env.local` 加 `GROQ_API_KEY`。tsc + build 通过（`/api/ai/transcribe-file` 已进路由表）。
- 2026-08-22 **词群「从合集导入」（待办 0b，含新建分类也支持）**：① **词群放开到「生词 + 例句」一起按主题归**（不只 word）——`queries.ts` 的 `listWordCards` / `listThemeCards` / `listThemeReviewItems` 三处把 `eq("kind","word")` 改成 `in("kind",["word","example"])`，于是词群页、主题详情、按主题背/测都同时收生词和例句；`themeOf`/`classifyWord` 不变（AI 语义归类优先、关键词兜底）。相关「词」文案改成「条」（`group-browser` 卡片数 + 未归类提示、`theme-detail` 页头/搜索占位、review 主题页头去掉「生词」）。② **新建 `src/lib/import-collections.ts`**：`listImportableCollections(excludeThemeId?)` 按笔记聚合 生词+例句 卡（客户端，两处复用；excludeThemeId 传某主题时跳过已收录的卡）。③ **`CollectWordsModal` 加第 4 种方式「从合集导入」**：按笔记标题/词面搜索，勾选一个或多个合集，一键把该项 `theme` 设成当前主题，已收录自动跳过。④ **「新建分类」也支持从合集导入**：`NewThemeForm` 加可选的「＋ 从合集导入」折叠区（默认收起），展开后搜索+勾选合集，创建分类时顺手把所选合集的生词+例句 `theme` 设成新分类 id；不选就走原来「名字+关键词+AI补关键词」。tsc + build 通过。
- 2026-08-22 **学伴增强 ②③④⑤（模式气泡 + 语音直发 + 场景对话/写美文 + 纠错）**：① 的「语言」下拉从「硬翻译 wrap」改成**纯目标语言上下文**——`/api/ai/assistant` 把 `lang` 变成一句加在系统提示里的 `langHint`（贴中文→翻译成该语言、写美文/场景对话/查词等指令→按指令用该语言执行），避免把「写美文 / 场景对话」这类指令也误翻一遍；② 输入框上方加一行低调「模式气泡」（翻译 / 场景对话 / 写美文 / 纠错），点了往输入框塞一句模板 prompt（可编辑、追加不覆盖已有文字，气泡文案引用当前所选语言）；③ 语音识别后**自动发送**继续对话（口语练习一句接一句）——把发送逻辑抽成 `sendText(text)`（文本发送与语音直发共用），图片/链接仍转文字填框、编辑后发；④ 场景对话 = 场景对话气泡（②）+ 语音直发（③）+ 纠错气泡（「结束并分析」：挑出不地道/有语病处、逐条纠正成知识点，复用 points 加入笔记/转卡）；⑤ 美文 = 写美文气泡（②）→ 生成 article 点 →「加入笔记」得双语对照 + 精读 callout（原文/生词/例句/语法）。⚠️ **延后**：原文段「分段发音」（SpeakButton 逐段朗读）——笔记始终是可编辑的 TipTap 编辑器、无独立只读视图，逐段发音需新增 inline 节点，改动较大，先记待办。tsc + build 通过。
- 2026-08-22 **学伴「语言」下拉 + 翻译模式（待办 0 第①项）**：`ai-assistant-panel.tsx` 输入框旁工具行右侧加「语言」下拉（默认「自由」，预设 韩/泰/日/英/西，`LANGS` 常量 + `lang` state），发送时把 `lang` 一并带进 body；`/api/ai/assistant` 加 `LANG_LABELS` 映射 + `wrapTranslation()`——选了目标语言时把用户输入的中文包成「翻译成地道自然的 X（front=译文 / back=中文原文）」prompt，让学伴把译文作为 `kind="example"` 知识点返回（译文=正面、中文原文=背面），照旧「加入笔记 / 转成闪卡」。不选语言仍是自由对话，字段通用（后续 ② 场景对话 / ⑤ 写美文 也复用这个 `lang`）。tsc + build 通过。
- 2026-08-22 **「原文」区块标签旁加「精读笔记」按钮**：在 `callout-nodeview.tsx` 的原文（article）callout 标签旁、和「加假名」并列，新增「精读笔记」小按钮——点它把**这一段原文**（`node.textContent`）交给 `/api/ai/analyze`，再把返回用 `analysisToNoteContent` 转成完整精读块，`deleteRange` 替换掉原原文区块、`insertContentAt` 在原位插入「带高亮/下划线/假名/译文的原文 + 生词/例句/语法」块。只对 `kind === "article"` 显示，任意语言都可用（不限于日语）；按钮是「点按钮」触发，与「✨ 解释」气泡（选中文字触发）互不冲突。tsc + build 通过。
- 2026-08-22 **语法接续规则改为「按词性分列」的 conjugations 数组，放在语法含义后面、每条配例句**：把 `conjugation: string`（单条）升级成 `conjugations: {rule, example}[]`（数组）——`/api/ai/analyze` 与 `/api/ai/assistant` 的 prompt 都改为要求日语 grammar 按词性**分别列出**接续规则、每条配一个例句（rule 写接什么形、example 是该规则的原语言例句；不同词性不同规则就多条，没有不同接续就一条，非日语空数组）；两个路由的 parse 都改成 map + filter 成数组。`ai-note.ts` 加 `GrammarConjugation` 类型，`AiAnalysis.grammar` / `AssistantPoint` 的接续字段改用 `conjugations[]`，`grammarBack()` 重写为「说明在前、每条 `接续：rule（example）` 依次跟在后面、用双空格分隔」——转成闪卡时 `wrapBackSpaces` 把双空格拆成换行，背面变成「语法说明 / 每条接续规则 / 例」各占一行、层次清楚。`analysisToNoteContent`（精读）与 `pointsToNoteContent`（学伴加入笔记）都传 `(g.conjugations ?? []).map(...)`。`ai-assistant-panel.tsx` 勾选列表把接续规则移到语法说明（back）**之后**逐条显示，`toCards()` 转卡背面也改成「back → 逐条 `接续：rule（example）` → 例」各占一行。tsc + build 通过。
- 2026-08-22 **日语语法加「接续规则」+ 原文「加假名」改成开关**：① 语法点新增 `conjugation`（接续规则）字段——`/api/ai/analyze` 与 `/api/ai/assistant` 的 prompt 都要求日语 grammar 必填「接什么内容 / 动词哪个形（辞書形 / て形 / た形 / ない形 / ます形去掉ます / 普通形 等）或名词/形容词接法」，用原语言形式写（如「動詞て形」）、非日语留空；`ai-note.ts` 加 `grammarBack()` 把接续用 `【接续：…】` 前缀放在语法点背面最前，`analysisToNoteContent`（精读）与 `pointsToNoteContent`（学伴加入笔记）都套用——转成闪卡时接续落在背面第一行（`makeCard` 对含 `【】` 的背面原样保留，不会误拆）。学伴勾选列表里接续用 teal 小字单行展示，`toCards` 直接转卡也把 `【接续：…】 ` 拼进背面。② 原文区块「加假名」按钮改成**开关**：`furigana-apply.ts` 加 `calloutHasFurigana(node)`（扫描是否有 furigana mark）与 `removeFuriganaFromCallout(editor,pos)`（`tr.removeMark` 只摘 furigana、保留高亮/下划线等其它标注）；`callout-nodeview.tsx` 按钮在「加假名 ↔ 去假名」间切换，点一下标音、再点一下恢复纯原文（方便编辑）。tsc + build 通过。
- 2026-08-22 **修复打开带假名的笔记崩「Content hole must be the only child of its parent node」**：根因是 `furigana-mark.ts` 的 `renderHTML` 写成了 `["ruby", attrs, 0, ["rt", …]]`——ProseMirror 规定 mark 的渲染结构里内容占位符 `0` 必须是其父节点的唯一子节点，`0` 和 `<rt>` 并排会被 `prosemirror-view` 的 `MarkViewDesc.create`（→ `DOMSerializer.renderSpec`）在编辑器渲染该 mark 时直接抛 RangeError。改成把正文包一层 `<span class="furigana-base">`（`0` 成为 span 的唯一子节点），`<rt>` 作为 `<ruby>` 的兄弟节点紧随其后，浏览器仍把假名渲染到汉字上方；`parseHTML`（`tag: "ruby"` + `el.querySelector("rt")`）不受影响。tsc + build 通过。
- 2026-08-22 **日语原文汉字加假名（上方对照）：搬到原文区块 + 精读自动标音 + 历史持久化**：按用户反馈改设计——「加假名」不再放 AI 学伴面板，而是**直接在笔记「原文」区块的标签旁**。① 新增 `src/lib/furigana.ts`（`FURIGANA_SYSTEM_PROMPT` + `parseSegments` + `furiganaSegments`：只给「汉字」标平假名，平假名/片假名/标点不标，换行单独成段；`/api/ai/furigana` 与 `/api/ai/analyze` 共用）；② `src/lib/furigana-apply.ts` 的 `addFuriganaToCallout(editor, pos)`——读「原文」区块里的日文正文（含假名的段落）→ 调 furigana → `replaceWith` 原地替换回原文区块（中文概括段/翻译段保持不变）；③ `callout-nodeview.tsx` 改接 `NodeViewProps`，「原文」标签旁加小「加假名」按钮（忙时转圈、出错红字），点一下给该段汉字上方标假名；④ **精读自动标音**：`/api/ai/analyze` 检测到 transcript 是日语（含假名）时自动调 furigana 并回填 `transcriptFurigana`；`ai-note.ts` 的 `markedTranscriptPara` 多接 `readings` 参数，把汉字读音与生词高亮/语法下划线/片假名读音合并成段（只在该段恰好等于读音区间时套假名，避免把整串读音错挂在被切开的子段上），新精读的日语原文直接带上方假名；⑤ `ai-assistant-panel.tsx` 学伴历史按 noteId 存 localStorage（messages + points + selected），重开面板还能看到之前的对话。⚠️ 只给汉字标音、片假名不动（片假名本身就是注音）；旧日语笔记要重新「AI 精读」或点原文标签旁的「加假名」才有。tsc + build 通过。
- 2026-08-21 **AI 学伴多合一输入（图片 OCR + 语音 + 链接）**：`ai-client.ts` 加 `aiVision`（Claude 视觉，base64 图片 → 文字）；新增 `/api/ai/ocr`（视觉 OCR，原样读图、不翻译不解释）与 `/api/ai/transcribe-audio`（收浏览器录音 blob → Whisper）。`ai-assistant-panel.tsx` 输入框下方加「图片 / 语音 / 链接」三个小工具按钮：图片=选图 → OCR → 填输入框；语音=点一下录、再点停 → Whisper → 填输入框；链接=贴 YouTube/音频/播客 → 复用 `/api/ai/transcribe` → 填输入框。三者都先转文字、可编辑后再发给学伴。⚠️ 语音依赖 `getUserMedia`（需 https/localhost），手机局域网 http 暂不可用（与「手机端部署」待办同因），已做友好提示。tsc + build 通过（`/api/ai/ocr`、`/api/ai/transcribe-audio` 已进路由表）。
- 2026-08-21 **微调（callout 原文底色 + AI 学伴按钮图标）**：①「原文」callout 加极淡浅灰底 + 细浅灰边（`#fafafa` / `#eeeeee`，只做与白底的区分、不重）；② AI 学伴工具栏按钮改成无底色的图标按钮、图标用主题色 teal（不再灰、也不做品牌胶囊）。
- 2026-08-21 **AI 学伴（第一期核心：文本对话 + 知识点勾选 + 加入笔记 / 转成闪卡）**：① 新增 `/api/ai/assistant` 路由（复用 `aiChat`，返回 `{reply, points}`，points 为 生词/例句/语法/原文 四类结构化知识点，prompt 让模型按提问类型拆点，混发「段落+词」也能拆开）；② `ai-note.ts` 加 `AssistantPoint` 类型 + `pointsToNoteContent`（知识点 → callout 块，复用现有 item/callout/bulletList，格式与手写精读一致，转卡自动识别）；③ 新组件 `ai-assistant-panel.tsx`（右侧抽屉：聊天流 + 知识点勾选列表 + 「加入笔记」「转成闪卡」，标签/配色复用 `KIND_META`）；④ `rich-text-editor.tsx` 工具栏加「✨ AI 学伴」按钮（普通灰图标，与其它工具栏图标融为一体，不另用品牌色突出）打开抽屉。「加入笔记」= 勾选知识点 `focus("end").insertContent(pointsToNoteContent(...))` 插到笔记末尾；「转成闪卡」= 勾选知识点（原文跳过）按 kind 生成 word/example/grammar 卡（`note_id/front/back/kind/position`，同笔记同正面去重）。tsc + `npm run build` 均通过，`/api/ai/assistant` 已进路由表。⚠️ **决策**：转成闪卡当前挂到「当前笔记」（与精读转卡一致），未做「独立默认合集按语言」——待确认第 5 条若要把学伴做成「不依赖笔记的全局助手」再补独立合集。**仍待做**：多合一输入（图片 OCR/语音/链接）+ 模型路由 + 设置页 AI 模型。
- 2026-08-21 **Callout 配色改成「奶油粉彩」低饱和暖色**：把生词/例句/语法从高饱和 天蓝/草绿/紫 换成柔和低饱和粉彩——生词=奶油金（bg `#fef8e4` / 边框 `#f2dfae` / 标签 `#9a7328`）、例句=鼠尾草绿（`#f0f6ec` / `#d8e7d2` / `#2b795c`，标签即主色 `--color-teal-700` 深一档，靠近站点主色）、语法=薰衣草紫（`#dfdefe` / `#ccc8f2` / `#5a4ac0`）、原文保持原灰色不动。同步改三处：`callout-extension.ts` 的 `KIND_META`（编辑器标签色）、`globals.css` 的 `.tiptap .callout` 与 `.share-content .callout[data-kind]`（背景/边框/标签色，编辑器与分享成图一致）、`rich-text-editor.tsx` 工具栏三个插入按钮（hover 用边框色）。⚠️ 附带暖色 `#feecec` 粉暂未用，留作以后加新类型。
- 2026-08-21 **闪卡页排版收拢（方案 A，回滚点 `1fae5d9`）**：按「干净清爽」重排卡片页。①「按来源」去掉「全部/独立卡片/笔记闪卡」那排筛选，改成一行「搜索框 + 语言 chip」（`cards-view`：搜索按合集标题/孤儿卡正反面过滤，语言 chip 激活色统一成 teal-600）；②「按主题」去掉每张主题卡底部的「背/测」，卡片统一成「图标 + 名称 + 数量」一种样式（与按来源合集卡一致，`group-browser`），点卡进详情页；③ AI 智能整理/新建分类/批量操作 从语言行挪到一行低调的次级描边工具栏（右侧），批量栏补「完成」退出；④ 复习入口移到词群详情页头部（`theme-detail` 加「背/测」两个小按钮）。总览页只留「浏览 + 点进去」，动作用点击卡片完成。
- 2026-08-21 **视觉打磨：callout 调淡 + 分享成图「笔记样式」+ 日语精读片假名上标平假名**：① callout 配色再调淡一档（生词/例句/语法背景、描边降到 -100 级，标签色从 -500 降到 -400，`callout-extension.ts` 的 `KIND_META` 同步改淡）；② 分享成图新增「视图」切换——「笔记样式」（默认）按笔记内页渲染：生词蓝框/例句绿框/语法紫框整块保留，「按条切页」保留原来的逐条分页（`share-modal.tsx` + `globals.css` 的 `.share-content .callout[data-kind]`）；③ 新增 `kana.ts`（片假名→平假名确定性映射，不用 AI）+ `furigana-mark.ts`（`<ruby>` mark），`ai-note.ts` 精读原文在片假名上方标平假名（平假名/汉字不动、长音「ー」保留），`docToText` 只取正文不影响转卡。⚠️ 旧笔记要重新「AI 精读」才有 furigana。
- 2026-08-21 **闪卡页合并「词群」（按来源 / 按主题切换）+ 词群批量操作去红统一**：把原来独立的「词群」页并入「闪卡」页，`cards/page.tsx` 顶部加「按来源 / 按主题」分段切换（用 `searchParams.view` 查询参数驱动，`/cards?view=theme` 直达主题视图、可退回/可分享）。「按来源」= 原 `CardsView`（按来源笔记分组，含生词/例句/语法），「按主题」= 原 `GroupBrowser`（只归类生词 `kind='word'` 到场景主题）。`/groups` 页改成 `redirect("/cards?view=theme")`（旧链接不 404），词群详情页 `/groups/[key]` 保留；`app-shell` 删「词群」导航项；`theme-detail` / `review` / `cards/[id]` 的返回链接统一改指 `/cards?view=theme`。**词群批量操作去红**：批量栏从红底红字改成白底 + 灰边，「全选」用品牌绿、「已选 N」用灰字、「删除所选」改成细红描边按钮（不再大红实心），与全站 teal/zinc 风格统一。
- 2026-08-21 **按来源卡片来源标识 + 卡片列表语言徽章统一**：`cards-view` 每张「合集卡」现在都有来源标注——有文件夹显示文件夹名，否则「笔记添加」（普通笔记转的）/「外部添加」（独立粘贴/导入的卡片文件），与孤儿卡的「外部添加」一致（图标用 Inbox，不用 ＋，避免误以为可点击）；`note-cards` 的卡片也补上语言徽章（`detectLang` + `LANG_COLOR`），和词群详情的卡片样式对齐。**暂未做**：按来源「合集卡」批量操作（低频 + 破坏性强）、两个 tab 卡片「翻面 vs 平铺」保留差异（管理/自测 vs 浏览/移词）。
- 2026-08-19 **精读笔记：对照翻译 + 生词高亮 + 语法下划线**：`/api/ai/analyze` 新增返回 `transcriptTranslation`（原文逐段中文翻译，段数与 transcript 一一对应）和 `grammar[].phrase`（语法点在原文里的原语言短语）；`analysisToNoteContent` 把「原文」callout 改成**上下逐段对照**——每段原文下方跟一段淡灰小字中文翻译，生词用 `<mark>` 淡黄高亮（只标全篇首次出现，大小写不敏感子串匹配），语法短语用 `<u>` 下划线（也只标首现）。新增自定义 mark `translation`（`translation-mark.ts`，渲染成 `<span class="translation">`，配色在 globals.css 的 `.translation`），注册进 `editorExtensions`。例句仍只在下方列表、不在原文标注；`docToText` 忽略 mark，不影响「转成闪卡」。**注意**：旧笔记没翻译/高亮（生成时还没有这功能），要重新「AI 精读」才有。
- 2026-08-19 **字幕语言改按「原始语言轨」优先**：之前 `LANG_PREF` 把 `en` 排在 `ko/ja/th` 前面，韩语/日语/泰语视频会误抓英文翻译字幕。改为先抓各目标语言的 `-orig`（视频本身语言，每视频仅一个，如 `ko-orig`=韩语原始轨）再兜底普通轨：韩语视频出韩语、英语视频出英语。另把 `blocked` 报错判定补上 `SSL/EOF/violation`，代理偶发断连时给「网络问题」提示而非「没字幕」。
- 2026-08-19 **YouTube 字幕改走 yt-dlp + Chrome 登录态（修 bot 拦截）**：实测 `youtu.be/tB88DEBk5tw` 等**所有**视频抓不到字幕，根因不是「没字幕」，而是 YouTube 对「数据中心 IP + 无登录态」的请求直接 bot 拦截（`Sign in to confirm you're not a bot` / `Precondition check failed` 400），`youtube-transcript` 的裸 innertube 请求已全量失效。改法：`/api/ai/transcribe` 的 YouTube 分支弃用 `youtube-transcript`，改调 `python3 -m yt_dlp --cookies-from-browser chrome --skip-download --ignore-no-formats-error --write-subs --write-auto-subs --sub-format vtt` 抓字幕（含自动字幕），再按语言偏好（`-orig` 优先）读 .vtt 并用新写的 `parseVtt`（只取「整行快照」、跳过逐字滚动 `<c>` 行）拼成纯文本。**前提：本机 Chrome 登录了 YouTube**；cookie 只在本机、只用于向 YouTube 请求。依赖：本机已装 `python3` + `yt_dlp`（2026.07.04）。
- 2026-08-19 **修复转录代理失效（undici 版本不匹配）**：`serverFetch` 原先用全局 `fetch` + npm `undici` 的 `ProxyAgent`，两者是不同实例，传 `dispatcher` 报 `UND_ERR_INVALID_ARG`，导致即使配了 `HTTPS_PROXY` 也「fetch failed」、转录报「没能抓到字幕」。改为用 `undici` 自带的 `fetch`（与 `ProxyAgent` 同包配对）；Whisper 的 multipart 改用 `undici` 的 `FormData`（全局 FormData 会被 undici fetch 当普通对象序列化）。`fetchYouTubeTranscript` 报错区分「连不上 YouTube（代理没生效）」和「真没字幕」。
- 2026-08-19 **修复新建笔记报「Adding different instances of a keyed plugin」**：精读「已收录」下划线装饰插件原先用 `useEffect` + `editor.registerPlugin` 挂载，React 严格模式（开发）下会重复注册同一 `PluginKey` 的两个不同实例而崩。改成 `Extension.create({ addProseMirrorPlugins })` 塞进 `useEditor` 的 `extensions`，在编辑器创建时一次性挂上，去掉 `registerPlugin`/`unregisterPlugin` 那套。
- 2026-08-19 **修复媒体转录失败（网络代理）**：根因是 Node 服务端 `fetch` 不读 macOS 系统代理（Clash 127.0.0.1:7897），YouTube / OpenAI 直连超时导致「转录不了文字稿」（浏览器能开是因为走了系统代理）。新增 `src/lib/server-fetch.ts`（undici `ProxyAgent`，读 `HTTPS_PROXY`/`HTTP_PROXY`/`ALL_PROXY` 环境变量，没配就退回普通 fetch）；`/api/ai/transcribe`（YouTube 字幕、音频下载、Whisper）、`/api/rss`（抓订阅）、`ai-client`（Anthropic/DeepSeek）全部改走 `serverFetch`。**使用前需在 `.env.local` 加一行 `HTTPS_PROXY=http://127.0.0.1:7897`**（换成你自己的代理端口）。
- 2026-08-19 **AI 精读整理原文文稿**：`/api/ai/analyze` 新增 `transcript` 字段——让模型把原始字幕/文字稿整理成干净原文（修正错别字、删重复和口水话、去掉无关碎片、按语义分段，保留原语言、不翻译不概括）；`analysisToNoteContent` 的「原文」区块优先用整理后的原文，旧原始文字稿作兜底；`maxTokens` 提到 12000 以容纳完整整理稿。
- 2026-08-19 **精读解析去表格 + 分享图按条切页不裁切**：`analysisToNoteContent` 把生词/例句/语法从「表格」改成彩色 callout 内的逐条列表「词：释义  拓展」（语法为「语法点：说明  例：例句」），转成闪卡仍按冒号规则归入对应类型，且双空格让拓展落到背面释义的下一行（不跟释义挤一行、避免括号混淆）；`share-modal` 把 callout / 列表「摊平」成独立可切页单元（彩色标签 + 逐条），一个类型尽量放一张图、放不下自动加图，不再裁切省略。
- 2026-08-19 **修复转卡误收媒体链接与概括文字**：`parse-cards` 跳过「[媒体] 链接」占位行；`analysisToNoteContent` 把「概括 + 原文」一起放进只读「原文」区块（不再裸放概括段落），两者都不再被转成闪卡。
- 2026-08-19 **转成闪卡按类型勾选 + 侧栏续转入口**：`convert-to-cards` 顶部加「生词 / 例句 / 语法」chip（默认全选，可取消某类，只把生词入库）；闪卡侧栏加「转成闪卡」按钮，已有卡的笔记也能一键继续转（如补充例句）。
- 2026-08-19 **「原文」区块 + AI 精读 callout 化**：新增「原文」callout 类型（灰色，只读），`parse-sections` 对原文/文章区域整段跳过不转卡；`analysisToNoteContent` 把生词/例句/语法改成彩色 callout（不再是标题+表格）；「生成文字稿」「AI 精读」自动把文字稿包进原文区块，避免原文被误转成卡。工具栏新增「原文」按钮。
- 2026-08-19 **精读「拓展」列内容约束**：analyze prompt 明确「拓展」只放常见搭配 / 例句 / 相关词，不写词源或历史背景。
- 2026-08-19 **精读解释加「音标 + 词性」**：`/api/ai/explain` 返回新增 `phonetic`（英语 IPA / 韩日罗马字 / 泰语罗马化）、`partOfSpeech`（中文词性）两字段；气泡头显示 词性徽标 + 音标行；「收录到闪卡」背面第一行写「词性 · /音标/」元信息。
- 2026-08-19 **精读标记改为「只标点过解释且收录的词」**：`rich-text-editor.tsx` 去掉「载入时按 `cards.front` 回填」的逻辑，改为只在本次会话里点过「解释」并「收录到闪卡」的词才加淡下划线（不跨刷新持久化）。
- 2026-08-18 **本地转录工具** `scripts/transcribe.py`：字幕优先（yt-dlp），无字幕则抽音频 → ffmpeg 转码/切片 → OpenAI Whisper 逐段转录；输出存 txt + 复制剪贴板。
- 2026-08-18 **词群列表重排 + 词群页重构**（A）：`theme-detail.tsx` 卡片网格（两列、正反面直显、不翻面）；批量选择/删除分类在外层 `group-browser`。
- 2026-08-18 **精读「高亮即解释」+ 原形还原**（B）：选中文字浮「✨ 解释」气泡，点才调 AI；结果含「收录到闪卡」；AI 返回词典原形当正面；手动收录加「✨ 还原原形」。
- 2026-08-18 **识别规则重排 + 新增自定义规则**（C）：`SplitRule` 自定义分隔规则；原 `parseCards` 逻辑不动。
- 2026-08-18 **复习翻面空格键**（D）：`review-session.tsx` 空格翻面（输入框内不触发、不连发）。
- 2026-08-18 **媒体悬浮播放可拖拽 + 最小化**（E）：`media-embed-nodeview.tsx`。
- 2026-08-18 **媒体内嵌 Spotify**：`media.ts` 新增 spotify 解析。
- 2026-08-18 **故事模式 + 闪卡合集搜索 + 词群分类删除/隐藏**。
- 2026-08-18 **Round G**：视觉升级（sage-mint 主题）+ 媒体内嵌笔记 + 复习/设置/词群/分享打磨（git `16e1778`）。

## 待办（未做）

- 2026-08-30 **语伴「加入笔记」去重覆盖只扫 callout（待完善，暂缓）**：`existingCardsOf` 只遍历 callout 节点，不扫「生词」标题下与裸段落的词条。用标题/裸段落组织笔记时，语伴加同名词会重复追加一行（而非覆盖），与解释链路的「词条行覆盖」不一致。修复需扩展扫描到标题区/裸段落词条并逐行替换（复用解释链路「词在行首」词条判断），改动较大、风险较高，本轮先部署、后续再完善。

### 0. 本轮主线：学伴增强 + 输出练习（2026-08-22 定稿，按 ①~⑤ 顺序做）
> 原则：学伴保持**简单、高自由度**；文本框附近只放**通用**按钮（图片/语音/链接），模式靠低调「气泡」快速填 prompt，不另起复杂 UI。

- ① ✅ **学伴「语言」下拉 + 翻译模式**（已完成 2026-08-22）：输入框旁加「语言」下拉（默认自由；预设 韩/泰/日/英/西）。选了语言 + 贴中文 → 学伴翻译成**地道自然**的该语言，译文作为 `example` 点（front=译文 / back=中文原文）加入笔记/转卡。
- ② ✅ **低调「模式气泡」**（已完成 2026-08-22）：输入框上方一行小气泡（翻译 / 场景对话 / 写美文 / 纠错），点了只往输入框塞一句模板 prompt、可自由编辑；不新增专用按钮。
- ③ ✅ **语音消息直接发送**（已完成 2026-08-22）：语音识别后**自动发送**、继续对话（口语练习一句接一句）；图片/链接仍是「转文字填框、编辑后发」。
- ④ ✅ **场景对话（= 学伴轻量模式）**（已完成 2026-08-22，轻量版）：选场景 + 语言 → AI 扮演角色 → 用户用目标语言对话（可语音直发）→ 「纠错」气泡触发结束并分析 → 纠错 / 自然度 / 更好说法，整理成知识点（复用 points）→ 加入笔记/转卡。结束：以「纠错」气泡为主，退出不强制分析（历史已存 localStorage，可回来再点分析）。二期：关键句打主题标签、同步词场「主题场景对话闪卡」。
- ⑤ ✅ **美文 = 学伴「写美文」+ 笔记阅读复用**（已完成 2026-08-22，写美文气泡）：选主题 + 语言 → 生成短文（article 点 + 生词/例句/语法点）→「加入笔记」得到精读笔记（原文双语对照 + 精读 callout + 解释气泡）。**不做独立美文库/阅读器**。⏳ **延后**：原文段「分段发音」（SpeakButton 逐段朗读）——笔记是可编辑 TipTap、无独立只读视图，需新增 inline 节点，改动较大，另记待办。

### 0b. 本轮其他独立项（顺序稍后）
- **词群「从合集导入」**：✅ 已完成 2026-08-22（见「已完成」顶部）。`CollectWordsModal` 加第 4 种方式「从合集导入」（按笔记/合集搜索）→ 一键把该篇的**生词 + 例句**一起 `theme` 设成当前主题；**新建分类**也支持从合集导入（可选折叠区）；词群分类已放开 `kind='word'` 限制到 `word`+`example`（按主题不分开）。
- **完形填空复习题型**：✅ 已完成 2026-08-22（见「已完成」顶部 完形条目）——`mode=cloze`；有语境句用现成句、没语境句由 `/api/ai/cloze-examples` 造例句（带小 AI 标识）；「显示答案」= 开关（只填空、可隐藏），核对后进整句（例句高亮 + 紧跟其下的整句译 + 词义），直接「下一题」，不计 SM-2。遗留：只给「生词」卡出题（例句/语法卡不挖空），`i18n` 交互未做。
- **转录多端（文件上传）**：后端端点 `/api/ai/transcribe-file`，**云 STT（Deepgram/AssemblyAI）为主** + 本地 ffmpeg+Whisper 回退，共存一个端点；网页/App 共用、Supabase 天然同步。突破 25MB：ffmpeg 切片 或 换无限制云 STT。App 端可另加系统离线识别（安卓 SpeechRecognizer / iOS SFSpeechRecognizer）。

### 1. 语音转文字稿（无字幕视频 + 播客听声转录）
- ✅ **云端转录方案已敲定并落地（2026-08-22）**：云 STT 用 **Groq**（免费额度合适，OpenAI 兼容），新建 **`/api/ai/transcribe-file`**（任意客户端上传音频→`{ text }`，Web/手机/未来 App 共用，Key 只留服务端），`transcribe-audio`（浏览器录音）/`transcribe`（音频直链）都改走共享 `src/lib/transcribe.ts`；本地 Whisper 保留。**FunASR（长音频，自托管）+ 手机 Web Speech（短语音）＝ 可选二期增强**，都不是「手机随时转长文件」的解（FunASR 上不了 Vercel / 手机外网连不到；Web Speech 仅 Chrome、发到 Google 且长句会被切）。见「已完成」顶部。
- **YouTube「无字幕 → 听声转录」回退**：有字幕（含自动字幕）已经走通（yt-dlp + Chrome 登录态）；但「下载音频 → 语音识别」这条没做。实测 `yt-dlp` 提取音频直链后，下载 googlevideo 数据会 **HTTP 403**——YouTube 现强制 JS 挑战（n-sig）验证，需装 **deno**（JS 运行时挑战求解器）+ **ffmpeg** + 接语音识别 API（接 **Groq** 即可，跟 `transcribe-file` 一个 Key）。⚠️ 该路依赖本地代理、受 25MB 限制、上不了 Vercel。真没字幕的极端情况暂用本地 `scripts/transcribe.py` 或手动复制。
- **Apple Podcast 单集转录**：✅ 已完成 2026-08-22 —— Apple / Google / 播客主页链接都会经 `podcast-resolve.ts` 解析成源，挑集后再走 Groq 转录（见「已完成」顶部）。未做：按 `?i=<episodeId>` 自动选到那一集（现在是挑集列表手选，够用）。
- **Spotify 单集转录：做不了**（音频被登录/API 挡，无公开 mp3 直链；内嵌播放可以、转录不行）。
- **ffmpeg 安装**（brew，供 `scripts/transcribe.py` 与后端抽音频用）。

### 2. 手机端正式使用 / 是否需要部署
- 【待决策】手机现在通过局域网 HTTP 打开，登录会**静默失败**（非 https 环境）。
- 方案 A **Vercel 部署**：CLI 已装（59.1.4）；登录未成功（vercel.com 慢、device flow 超时，`vercel whoami` 仍是 Logged out）。恢复：`vercel login` → `vercel link` → 填环境变量（`NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `ANTHROPIC_API_KEY` / `OPENAI_API_KEY`）→ `vercel --prod`。⚠️ 部署后**本地才能跑的转录（yt-dlp + Chrome cookie、ffmpeg、本地代理）在 Vercel 上不可用**，得重新想这些 AI 能力怎么在线上跑（或接受线上版没有转录）。
- 方案 B **本地起 https**（`next dev` 加 https 或反向代理），手机继续连本机——不部署，但手机得和电脑同一 WiFi。
- 明天先聊清「手机是外网用、还是只在同一 WiFi 用」，再定方案。

### 3. 其他
- **拍照 OCR**：✅ 已完成 2026-08-22 —— 笔记编辑器媒体弹窗「上传图片 → 识别文字（OCR）」，识别文字按段落插入光标处（见「已完成」顶部）。

### 4. 词场升级（2026-08-21 决定不做，搁置）
> 用户判断：词群页只是「查看 + 轻管理」，把词移出、把单词闪卡收录进词群已经够用；掌握率 / 移到别的词群 / 一词多词群 都是锦上添花、非必需，暂不升级。以后真要再说。
- **多词群**（核心）：一张卡可属于多个词群。建议把 `cards.theme`（单字符串）升级成 `cards.themes text[]`（数组）：关键词匹配返回全部命中、AI 聚类每词给 1~2 个主题、手动移卡 = push/pop，不加新表。
- **每个词群显示掌握率**：用 `review_state`（ease/reps/lapses）聚合，纯前端、便宜。
- **手动移卡 / 加到别的词群**：词群详情页给每张卡「移到别的词群」。
- **AI 归类到自定义词群**：`cluster-cards` 现在只能归到 13 个内置主题，改成也能归到用户 `word_themes`。
- ⏸️ **地图/关系图视图**：先不做（学习价值存疑、成本高）；以后可用「相关词群」一行小字替代。

### 5. 素材库（收件箱切片，2026-08-21 重新立项，排在 AI 学伴之后）
> 定位：囤积语言学习素材的「收件箱」（视频/播客/文章），与「笔记」互补——笔记管加工、素材库管囤积与追踪；最终流 = 素材 → 导入笔记 → 精读 → 转卡。
> **状态**：✅ 已完成（2026-08-22，见「已完成」顶部）——一键收藏 + 列表筛选 + 合集 + 导入笔记；库内不做转写（转写/AI 精读全在笔记里做）。**增强**：YouTube 合辑一键导入（每集一条 + 同名合集）、站内观看页 `/materials/[id]` 替换预览弹窗。
- **一键收藏**：贴链接 → 自动抓标题/缩略图/频道/语言（YouTube 用 oEmbed、文章用 OG 标签、播客用 Apple lookup），语言 detectLang 猜 + 可手动改。
- **列表 + 筛选**：时间倒序；按 语言/类型/来源频道 筛选；每条带「待处理/已导入」状态徽章。
- **合集**：手动建合集归类素材（新表 `material_collections`，范式照抄 `word_themes`）。
- **库内预览**：素材详情里直接预览——视频 iframe / 播客 `<audio>`（复用现有 media-embed），文章打开原文链接（完整阅读器延后）。
- **导入到笔记**：点「导入到笔记」→ 新建/选已有笔记 → 链接(+可选转录文字稿)填进笔记 → 素材标「已导入」→ 笔记里走现有精读/转卡。
- **数据**：新表 `materials`（url/type/title/source/thumbnail/lang/status/collection_id）+ `material_collections`。
- **不做/延后**：内嵌播放器+双语字幕同步+字幕选词（大工程；且选词解释=学伴已覆盖）、多源导入（网盘是新集成、拍照=OCR 已含于学伴、文件已延后）、独立语料库（N3 语法列表那种 = AI 精读生成笔记，不另建）。

### 6. 视觉 / 分享 / 精读打磨（✅ 已完成 2026-08-21，见上方已完成）
- **Callout 颜色更淡**：生词/例句/语法/原文 callout 的背景、描边再调淡一档、降饱和度。
- **分享成图「默认视图」**：按笔记内页的 callout 样式生成图（生词蓝框/例句绿框/语法紫框），share-modal 加默认视图。
- **日语精读片假名上标平假名（furigana）**：精读生成的原文+例句里，连续片假名转平假名用 `<ruby>` 标在上方，平假名/汉字跳过；纯字符映射、不用 AI。⚠️ 这里只做「精读输出」范围，不是素材库那条已砍掉的「全站自动注音」。

### 7. AI 学伴（第一期，2026-08-21 定稿范围）
> 定位：笔记编辑栏入口的「AI 学伴」（类 ChatGPT 对话弹窗），统一「自由输入 → 生成学习内容 → 笔记/闪卡」这一族；不做全局悬浮按钮（避免和右下角「闪卡」按钮冲突、避免碍眼），最终仍回到「存卡 → 复习」主线。
> **状态**：✅ 全部完成（2026-08-22）——模型路由 + 设置页「AI 模型」板块（见「已完成」顶部），AI 学伴第一期至此收尾。
- **入口**：笔记工具栏「✨ AI 学伴」→ 弹对话面板（就在笔记编辑器内，插入当前笔记直接调编辑器，无需全局桥）。
- **多合一输入**：文本 / 图片（OCR，视觉模型读图取文字）/ 语音（Whisper 转写，可手动选语言）/ 链接（YouTube/播客转录，复用 `/api/ai/transcribe`；播客需补 parseMediaUrl + Apple Podcast RSS 扩展）。
- **输出**：聊天回答（自由文本）+ 结构化「知识点」列表（生词/例句/语法/原文，各带「加入笔记」「转成闪卡」）。逐个勾选——加入笔记 = 当前笔记插对应 callout 块（复用现有、转卡自动识别）；转成闪卡 = 进默认合集按语言。混发「段落+词」由 AI 拆成多个知识点分别归类。
- **生成主题短文**：原文+译文+精读 callout+朗读（复用 `/api/ai/story` + `/api/ai/analyze` + SpeakButton）。
- **模型路由**：按任务分流——文字=Claude（默认，质量）/ DeepSeek（省钱档，你已有 key）、语音=OpenAI Whisper（你已有）/ Groq（免费额度大）、图片=Claude/OpenAI 视觉。设置页加小「AI 模型」板块（每任务一个下拉、只给预设选项），默认定好、用户可换。
- **复用不重造**：`/api/ai/explain`、`/api/ai/analyze`、`/api/ai/story`、`/api/ai/transcribe`、callout、转卡、默认合集（卡片文件）全是现成。
- **不做/延后**：电子书（第二期只做「导入文字→精读→转卡」切片；完整阅读器、全书翻译不做）、复杂文件解析（docx/扫描 PDF）、自由闲聊（冲淡闪卡核心）。

## 待确认
1. **手机端部署方案**（见待办第 2 条）：是否部署、线上转录能力怎么办。
2. **闪卡背面自动补全**（音标/词性/解释/搭配/相关词/例句）：是否做、如何与手写内容融合——讨论中，倾向「单卡按需补全、只填空、预览确认」，不塞进「一键转成闪卡」。
3. **按来源「合集卡」是否加批量删除**——暂保留现状（总览卡片已统一成「图标+名称+数量」一种样式；详情列表仍保留「翻面 vs 平铺」差异），需要再定。
4. **一键补全缺的释义（批量补释义）**——决定不做（2026-08-21）：用户手动记录即可，已有单点「解释」功能，不想再加太多入口。以后真要再说。
5. **AI 语言助手两个实现细节**（见待办第 7 条）：① 解释卡是否「AI 原文 + 可手动编辑」再存卡（倾向：可编辑再存，呼应「预览确认」偏好）；② 查询历史「轻量本地 vs 跨设备持久化」（倾向：先轻量本地，跑通再决定是否上 `lookups` 表）。
6. **AI 学伴入口已定稿**（并入待办第 7 条）：入口 = 笔记编辑栏弹窗（不做悬浮按钮）；多合一输入（文本/图片/语音/链接）+ 一条管线 + 知识点勾选；模型按任务路由。
7. **自定义模型管理 / BYOK（后期，已出设计未实现）**：用户想后期在设置里做「AI 模型区域」，能粘贴自己的 API endpoint + key、管理现有 provider，且考虑分享给别的用户（多租户、每人贴自己的 key）。方案要点：① 现有 provider 都是「服务端 env 集中配置、不落库」；BYOK 是「每用户各自 key」——需新增 `api_keys` 表（`user_id + provider + key + base_url + model`，RLS 只本人可见）；② key 以**明文**存 Supabase（受 RLS 保护）是常见折中，或选 pgsodium/Vault 加密——需明确「能进 DB 的人能看到 key」；③ 服务端读取改用「用户会话」触达该用户自己的 key（`createClient()` 走 cookie + RLS），不落客户端、不回传；④ 路由层做成「provider 名 → (base_url/model/key)」查表，环境默认 key 当「管理员预置」，用户自定义 key 优先、其次环境 key。安全提醒：分享给他人 = 每人贴自己的 key、App 不代存；泄露面从「一人的 env」扩到「每人的 DB 行」，RLS + 审计要做扎实。**存储方式已定（2026-08-23）：明文 + RLS（方案 A）**——先自己用/小范围分享，简单够用，等对外放量再升级加密；**未实现，等开做。**（2026-08-23 用户再次确认「先不做」，继续用环境里已配好的 key。）
8. **公有云免费层（AWS/Azure）STT 评估结论：本轮未接入**。① **Azure Speech** 免费层 F0（每月 5h STT）真实存在，但 REST 端点要求 PCM WAV（16kHz/16bit mono），Web 端 MediaRecorder 录的是 webm/opus——不转码即被拒，需服务端加 ffmpeg/sox 转码，成本高；② **AWS Transcribe** 免费层（每月 60min）需 SigV4 签名 + S3 异步任务，raw fetch 做不了、要引 `@aws-sdk/*`；③ **Azure OpenAI** 文字是 OpenAI 兼容（换 resource 端点 + `api-key` 头 + deployment 名）但**无免费层**、按量付费，本质是「自定义 OpenAI 兼容端点」，更适合并进上面 BYOK 的自定义模型。结论：免费需求已被 Groq（额度大）+ AssemblyAI（$50 一次性额度）覆盖；真要公有云，走 BYOK 自定义端点最省事。**若仍要 Azure Speech，先解决音频转码。**
9. **发音/TTS 后端（2026-08-23 提出，待定）**：用户问「手机端闪卡发音没声、日语纯汉字卡读成中文、日语发音人机、能否用 Groq 做全部发音」。① 语言标签 bug 已修（见「已完成」顶部）；② **Groq TTS 不可行**——`api.groq.com/openai/v1/audio/speech` 只支持英语（`canopylabs/orpheus-v1-english`）与沙特阿拉伯语（`canopylabs/orpheus-arabic-saudi`），**覆盖不了 泰/韩/中/日/英 五种语言**；③ **推荐 Azure Speech TTS**（神经音色最真人、F0 免费层每月 50 万字符、REST + SSML 简单、文本→音频无 webm 转码问题，五种语言都有多款自然音色），可先解决「手机端统一云端发音 + 各语言真人化」；④ 备选：继续浏览器 speechSynthesis（移动端 iOS 需在用户手势里调用、音色设备相关、无法保证一致）。**已定（2026-08-23）：继续系统 speechSynthesis，不换云端 TTS。** 已知限制：非 Chrome 安卓浏览器（UC/夸克/微信/三星/华为自带等）对 speechSynthesis 支持参差，可能无声——属浏览器/系统语音引擎限制，代码层无法保证；要任意浏览器都出声只能上云端 TTS（返回 mp3 用 `<audio>` 播），以后再议。
10. **闪卡背面排版（2026-08-28 最终定稿，见「已完成」顶部最新一条）**：撤销旧模板功能后，用户再参考别的 App 设计敲定最终背面布局——**背诵（review-session 主翻面）背面 = 顶部居中「正面词 + 含义（释义/译文，含 `[词性]`）」+ 下方靠左「读音/例句/拓展 等带标签内容」；自建（无标签）卡 = 正面词 + 整段自由释义居中、无下方标签；详情页/翻面卡/错题集仍左轴 chip 模板、不居中不重复正面**。实现：`card-back` 的 `CardBack` 加 `front?`/`reading?` 并用 `splitMeaning()` 拆含义/其他标签，`review-session` 传 `front`/`reading`；「相关词」标签经 `LABEL_ALIAS` 归一化成「相关」；语伴生词精简为 释义/例句/拓展（拓展合并搭配/相关）。
